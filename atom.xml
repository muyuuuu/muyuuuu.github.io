<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Just for Life.</title>
  
  <subtitle>明月更几时</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://muyuuuu.github.io/"/>
  <updated>2021-05-25T07:50:14.163Z</updated>
  <id>https://muyuuuu.github.io/</id>
  
  <author>
    <name>兰铃</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>分割篇：Mask R-CNN</title>
    <link href="https://muyuuuu.github.io/2021/05/25/Mask-R-CNN/"/>
    <id>https://muyuuuu.github.io/2021/05/25/Mask-R-CNN/</id>
    <published>2021-05-25T02:15:16.000Z</published>
    <updated>2021-05-25T07:50:14.163Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>不同于 FCN 的语义分割，Mask R-CNN 是用于实体分割的。借鉴 FCN 的思想，通过在 Faster R-CNN 的用于边界框识别分支上添加了一个并行的用于预测目标掩码的分支 Mask，在实现目标检测的同时，实现实例分割（object instance segmentation），即把每个目标像素分割出来。而 Faster R-CNN 和 FCN 在之前介绍过，所以本文的重点将会放在损失函数的设计和 ROI-Align 上。</p><p>实体分割不同于语义分割的是，不仅要检测出所属类别，还要区分同一类别下的不同实例。</p><p><img data-src="https://z3.ax1x.com/2021/05/25/gxXEss.png" alt></p><a id="more"></a><p>按着前文描述，网络结构如下，蓝色分支是 Faster R-CNN 中用于预测类别和回归框的分支，红色分支是新加入的预测掩码的分支。</p><p><img data-src="https://z3.ax1x.com/2021/05/25/gxOoa6.png" alt></p><h1 id="基本思想"><a href="#基本思想" class="headerlink" title="基本思想"></a>基本思想</h1><p>语义分割问题只需要在语义上对像素点进行区分即可。而实例分割问题不仅需要正确地检测出所有的目标，还需要在单个目标的基础上对每一个实例进行准确的分割。而作者基于 Faster R-CNN 目标检测的框架和 FCN 的语义分割提出了 Mask R-CNN。所以流程就是：</p><ol><li>输入一副图片，将图片输入到特征提取层 backbone，也可以融合 FPN 机制，最终获得对应的 feature map。下图中，左图是 Mask R-CNN 和 Faster R-CNN 结合的方式，右图是 Mask R-CNN 和 FPN 结合的方式。</li><li>对 feature map 中获得多个候选 ROI，候选的 ROI 送入 RPN 网络进行二值分类和 bbox 回归，截止到这一步，Mask R-CNN 和 Faster R-CNN 完全相同</li><li>Mask R-CNN 中，取消 ROI pooling，替换为 ROI-Align</li><li>对这些 ROI 进行分类、box 回归和 MASK 生成</li></ol><p><del>如果看不懂建议重新看 Faster R-CNN 和 FCN</del></p><p><img data-src="https://z3.ax1x.com/2021/05/25/gxvu2F.png" alt></p><h1 id="ROI-Align"><a href="#ROI-Align" class="headerlink" title="ROI-Align"></a>ROI-Align</h1><p>对于传统的 ROI pooling 而言，难免会有精度损失。比如 <code>20X20</code> 的特征图 pool 到 <code>7X7</code>，自然无法整除，就需要对像素点进行取舍，带来精度上的损失。RoI 对应至特征图和 ROI 进行划分这两步取整量化操作会导致 ROI 与抽取出的特征图在空间位置上不匹配。这一问题不会对目标的分类造成大的影响，但会对 mask 预测造成极大的负面影响。如下图所示 <sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup> ，并非均匀划分：</p><p><img data-src="https://z3.ax1x.com/2021/05/25/gzSIZd.gif" alt></p><p>对于 ROI-Align 而言，不在对 ROI 边界进行取整。假设 pool 到 <code>3X3</code> 的格子，那么计算每个格子中心四个点的坐标。对于每个点而言，对每个周围的四个点进行采样，使用双线性插值计算当前点的取值，而后 max pool 获取当前格子里面的值。无论采样点的多少与采样方式，结果都不会很差。如果使用的量化取整，结果就会很差。如下图，作者也只使用了这一张图介绍了 ROI-Align。</p><p><img data-src="https://z3.ax1x.com/2021/05/25/gzptwd.png" alt></p><p>这个过程佷繁琐不易理解，但其实并不难，可以来这篇博客看下动图。<sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup></p><p><img data-src="https://z3.ax1x.com/2021/05/25/gziG9I.gif" alt></p><h1 id="Mask-损失"><a href="#Mask-损失" class="headerlink" title="Mask 损失"></a>Mask 损失</h1><p>Mask R-CNN 的分支由三部分组成，其中 $L_{\text{cls}}$ 和 $L_{\text{box}}$ 与 Faster R-CNN 没有本质区别，所以重点是 $ L_{\text{mask}}$，它是平均二分类交叉熵损失。</p><p>\begin{equation}<br>L = L_{\text{cls}} + L_{\text{box}} + L_{\text{mask}}<br>\end{equation}</p><p>假设一共有 $K$ 个类别，则 Mask 分割分支的输出维度是 $K m m$。所以对于 $m\times m$ 中的每个点，都会输出 $K$ 个二值 Mask（每个类别使用 <code>sigmoid</code> 输出）。这里与 FCN 的做法不同，FCN 在每个像素点上应用 softmax 函数，整体采用多任务交叉熵，这样会导致类间竞争（class competition），最终导致分割效果差。其实上文也能看到，分割效果的确很差。</p><p><img data-src="https://z3.ax1x.com/2021/05/25/gzgnb9.png" alt></p><blockquote><p>在 Faster RCNN 做 object detection 的时候，已经把某一块 RoI 识别为汽车，但这个 RoI 内可能存在其他物体的一部分，因此分割的 mask 中，除了要将汽车分割出来外，还要把另外那个物体也分割出来。这就导致这样的情况，在 object detection 的分支中，这块 RoI 整体被识别为汽车，但在 segmentation 的时候，这块 RoI 一部分被识别为汽车，一部分又要当作其他物体。如此一来，这两个分支回传到前面的梯度多少存在冲突，而前面的特征提取网络可是共享的，结果网络在学习的时候就可能出现左右为难的情况。</p></blockquote><p>那么来考虑二分类。To this we apply a per-pixel sigmoid, and define $L_{\text{mask}}$ as the average binary cross-entropy loss. For an RoI associated with ground-truth class $k$, $L_{\text{mask}}$ is only defined on the $k$-th mask (other mask outputs do not contribute to the loss). 也就是，只考虑一种类别，如果 ground truth 中标记了这个 bounding box 中是个人的话，那我们就只针对人的 mask 进行分割，而对这个 bounding box 中其他可能存在的物体一律忽视。如下图所示。这里画图的思想参考了这篇文章 <sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup>。</p><p><img data-src="https://z3.ax1x.com/2021/05/25/gzRBjS.png" alt></p><p>在测试阶段，通过分类分支预测的类别来选择相应的 Mask 预测。这样，Mask 预测和分类预测就彻底解耦了。</p><h1 id="程序"><a href="#程序" class="headerlink" title="程序"></a>程序</h1><ol><li><a href="https://pytorch.org/tutorials/intermediate/torchvision_tutorial.html" target="_blank" rel="noopener">torchvision</a> 上有现成的示例。</li><li>等我以后用到语义分割的时候再回来补冲这里的程序，那时候肯定是 open-mmlab 系列的东西了。</li></ol><h1 id="reference"><a href="#reference" class="headerlink" title="reference"></a>reference</h1><p><sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup>：<a href="https://en.wikipedia.org/wiki/File:RoI_pooling_animated.gif" target="_blank" rel="noopener">ROI pooling 动图</a><br><sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup>：<a href="https://towardsdatascience.com/understanding-region-of-interest-part-2-roi-align-and-roi-warp-f795196fc193" target="_blank" rel="noopener">ROI Align 动图</a><br><sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup>：<a href="https://zhuanlan.zhihu.com/p/65321082" target="_blank" rel="noopener">二分类损失代替多分类损失</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;不同于 FCN 的语义分割，Mask R-CNN 是用于实体分割的。借鉴 FCN 的思想，通过在 Faster R-CNN 的用于边界框识别分支上添加了一个并行的用于预测目标掩码的分支 Mask，在实现目标检测的同时，实现实例分割（object instance segmentation），即把每个目标像素分割出来。而 Faster R-CNN 和 FCN 在之前介绍过，所以本文的重点将会放在损失函数的设计和 ROI-Align 上。&lt;/p&gt;
&lt;p&gt;实体分割不同于语义分割的是，不仅要检测出所属类别，还要区分同一类别下的不同实例。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://z3.ax1x.com/2021/05/25/gxXEss.png&quot; alt&gt;&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="CV" scheme="https://muyuuuu.github.io/tags/CV/"/>
    
  </entry>
  
  <entry>
    <title>分割篇：开山之作 FCN</title>
    <link href="https://muyuuuu.github.io/2021/05/24/FCN/"/>
    <id>https://muyuuuu.github.io/2021/05/24/FCN/</id>
    <published>2021-05-24T07:04:53.000Z</published>
    <updated>2021-05-25T02:21:49.591Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>CV 系列的论文和程序得一点点开坑了。目前准备的计划任务是：FCN，OHEM，Mask RCNN，YOLO，Focal loss，Seesaw loss。别问，问就是网上一点点查阅得到的，然后写写代码。这个系列完结后，大概会结合对抗样本 and 目标检测做一些东西。自己还是差的太远。</p><a id="more"></a><h1 id="什么是语义分割"><a href="#什么是语义分割" class="headerlink" title="什么是语义分割"></a>什么是语义分割</h1><p>语义分割的直观解释可以见下图，计算照片中的每一个像素点的类别，进而得到哪些像素点属于同一类，把一些物体给分割出来：</p><p><img data-src="http://mi.eng.cam.ac.uk/research/projects/VideoRec/CamVid/pr/DBOverview1_1_huff_0000964.jpg" alt></p><h1 id="FCN"><a href="#FCN" class="headerlink" title="FCN"></a>FCN</h1><p>CNN 能够对图片进行分类，可是怎么样才能识别图片中特定部分的物体，在这篇论文之前，还是一个未解难题。</p><ul><li>对于传统的分类网络，经过 CNN 不断卷积、池化的处理，最后进入全连接网络，预测当前图片的分类。但会丢失空间信息，无法预测每个像素的分类。</li><li>对于目标检测的网络，也是经过 CNN 不断卷积、池化的处理，在最后的特征图上预测类别和位置。但识别出来的是目标框，并非物体的轮廓边界。</li></ul><p>而 FCN 的创新之处在于，使用卷积操作替换了分类网络的全连接，使得输入输出保持在相同尺寸，这样就可以预测每个像素点的类别。加上使用了卷积，自然而然也就可以处理任意尺寸的图像。网络结构如下，用下面的卷积替换上面的全连接：</p><p><img data-src="https://z3.ax1x.com/2021/05/24/gvGwrQ.png" alt></p><h1 id="上采样"><a href="#上采样" class="headerlink" title="上采样"></a>上采样</h1><p>经过不断的卷积，图像的尺寸会减少而维度会增加。所以为了使得网络输出的图像尺寸和原图像一致，需要进行一些上采样，使它恢复到输入图像相同的尺寸，从而可以对每个像素都产生了一个预测, 同时保留了原始输入图像中的空间信息, 最后在与输入图等大小的特征图上对每个像素进行分类，逐像素地用 softmax 分类计算损失，相当于每个像素对应一个训练样本。这部分在论文的第三章有所描述。</p><p>而上采样采用的操作是<strong>转置卷积</strong>，如下图 <sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup> 所示，蓝色是输入，青色是输出，将图像的尺寸瞬间增加了一倍。而文中发现，这种形式的上采样是最有效的，且，可以通过叠加之前的输入（类此残差），获得更好的精度。此外文中特意表明了转置卷积也是卷积层，按照普通的卷积层进行训练即可。</p><p><img data-src="https://z3.ax1x.com/2021/05/24/gvNSo9.gif" alt></p><h1 id="融合操作"><a href="#融合操作" class="headerlink" title="融合操作"></a>融合操作</h1><p><img data-src="https://z3.ax1x.com/2021/05/24/gva3Vg.png" alt></p><p>如上图所示，论文给出了 FCN 的三种版本。</p><ul><li>对于 FCN-32s，直接在最后一层进行 32 倍的上采样，原始空间信息倍大量丢失</li><li>对于 FCN-16s，将 pool5 后的结果进行 2 倍上采样，与 pool4 的结果相加，得到结果 $F$，而后进行 16 倍上采样</li><li>对于 FCN-8s，将 $F$ 与 pool3 后的结果相加，而后进行 8 倍上采样</li></ul><p>论文中的结论是，FCN-8s 的效果要好一些，毕竟更多的利用了原始空间信息。网络结构图如下 <sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup> ：</p><p><img data-src="https://z3.ax1x.com/2021/05/24/gvwICd.png" alt></p><h1 id="程序"><a href="#程序" class="headerlink" title="程序"></a>程序</h1><p>网上看到了份程序，逻辑写的还不错：</p><p><a href="https://github.com/pochih/FCN-pytorch/blob/master/python/fcn.py" target="_blank" rel="noopener">https://github.com/pochih/FCN-pytorch/blob/master/python/fcn.py</a></p><p>尺寸在我裁剪图片的时候进行了放缩，不要太在意。</p><p><img data-src="https://z3.ax1x.com/2021/05/24/gvBWfH.png" alt></p><h2 id="解码"><a href="#解码" class="headerlink" title="解码"></a>解码</h2><p>若要可视化展示结果，需要对网络输出的结果进行解码。如标注图片上的类别等。假设输入图像的尺寸是 [800, 800] 的，当前类别数量是 21，会得到 [bacthsize, num_classes, height, width] 的输出。假设当前 batchsize 是 1，那么就需要在 num_classes 张 [height, width] 大小的图片中选择出每个像素点的类别。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">out = fcn(data)[<span class="string">'out'</span>]</span><br><span class="line"><span class="comment"># 选择每个像素点的最大的类别</span></span><br><span class="line">om = torch.argmax(out.squeeze(), dim=<span class="number">0</span>).detach().cpu().numpy()</span><br><span class="line">decode_segmap(om)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">decode_segmap</span><span class="params">(image, nc=<span class="number">21</span>)</span>:</span></span><br><span class="line">    <span class="comment"># 类别颜色，画图用</span></span><br><span class="line">    label_colors = np.array([</span><br><span class="line">        (<span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>),  <span class="comment"># 0=background</span></span><br><span class="line">        <span class="comment"># 1=aeroplane, 2=bicycle, 3=bird, 4=boat, 5=bottle</span></span><br><span class="line">        (<span class="number">128</span>, <span class="number">0</span>, <span class="number">0</span>),</span><br><span class="line">        (<span class="number">0</span>, <span class="number">128</span>, <span class="number">0</span>),</span><br><span class="line">        (<span class="number">128</span>, <span class="number">128</span>, <span class="number">0</span>),</span><br><span class="line">        (<span class="number">0</span>, <span class="number">0</span>, <span class="number">128</span>),</span><br><span class="line">        (<span class="number">128</span>, <span class="number">0</span>, <span class="number">128</span>),</span><br><span class="line">        <span class="comment"># 6=bus, 7=car, 8=cat, 9=chair, 10=cow</span></span><br><span class="line">        (<span class="number">0</span>, <span class="number">128</span>, <span class="number">128</span>),</span><br><span class="line">        (<span class="number">128</span>, <span class="number">128</span>, <span class="number">128</span>),</span><br><span class="line">        (<span class="number">64</span>, <span class="number">0</span>, <span class="number">0</span>),</span><br><span class="line">        (<span class="number">192</span>, <span class="number">0</span>, <span class="number">0</span>),</span><br><span class="line">        (<span class="number">64</span>, <span class="number">128</span>, <span class="number">0</span>),</span><br><span class="line">        <span class="comment"># 11=dining table, 12=dog, 13=horse, 14=motorbike, 15=person</span></span><br><span class="line">        (<span class="number">192</span>, <span class="number">128</span>, <span class="number">0</span>),</span><br><span class="line">        (<span class="number">64</span>, <span class="number">0</span>, <span class="number">128</span>),</span><br><span class="line">        (<span class="number">192</span>, <span class="number">0</span>, <span class="number">128</span>),</span><br><span class="line">        (<span class="number">64</span>, <span class="number">128</span>, <span class="number">128</span>),</span><br><span class="line">        (<span class="number">192</span>, <span class="number">128</span>, <span class="number">128</span>),</span><br><span class="line">        <span class="comment"># 16=potted plant, 17=sheep, 18=sofa, 19=train, 20=tv/monitor</span></span><br><span class="line">        (<span class="number">0</span>, <span class="number">64</span>, <span class="number">0</span>),</span><br><span class="line">        (<span class="number">128</span>, <span class="number">64</span>, <span class="number">0</span>),</span><br><span class="line">        (<span class="number">0</span>, <span class="number">192</span>, <span class="number">0</span>),</span><br><span class="line">        (<span class="number">128</span>, <span class="number">192</span>, <span class="number">0</span>),</span><br><span class="line">        (<span class="number">0</span>, <span class="number">64</span>, <span class="number">128</span>)</span><br><span class="line">    ])</span><br><span class="line">    r = np.zeros_like(image).astype(np.uint8)</span><br><span class="line">    g = np.zeros_like(image).astype(np.uint8)</span><br><span class="line">    b = np.zeros_like(image).astype(np.uint8)</span><br><span class="line">    <span class="keyword">for</span> l <span class="keyword">in</span> range(<span class="number">0</span>, nc):</span><br><span class="line">        <span class="comment"># 目标类的索引</span></span><br><span class="line">        idx = image == l</span><br><span class="line">        r[idx] = label_colors[l, <span class="number">0</span>]</span><br><span class="line">        g[idx] = label_colors[l, <span class="number">1</span>]</span><br><span class="line">        b[idx] = label_colors[l, <span class="number">2</span>]</span><br><span class="line">    rgb = np.stack([r, g, b], axis=<span class="number">2</span>)</span><br><span class="line">    plt.imshow(rgb)</span><br><span class="line">    plt.savefig(<span class="string">'result.png'</span>)</span><br></pre></td></tr></table></figure><h1 id="延伸"><a href="#延伸" class="headerlink" title="延伸"></a>延伸</h1><p>此外，U-Net <sup id="fnref:5"><a href="#fn:5" rel="footnote">5</a></sup> 的网络结构也适合做分割，先记下来，也许某一天做语义分割的任务会用到。</p><p><img data-src="https://z3.ax1x.com/2021/05/24/gv6rd0.png" alt></p><h1 id="reference"><a href="#reference" class="headerlink" title="reference"></a>reference</h1><p><sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup>：<a href="https://arxiv.org/abs/1411.4038" target="_blank" rel="noopener">Fully Convolutional Networks for Semantic Segmentation</a><br><sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup>：<a href="https://github.com/vdumoulin/conv_arithmetic" target="_blank" rel="noopener">卷积动画</a><br><sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup>：<a href="http://flyrie.top/2018/06/22/%E5%85%A8%E5%8D%B7%E7%A7%AF%E7%BD%91%E7%BB%9C%EF%BC%88FCN%EF%BC%89/" target="_blank" rel="noopener">FCN-8s 结构图</a><br><sup id="fnref:4"><a href="#fn:4" rel="footnote">4</a></sup>：<a href="https://learnopencv.com/pytorch-for-beginners-semantic-segmentation-using-torchvision/" target="_blank" rel="noopener">分割后的图片解码</a><br><sup id="fnref:5"><a href="#fn:5" rel="footnote">5</a></sup>：<a href="https://github.com/milesial/Pytorch-UNet" target="_blank" rel="noopener">U-Net 网络实现</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;CV 系列的论文和程序得一点点开坑了。目前准备的计划任务是：FCN，OHEM，Mask RCNN，YOLO，Focal loss，Seesaw loss。别问，问就是网上一点点查阅得到的，然后写写代码。这个系列完结后，大概会结合对抗样本 and 目标检测做一些东西。自己还是差的太远。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="CV" scheme="https://muyuuuu.github.io/tags/CV/"/>
    
  </entry>
  
  <entry>
    <title>目标检测篇：自定义 MMDetection 的 Pipeline</title>
    <link href="https://muyuuuu.github.io/2021/05/20/MMdetection-pipeline/"/>
    <id>https://muyuuuu.github.io/2021/05/20/MMdetection-pipeline/</id>
    <published>2021-05-20T07:57:34.000Z</published>
    <updated>2021-05-23T06:06:52.388Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>经过几天连续的开坑和读源代码，对 mmdetection 的配置流程了解的差不多了。考虑一个实例应用，尝试着将 <code>FGSM</code> 攻击算法的制作的对抗样本植入目标检测中，企图增加网络的鲁棒性，也就是一个自定义输出处理 <code>Pipeline</code> 的实际流程。之后会尝试自定义损失函数，支持简单的对抗训练，如 MART 算法等 <sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup>。</p><a id="more"></a><h1 id="对抗攻击之植入-mmdetection"><a href="#对抗攻击之植入-mmdetection" class="headerlink" title="对抗攻击之植入 mmdetection"></a>对抗攻击之植入 mmdetection</h1><p>众所周知，攻击算法基于原始样本制作一种含有梯度的扰动信息，将扰动信息叠加至原始样本，就得到了对抗样本。因此，对抗样本应该添加在数据处理的 pipeline 中，而不是网络层。首先实现最简单的对抗训练：</p><p>\begin{equation}<br>\min_\theta \biggl( \max \Bigl( l \bigl(T_\theta(x’), y_i \bigl) \Bigr) \biggr)<br>\end{equation}</p><p>内部的对抗样本 $x’$ 企图最大化模型的分类误差，外部的训练企图最小化对抗样本带来的误差。查看训练数据的 pipeline，我们发现是在最后一步转为 tensor，而攻击算法是在tensor上进行操作的。所以，对抗攻击应该加在最后一步之后或之前。我们查看返回 tensor 的源代码。此部分代码在 <code>mmdet/datasets/pipelines</code> 目录下。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">__call__</span><span class="params">(self, results)</span>:</span></span><br><span class="line">    <span class="string">"""Call function to transform and format common fields in results.</span></span><br><span class="line"><span class="string">    Args:</span></span><br><span class="line"><span class="string">        results (dict): Result dict contains the data to convert.</span></span><br><span class="line"><span class="string">    Returns:</span></span><br><span class="line"><span class="string">        dict: The result dict contains the data that is formatted with \</span></span><br><span class="line"><span class="string">            default bundle.</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> <span class="string">'img'</span> <span class="keyword">in</span> results:</span><br><span class="line">        img = results[<span class="string">'img'</span>]</span><br><span class="line">        <span class="comment"># add default meta keys</span></span><br><span class="line">        results = self._add_default_meta_keys(results)</span><br><span class="line">        <span class="keyword">if</span> len(img.shape) &lt; <span class="number">3</span>:</span><br><span class="line">            img = np.expand_dims(img, <span class="number">-1</span>)</span><br><span class="line">        img = np.ascontiguousarray(img.transpose(<span class="number">2</span>, <span class="number">0</span>, <span class="number">1</span>))</span><br><span class="line">        results[<span class="string">'img'</span>] = DC(to_tensor(img), stack=<span class="literal">True</span>)</span><br><span class="line">    <span class="keyword">for</span> key <span class="keyword">in</span> [<span class="string">'proposals'</span>, <span class="string">'gt_bboxes'</span>, <span class="string">'gt_bboxes_ignore'</span>, <span class="string">'gt_labels'</span>]:</span><br><span class="line">        <span class="keyword">if</span> key <span class="keyword">not</span> <span class="keyword">in</span> results:</span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line">        results[key] = DC(to_tensor(results[key]))</span><br><span class="line">    <span class="keyword">if</span> <span class="string">'gt_masks'</span> <span class="keyword">in</span> results:</span><br><span class="line">        results[<span class="string">'gt_masks'</span>] = DC(results[<span class="string">'gt_masks'</span>], cpu_only=<span class="literal">True</span>)</span><br><span class="line">    <span class="keyword">if</span> <span class="string">'gt_semantic_seg'</span> <span class="keyword">in</span> results:</span><br><span class="line">        results[<span class="string">'gt_semantic_seg'</span>] = DC(</span><br><span class="line">            to_tensor(results[<span class="string">'gt_semantic_seg'</span>][<span class="literal">None</span>, ...]), stack=<span class="literal">True</span>)</span><br><span class="line">    <span class="keyword">return</span> results</span><br></pre></td></tr></table></figure><p>而对抗攻击只关注图像与标签，所以，只关注图像与标签，而这部分一目了然。所以，开始写自己的对抗样本：</p><ul><li>黑盒攻击，基于 resnet18 制作攻击样本，插入到 pipeline 中</li><li>我暂时的想法是只攻击目标区域，所以筛选出目标区域的位置与标签，按照梯度上升的方向制作对抗样本。</li></ul><h1 id="自定义-backbone"><a href="#自定义-backbone" class="headerlink" title="自定义 backbone"></a>自定义 backbone</h1><p><strong>注意这步不做也行</strong>。可以直接模改原有的 <code>backbone</code>，只是不推荐。而为了方便调试，我们使用自己定义的网络，网络只打印输入观察数据是否修改成功。因此，自定义的网络如下，并放在 <code>mmdet/models/backbones/mybackbone</code>下，</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"><span class="keyword">from</span> ..builder <span class="keyword">import</span> BACKBONES</span><br><span class="line"></span><br><span class="line"><span class="meta">@BACKBONES.register_module()</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">mynet</span><span class="params">(nn.Module)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">pass</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span><span class="params">(self, x)</span>:</span></span><br><span class="line">        print(x)</span><br></pre></td></tr></table></figure><p>而后为了导入模块，在 <code>mmdet/models/backbones/__init__.py</code> 中添加 <code>from .mybackbone import mynet</code>。为了适配 faster rcnn 的其它参数，我这里的 backbone 直接使用了 ResNet 里面的内容，只是打印了 x，这里是为了方便观察结果。这里需要注意，那些 <code>super</code> 初始化也要改，防止重名，因为不具备普适性，所以这里就不演示了。而后配置文件中指定以下就可以自由使用，我的配置文件是 <code>adv_faster_rcnn.py</code>：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">model = dict(</span><br><span class="line">    ...</span><br><span class="line">    backbone=dict(</span><br><span class="line">        type=<span class="string">'mynet'</span>,</span><br><span class="line">        arg1=xxx,</span><br><span class="line">        arg2=xxx),</span><br><span class="line">    ...</span><br><span class="line"><span class="comment"># 测试用，跑一轮就停</span></span><br><span class="line">runner = dict(type=<span class="string">'EpochBasedRunner'</span>, max_epochs=<span class="number">1</span>)</span><br></pre></td></tr></table></figure><p>而后执行一下，成功的看到输入被打印了出来。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> mmdet.apis <span class="keyword">import</span> init_detector, inference_detector</span><br><span class="line"><span class="comment"># 目标检测配置文件</span></span><br><span class="line">config_file = <span class="string">'mmdetection/configs/faster_rcnn/adv_faster_rcnn.py'</span></span><br><span class="line"><span class="comment"># 训练模型</span></span><br><span class="line">checkpoint_file = <span class="string">'mmdetection/checkpoints/faster_rcnn_r50_fpn_1x_coco_20200130-047c8118.pth'</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置模型</span></span><br><span class="line">model = init_detector(config=config_file,</span><br><span class="line">                      checkpoint=checkpoint_file,</span><br><span class="line">                      device=<span class="string">'cuda:0'</span>)</span><br><span class="line"></span><br><span class="line">img = <span class="string">'a.png'</span></span><br><span class="line"><span class="comment">#  推理实际调用语句</span></span><br><span class="line"><span class="comment"># results = model(return_loss=False, rescale=True, **data)</span></span><br><span class="line">result = inference_detector(model=model, imgs=img)</span><br></pre></td></tr></table></figure><h1 id="对抗样本代码思想"><a href="#对抗样本代码思想" class="headerlink" title="对抗样本代码思想"></a>对抗样本代码思想</h1><p>首先在 <code>pipeline</code> 文件夹下创建自己的 <code>my_pipeline</code>，而后定义自己的处理流程，比如我就看看处理的数据都有哪些：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> mmdet.datasets <span class="keyword">import</span> PIPELINES</span><br><span class="line"></span><br><span class="line"><span class="meta">@PIPELINES.register_module()</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyTransform</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__call__</span><span class="params">(self, results)</span>:</span></span><br><span class="line">        print(type(results))</span><br><span class="line">        <span class="keyword">for</span> key <span class="keyword">in</span> results:</span><br><span class="line">            print(key, type(results[key]))</span><br><span class="line">        <span class="keyword">return</span> results</span><br></pre></td></tr></table></figure><p>最后在 <code>__init__.py</code> 中导入自己的东西，<code>from .my_pipeline import MyTransform</code>。综上发现，在 <code>DefaultFormatBundle</code> 和 <code>Collect</code> 处理后，数据类型不是我能驾驭的常见数据类型： <code>mmcv.parallel.data_container.DataContainer</code>。</p><p>所以选择在 <code>Normalize</code> 后加入对抗攻击，因为常见的攻击算法也都是在标准化之后，此外那里还是 <code>ndarray</code> 等常见类型，我能把握住这些基本的类似那个。<code>Normalize</code> 后的数据类型：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">img_prefix      &lt;<span class="class"><span class="keyword">class</span> '<span class="title">str</span>'&gt;</span></span><br><span class="line"><span class="class"><span class="title">seg_prefix</span>      &lt;<span class="title">class</span> '<span class="title">NoneType</span>'&gt;</span></span><br><span class="line"><span class="class"><span class="title">proposal_file</span>   &lt;<span class="title">class</span> '<span class="title">NoneType</span>'&gt;</span></span><br><span class="line"><span class="class"><span class="title">bbox_fields</span>     &lt;<span class="title">class</span> '<span class="title">list</span>'&gt;</span></span><br><span class="line"><span class="class"><span class="title">mask_fields</span>     &lt;<span class="title">class</span> '<span class="title">list</span>'&gt;</span></span><br><span class="line"><span class="class"><span class="title">seg_fields</span>      &lt;<span class="title">class</span> '<span class="title">list</span>'&gt;</span></span><br><span class="line"><span class="class"><span class="title">filename</span>        &lt;<span class="title">class</span> '<span class="title">str</span>'&gt;</span></span><br><span class="line"><span class="class"><span class="title">ori_filename</span>    &lt;<span class="title">class</span> '<span class="title">str</span>'&gt;</span></span><br><span class="line"><span class="class"><span class="title">img</span>             &lt;<span class="title">class</span> '<span class="title">numpy</span>.<span class="title">ndarray</span>'&gt;</span></span><br><span class="line"><span class="class"><span class="title">img_shape</span>       &lt;<span class="title">class</span> '<span class="title">tuple</span>'&gt;</span></span><br><span class="line"><span class="class"><span class="title">ori_shape</span>       &lt;<span class="title">class</span> '<span class="title">tuple</span>'&gt;</span></span><br><span class="line"><span class="class"><span class="title">img_fields</span>      &lt;<span class="title">class</span> '<span class="title">list</span>'&gt;</span></span><br><span class="line"><span class="class"><span class="title">gt_bboxes</span>       &lt;<span class="title">class</span> '<span class="title">numpy</span>.<span class="title">ndarray</span>'&gt;</span></span><br><span class="line"><span class="class"><span class="title">gt_bboxes_ignore</span>        &lt;<span class="title">class</span> '<span class="title">numpy</span>.<span class="title">ndarray</span>'&gt;</span></span><br><span class="line"><span class="class"><span class="title">gt_labels</span>       &lt;<span class="title">class</span> '<span class="title">numpy</span>.<span class="title">ndarray</span>'&gt;</span></span><br><span class="line"><span class="class"><span class="title">flip</span>            &lt;<span class="title">class</span> '<span class="title">bool</span>'&gt;</span></span><br><span class="line"><span class="class"><span class="title">flip_direction</span>  &lt;<span class="title">class</span> '<span class="title">NoneType</span>'&gt;</span></span><br><span class="line"><span class="class"><span class="title">img_norm_cfg</span>    &lt;<span class="title">class</span> '<span class="title">dict</span>'&gt;</span></span><br></pre></td></tr></table></figure><p>我利用的信息只有 <code>img</code>，<code>gt_labels</code> 和 <code>gt_bboxes</code>。其实我也不知道以上字段是啥，源码和文档的信息很少，所以我只能自己都打印了一下。所以此时的任务就是，按照 <code>gt_bboxes</code>，截取 <code>img</code>，根据 <code>gt_labels</code>，制作对抗样本。但对抗样本返回的是 tensor，所以最后要转回到 numpy，在覆盖原来的数据。而 PGD、CW 等攻击算法过程会很慢，所以选用攻击强度大且快捷的 <code>FGSM</code> 单步算法。</p><p>而为了防止对抗样本带来的过大负担，所以添加了两个额外参数 $p_1$ 和 $p_2$。</p><ul><li>如果概率小于 $p_1$，在目标区域叠加高斯噪音</li><li>如果概率在 $p_1$ 到 $p_2$ 之间，在目标区域制作对抗样本</li><li>如果概率大于 $p_2$，使用原图，不做任何处理</li></ul><p>综上，此时的配置文件应该为：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">adv_para = dict(mu=<span class="number">0</span>, std=<span class="number">0.1</span>, epsilon=<span class="number">0.1</span>, pro1=<span class="number">0.3</span>, pro2=<span class="number">0.6</span>, adv=<span class="string">'fgsm'</span>)</span><br><span class="line"></span><br><span class="line">train_pipeline = [</span><br><span class="line">    dict(type=<span class="string">'LoadImageFromFile'</span>),</span><br><span class="line">    dict(type=<span class="string">'LoadAnnotations'</span>, with_bbox=<span class="literal">True</span>),</span><br><span class="line">    dict(type=<span class="string">'Resize'</span>, img_scale=(<span class="number">1333</span>, <span class="number">800</span>), keep_ratio=<span class="literal">True</span>),</span><br><span class="line">    dict(type=<span class="string">'RandomFlip'</span>, flip_ratio=<span class="number">0.0001</span>),</span><br><span class="line">    dict(type=<span class="string">'Normalize'</span>, **img_norm_cfg_trian),</span><br><span class="line">    <span class="comment"># 自己的 pipeline</span></span><br><span class="line">    dict(type=<span class="string">'advTransform'</span>, **adv_para),</span><br><span class="line">    dict(type=<span class="string">'Pad'</span>, size_divisor=<span class="number">32</span>),</span><br><span class="line">    dict(type=<span class="string">'DefaultFormatBundle'</span>),</span><br><span class="line">    dict(type=<span class="string">'Collect'</span>, keys=[<span class="string">'img'</span>, <span class="string">'gt_bboxes'</span>, <span class="string">'gt_labels'</span>]),</span><br><span class="line">]</span><br></pre></td></tr></table></figure><h1 id="开始制作"><a href="#开始制作" class="headerlink" title="开始制作"></a>开始制作</h1><p>为了使代码易于维护和扩展，我尽力使额外添加的程序符合设计模式，对扩展开放，对修改封闭，针对接口编程。</p><ul><li>在 <code>mmdet/datasets/pipelines/</code> 目录下增加 <code>adv_example</code> pipeline，生成对抗样本。</li><li>至于攻击算法，依据设计模式，应使用额外的类来实现。位于 <code>datasets/</code> 目录下，命名为 attack 文件夹。</li></ul><h2 id="对抗样本-pipeline"><a href="#对抗样本-pipeline" class="headerlink" title="对抗样本 pipeline"></a>对抗样本 pipeline</h2><p>这里有几个点需要注意下：</p><ul><li>通过获得的 <code>results</code>，图像的维度是：高、宽、通道</li><li><code>bboxes</code> 中的信息是 <code>x1,x2,y1,y2</code> </li></ul><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> mmdet.datasets <span class="keyword">import</span> PIPELINES</span><br><span class="line"><span class="comment"># 导入攻击算法</span></span><br><span class="line"><span class="keyword">from</span> mmdet.datasets <span class="keyword">import</span> attack</span><br><span class="line"><span class="keyword">import</span> random</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@PIPELINES.register_module()</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">advTransform</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self,</span></span></span><br><span class="line"><span class="function"><span class="params">                 mu=<span class="number">0</span>,</span></span></span><br><span class="line"><span class="function"><span class="params">                 std=<span class="number">0.1</span>,</span></span></span><br><span class="line"><span class="function"><span class="params">                 epsilon=<span class="number">0.1</span>,</span></span></span><br><span class="line"><span class="function"><span class="params">                 pro1=<span class="number">0.3</span>,</span></span></span><br><span class="line"><span class="function"><span class="params">                 pro2=<span class="number">0.6</span>,</span></span></span><br><span class="line"><span class="function"><span class="params">                 adv=<span class="string">'fgsm'</span>)</span>:</span></span><br><span class="line">        <span class="comment"># 高斯噪音的均值和方差</span></span><br><span class="line">        <span class="keyword">if</span> isinstance(mu, int) <span class="keyword">or</span> isinstance(mu, float):</span><br><span class="line">            self.mu = mu</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            self.mu = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> isinstance(mu, int) <span class="keyword">or</span> isinstance(mu, float):</span><br><span class="line">            self.std = std</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            self.std = <span class="number">0.1</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 对抗扰动值</span></span><br><span class="line">        <span class="keyword">if</span> isinstance(mu, int) <span class="keyword">or</span> isinstance(mu, float):</span><br><span class="line">            self.epsilon = epsilon</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            self.epsilon = <span class="number">0.5</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 概率 p1 和 p2</span></span><br><span class="line">        <span class="keyword">if</span> isinstance(pro1, float):</span><br><span class="line">            self.pro1 = pro1</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            self.pro1 = <span class="number">0.3</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> isinstance(pro2, float):</span><br><span class="line">            self.pro2 = pro2</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            self.pro2 = <span class="number">0.6</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 使用的攻击算法</span></span><br><span class="line">        <span class="keyword">if</span> isinstance(adv, str):</span><br><span class="line">            self.adv = adv</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            self.adv = <span class="string">'fgsm'</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 产生一个随机数</span></span><br><span class="line">        <span class="comment"># 如果位于区间 [0, pro1), 目标区域添加噪音</span></span><br><span class="line">        <span class="comment"># 如果位于区间 [pro1, pro2), 目标区域用 adv 算法攻击</span></span><br><span class="line">        <span class="comment"># 如果区间位于 [pro2, 1) 不做任何操作，返回原图</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">assert</span> <span class="number">0</span> &lt; pro1 &lt; pro2 &lt; <span class="number">1</span></span><br><span class="line"></span><br><span class="line">        self.pro1 = pro1</span><br><span class="line">        self.pro2 = pro2</span><br><span class="line"></span><br><span class="line">        self.rand_ = random.random()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 复写这个方法</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__call__</span><span class="params">(self, results)</span>:</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># print(len(results['gt_bboxes']))</span></span><br><span class="line">        <span class="comment"># print(len(results['gt_labels']))</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 返回原图</span></span><br><span class="line">        <span class="keyword">if</span> self.rand_ &gt; self.pro2:</span><br><span class="line">            <span class="keyword">return</span> results</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 目标区域叠加高斯噪音</span></span><br><span class="line">        <span class="keyword">elif</span> self.rand_ &lt; self.pro1:</span><br><span class="line">            <span class="comment"># 可能有好几个盒子</span></span><br><span class="line">            bboxes = results[<span class="string">'gt_bboxes'</span>]</span><br><span class="line">            img = results[<span class="string">'img'</span>]</span><br><span class="line"></span><br><span class="line">            <span class="keyword">for</span> box <span class="keyword">in</span> bboxes:</span><br><span class="line">                box = box.tolist()</span><br><span class="line">                box = [int(i) <span class="keyword">for</span> i <span class="keyword">in</span> box]</span><br><span class="line">                x1, y1, x2, y2 = box[<span class="number">0</span>], box[<span class="number">1</span>], box[<span class="number">2</span>], box[<span class="number">3</span>]</span><br><span class="line">                noise = np.random.normal(</span><br><span class="line">                    self.mu, self.std, size=(y2 - y1, x2 - x1, <span class="number">3</span>))</span><br><span class="line">                img[y1:y2, x1:x2] += noise</span><br><span class="line"></span><br><span class="line">            results[<span class="string">'img'</span>] = img</span><br><span class="line">            <span class="keyword">return</span> results</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 对抗攻击</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="comment"># labels = results['ann_info']['labels']</span></span><br><span class="line">            <span class="comment"># bboxes = results['ann_info']['bboxes']</span></span><br><span class="line">            labels = results[<span class="string">'gt_labels'</span>]</span><br><span class="line">            bboxes = results[<span class="string">'gt_bboxes'</span>]</span><br><span class="line">            img = results[<span class="string">'img'</span>]</span><br><span class="line">            <span class="comment"># 针对接口编程，只需要给攻击算法提供 图像、位置、标签和扰动值</span></span><br><span class="line">            img = attack.fgsm.fgsm_attack(img, bboxes, labels, self.epsilon)</span><br><span class="line">            results[<span class="string">'img'</span>] = img</span><br><span class="line">            <span class="keyword">return</span> results</span><br></pre></td></tr></table></figure><h2 id="攻击算法"><a href="#攻击算法" class="headerlink" title="攻击算法"></a>攻击算法</h2><p>攻击算法位于 <code>datasets/attack/</code> 文件夹下。此外，为了导入包，需要添加 <code>__init__.py</code>，并按以下格式添加内容：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> .fgsm <span class="keyword">import</span> fgsm_attack</span><br><span class="line"></span><br><span class="line">__all__ = [<span class="string">'fgsm_attack'</span>]</span><br></pre></td></tr></table></figure><p>同样这里也有一些需要注意的地方：</p><ul><li>针对接口编程，只需要给攻击算法提供图像、目标区域、标签和扰动值，至于内部自己如何实现，不重要</li><li>攻击算法返回的就是图像，内部如何实现，不重要，减少两个模块的耦合</li></ul><p><code>fgsm.py</code> 的内容为：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">fgsm_attack</span><span class="params">(img, bboxes, labels, epsilon)</span>:</span></span><br><span class="line">    <span class="comment"># data_grad 转 tensor</span></span><br><span class="line">    <span class="keyword">import</span> torch</span><br><span class="line">    <span class="keyword">import</span> torch.nn.functional <span class="keyword">as</span> F</span><br><span class="line">    model = torch.hub.load(</span><br><span class="line">        <span class="string">'pytorch/vision:v0.9.0'</span>, <span class="string">'resnet18'</span>, pretrained=<span class="literal">True</span>)</span><br><span class="line">    model.eval()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 增加 batch 维度，然后 channel first</span></span><br><span class="line">    tmp_img = torch.from_numpy(img).clone().unsqueeze(dim=<span class="number">0</span>).transpose(</span><br><span class="line">        <span class="number">1</span>, <span class="number">3</span>).transpose(<span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># tenor 转 numpy</span></span><br><span class="line">    <span class="keyword">for</span> box, target <span class="keyword">in</span> zip(bboxes, labels):</span><br><span class="line">        <span class="comment"># numpy 2 list</span></span><br><span class="line">        box = box.tolist()</span><br><span class="line">        box = [int(i) <span class="keyword">for</span> i <span class="keyword">in</span> box]</span><br><span class="line">        <span class="comment"># print(box, target)</span></span><br><span class="line">        x1, y1, x2, y2 = box[<span class="number">0</span>], box[<span class="number">1</span>], box[<span class="number">2</span>], box[<span class="number">3</span>]</span><br><span class="line"></span><br><span class="line">        <span class="comment"># deep copy</span></span><br><span class="line">        input_ = tmp_img[:, :, y1:y2, x1:x2].clone()</span><br><span class="line">        input_.requires_grad = <span class="literal">True</span></span><br><span class="line">        label = torch.tensor([int(target)])</span><br><span class="line"></span><br><span class="line">        output = model(input_)</span><br><span class="line"></span><br><span class="line">        loss = F.nll_loss(output, label)</span><br><span class="line">        <span class="comment"># model.zero_grad()</span></span><br><span class="line">        loss.backward()</span><br><span class="line">        sign_data = input_.grad.data.sign()</span><br><span class="line">        <span class="comment"># 脱离计算图</span></span><br><span class="line">        perturbed_image = input_.detach() + epsilon * sign_data</span><br><span class="line">        <span class="comment"># 指定区域生成对抗样本</span></span><br><span class="line">        tmp_img[:, :, y1:y2, x1:x2] += perturbed_image</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 删除 batch 维度</span></span><br><span class="line">    tmp_img = tmp_img.squeeze().detach().cpu().numpy().transpose(<span class="number">1</span>, <span class="number">2</span>, <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> tmp_img</span><br></pre></td></tr></table></figure><h1 id="踩坑记录"><a href="#踩坑记录" class="headerlink" title="踩坑记录"></a>踩坑记录</h1><ul><li>也许你看过一些对抗攻击的算法，知道最后的对抗样本应该 <code>torch.clamp</code> 到 $[0,1]$。可那是论文里用的玩具数据集才会做的事情，那些数据集知道均值和方差。现实世界的真实数据，又怎么会知道均值和方差，怎么去标准化到 $[0,1]$ 之间呢？</li><li>我遇到了一个佷头疼的 bug，是多线程导致的反向传播异常，大概错误信息是：terminate called after throwing an instance of ‘c10::Error what():  CUDA error: initialization error。网上翻阅了无数 bug issue，才找到一篇有用的，也不知道那些 github 仓库的作者咋想的，问题还没解决就 close 掉，冲业绩还是图仓库 bug 少？这里把每个 GPU 的线程数量设为 0 就可以了 <sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup> 。而至于写多线程下的梯度反向传播，我貌似还没这个本事。<strong>但是，我服务器上没发现有这个 bug，应该是版本问题。</strong></li></ul><h1 id="程序"><a href="#程序" class="headerlink" title="程序"></a>程序</h1><p>官方的程序如何使用，我的就怎么使用，毕竟是直接 <code>fork</code> 过来的，从安装一步步来就行。此外，为了方便使用，我把配置文件放到了 <code>configs/faster_rcnn/faster_rcnn_r50_fpn_1x_coco.py</code> 文件中，可以进去参观下。</p><p><a href="https://github.com/muyuuuu/mmdetection" target="_blank" rel="noopener">https://github.com/muyuuuu/mmdetection</a></p><p>此外，我在 colab 也创建了一份能用的，在不会用就真没救了。</p><p><a href="https://github.com/muyuuuu/open-mmlab-colab/blob/main/Detection/mmdet_adv.ipynb" target="_blank" rel="noopener">https://github.com/muyuuuu/open-mmlab-colab/blob/main/Detection/mmdet_adv.ipynb</a></p><h1 id="reference"><a href="#reference" class="headerlink" title="reference"></a>reference</h1><p><sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup>：<a href="https://openreview.net/forum?id=rklOg6EFwS" target="_blank" rel="noopener">https://openreview.net/forum?id=rklOg6EFwS</a><br><sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup>：<a href="https://github.com/pytorch/pytorch/issues/1355#issuecomment-299094286" target="_blank" rel="noopener">https://github.com/pytorch/pytorch/issues/1355#issuecomment-299094286</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;经过几天连续的开坑和读源代码，对 mmdetection 的配置流程了解的差不多了。考虑一个实例应用，尝试着将 &lt;code&gt;FGSM&lt;/code&gt; 攻击算法的制作的对抗样本植入目标检测中，企图增加网络的鲁棒性，也就是一个自定义输出处理 &lt;code&gt;Pipeline&lt;/code&gt; 的实际流程。之后会尝试自定义损失函数，支持简单的对抗训练，如 MART 算法等 &lt;sup id=&quot;fnref:1&quot;&gt;&lt;a href=&quot;#fn:1&quot; rel=&quot;footnote&quot;&gt;1&lt;/a&gt;&lt;/sup&gt;。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="CV" scheme="https://muyuuuu.github.io/tags/CV/"/>
    
  </entry>
  
  <entry>
    <title>目标检测篇：MMDetection 推理使用与详细踩坑记录</title>
    <link href="https://muyuuuu.github.io/2021/05/11/MMDetection-use/"/>
    <id>https://muyuuuu.github.io/2021/05/11/MMDetection-use/</id>
    <published>2021-05-11T14:13:23.000Z</published>
    <updated>2021-05-17T13:50:18.313Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>最近喜欢上了听音乐，B 站关注了个 UP 主叫『<strong>咻咻满</strong>』，长得好看，戏腔唱『<strong>青花瓷</strong>』入坑了，也听了其它的『星辰大海』和『白月光和朱砂痣』，都挺好听。以后得关注点女 UP 了，看着多可爱，生活又不是只有代码。卧艹不对说回正题。</p><p>MMDetection 是一个基于 PyTorch 的目标检测开源工具箱 <sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup>，支持了众多主流的和最新的检测算法，例如 Faster R-CNN，Mask R-CNN，RetinaNet 等，官网也给出了详细的教程。既然如此，生命不息，开坑不止。前前后后被各种事情打断，大概花了一周搞懂了如何使用 <code>MMdetection</code> 去做检测的任务。本文收录：</p><ul><li>安装</li><li>修改配置文件</li><li>调用模型与训练好的参数，进行推理</li><li>自定义训练</li><li>数据处理流程</li></ul><p>注意，本文更多像是记录学习过程，这也是我第一次用这个工具，遇到问题一步一步的 debug 与记录，并不是直接的教程。且，本文程序大多能直接复制运行，需要对应到自己的路径。我希望读者看完本文后具有解决问题的能力，而不只是会解决问题。</p><a id="more"></a><h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><p>MMDetection 检测框架包括 mmdetection 和 mmcv，两者是不可分割的。首先安装 mmcv，掏出官方文档 <sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup>，发现有两个版本可供安装，支持 <code>CUDA</code> 操作的 mmcv-full 和不支持 <code>CUDA</code> 的 mmcv，而图片处理显然需要 <code>GPU</code>。所以知道了要安装的是 mmcv-full，之后就是确定安装版本。</p><p>首先查看自己的 <code>CUDA</code> 和 <code>pytorch</code> 版本，然后查阅官方提供的表格，找到对应的安装命令。综上，我本地服务器的安装指令就是（我现在写代码都默认 Linux 系统了，本教程不知道适不适合 windows）</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">pip install mmcv-full -f https://download.openmmlab.com/mmcv/dist/cu92/torch1<span class="number">.7</span><span class="number">.0</span>/index.html</span><br></pre></td></tr></table></figure><p>而后安装 mmdetection，继续掏出官方文档 <sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup>，按着步骤一步一步来：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">git clone https://github.com/open-mmlab/mmdetection.git</span><br><span class="line">cd mmdetection</span><br><span class="line">pip install -r requirements/build.txt</span><br><span class="line">pip install -v -e .</span><br></pre></td></tr></table></figure><p>也就是安装完成后，当前目录会保留一个 <code>mmdetection</code> 的文件夹，一些模型的配置文件都在里面，不要删除这个文件夹。在安装完成后，执行一段代码查看是否安装成功</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> mmdet.apis <span class="keyword">import</span> init_detector, inference_detector</span><br></pre></td></tr></table></figure><h1 id="简单推理"><a href="#简单推理" class="headerlink" title="简单推理"></a>简单推理</h1><p>在网上查阅相关用法时，发现绝大多数教程<strong>已经过时</strong>，软件迭代重构、接口更新很正常。即使某一天本文被骂陈旧过时，我也不会感觉到任何意外。接口问题多查阅官方文档，其余问题可以多用谷歌搜索。但官方文档 <sup id="fnref:4"><a href="#fn:4" rel="footnote">4</a></sup> 写的实在是烂，甚至连返回类型都没写，是 tensor，还是 list。不仅没写类型，输出是什么也没写，是得分，是窗口还是类别，一个字都不肯多说，<strong>以上结论仅限博客发布的日期</strong>。所以只能自己一个一个打印了，然后自己分析了下，结果的形式是多个列表，因为有多个目标。每个列表的形式是盒子 <code>xmin, ymin, xmax, ymax</code> 以及得分。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> mmdet.apis <span class="keyword">import</span> init_detector, inference_detector</span><br><span class="line"></span><br><span class="line"><span class="comment"># 目标检测配置文件</span></span><br><span class="line">config_file = <span class="string">'mmdetection/configs/faster_rcnn/faster_rcnn_r50_fpn_1x_coco.py'</span></span><br><span class="line"><span class="comment"># 训练模型</span></span><br><span class="line">checkpoint_file = <span class="string">'mmdetection/checkpoints/faster_rcnn_r50_fpn_1x_coco_20200130-047c8118.pth'</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置模型</span></span><br><span class="line">model = init_detector(config=config_file,</span><br><span class="line">                      checkpoint=checkpoint_file,</span><br><span class="line">                      device=<span class="string">'cuda:0'</span>)</span><br><span class="line"></span><br><span class="line">img = <span class="string">'woman-3377839_1920.jpg'</span></span><br><span class="line"><span class="comment">#  推理实际调用语句</span></span><br><span class="line"><span class="comment"># results = model(return_loss=False, rescale=True, **data)</span></span><br><span class="line">result = inference_detector(model=model, imgs=img)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 打印结果</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> result:</span><br><span class="line">    print(i)</span><br></pre></td></tr></table></figure><p>在我打印 result 后，发现了一个震惊的消息，只有盒子、概率，没有类别，也就时说代码只能预测当前目标在哪，目标的概率，但不能预测目标是什么 <sup id="fnref:5"><a href="#fn:5" rel="footnote">5</a></sup>，没有类别输出。来看一下官方的回答。我当时属实没看懂，数据集有类别信息？既然有了类别信息还推理干啥…</p><p><img data-src="https://z3.ax1x.com/2021/05/13/gBkChn.png" alt></p><p>没办法接着去翻 github，自己提了个问 <sup id="fnref:6"><a href="#fn:6" rel="footnote">6</a></sup>。才知道 <code>result</code> 的长度就是类别长度，而 <code>print(model.CLASSES)</code> 可以打印类别。比如 <code>model.ClASSES</code> 的长度是 80，那么 <code>result</code> 的长度也就是 80，<code>result[0]</code> 就对应 <code>model.CLASSES</code> 的第一个类别，表示第一个类别的盒子、概率。这样一切就解释通了。</p><ul><li>如果一个图片里只有一个目标，那么 <code>result</code> 类别索引对应的元素中，只有一组数据。</li><li>如果一个图片里有多个目标，那么 <code>result</code> 类别索引对应的元素中，就有多组数据。</li></ul><p>因为我的服务器没有 GUI，而官方画图程序需要调用 matplotlib 和 tkiner，所以无法显示图片，只能读取结果信息，自己用 PIL 画一下了。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> PIL <span class="keyword">import</span> Image, ImageDraw</span><br><span class="line"><span class="comment"># 打开原图</span></span><br><span class="line">img = Image.open(<span class="string">'Hippopx.jpg'</span>).convert(<span class="string">'RGB'</span>)</span><br><span class="line"><span class="comment"># 画出目标框，因为一个类别可能对应多个目标</span></span><br><span class="line"><span class="keyword">for</span> rec <span class="keyword">in</span> result[<span class="number">0</span>]:</span><br><span class="line">    x, y, w, h = rec[<span class="number">0</span>], rec[<span class="number">1</span>], rec[<span class="number">2</span>], rec[<span class="number">3</span>]</span><br><span class="line">    draw = ImageDraw.Draw(img)</span><br><span class="line">    draw.rectangle((x, y, w, h), width=<span class="number">2</span>, outline=<span class="string">'#41fc59'</span>)</span><br><span class="line"><span class="comment"># 保存结果图片</span></span><br><span class="line">img.save(<span class="string">'result.png'</span>)</span><br></pre></td></tr></table></figure><p>一个类对应一个目标：<img data-src="https://z3.ax1x.com/2021/05/13/gBksu8.jpg" alt> 一个类有多个目标：<img data-src="https://z3.ax1x.com/2021/05/13/gBAUqU.jpg" alt></p><h1 id="自定义推理"><a href="#自定义推理" class="headerlink" title="自定义推理"></a>自定义推理</h1><h2 id="准备数据"><a href="#准备数据" class="headerlink" title="准备数据"></a>准备数据</h2><p>将数据的软链接挂到 <code>mmdetection</code> 文件夹下，目录结构如下。 <code>ln -s minidata /mmdetection/data/</code>，创建软链时需要注意，使用绝对路径。因为之后会沿着软链访问数据，相对路径可能找不到数据。此时的目录结构如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mmdetection</span><br><span class="line">├── mmdet</span><br><span class="line">├── tools</span><br><span class="line">├── configs</span><br><span class="line">├── data</span><br><span class="line">│   ├── minidata</span><br><span class="line">│   │   ├── annotations/</span><br><span class="line">│   │   ├── test_data/</span><br></pre></td></tr></table></figure><p><code>annotations</code> 是对图片信息的说明，<code>test_data</code> 就是要推理的图片。其实这里的结构也不太重要，重点是后面的配置文件要把路径给指对了。而后下载预训练的模型，对目标数据集进行推理。模型都可以在官方文档的 <code>Model Zoo</code> 中下载到。</p><h3 id="修改配置文件-不建议"><a href="#修改配置文件-不建议" class="headerlink" title="修改配置文件(不建议)"></a>修改配置文件(不建议)</h3><p>因为源代码中指定的配置文件是 <code>configs/faster_rcnn/faster_rcnn_r50_fpn_1x_coco.py</code>，而打开这个文件，我们会发现以下几行信息：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">_base_ = [</span><br><span class="line">    <span class="string">'../_base_/models/faster_rcnn_r50_fpn.py'</span>,</span><br><span class="line">    <span class="string">'../_base_/datasets/coco_detection.py'</span>,</span><br><span class="line">    <span class="string">'../_base_/schedules/schedule_1x.py'</span>, <span class="string">'../_base_/default_runtime.py'</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure><p>也就是，配置文件是继承自 <code>_base_</code> 文件夹下面的模型、数据、学习率、运行时设置这四个文件。所以要按照自己想要的方式运行，就需要修改这四个配置文件。</p><p>比如以数据为例，因为目标数据集位于 <code>data/mini_data</code> 下，所以要修改配置文件，使配置文件找到正确的数据路径。打开 <code>configs/_base_/datasets/coco_detection.py</code>，修改 <code>data</code> 字典中的 <code>test</code>。<strong>类别数量修改同理，在models文件夹下。</strong></p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">test=dict(</span><br><span class="line">    type=dataset_type,</span><br><span class="line">    <span class="comment"># 找到自己的 json</span></span><br><span class="line">    ann_file=data_root + <span class="string">'annotations/openbrand_train.json'</span>,</span><br><span class="line">    <span class="comment"># 找到自己的图片路径</span></span><br><span class="line">    img_prefix=data_root + <span class="string">'test_data/'</span>,</span><br><span class="line">    pipeline=test_pipeline)</span><br></pre></td></tr></table></figure><ul><li>而后在 <code>mmdetection/mmdet/core/evaluation/classes_names</code> 中的 <code>coco_classes</code> 函数中修改类别名称信息。（这个我没找到如何在配置文件中修改，只能在源文件中修改了）</li><li>也在<code>mmdetection/mmdet/datasets/coco.py</code> 中的 <code>class CocoDataset(CustomDataset):</code> 类中的 <code>CLASSES</code> 字段修改为自己需要的类别信息。</li></ul><h2 id="开始推理"><a href="#开始推理" class="headerlink" title="开始推理"></a>开始推理</h2><p>而最后执行推理的代码如下：</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">python tools/test.py \</span><br><span class="line">    <span class="variable">$&#123;CONFIG_FILE&#125;</span> \</span><br><span class="line">    <span class="variable">$&#123;CHECKPOINT_FILE&#125;</span> \</span><br><span class="line">    [--out <span class="variable">$&#123;RESULT_FILE&#125;</span>] \</span><br><span class="line">    [--<span class="built_in">eval</span> <span class="variable">$&#123;EVAL_METRICS&#125;</span>] \</span><br></pre></td></tr></table></figure><ul><li><code>RESULT_FILE</code> 结果序列化输出到指定文件中，必须是 <code>.pkl</code> 文件</li><li><code>EVAL_METRICS</code> 结果要评估的项目，结果会打印到屏幕，对于 COCO 数据接而言，有 <code>proposal, bbox, segm</code> 等。但是需要注意的是，faster rcnn 没有 segm，mask rcnn 才有。</li></ul><p>比如：</p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="comment"># mmdetection 文件夹下执行</span></span><br><span class="line">python tools/test.py \</span><br><span class="line">    <span class="comment"># 配置文件</span></span><br><span class="line">    configs/faster_rcnn/faster_rcnn_r50_fpn_1x_coco.py \</span><br><span class="line">    <span class="comment"># 模型</span></span><br><span class="line">    checkpoints/faster_rcnn_r50_fpn_1x_coco_20200130-047c8118.pth \</span><br><span class="line">    <span class="comment"># 输出</span></span><br><span class="line">    --out /mmnet/out.pkl \</span><br><span class="line">    --<span class="built_in">eval</span> bbox proposal \</span><br><span class="line">    --show-score-thr 0.5 <span class="comment"># 概率低于 0.5 的预测结果都要被删除</span></span><br></pre></td></tr></table></figure><p>打开 <code>test.py</code>，一步步追踪源码，发现推理时调用的东西和 <code>inference_detector</code> 接口调用的一样，所以推理阶段不输出类别，类别需要自己从 <code>model.CLASSES</code> 中获取。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">with</span> torch.no_grad():</span><br><span class="line">    result = model(return_loss=<span class="literal">False</span>, rescale=<span class="literal">True</span>, **data)</span><br></pre></td></tr></table></figure><p>因为最后输出的结果保存到了 <code>out.pkl</code> 文件中，所以给一份精简代码，关于如何读取序列化数据。<del>其实跟 json 的读取挺像的。</del></p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line">data = <span class="literal">None</span></span><br><span class="line"><span class="keyword">with</span> open(<span class="string">'out.pkl'</span>, <span class="string">'rb'</span>) <span class="keyword">as</span> f:</span><br><span class="line">    data = pickle.load(f)</span><br><span class="line">process(data)</span><br></pre></td></tr></table></figure><p>推理结束后可以删除数据的软链，以备下次使用。删除软件时需要注意，可能一不小心删除原始数据。正确的删除软链方式是 <code>rm minidata</code>，注意不要加斜杠。</p><p>理论上应该是按着上面描述的那样执行，但其实我这里报错了：<code>KeyError: &quot;CocoDataset: &#39;categories&#39;&quot;</code>。定位到报错代码，发现是因为使用 <code>pycocotools</code> 读取 json 文件时，没有读到分类这个属性。<strong>所以这里需要注意的是，推理要用的 json，自己要添加一下类别的信息。</strong></p><h2 id="通过配置文件推理"><a href="#通过配置文件推理" class="headerlink" title="通过配置文件推理"></a>通过配置文件推理</h2><p>当我本能的以为又要修改源代码时，我意识到一个问题。以任何学过『设计模式』而言的人来说，初衷绝对不是让用户去修改源代码，每次修改来修改去，程序到最后可能都没法用了。而是应该通过添加额外配置文件，来达到用户想要的目的。<strong>对修改封闭，对扩展开放。</strong>所以以上修改我又还原了，决定自己写脚本进行推理。</p><h3 id="自定义脚本推理"><a href="#自定义脚本推理" class="headerlink" title="自定义脚本推理"></a>自定义脚本推理</h3><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> json</span><br><span class="line"><span class="keyword">from</span> mmdet.apis <span class="keyword">import</span> init_detector, inference_detector</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">from</span> pycocotools.coco <span class="keyword">import</span> COCO</span><br><span class="line"><span class="keyword">from</span> tqdm <span class="keyword">import</span> tqdm</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line"><span class="comment"># 提交要求</span></span><br><span class="line"><span class="comment"># [&#123;</span></span><br><span class="line"><span class="comment">#     "image_id": int,</span></span><br><span class="line"><span class="comment">#     "category_id": int,</span></span><br><span class="line"><span class="comment">#     "bbox": [x_min,y_min,width,height],</span></span><br><span class="line"><span class="comment">#     "score": float,</span></span><br><span class="line"><span class="comment"># &#125;]</span></span><br><span class="line"></span><br><span class="line">anno_path = <span class="string">'test.json'</span></span><br><span class="line">coco = COCO(anno_path)</span><br><span class="line">ids = list(coco.imgs.keys())</span><br><span class="line"></span><br><span class="line">filename_id = &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取文件名与对应的 ID</span></span><br><span class="line"><span class="comment"># 如 &#123;'000000.jpg': 12&#125;</span></span><br><span class="line"><span class="keyword">for</span> idx <span class="keyword">in</span> tqdm(range(len(ids))):</span><br><span class="line">    img_id = ids[idx]</span><br><span class="line">    img_ids = coco.getImgIds(imgIds=img_id)</span><br><span class="line">    image = coco.loadImgs(img_ids)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> image:</span><br><span class="line">        filename_id[i[<span class="string">'file_name'</span>]] = i[<span class="string">'id'</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 目标检测配置文件</span></span><br><span class="line">config_file = <span class="string">'mmnet/mmdetection/configs/faster_rcnn/faster_rcnn_r50_fpn_1x_coco.py'</span></span><br><span class="line"><span class="comment"># 训练模型</span></span><br><span class="line">checkpoint_file = <span class="string">'mmnet/mmdetection/checkpoints/faster_rcnn_r50_fpn_1x.pth'</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置模型</span></span><br><span class="line">model = init_detector(config=config_file,</span><br><span class="line">                      checkpoint=checkpoint_file,</span><br><span class="line">                      device=<span class="string">'cuda:0'</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 需要推理的图片的路径</span></span><br><span class="line">root = <span class="string">'val_data/'</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 存储结果，并生成 json</span></span><br><span class="line">results = []</span><br><span class="line"></span><br><span class="line"><span class="comment"># 开始推理</span></span><br><span class="line"><span class="keyword">for</span> file <span class="keyword">in</span> tqdm(os.listdir(root)):</span><br><span class="line">    result = inference_detector(model=model, imgs=root + file)</span><br><span class="line">    <span class="keyword">for</span> cate, items <span class="keyword">in</span> enumerate(result, <span class="number">1</span>):</span><br><span class="line">        <span class="comment"># 同一类别的有很多结果</span></span><br><span class="line">        <span class="keyword">for</span> item <span class="keyword">in</span> items:</span><br><span class="line">            item = item.tolist()</span><br><span class="line">            x, y, w, h, s = item[<span class="number">0</span>], item[<span class="number">1</span>], item[<span class="number">2</span>], item[<span class="number">3</span>], item[<span class="number">4</span>]</span><br><span class="line">            d = &#123;&#125;</span><br><span class="line">            d[<span class="string">'image_id'</span>] = filename_id[file]</span><br><span class="line">            d[<span class="string">'category_id'</span>] = cate</span><br><span class="line">            d[<span class="string">'bbox'</span>] = [x, y, w, h]</span><br><span class="line">            d[<span class="string">'score'</span>] = s</span><br><span class="line">            results.append(d)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 保存</span></span><br><span class="line"><span class="keyword">with</span> open(<span class="string">'result.json'</span>, <span class="string">'w'</span>) <span class="keyword">as</span> f:</span><br><span class="line">    json.dump(results, f, indent=<span class="number">4</span>)</span><br></pre></td></tr></table></figure><h3 id="配置文件推理"><a href="#配置文件推理" class="headerlink" title="配置文件推理"></a>配置文件推理</h3><p>之前说过，配置文件继承自以下四个文件：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">_base_ = [</span><br><span class="line">    <span class="string">'../_base_/models/faster_rcnn_r50_fpn.py'</span>,</span><br><span class="line">    <span class="string">'../_base_/datasets/coco_detection.py'</span>,</span><br><span class="line">    <span class="string">'../_base_/schedules/schedule_1x.py'</span>, <span class="string">'../_base_/default_runtime.py'</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure><p>所以我们自己定义一个配置文件，称为 <code>mynet.py</code>，位于 <code>mmdetection/configs/faster_rcnn/</code> 文件夹下，把以上四个文件的内容全部拷贝到 <code>mynet.py</code> 中，并修改自己需要改的地方即可。<code>mynet.py</code> 文件如下：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">_base_ = [</span><br><span class="line">    <span class="string">'../_base_/models/faster_rcnn_r50_fpn.py'</span>,</span><br><span class="line">    <span class="string">'../_base_/datasets/coco_detection.py'</span>,</span><br><span class="line">    <span class="string">'../_base_/schedules/schedule_1x.py'</span>, <span class="string">'../_base_/default_runtime.py'</span></span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># model settings</span></span><br><span class="line">model = dict(</span><br><span class="line">    type=<span class="string">'FasterRCNN'</span>,</span><br><span class="line">    pretrained=<span class="string">'torchvision://resnet50'</span>,</span><br><span class="line">    backbone=dict(</span><br><span class="line">        type=<span class="string">'ResNet'</span>,</span><br><span class="line">        depth=<span class="number">50</span>,</span><br><span class="line">        num_stages=<span class="number">4</span>,</span><br><span class="line">        out_indices=(<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>),</span><br><span class="line">        frozen_stages=<span class="number">1</span>,</span><br><span class="line">        norm_cfg=dict(type=<span class="string">'BN'</span>, requires_grad=<span class="literal">True</span>),</span><br><span class="line">        norm_eval=<span class="literal">True</span>,</span><br><span class="line">        style=<span class="string">'pytorch'</span>),</span><br><span class="line">    neck=dict(</span><br><span class="line">        type=<span class="string">'FPN'</span>,</span><br><span class="line">        in_channels=[<span class="number">256</span>, <span class="number">512</span>, <span class="number">1024</span>, <span class="number">2048</span>],</span><br><span class="line">        out_channels=<span class="number">256</span>,</span><br><span class="line">        num_outs=<span class="number">5</span>),</span><br><span class="line">    rpn_head=dict(</span><br><span class="line">        type=<span class="string">'RPNHead'</span>,</span><br><span class="line">        in_channels=<span class="number">256</span>,</span><br><span class="line">        feat_channels=<span class="number">256</span>,</span><br><span class="line">        anchor_generator=dict(</span><br><span class="line">            type=<span class="string">'AnchorGenerator'</span>,</span><br><span class="line">            scales=[<span class="number">8</span>],</span><br><span class="line">            ratios=[<span class="number">0.5</span>, <span class="number">1.0</span>, <span class="number">2.0</span>],</span><br><span class="line">            strides=[<span class="number">4</span>, <span class="number">8</span>, <span class="number">16</span>, <span class="number">32</span>, <span class="number">64</span>]),</span><br><span class="line">        bbox_coder=dict(</span><br><span class="line">            type=<span class="string">'DeltaXYWHBBoxCoder'</span>,</span><br><span class="line">            target_means=[<span class="number">.0</span>, <span class="number">.0</span>, <span class="number">.0</span>, <span class="number">.0</span>],</span><br><span class="line">            target_stds=[<span class="number">1.0</span>, <span class="number">1.0</span>, <span class="number">1.0</span>, <span class="number">1.0</span>]),</span><br><span class="line">        loss_cls=dict(</span><br><span class="line">            type=<span class="string">'CrossEntropyLoss'</span>, use_sigmoid=<span class="literal">True</span>, loss_weight=<span class="number">1.0</span>),</span><br><span class="line">        loss_bbox=dict(type=<span class="string">'L1Loss'</span>, loss_weight=<span class="number">1.0</span>)),</span><br><span class="line">    roi_head=dict(</span><br><span class="line">        type=<span class="string">'StandardRoIHead'</span>,</span><br><span class="line">        bbox_roi_extractor=dict(</span><br><span class="line">            type=<span class="string">'SingleRoIExtractor'</span>,</span><br><span class="line">            roi_layer=dict(type=<span class="string">'RoIAlign'</span>, output_size=<span class="number">7</span>, sampling_ratio=<span class="number">0</span>),</span><br><span class="line">            out_channels=<span class="number">256</span>,</span><br><span class="line">            featmap_strides=[<span class="number">4</span>, <span class="number">8</span>, <span class="number">16</span>, <span class="number">32</span>]),</span><br><span class="line">        bbox_head=dict(</span><br><span class="line">            type=<span class="string">'Shared2FCBBoxHead'</span>,</span><br><span class="line">            in_channels=<span class="number">256</span>,</span><br><span class="line">            fc_out_channels=<span class="number">1024</span>,</span><br><span class="line">            roi_feat_size=<span class="number">7</span>,</span><br><span class="line">            <span class="comment"># 修改类别</span></span><br><span class="line">            num_classes=<span class="number">515</span>,</span><br><span class="line">            bbox_coder=dict(</span><br><span class="line">                type=<span class="string">'DeltaXYWHBBoxCoder'</span>,</span><br><span class="line">                target_means=[<span class="number">0.</span>, <span class="number">0.</span>, <span class="number">0.</span>, <span class="number">0.</span>],</span><br><span class="line">                target_stds=[<span class="number">0.1</span>, <span class="number">0.1</span>, <span class="number">0.2</span>, <span class="number">0.2</span>]),</span><br><span class="line">            reg_class_agnostic=<span class="literal">False</span>,</span><br><span class="line">            loss_cls=dict(</span><br><span class="line">                type=<span class="string">'CrossEntropyLoss'</span>, use_sigmoid=<span class="literal">False</span>, loss_weight=<span class="number">1.0</span>),</span><br><span class="line">            loss_bbox=dict(type=<span class="string">'L1Loss'</span>, loss_weight=<span class="number">1.0</span>))),</span><br><span class="line">    <span class="comment"># model training and testing settings</span></span><br><span class="line">    train_cfg=dict(</span><br><span class="line">        rpn=dict(</span><br><span class="line">            assigner=dict(</span><br><span class="line">                type=<span class="string">'MaxIoUAssigner'</span>,</span><br><span class="line">                pos_iou_thr=<span class="number">0.7</span>,</span><br><span class="line">                neg_iou_thr=<span class="number">0.3</span>,</span><br><span class="line">                min_pos_iou=<span class="number">0.3</span>,</span><br><span class="line">                match_low_quality=<span class="literal">True</span>,</span><br><span class="line">                ignore_iof_thr=<span class="number">-1</span>),</span><br><span class="line">            sampler=dict(</span><br><span class="line">                type=<span class="string">'RandomSampler'</span>,</span><br><span class="line">                num=<span class="number">256</span>,</span><br><span class="line">                pos_fraction=<span class="number">0.5</span>,</span><br><span class="line">                neg_pos_ub=<span class="number">-1</span>,</span><br><span class="line">                add_gt_as_proposals=<span class="literal">False</span>),</span><br><span class="line">            allowed_border=<span class="number">-1</span>,</span><br><span class="line">            pos_weight=<span class="number">-1</span>,</span><br><span class="line">            debug=<span class="literal">False</span>),</span><br><span class="line">        rpn_proposal=dict(</span><br><span class="line">            nms_pre=<span class="number">2000</span>,</span><br><span class="line">            max_per_img=<span class="number">1000</span>,</span><br><span class="line">            nms=dict(type=<span class="string">'nms'</span>, iou_threshold=<span class="number">0.7</span>),</span><br><span class="line">            min_bbox_size=<span class="number">0</span>),</span><br><span class="line">        rcnn=dict(</span><br><span class="line">            assigner=dict(</span><br><span class="line">                type=<span class="string">'MaxIoUAssigner'</span>,</span><br><span class="line">                pos_iou_thr=<span class="number">0.5</span>,</span><br><span class="line">                neg_iou_thr=<span class="number">0.5</span>,</span><br><span class="line">                min_pos_iou=<span class="number">0.5</span>,</span><br><span class="line">                match_low_quality=<span class="literal">False</span>,</span><br><span class="line">                ignore_iof_thr=<span class="number">-1</span>),</span><br><span class="line">            sampler=dict(</span><br><span class="line">                type=<span class="string">'RandomSampler'</span>,</span><br><span class="line">                num=<span class="number">512</span>,</span><br><span class="line">                pos_fraction=<span class="number">0.25</span>,</span><br><span class="line">                neg_pos_ub=<span class="number">-1</span>,</span><br><span class="line">                add_gt_as_proposals=<span class="literal">True</span>),</span><br><span class="line">            pos_weight=<span class="number">-1</span>,</span><br><span class="line">            debug=<span class="literal">False</span>)),</span><br><span class="line">    test_cfg=dict(</span><br><span class="line">        rpn=dict(</span><br><span class="line">            nms_pre=<span class="number">1000</span>,</span><br><span class="line">            max_per_img=<span class="number">1000</span>,</span><br><span class="line">            nms=dict(type=<span class="string">'nms'</span>, iou_threshold=<span class="number">0.7</span>),</span><br><span class="line">            min_bbox_size=<span class="number">0</span>),</span><br><span class="line">        rcnn=dict(</span><br><span class="line">            score_thr=<span class="number">0.05</span>,</span><br><span class="line">            nms=dict(type=<span class="string">'nms'</span>, iou_threshold=<span class="number">0.3</span>),</span><br><span class="line">            max_per_img=<span class="number">100</span>)</span><br><span class="line">        <span class="comment"># soft-nms is also supported for rcnn testing</span></span><br><span class="line">        <span class="comment"># e.g., nms=dict(type='soft_nms', iou_threshold=0.5, min_score=0.05)</span></span><br><span class="line">    ))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># dataset settings</span></span><br><span class="line">dataset_type = <span class="string">'CocoDataset'</span></span><br><span class="line">data_root = <span class="string">'mmnet/mmdetection/data/ljw/'</span></span><br><span class="line">img_norm_cfg = dict(</span><br><span class="line">    mean=[<span class="number">123.675</span>, <span class="number">116.28</span>, <span class="number">103.53</span>], std=[<span class="number">58.395</span>, <span class="number">57.12</span>, <span class="number">57.375</span>], to_rgb=<span class="literal">True</span>)</span><br><span class="line">train_pipeline = [</span><br><span class="line">    dict(type=<span class="string">'LoadImageFromFile'</span>),</span><br><span class="line">    dict(type=<span class="string">'LoadAnnotations'</span>, with_bbox=<span class="literal">True</span>),</span><br><span class="line">    dict(type=<span class="string">'Resize'</span>, img_scale=(<span class="number">1333</span>, <span class="number">800</span>), keep_ratio=<span class="literal">True</span>),</span><br><span class="line">    dict(type=<span class="string">'RandomFlip'</span>, flip_ratio=<span class="number">0.5</span>),</span><br><span class="line">    dict(type=<span class="string">'Normalize'</span>, **img_norm_cfg),</span><br><span class="line">    dict(type=<span class="string">'Pad'</span>, size_divisor=<span class="number">32</span>),</span><br><span class="line">    dict(type=<span class="string">'DefaultFormatBundle'</span>),</span><br><span class="line">    dict(type=<span class="string">'Collect'</span>, keys=[<span class="string">'img'</span>, <span class="string">'gt_bboxes'</span>, <span class="string">'gt_labels'</span>]),</span><br><span class="line">]</span><br><span class="line">test_pipeline = [</span><br><span class="line">    dict(type=<span class="string">'LoadImageFromFile'</span>),</span><br><span class="line">    dict(</span><br><span class="line">        type=<span class="string">'MultiScaleFlipAug'</span>,</span><br><span class="line">        img_scale=(<span class="number">1333</span>, <span class="number">800</span>),</span><br><span class="line">        flip=<span class="literal">False</span>,</span><br><span class="line">        transforms=[</span><br><span class="line">            dict(type=<span class="string">'Resize'</span>, keep_ratio=<span class="literal">True</span>),</span><br><span class="line">            dict(type=<span class="string">'RandomFlip'</span>),</span><br><span class="line">            dict(type=<span class="string">'Normalize'</span>, **img_norm_cfg),</span><br><span class="line">            dict(type=<span class="string">'Pad'</span>, size_divisor=<span class="number">32</span>),</span><br><span class="line">            dict(type=<span class="string">'ImageToTensor'</span>, keys=[<span class="string">'img'</span>]),</span><br><span class="line">            dict(type=<span class="string">'Collect'</span>, keys=[<span class="string">'img'</span>]),</span><br><span class="line">        ])</span><br><span class="line">]</span><br><span class="line">data = dict(</span><br><span class="line">    samples_per_gpu=<span class="number">2</span>,</span><br><span class="line">    workers_per_gpu=<span class="number">2</span>,</span><br><span class="line">    train=dict(</span><br><span class="line">        type=dataset_type,</span><br><span class="line">        ann_file=data_root + <span class="string">'annotations/instances_train2017.json'</span>,</span><br><span class="line">        img_prefix=data_root + <span class="string">'train2017/'</span>,</span><br><span class="line">        pipeline=train_pipeline),</span><br><span class="line">    val=dict(</span><br><span class="line">        type=dataset_type,</span><br><span class="line">        ann_file=data_root + <span class="string">'annotations/instances_val2017.json'</span>,</span><br><span class="line">        img_prefix=data_root + <span class="string">'val2017/'</span>,</span><br><span class="line">        pipeline=test_pipeline),</span><br><span class="line">    test=dict(</span><br><span class="line">        type=dataset_type,</span><br><span class="line">        ann_file=data_root + <span class="string">'test.json'</span>,</span><br><span class="line">        img_prefix=data_root + <span class="string">'val_data/'</span>,</span><br><span class="line">        pipeline=test_pipeline))</span><br><span class="line">evaluation = dict(interval=<span class="number">1</span>, metric=<span class="string">'bbox'</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># optimizer</span></span><br><span class="line">optimizer = dict(type=<span class="string">'SGD'</span>, lr=<span class="number">0.02</span>, momentum=<span class="number">0.9</span>, weight_decay=<span class="number">0.0001</span>)</span><br><span class="line">optimizer_config = dict(grad_clip=<span class="literal">None</span>)</span><br><span class="line"><span class="comment"># learning policy</span></span><br><span class="line">lr_config = dict(</span><br><span class="line">    policy=<span class="string">'step'</span>,</span><br><span class="line">    warmup=<span class="string">'linear'</span>,</span><br><span class="line">    warmup_iters=<span class="number">500</span>,</span><br><span class="line">    warmup_ratio=<span class="number">0.001</span>,</span><br><span class="line">    step=[<span class="number">8</span>, <span class="number">11</span>])</span><br><span class="line">runner = dict(type=<span class="string">'EpochBasedRunner'</span>, max_epochs=<span class="number">12</span>)</span><br></pre></td></tr></table></figure><p>最终，<code>python tools/test.py configs/faster_rcnn/mynet.py ......</code> 就可以运行自己的配置文件，完成推理。尽量避免了破坏原有的代码结构。如果想加速执行，就需要多显卡，而 <code>mmdetection</code> 对这方面支持的很好：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">bash tools/dist_test.sh \</span><br><span class="line">    configs/faster_rcnn/my_faster_rcnn.py \</span><br><span class="line">    checkpoints/faster_rcnn_r50_fpn_1x.pth \</span><br><span class="line">    <span class="comment"># 4 个 GPU</span></span><br><span class="line">    <span class="number">4</span> \</span><br><span class="line">    --format-only \</span><br><span class="line">    <span class="comment"># 输出推理得到的 json 文件</span></span><br><span class="line">    --options <span class="string">"jsonfile_prefix=results6"</span></span><br></pre></td></tr></table></figure><h1 id="推理程序"><a href="#推理程序" class="headerlink" title="推理程序"></a>推理程序</h1><p>然后，我把一些简单的程序传到 <code>colab</code> 了，可以<a href="https://github.com/muyuuuu/open-mmlab-colab" target="_blank" rel="noopener">打开</a>去实际执行一下，虽然也有一些分类的任务，但思想是一样的。</p><h1 id="训练"><a href="#训练" class="headerlink" title="训练"></a>训练</h1><h2 id="Finetune"><a href="#Finetune" class="headerlink" title="Finetune"></a>Finetune</h2><p>这个恐怕是最简单但也是最常用的一种方案，和前文一样，也是基于配置文件、命令行启动的方式进行训练。修改类别同前文。同样以 Faster RCNN 为例，先写好自己的配置文件，执行方式为：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">python tools/train.py \</span><br><span class="line">    $&#123;CONFIG_FILE&#125; \</span><br><span class="line">    [optional arguments]</span><br></pre></td></tr></table></figure><p>optional arguments 是可选参数，这里需要加上模型的 log 输出文件目录和预加载模型的地址：</p><ul><li><code>--work-dir work_dirs/</code>，将日志、模型输出到 <code>work_dirs</code> 文件夹下</li><li><code>--load-from model.pth</code>，只加载模型参数，从第 0 个 epoch 开始训练，但是我试了一下，这个参数不太行</li><li><code>--resume-from model.pth</code>，加载模型参数与优化器状态，继承上次的 epoch，通常用于恢复意外中断的训练过程</li></ul><p>当然如果想在多个 GPU 上训练也是可以的：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bash ./tools/dist_train.sh \</span><br><span class="line">    <span class="variable">$&#123;CONFIG_FILE&#125;</span> \</span><br><span class="line">    <span class="variable">$&#123;GPU_NUM&#125;</span> \</span><br><span class="line">    [optional arguments]</span><br></pre></td></tr></table></figure><h2 id="配置文件训练"><a href="#配置文件训练" class="headerlink" title="配置文件训练"></a>配置文件训练</h2><p>官方文档给出了两种修改配置的方案 <sup id="fnref:7"><a href="#fn:7" rel="footnote">7</a></sup>：</p><ul><li>一种是通过 <code>python tools/train.py</code> 时添加 <code>--cfg-options</code> 参数，例如 <code>--cfg-options model.backbone.norm_eval=False</code> 会将模型的 BN 层调为运行时 <sup id="fnref:8"><a href="#fn:8" rel="footnote">8</a></sup> 状态。但是这样参数会很多，且很容易出错，个人不建议这么修改。</li><li>第二种方案就是自定义配置文件，并继承 <code>_base_</code>，具体操作可以见上文。模型、数据、学习率等都可以轻松设置。官网也给出了 Mask RCNN 的具体配置文件与解释 <sup id="fnref:9"><a href="#fn:9" rel="footnote">9</a></sup>。因为官方支持可以只在配置文件中写入修改部分，不修改的部分可以不必声明。而在子文件中修改配置文件时，如果重新定义了自己的变量，一定记得传入，否则默认还是原来的配置。如下：</li></ul><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 新的修改</span></span><br><span class="line">test_pipeline = [</span><br><span class="line">    dict(type=<span class="string">'LoadImageFromFile'</span>),</span><br><span class="line">    dict(</span><br><span class="line">        type=<span class="string">'MultiScaleFlipAug'</span>,</span><br><span class="line">        img_scale=(<span class="number">1333</span>, <span class="number">800</span>),</span><br><span class="line">        flip=<span class="literal">False</span>,</span><br><span class="line">        transforms=[</span><br><span class="line">            dict(type=<span class="string">'Resize'</span>, keep_ratio=<span class="literal">True</span>),</span><br><span class="line">            dict(type=<span class="string">'RandomFlip'</span>),</span><br><span class="line">            dict(type=<span class="string">'Normalize'</span>, **img_norm_cfg),</span><br><span class="line">            dict(type=<span class="string">'Pad'</span>, size_divisor=<span class="number">32</span>),</span><br><span class="line">            dict(type=<span class="string">'ImageToTensor'</span>, keys=[<span class="string">'img'</span>]),</span><br><span class="line">            dict(type=<span class="string">'Collect'</span>, keys=[<span class="string">'img'</span>]),</span><br><span class="line">        ])</span><br><span class="line">]</span><br><span class="line"><span class="comment"># 传入自己的修改，其它不用写，用默认的</span></span><br><span class="line">data = dict(</span><br><span class="line">    test=dict(pipeline=test_pipeline))</span><br></pre></td></tr></table></figure><h1 id="数据处理流程"><a href="#数据处理流程" class="headerlink" title="数据处理流程"></a>数据处理流程</h1><p>这个在程序中的字段是 <code>pipeline</code>，也就是数据处理的一个管道。来看默认配置文件中的内容：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">img_norm_cfg = dict(</span><br><span class="line">    mean=[<span class="number">123.675</span>, <span class="number">116.28</span>, <span class="number">103.53</span>], std=[<span class="number">58.395</span>, <span class="number">57.12</span>, <span class="number">57.375</span>], to_rgb=<span class="literal">True</span>)</span><br><span class="line">train_pipeline = [</span><br><span class="line">    <span class="comment"># 从文件加载图片</span></span><br><span class="line">    dict(type=<span class="string">'LoadImageFromFile'</span>),</span><br><span class="line">    <span class="comment"># 加载 boxes，boxes_ignore，label 等信息</span></span><br><span class="line">    dict(type=<span class="string">'LoadAnnotations'</span>, with_bbox=<span class="literal">True</span>),</span><br><span class="line">    <span class="comment"># 重新定义图像大小、bbox 等区域的大小也会变换</span></span><br><span class="line">    dict(type=<span class="string">'Resize'</span>, img_scale=(<span class="number">1333</span>, <span class="number">800</span>), keep_ratio=<span class="literal">True</span>),</span><br><span class="line">    <span class="comment"># 随机翻转，bbox 等内容也会变化</span></span><br><span class="line">    dict(type=<span class="string">'RandomFlip'</span>, flip_ratio=<span class="number">0.5</span>),</span><br><span class="line">    <span class="comment"># 数据标准化</span></span><br><span class="line">    dict(type=<span class="string">'Normalize'</span>, **img_norm_cfg),</span><br><span class="line">    <span class="comment"># https://github.com/open-mmlab/mmdetection/blob/master/mmdet/datasets/pipelines/transforms.py</span></span><br><span class="line">    <span class="comment"># https://mmcv.readthedocs.io/en/latest/api.html</span></span><br><span class="line">    <span class="comment"># 填充除数，追踪源代码到 mmcv，意思是，填充后的图像边，是 32 的倍数</span></span><br><span class="line">    dict(type=<span class="string">'Pad'</span>, size_divisor=<span class="number">32</span>),</span><br><span class="line">    <span class="comment"># https://github.com/open-mmlab/mmdetection/blob/master/mmdet/datasets/pipelines/formating.py</span></span><br><span class="line">    <span class="comment"># 开始批处理。对于图片，处理、转为向量、收集到 batchsize。bbox，label 同理</span></span><br><span class="line">    dict(type=<span class="string">'DefaultFormatBundle'</span>),</span><br><span class="line">    dict(type=<span class="string">'Collect'</span>, keys=[<span class="string">'img'</span>, <span class="string">'gt_bboxes'</span>, <span class="string">'gt_labels'</span>]),</span><br><span class="line">]</span><br><span class="line">test_pipeline = [</span><br><span class="line">    dict(type=<span class="string">'LoadImageFromFile'</span>),</span><br><span class="line">    dict(</span><br><span class="line">        type=<span class="string">'MultiScaleFlipAug'</span>,</span><br><span class="line">        img_scale=(<span class="number">1333</span>, <span class="number">800</span>),</span><br><span class="line">        <span class="comment"># 禁止翻转</span></span><br><span class="line">        flip=<span class="literal">False</span>,</span><br><span class="line">        transforms=[</span><br><span class="line">            dict(type=<span class="string">'Resize'</span>, keep_ratio=<span class="literal">True</span>),</span><br><span class="line">            dict(type=<span class="string">'RandomFlip'</span>),</span><br><span class="line">            dict(type=<span class="string">'Normalize'</span>, **img_norm_cfg),</span><br><span class="line">            dict(type=<span class="string">'Pad'</span>, size_divisor=<span class="number">32</span>),</span><br><span class="line">            dict(type=<span class="string">'ImageToTensor'</span>, keys=[<span class="string">'img'</span>]),</span><br><span class="line">            dict(type=<span class="string">'Collect'</span>, keys=[<span class="string">'img'</span>]),</span><br><span class="line">        ])</span><br><span class="line">]</span><br></pre></td></tr></table></figure><p>而如果想扩展自己的数据处理流程 <sup id="fnref:10"><a href="#fn:10" rel="footnote">10</a></sup> ，先自己定义一个数据处理的类，然后导入进来，加入到 <code>train_pipeline</code> 即可。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> mmdet.datasets <span class="keyword">import</span> PIPELINES</span><br><span class="line"></span><br><span class="line"><span class="meta">@PIPELINES.register_module()</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyTransform</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__call__</span><span class="params">(self, results)</span>:</span></span><br><span class="line">        results[<span class="string">'dummy'</span>] = <span class="literal">True</span></span><br><span class="line">        <span class="keyword">return</span> results</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> .my_pipeline <span class="keyword">import</span> MyTransform</span><br><span class="line"></span><br><span class="line">train_pipeline = [</span><br><span class="line">    dict(type=<span class="string">'LoadImageFromFile'</span>),</span><br><span class="line">    dict(type=<span class="string">'LoadAnnotations'</span>, with_bbox=<span class="literal">True</span>),</span><br><span class="line">    dict(type=<span class="string">'Resize'</span>, img_scale=(<span class="number">1333</span>, <span class="number">800</span>), keep_ratio=<span class="literal">True</span>),</span><br><span class="line">    dict(type=<span class="string">'RandomFlip'</span>, flip_ratio=<span class="number">0.5</span>),</span><br><span class="line">    dict(type=<span class="string">'Normalize'</span>, **img_norm_cfg),</span><br><span class="line">    dict(type=<span class="string">'Pad'</span>, size_divisor=<span class="number">32</span>),</span><br><span class="line">    <span class="comment"># 添加自己的</span></span><br><span class="line">    dict(type=<span class="string">'MyTransform'</span>),</span><br><span class="line">    dict(type=<span class="string">'DefaultFormatBundle'</span>),</span><br><span class="line">    dict(type=<span class="string">'Collect'</span>, keys=[<span class="string">'img'</span>, <span class="string">'gt_bboxes'</span>, <span class="string">'gt_labels'</span>]),</span><br><span class="line">]</span><br></pre></td></tr></table></figure><p>而至于自定义模型的 backbone，neck，head <sup id="fnref:11"><a href="#fn:11" rel="footnote">11</a></sup> 、学习率和优化方案 <sup id="fnref:12"><a href="#fn:12" rel="footnote">12</a></sup>、自定义损失函数 <sup id="fnref:13"><a href="#fn:13" rel="footnote">13</a></sup> 等，官网也给出了详细的解决方案。</p><h1 id="references"><a href="#references" class="headerlink" title="references"></a>references</h1><p><sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup>：<a href="https://github.com/open-mmlab/mmdetection/blob/master/README_zh-CN.md" target="_blank" rel="noopener">MMDetection 介绍</a><br><sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup>：<a href="https://github.com/open-mmlab/mmcv#install-with-pip" target="_blank" rel="noopener">mmcv-full 安装文档</a><br><sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup>：<a href="https://mmdetection.readthedocs.io/en/latest/get_started.html#installation" target="_blank" rel="noopener">mmdetection 安装文档</a><br><sup id="fnref:4"><a href="#fn:4" rel="footnote">4</a></sup>：<a href="https://mmdetection.readthedocs.io/en/latest/api.html#mmdet.apis.init_detector" target="_blank" rel="noopener">mmdetection 官方文档</a><br><sup id="fnref:5"><a href="#fn:5" rel="footnote">5</a></sup>：<a href="https://github.com/open-mmlab/mmdetection/issues/3755" target="_blank" rel="noopener">inference_detector 无法输出每个盒子的所属类别</a><br><sup id="fnref:6"><a href="#fn:6" rel="footnote">6</a></sup>：<a href="https://github.com/open-mmlab/mmdetection/issues/3755#issuecomment-840228347" target="_blank" rel="noopener">inference_detector 获取类别</a><br><sup id="fnref:7"><a href="#fn:7" rel="footnote">7</a></sup>：<a href="https://mmdetection.readthedocs.io/en/latest/tutorials/config.html" target="_blank" rel="noopener">MMdetection 配置文件修改</a><br><sup id="fnref:8"><a href="#fn:8" rel="footnote">8</a></sup>：<a href="https://muyuuuu.github.io/2021/05/07/DNN-data-normal/">BN 层在运行时状态</a><br><sup id="fnref:9"><a href="#fn:9" rel="footnote">9</a></sup>：<a href="https://mmdetection.readthedocs.io/en/latest/tutorials/config.html" target="_blank" rel="noopener">Mask RCNN 配置文件说明</a><br><sup id="fnref:10"><a href="#fn:10" rel="footnote">10</a></sup>：<a href="https://mmdetection.readthedocs.io/en/latest/tutorials/data_pipeline.html" target="_blank" rel="noopener">扩展数据处理流程</a><br><sup id="fnref:11"><a href="#fn:11" rel="footnote">11</a></sup>：<a href="https://mmdetection.readthedocs.io/en/latest/tutorials/customize_models.html" target="_blank" rel="noopener">自定义模型</a><br><sup id="fnref:12"><a href="#fn:12" rel="footnote">12</a></sup>：<a href="https://mmdetection.readthedocs.io/en/latest/tutorials/customize_runtime.html" target="_blank" rel="noopener">自定义学习率和优化方案</a><br><sup id="fnref:13"><a href="#fn:13" rel="footnote">13</a></sup>：<a href="https://mmdetection.readthedocs.io/en/latest/tutorials/customize_losses.html" target="_blank" rel="noopener">自定义损失函数</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;最近喜欢上了听音乐，B 站关注了个 UP 主叫『&lt;strong&gt;咻咻满&lt;/strong&gt;』，长得好看，戏腔唱『&lt;strong&gt;青花瓷&lt;/strong&gt;』入坑了，也听了其它的『星辰大海』和『白月光和朱砂痣』，都挺好听。以后得关注点女 UP 了，看着多可爱，生活又不是只有代码。卧艹不对说回正题。&lt;/p&gt;
&lt;p&gt;MMDetection 是一个基于 PyTorch 的目标检测开源工具箱 &lt;sup id=&quot;fnref:1&quot;&gt;&lt;a href=&quot;#fn:1&quot; rel=&quot;footnote&quot;&gt;1&lt;/a&gt;&lt;/sup&gt;，支持了众多主流的和最新的检测算法，例如 Faster R-CNN，Mask R-CNN，RetinaNet 等，官网也给出了详细的教程。既然如此，生命不息，开坑不止。前前后后被各种事情打断，大概花了一周搞懂了如何使用 &lt;code&gt;MMdetection&lt;/code&gt; 去做检测的任务。本文收录：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;安装&lt;/li&gt;
&lt;li&gt;修改配置文件&lt;/li&gt;
&lt;li&gt;调用模型与训练好的参数，进行推理&lt;/li&gt;
&lt;li&gt;自定义训练&lt;/li&gt;
&lt;li&gt;数据处理流程&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;注意，本文更多像是记录学习过程，这也是我第一次用这个工具，遇到问题一步一步的 debug 与记录，并不是直接的教程。且，本文程序大多能直接复制运行，需要对应到自己的路径。我希望读者看完本文后具有解决问题的能力，而不只是会解决问题。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="CV" scheme="https://muyuuuu.github.io/tags/CV/"/>
    
  </entry>
  
  <entry>
    <title>神经网络中的数据问题</title>
    <link href="https://muyuuuu.github.io/2021/05/07/DNN-data-normal/"/>
    <id>https://muyuuuu.github.io/2021/05/07/DNN-data-normal/</id>
    <published>2021-05-07T09:44:12.000Z</published>
    <updated>2021-05-08T07:03:26.677Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>今日在写程序时，遇到了一个蜜汁 <code>bug</code>，加载别人训练好的 <code>ResNet18</code>，识别精度很低，只有 16%，但理论上而言应该有 92%，我也好奇那 80% 的准确率去哪里了。而程序和数据本身又无错误，所以来探究一下这是为什么。</p><a id="more"></a><p>首先，网络结构和预训练的模型来自这里 <sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup>，这里声明一下，他提供的网络、参数都是没任何问题的，准确率低是我自己的原因。</p><h1 id="错误程序"><a href="#错误程序" class="headerlink" title="错误程序"></a>错误程序</h1><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">from</span> resnet18 <span class="keyword">import</span> ResNet18</span><br><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> Dataset, DataLoader</span><br><span class="line"><span class="keyword">from</span> torch.autograd <span class="keyword">import</span> Variable</span><br><span class="line"><span class="keyword">from</span> torchvision <span class="keyword">import</span> transforms</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">testdataset</span><span class="params">(Dataset)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, data_path, label_path)</span>:</span></span><br><span class="line">        <span class="comment"># 模型是预训练好的，取后面 10000 个做测试</span></span><br><span class="line">        self.x_data = np.load(data_path)</span><br><span class="line">        <span class="comment"># 数据到 [0, 1] 之间</span></span><br><span class="line">        self.x_data = self.x_data / <span class="number">255</span></span><br><span class="line">        self.x_data = self.x_data[<span class="number">50000</span>:]</span><br><span class="line">        self.y_data = np.load(label_path)</span><br><span class="line">        self.y_data = self.y_data[<span class="number">50000</span>:]</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__getitem__</span><span class="params">(self, index)</span>:</span></span><br><span class="line">        x_ = self.x_data[index]</span><br><span class="line">        x_ = x_.transpose(<span class="number">2</span>, <span class="number">1</span>, <span class="number">0</span>)</span><br><span class="line">        y_ = self.y_data[index]</span><br><span class="line">        <span class="keyword">return</span> torch.from_numpy(x_), torch.from_numpy(y_)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__len__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> len(self.x_data)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">_error</span><span class="params">(model, X, y)</span>:</span></span><br><span class="line">    out = model(X)</span><br><span class="line">    prediction = torch.argmax(out, <span class="number">1</span>)</span><br><span class="line">    prediction = prediction.unsqueeze(<span class="number">1</span>)</span><br><span class="line">    correct = (prediction == y).sum().float()</span><br><span class="line">    <span class="keyword">return</span> correct</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">_eval</span><span class="params">(model, device, test_loader)</span>:</span></span><br><span class="line">    model.eval()</span><br><span class="line">    model.to(device)</span><br><span class="line">    natural_err_total = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> data, target <span class="keyword">in</span> test_loader:</span><br><span class="line">        data, target = data.to(device,</span><br><span class="line">                               dtype=torch.float), target.to(device,</span><br><span class="line">                                                             dtype=torch.float)</span><br><span class="line">        X, y = Variable(data, requires_grad=<span class="literal">True</span>), Variable(target)</span><br><span class="line">        err_natural = _error(model, X, y)</span><br><span class="line">        natural_err_total += err_natural</span><br><span class="line">    print(<span class="string">'acc: '</span>, natural_err_total / <span class="number">10000</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 加载 resnet</span></span><br><span class="line">    resnet = ResNet18()</span><br><span class="line">    resnet_path = <span class="string">"resnet18_ckpt.pth"</span></span><br><span class="line">    checkpoint = torch.load(resnet_path, map_location=<span class="string">'cpu'</span>)</span><br><span class="line">    print(<span class="string">'loaded model...'</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 这里只是为了对应模型参数</span></span><br><span class="line">    net_state = &#123;&#125;</span><br><span class="line">    <span class="keyword">for</span> key <span class="keyword">in</span> checkpoint[<span class="string">'net'</span>]:</span><br><span class="line">        net_state[key[<span class="number">7</span>:]] = checkpoint[<span class="string">'net'</span>][key]</span><br><span class="line">    resnet.load_state_dict(net_state)</span><br><span class="line">    print(<span class="string">'set model...'</span>)</span><br><span class="line"></span><br><span class="line">    data_path = <span class="string">"cifar10_data.npy"</span></span><br><span class="line">    label_path = <span class="string">"cifar10_label.npy"</span></span><br><span class="line">    test_data = testdataset(data_path=data_path, label_path=label_path)</span><br><span class="line">    test_loader = DataLoader(test_data, batch_size=<span class="number">128</span>)</span><br><span class="line">    print(<span class="string">'load data...'</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 干净样本准确率</span></span><br><span class="line">    print(<span class="string">'natural'</span>, end=<span class="string">', '</span>)</span><br><span class="line">    _eval(model=resnet, device=<span class="string">'cpu'</span>, test_loader=test_loader)</span><br></pre></td></tr></table></figure><p>在这样操作下，准确率只有 <code>16.89%</code> ，我也很奇怪是哪里错了。</p><h1 id="正确程序"><a href="#正确程序" class="headerlink" title="正确程序"></a>正确程序</h1><p>从师兄那里找到了一份正确的程序，准确率是 <code>92.84%</code>。后文会进行说明，这份代码只是结果对，但逻辑不对。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">from</span> resnet18 <span class="keyword">import</span> ResNet18</span><br><span class="line"><span class="keyword">from</span> collections <span class="keyword">import</span> OrderedDict</span><br><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> Dataset</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TensorDataset</span><span class="params">(Dataset)</span>:</span></span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, dataPath, labelPath)</span>:</span></span><br><span class="line">        x = np.load(dataPath)</span><br><span class="line">        x = x[<span class="number">50000</span>:] / <span class="number">255.</span></span><br><span class="line">        x = x.astype(<span class="string">"float32"</span>)</span><br><span class="line">        data = x.transpose(<span class="number">0</span>, <span class="number">3</span>, <span class="number">1</span>, <span class="number">2</span>)</span><br><span class="line">        label = np.load(labelPath)[<span class="number">50000</span>:]</span><br><span class="line">        label = np.reshape(label, (data.shape[<span class="number">0</span>], ))</span><br><span class="line">        data, label = torch.from_numpy(data), torch.from_numpy(label)</span><br><span class="line">        self.data_tensor = data</span><br><span class="line">        self.target_tensor = label</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__getitem__</span><span class="params">(self, index)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> self.data_tensor[index], self.target_tensor[index]</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__len__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> self.data_tensor.size(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">net = ResNet18()</span><br><span class="line">resnet_path = <span class="string">"resnet18_ckpt.pth"</span></span><br><span class="line">d = torch.load(resnet_path, map_location=torch.device(<span class="string">'cpu'</span>))[<span class="string">'net'</span>]</span><br><span class="line">d = OrderedDict([(k[<span class="number">7</span>:], v) <span class="keyword">for</span> (k, v) <span class="keyword">in</span> d.items()])</span><br><span class="line">net.load_state_dict(d)</span><br><span class="line">dataPath = <span class="string">"cifar10_data.npy"</span></span><br><span class="line">labelPath = <span class="string">"cifar10_label.npy"</span></span><br><span class="line">dataset = TensorDataset(dataPath, labelPath)</span><br><span class="line">dataloader = torch.utils.data.DataLoader(dataset,</span><br><span class="line">                                         batch_size=<span class="number">64</span>,</span><br><span class="line">                                         shuffle=<span class="literal">True</span>,</span><br><span class="line">                                         num_workers=<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">total = <span class="number">0</span></span><br><span class="line">correct = <span class="number">0</span></span><br><span class="line"><span class="keyword">for</span> batch_idx, (inputs, targets) <span class="keyword">in</span> enumerate(dataloader):</span><br><span class="line">    outputs = net(inputs)</span><br><span class="line">    _, predicted = outputs.max(<span class="number">1</span>)</span><br><span class="line">    total += targets.size(<span class="number">0</span>)</span><br><span class="line">    correct += predicted.eq(targets).sum().item()</span><br><span class="line">acc = <span class="number">100.</span> * correct / total</span><br><span class="line">print(acc)</span><br></pre></td></tr></table></figure><h1 id="问题分析"><a href="#问题分析" class="headerlink" title="问题分析"></a>问题分析</h1><p>两份代码的数据加载、准确率计算都是正确的。经过二分法逐行注释，终于找到了问题所在，来依次分析一下。</p><p>第一个问题，因为网络的输入是 <code>channel first</code>，而 <code>numpy</code> 的数据中 <code>channel</code> 位于最后面，所以需要对数据进行转置处理。<strong>但是这里有坑</strong>。假设，图片之前的维度是 <code>height, width, channel</code>，大小是 <code>32 X 32 X 3</code>。</p><ul><li>假设此时转置的方法是 <code>np.transpose(2, 0, 1)</code>，得到矩阵的维度是 <code>channel, height, width</code>，大小是 <code>3 X 32 X 32</code>，这样输入网络是没有问题的。</li><li>假设此时转置的方法是 <code>np.transpose(2, 1, 0)</code>，得到矩阵的维度是 <code>channel, width, height</code>，大小是 <code>3 X 32 X 32</code>，这样输入网络不会报错，但准确率会很低，大概只有 <code>50%</code>，究其原因是数据增强所导致的。</li></ul><p>第二个问题，是 <code>model.eval()</code> 导致的，如果开启这个，准确率就会很低；如果不开启，准确率又正常，所以来深究一下这是为哈。<del>踩坑无数。</del>首先来回顾下 <code>pytorch</code> 的训练过程：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> x, y <span class="keyword">in</span> dataloader:</span><br><span class="line">    <span class="comment"># 模型输出</span></span><br><span class="line">    y_pre = model(x)</span><br><span class="line">    <span class="comment"># 计算损失</span></span><br><span class="line">    loss = criterion(y_pre, y)</span><br><span class="line">    <span class="comment"># 上次计算遗留的梯度清空，准备反向传播</span></span><br><span class="line">    optimizer.zero_grad()</span><br><span class="line">    <span class="comment"># 计算梯度，不反向传播</span></span><br><span class="line">    loss.backward()</span><br><span class="line">    <span class="comment"># 更新参数</span></span><br><span class="line">    optimizer.step()</span><br></pre></td></tr></table></figure><p>而 <code>model.eval()</code> 会将 <code>batchnormal</code> 和 <code>dropout</code> 固定住，用训练好的值。否则，一旦测试时数据的 <code>batchsize</code> 变小，就会导致图像失真，模型准确率降低。这<strong>可能</strong>是问题所在。且在使用 <code>pytorch</code> 训练模型时，一定要注意 <code>train</code> 和 <code>eval</code> 模式的切换。</p><p>沿着这个思路，先在一些网站先找到一些<strong>可能</strong>的解决方案 <sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup> <sup id="fnref:4"><a href="#fn:4" rel="footnote">4</a></sup>。这里说可能是因为：任何问题都需要上下文才能知道准确的解决方案，即使描述的是同一个问题，也可能有多种解决方案。里面大概的解决方案如下：</p><ul><li><code>batchsize</code> 很小导致的，但我这里 <code>batchsize</code> 已经是 128 了，显然并不是这个原因</li><li>许多地方用同一个 <code>bn</code> 层，但我打开训练代码 <sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup> ，里面 <code>bn</code> 层没有共用，所以也不是这个原因</li><li>在训练阶段，将模型设置为 <code>model.train()</code> 之前，一定要更新参数，包括 <code>zero_grad</code>，但我看了代码，不是这个问题</li><li><code>bn</code> 层的参数 <code>track_running_stats</code> 导致的。那我们来看看这个参数是啥意思，打开原文档 <sup id="fnref:5"><a href="#fn:5" rel="footnote">5</a></sup> ，发现描述的佷晦涩，我没看懂。所以在网上找了找 <sup id="fnref:6"><a href="#fn:6" rel="footnote">6</a></sup> ，意思是，如果这个参数为 <code>True</code>，<code>bn</code> 就会追踪历史数据，以滑动加权平均的方式来更新 $\mu$ 和 $\sigma$。而 <code>track_running_stats</code> 取值为 <code>False</code>，就不会追踪历史数据，只会根据当前的 <code>batch</code> 计算均值和方差。<ul><li>可能由于数据比较不稳且 <code>batchsize</code> 很小影响了精度，但我打开程序，也不是这个原因。何况 <code>model.eval()</code> 模式下，<code>track_running_stats</code> 取值为 <code>True</code>，使用训练好的模型参数，且 <code>bn</code> 的参数也不会更新。</li><li>多说一些，<code>bn</code> 的均值和方差是在 <code>forward</code> 方法中更新的 <sup id="fnref:7"><a href="#fn:7" rel="footnote">7</a></sup> ，而不是在 <code>optimizer.step</code> 中更新。所以处于训练模式，<code>bn</code> 的参数不需要反向传播仍然能更新。<strong>这里需要注意。</strong></li></ul></li></ul><p>以上，也就是打开搜索引擎，能看到了解决方案了。<del>我屏蔽了某DN，某园等，以及另外的一些抄袭社区等等，他们的解决方案我看不到。</del>捕获回过头来分析下我看到的解决方案，也能意识到是数据处理部分的问题了。反过来想，训练模式下精度很高，说明 <code>bn</code> 层在动，而 <code>bn</code> 影响的是数据。众所周知，颠覆结果的不是模型，是数据，所以猜测是数据的问题。</p><p>打开源程序，我们发现训练阶段对数据做了标准化处理 <sup id="fnref:8"><a href="#fn:8" rel="footnote">8</a></sup>；而我们处理测试数据时，只是单纯的除以 255，保证数据在 0 到 1 之间，<strong>但这是不对的</strong>。那么也大概找到了解决方案，加载测试数据的时候进行标准化，此时模型的精度终于正确了！！！原因居然是：数据没有标准化处理。而 <code>train</code> 模式下精度很高，可能是因为通过 <code>bn</code> 层调整了数据分布。所以正确代码如下：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">testdataset</span><span class="params">(Dataset)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, data_path, label_path)</span>:</span></span><br><span class="line">        <span class="comment"># 模型是预训练好的，取后面 10000 个做测试</span></span><br><span class="line">        self.x_data = np.load(data_path)</span><br><span class="line">        <span class="comment"># 数据到 [0, 1] 之间</span></span><br><span class="line">        self.x_data = self.x_data / <span class="number">255</span></span><br><span class="line">        self.x_data = self.x_data[<span class="number">50000</span>:]</span><br><span class="line">        self.y_data = np.load(label_path)</span><br><span class="line">        self.y_data = self.y_data[<span class="number">50000</span>:]</span><br><span class="line"></span><br><span class="line">        mean = np.array([<span class="number">0.4914</span>, <span class="number">0.4822</span>, <span class="number">0.4465</span>]).reshape(<span class="number">1</span>, <span class="number">3</span>)</span><br><span class="line">        var = np.array([<span class="number">0.2023</span>, <span class="number">0.1994</span>, <span class="number">0.2010</span>]).reshape(<span class="number">1</span>, <span class="number">3</span>)</span><br><span class="line">        self.x_data = (self.x_data - mean) / var</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__getitem__</span><span class="params">(self, index)</span>:</span></span><br><span class="line">        x_ = self.x_data[index]</span><br><span class="line">        x_ = x_.transpose(<span class="number">2</span>, <span class="number">0</span>, <span class="number">1</span>)</span><br><span class="line">        y_ = self.y_data[index]</span><br><span class="line">        <span class="keyword">return</span> torch.from_numpy(x_), torch.from_numpy(y_)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__len__</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> len(self.x_data)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    ...</span><br><span class="line">    net.eval()</span><br><span class="line">    ...</span><br><span class="line">    predict(net, testloader)</span><br><span class="line">    ...</span><br></pre></td></tr></table></figure><h1 id="标准化"><a href="#标准化" class="headerlink" title="标准化"></a>标准化</h1><p>既然标准化如此重要，就来回顾下标准化的用途。</p><ul><li>第一步，数据归一化，也就是映射到 0 到 1 之间，这是为了防止梯度爆炸以及特征尺度的缩放。<ul><li>因为误差反向传播求偏导的时候，会作用到原始数据 $x$，如果 $x$ 的取值是 255，梯度瞬间爆炸。</li><li>一个特征的变化范围可能是[1000,10000]，另一个特征的变化范围可能是[−0.1,0.2]，在进行距离有关的计算时，单位的不同会导致计算结果的不同，尺度大的特征会起决定性作用。所以要归一化，消除特征间单位和尺度差异的影响。刚才代码归一化的方式很简单，就是除以 255，当然还有很多其它的方式以及优缺点 <sup id="fnref:9"><a href="#fn:9" rel="footnote">9</a></sup> <sup id="fnref:10"><a href="#fn:10" rel="footnote">10</a></sup>。</li></ul></li></ul><p>在数据归一化后，可以标准化也可以不标准化，上述代码所用的 Z-Score 标准化的意思就是将数据映射到均值为 0，方差为 1 的分布空间中。</p><p>\begin{equation}<br>x = \frac{x-\mu}{\sigma}<br>\end{equation}</p><ul><li>此类标准化是通过特征的平均值和标准差，将特征缩放成一个标准的正态分布，缩放后均值为0，方差为1。特别适用于数据的最大值和最小值未知，或存在孤立点。</li><li>标准化是为了方便数据的下一步处理，而进行的数据缩放等变换，不同于归一化，并不是为了方便与其他数据一同处理或比较。归一化是为了消除纲量压缩到 <code>[0, 1]</code> 区间；标准化只是调整特征整体的分布，也就是平移到原点附近。且，归一化与最大，最小值有关；标准化与均值，标准差有关。</li><li>估算均值与方差需要总体的平均值与方差，但是这一值在真实的分析与挖掘中很难得到，大多数情况下是用样本的均值与标准差替代，所以一般要求数据符合正态分布。</li></ul><p>因此，也很容易定位到问题所在。训练的时候归一化处理的数据，而预测的时候没有，数据在两种不同的分布空间，所以导致预测精度降低。<del>本文记录了全部 debug 的过程，学到了好多东西（误</del></p><h1 id="reference"><a href="#reference" class="headerlink" title="reference"></a>reference</h1><p><sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup>：<a href="https://github.com/laisimiao/classification-cifar10-pytorch" target="_blank" rel="noopener">https://github.com/laisimiao/classification-cifar10-pytorch</a><br><sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup>：<a href="https://www.zhihu.com/question/354742972" target="_blank" rel="noopener">https://www.zhihu.com/question/354742972</a><br><sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup>：<a href="https://github.com/laisimiao/classification-cifar10-pytorch/blob/master/models/resnet.py#L60-L64" target="_blank" rel="noopener">https://github.com/laisimiao/classification-cifar10-pytorch/blob/master/models/resnet.py#L60-L64</a><br><sup id="fnref:4"><a href="#fn:4" rel="footnote">4</a></sup>：<a href="https://discuss.pytorch.org/t/performance-highly-degraded-when-eval-is-activated-in-the-test-phase/3323" target="_blank" rel="noopener">https://discuss.pytorch.org/t/performance-highly-degraded-when-eval-is-activated-in-the-test-phase/3323</a><br><sup id="fnref:5"><a href="#fn:5" rel="footnote">5</a></sup>：<a href="https://pytorch.org/docs/stable/generated/torch.nn.BatchNorm2d.html" target="_blank" rel="noopener">https://pytorch.org/docs/stable/generated/torch.nn.BatchNorm2d.html</a><br><sup id="fnref:6"><a href="#fn:6" rel="footnote">6</a></sup>：<a href="https://www.zhihu.com/question/282672547" target="_blank" rel="noopener">https://www.zhihu.com/question/282672547</a><br><sup id="fnref:7"><a href="#fn:7" rel="footnote">7</a></sup>：<a href="https://pytorch.org/docs/stable/_modules/torch/nn/modules/batchnorm.html#BatchNorm2d" target="_blank" rel="noopener">https://pytorch.org/docs/stable/_modules/torch/nn/modules/batchnorm.html#BatchNorm2d</a><br><sup id="fnref:8"><a href="#fn:8" rel="footnote">8</a></sup>：<a href="https://github.com/laisimiao/classification-cifar10-pytorch/blob/master/main.py#L38-L41" target="_blank" rel="noopener">https://github.com/laisimiao/classification-cifar10-pytorch/blob/master/main.py#L38-L41</a><br><sup id="fnref:9"><a href="#fn:9" rel="footnote">9</a></sup>：<a href="https://ssjcoding.github.io/2019/03/27/normalization-and-standardization/" target="_blank" rel="noopener">https://ssjcoding.github.io/2019/03/27/normalization-and-standardization/</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;今日在写程序时，遇到了一个蜜汁 &lt;code&gt;bug&lt;/code&gt;，加载别人训练好的 &lt;code&gt;ResNet18&lt;/code&gt;，识别精度很低，只有 16%，但理论上而言应该有 92%，我也好奇那 80% 的准确率去哪里了。而程序和数据本身又无错误，所以来探究一下这是为什么。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="DeepLearning" scheme="https://muyuuuu.github.io/tags/DeepLearning/"/>
    
  </entry>
  
  <entry>
    <title>对抗攻击篇：CW 攻击算法</title>
    <link href="https://muyuuuu.github.io/2021/05/04/CW-attack/"/>
    <id>https://muyuuuu.github.io/2021/05/04/CW-attack/</id>
    <published>2021-05-04T04:08:04.000Z</published>
    <updated>2021-05-13T03:52:21.787Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>五一闲的没事继续开坑，差不多也该做论文了。等看完一些经典的攻击算法后，做一些复现和对比实验，然后去看经典的防御算法。CW 算法是一种基于优化的攻击算法 <sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup>，而论文的创新点在于损失函数的定义与梯度的截断。</p><a id="more"></a><h1 id="L-BFGS"><a href="#L-BFGS" class="headerlink" title="L-BFGS"></a>L-BFGS</h1><p>从最开始的 L-BFGS <sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup> 出发，通过 $L_2$ 范数距离寻找和原始样本 $x$ 最接近的对抗样本 $x’$，而寻找对抗样本的过程就是解决以下优化问题：</p><p>\begin{equation}<br>\begin{aligned}<br>\min &amp; \; c\Vert x-x’ \Vert_2 + \text{loss}_{F,t}(x’) \\<br>\text{s.t.} &amp; \; x’ \in [0,1]<br>\end{aligned}<br>\end{equation}</p><p>其中 $c$ 是一个常数，表示损失函数权重为 1 的情况下，对抗样本与原始样本距离的权重应该是多少；$t$ 表示错误分类的标签，$x$ 是已知的可以视为常量，那么唯一的变量就是 $x’$，可以通过一维线搜索的问题求解到 $c$ 的取值。</p><p>论文中给出了 $t$ 的选择方法。假设当前为 10 分类任务，正确类别是 0，那么 $t$ 可以取 $1,2,\cdots,9$，那么取哪一个合适呢？换句话说，手写字体识别中，1 和 7 在字迹潦草的情况下容易被认错，那么正确类别是 1 时，对抗样本向 7 靠近，更容易攻击成功。</p><ul><li>平均选择，均匀的在错误类别中随机选择</li><li>最佳选择，在所有错误类别上进行攻击，选择最容易攻击的</li><li>最差选择，在所有错误类别上进行攻击，选择最难以攻击的</li></ul><h1 id="CW"><a href="#CW" class="headerlink" title="CW"></a>CW</h1><p>从最终输出概率的角度而言，获取最容易攻击的类别和最难以攻击的类别并不难。理解上述公式后，转换到 CW 算法论文中的攻击算法，用公式描述：</p><p>\begin{equation}<br>\begin{aligned}<br>\min &amp; \;  D(x, x+\delta) \\<br>\text{s.t.} &amp; \;  C(x+\delta) = t \\<br>{ } &amp; \;  x + \delta \in [0,1]<br>\end{aligned}<br>\end{equation}</p><p>$t$ 依然是被错误分类的标签，$C$ 表示分类结果，$D$是距离度量函数，而本文选择的距离是 $L_0, L_2, L_\inf$ 三种范数距离。论文中指出：没有任何一种距离能衡量人类感知的差异，现有距离的度量方式都有或多或少的缺陷，所以本文选取了三种范数距离，但每一种距离也达到了目前最好的攻击效果。未来的工作中，可以好好研究下如何构造合适的距离度量函数。</p><h1 id="解非线性优化"><a href="#解非线性优化" class="headerlink" title="解非线性优化"></a>解非线性优化</h1><p>回到主题，将攻击公式化后，尝试借助优化的方法去求解问题。但由于 $C(x+\delta)=t$ 具有高度的非线性，所以选择一种更适合优化的表达方式。定义一个目标函数 $f$，当且仅当 $f(x+\delta) \leq 0$ 时，$C(x+\delta)=t$。论文中给出了 7 种 $f$ 的选择，但我懒，准备少写几个，不影响</p><p>\begin{equation}<br>\begin{aligned}<br>f_1(x’) &amp;= -\text{loss}_{F,t}(x’) + 1 \\<br>f_2(x’) &amp;= \Big( \max_{i\neq t} \big(Z(x’)_i\big) - Z(x’)_t \Big)^+ \\<br>f_3(x’) &amp;= \big( 0.5-F(x’)_t \big)<br>\end{aligned}<br>\end{equation}</p><p>其中，$e^+$ 运算表示 $\max(e,0)$；$-\text{loss}$ 表示交叉熵损失；$F(x’)_i$ 表示神经网络使用 $x’$ 作为输入，产生类别是 $i$ 的概率；$Z(x’)$ 表示 softmax 前的输出，即 $F(x)=\text{softmax}(Z(x))$。其中，效果最好的是第二个。那我们就用第二个公式解释下这些都是什么。</p><p>在论文中，$i$ 表示 $x’$ 对应的正确类别；$t$ 表示 $x’$ 对应的错误类别。而 $C(x+\delta)=t$ 表示希望对抗样本被错误分类，此时按照假设，$f(x+\delta) \leq 0$。我们带入$f_2$，$\max_{i\neq t} \big(Z(x’)_i\big)$ 表示除了类别 $t$ 以外，网络以最大概率认为这是第 $i$ 个类，但第 $i$ 类的概率仍然低于第 $t$ 类的概率，以此认为攻击成功。$f_3$ 同理，表示分类错误的概率大于 0.5。因此，优化目标修改为：</p><p>\begin{equation}<br>\begin{aligned}<br>\min &amp; \;  D(x, x+\delta) \\<br>\text{s.t.} &amp; \;  f(x+\delta) \leq 0 \\<br>{ } &amp; \;  x + \delta \in [0,1]<br>\end{aligned}<br>\end{equation}</p><p>为了便于优化，按着 L-BFGS 的形式就行修改，将约束条件转为目标函数。第一项表示对抗样本要接近原始样本，第二项表示分类越错越好。</p><p>\begin{equation}<br>\begin{aligned}<br>\min &amp; \;  \Vert \delta \Vert_p + c f(x+\delta)\\<br>\text{s.t.} &amp; \;  x + \delta \in [0,1]<br>\end{aligned}<br>\end{equation}</p><p>其中，$c$ 的选择是一个坑，如果 $c\to 0$，那么梯度下降时，损失大部分来自图像误差；如果 $c$ 很大，那么分类损失将占主导，两者的损失应该相似。论文中，作者使用实验的方式确定了 $c$ 的最佳取值。</p><h1 id="梯度截断"><a href="#梯度截断" class="headerlink" title="梯度截断"></a>梯度截断</h1><p>因为对抗样本增加、减去梯度后很容易超出 $[0,1]$ 的范围，所以目前有一些截断的方法：</p><ul><li>梯度投影下降，对溢出部分直接截断，把截断后的对抗样本带入下一轮，但容易将截断带来的误差给传入下一轮迭代，这样会让误差越来越大。</li><li>梯度截断下降，不直接截断样本，而是将样本的截断代入要最小化的目标函数中，用 $f(\min(\max(x+\delta,0),1))$ 来代替 $f(x+\delta)$，但会带来梯度消失的问题。如当 $x$ 很大时取值为 1，且偏导数取值为 0。但反向传播时，无法更新 $x$。</li></ul><p>因此，本文引入变量 $w$，对抗样本可以表示为：</p><p>\begin{equation}<br>x+\delta=\frac{1}{2}\big( \tanh(w) + 1 \big)<br>\end{equation}</p><p>因为 $\tanh$ 的取值范围是 $[-1,1]$，所以 $x+\delta$ 的取值范围是 $[0,1]$，这样就满足了约束，也是一种光滑的截断，也算是本文比较大的创新点吧。</p><h1 id="开始攻击"><a href="#开始攻击" class="headerlink" title="开始攻击"></a>开始攻击</h1><p>我看论文中，$L_0$ 和 $L_\inf$ 都有一些缺陷，所以重点写 $L_2$ 攻击了。此时的方程为：</p><p>\begin{equation}<br>\begin{aligned}<br>\min &amp; \;  \Big| \frac{1}{2}\big(\tanh(w) + 1\big) - x \Big|_2 + c f\big(\frac{1}{2}(\tanh(w) + 1)\big) \\<br>f(x) &amp; = \max\Big( \max\big(Z(x)_{i:i\neq t}\big) - Z(x)_t, -k\Big)<br>\end{aligned}<br>\end{equation}</p><p>参数 $k$ 用于控制错误分类的置信度，论文中 $k=0$，即保证被正确分类的概率低于被错误分类的概率。他人实现的程序 <sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup>，但是程序的实现和论文的内容有一些出入。论文中，$Z(x)_t$ 表示错误分类。在实现用，用真实标签的概率 $y_\text{true}$ 减去被错误识别的概率 $y_\text{false}$ 中最大的一个。</p><ul><li>若这一项大于0，会有损失，表示要降低对真实标签的可信度；</li><li>若这一相小于0，取值为0，表示无法识别正确分类。此时没有损失，满足之前的当且仅当 $f(x+\delta) \leq 0$ 时，$C(x+\delta)=t$，也就是，分类错误后，就不用优化这一项了；换句话说，在不满足 $C(x+\delta)=t$ 约束下，是没有目标函数的损失值的。这里的确需要好好理解。</li></ul><h1 id="程序-4"><a href="#程序-4" class="headerlink" title="程序 4"></a>程序 <sup id="fnref:4"><a href="#fn:4" rel="footnote">4</a></sup></h1><ul><li><code>fgsm_attack.py</code>，使用 <code>fgsm</code> 算法制作对抗样本</li><li><code>resnet</code>，使用 <code>ResNet50</code> 制作对抗样本</li><li><code>vgg</code>，黑盒攻击，使用 <code>VGG16</code> 作为目标模型</li><li><code>eval.py</code>，验证目标模型在对抗样本攻击下的准确率</li></ul><div class="table-container"><table><thead><tr><th>攻击模型</th><th>目标模型</th><th>原始准确率</th><th>L2攻击</th></tr></thead><tbody><tr><td>ResNet50</td><td>VGG16</td><td>94.27</td><td>53.63</td></tr></tbody></table></div><p>预训练模型<a href="https://github.com/laisimiao/classification-cifar10-pytorch" target="_blank" rel="noopener">下载</a>。</p><h1 id="运行"><a href="#运行" class="headerlink" title="运行"></a>运行</h1><ul><li><code>python cw_attack.py</code>，制作对抗样本</li><li><code>python eval.py</code>，验证目标模型在对抗样本攻击下的准确率</li></ul><h1 id="references"><a href="#references" class="headerlink" title="references"></a>references</h1><p><sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup>：<a href="https://arxiv.org/abs/1608.04644" target="_blank" rel="noopener">https://arxiv.org/abs/1608.04644</a><br><sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup>：<a href="https://arxiv.org/pdf/1312.6199.pdf" target="_blank" rel="noopener">https://arxiv.org/pdf/1312.6199.pdf</a><br><sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup>：<a href="https://github.com/Harry24k/CW-pytorch/blob/master/CW.ipynb" target="_blank" rel="noopener">https://github.com/Harry24k/CW-pytorch/blob/master/CW.ipynb</a><br><sup id="fnref:4"><a href="#fn:4" rel="footnote">4</a></sup>：<a href="https://github.com/muyuuuu/Adversarial-Attack/tree/main/CW_L2" target="_blank" rel="noopener">https://github.com/muyuuuu/Adversarial-Attack/tree/main/CW_L2</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;五一闲的没事继续开坑，差不多也该做论文了。等看完一些经典的攻击算法后，做一些复现和对比实验，然后去看经典的防御算法。CW 算法是一种基于优化的攻击算法 &lt;sup id=&quot;fnref:1&quot;&gt;&lt;a href=&quot;#fn:1&quot; rel=&quot;footnote&quot;&gt;1&lt;/a&gt;&lt;/sup&gt;，而论文的创新点在于损失函数的定义与梯度的截断。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="DeepLearning" scheme="https://muyuuuu.github.io/tags/DeepLearning/"/>
    
  </entry>
  
  <entry>
    <title>目标检测篇：目标检测快速训练与推理框架</title>
    <link href="https://muyuuuu.github.io/2021/05/03/fast-train-eval-platform-fasterrcnn-torch/"/>
    <id>https://muyuuuu.github.io/2021/05/03/fast-train-eval-platform-fasterrcnn-torch/</id>
    <published>2021-05-03T06:24:52.000Z</published>
    <updated>2021-05-09T10:53:34.494Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>最早在天池玩耍的时候接触到了目标检测。当时真的啥都不知道，头铁，一点点的开坑造轮子。后来再看前几名开源的程序，发现有很多库可以使用，切图，目标检测等，比如数据增强、目标检测都有现成的工具箱。所以想着，就先用调库的形式写一个简单的 <code>baseline</code>，下一个任务直接用现成的代码，省点事，所以写了一个这样的简单的平台。</p><a id="more"></a><p>注意：</p><ul><li>本文调库，且只针对 <code>COCO</code> 数据集。</li><li>平台很简单很简单，我不想写的太复杂。原因是：复杂的代码容易让使用者头晕，其次，如果真的要改形如损失函数等细节，调库肯定满足不了，这时候就要自己写。但平台应该足够抽象和简单，只是用来观察初步效果，不应该包含这些复杂的东西。<strong>对修改封闭，对扩展开放。</strong></li><li>因为这次接的任务涉及到了对抗样本，所以后期肯定要改损失函数，到时候在写针对细节的程序。</li></ul><h1 id="程序"><a href="#程序" class="headerlink" title="程序"></a>程序</h1><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">torch             1.7.1+cu92</span><br><span class="line">torchvision       0.8.2+cu92</span><br><span class="line">pycocotools       2.0.2</span><br></pre></td></tr></table></figure><p><a href="https://github.com/muyuuuu/Faster-RCNN-COCO" target="_blank" rel="noopener">https://github.com/muyuuuu/Faster-RCNN-COCO</a></p><ul><li><code>preprocess</code> 文件夹，这俩代码都是单独执行的<ul><li><code>mean-std.py</code>，计算样本三通道的均值与方差，用于数据标准化</li><li><code>reconstruct-anno.py</code>，重构 <code>json</code>，支持多个 <code>batchsize</code></li></ul></li><li><code>model</code> 文件夹<ul><li><code>data_helper.py</code>，加载数据</li><li><code>engine.py</code>，训练与推理的具体过程</li><li><code>eval.py</code>，推理程序，<code>python eval.py</code> 执行</li><li><code>model.py</code>，模型</li><li><code>train.py</code>，训练程序，<code>python train.py</code> 执行</li><li><code>utils.py</code>，保存、加载模型和写入日志</li></ul></li></ul><h1 id="框架"><a href="#框架" class="headerlink" title="框架"></a>框架</h1><p>首先，掏出 <code>torchvision</code>，找到其实现的 <code>fasterrcnn_resnet50_fpn</code> 模型。查看其源程序：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">fasterrcnn_resnet50_fpn</span><span class="params">(pretrained=False, progress=True,</span></span></span><br><span class="line"><span class="function"><span class="params">                            num_classes=<span class="number">91</span>, pretrained_backbone=True,</span></span></span><br><span class="line"><span class="function"><span class="params">                            trainable_backbone_layers=None, **kwargs)</span>:</span></span><br><span class="line">    trainable_backbone_layers = _validate_trainable_layers(</span><br><span class="line">        pretrained <span class="keyword">or</span> pretrained_backbone, trainable_backbone_layers, <span class="number">5</span>, <span class="number">3</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> pretrained:</span><br><span class="line">        <span class="comment"># no need to download the backbone if pretrained is set</span></span><br><span class="line">        pretrained_backbone = <span class="literal">False</span></span><br><span class="line">    backbone = resnet_fpn_backbone(<span class="string">'resnet50'</span>, pretrained_backbone, </span><br><span class="line">    trainable_layers=trainable_backbone_layers)</span><br><span class="line">    model = FasterRCNN(backbone, num_classes, **kwargs)</span><br><span class="line">    <span class="keyword">if</span> pretrained:</span><br><span class="line">        state_dict = load_state_dict_from_url(</span><br><span class="line">            model_urls[<span class="string">'fasterrcnn_resnet50_fpn_coco'</span>],</span><br><span class="line">            progress=progress)</span><br><span class="line">        model.load_state_dict(state_dict)</span><br><span class="line">        overwrite_eps(model, <span class="number">0.0</span>)</span><br><span class="line">    <span class="keyword">return</span> model</span><br></pre></td></tr></table></figure><ul><li><code>pretrained</code> 取值为 <code>False</code> 的情况下，<code>pretrained_backbone</code> 取值会为 <code>True</code>，会返回在 <code>ImageNet</code> 上预训练的 backbone；<code>pretrained</code> 取值为 <code>True</code> 的情况下，<code>pretrained_backbone</code> 取值会为 <code>False</code>，将会返回一个在 <code>COCO train2017</code> 上预训练的模型；而无论如何，<code>backbone</code> 是使用了 <code>FPN</code> 机制的。</li><li><code>num_classes</code> 设置成自己需要检测的类的数量，<strong>注意多一个背景类</strong>；</li><li>观察 <code>**kwargs</code> 参数，传给了 <code>FasterRCNN</code>，所以追踪这个类，发现它能设置很多参数 <sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup> ，比如 <code>anchor</code> 的大小和比例。这个时候对原始数据 <code>EDA</code> 一下，设置合适的参数就可以了；</li></ul><p>此时创建模型的程序为：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torchvision.models.detection <span class="keyword">as</span> td</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_model</span><span class="params">(num_class)</span>:</span></span><br><span class="line">    anchor_sizes = ((<span class="number">64</span>, ), (<span class="number">128</span>, ), (<span class="number">256</span>, ), (<span class="number">512</span>, ), (<span class="number">1024</span>, ))</span><br><span class="line">    aspect_ratios = ((<span class="number">0.5</span>, <span class="number">1.0</span>, <span class="number">2.0</span>), ) * len(anchor_sizes)</span><br><span class="line">    rpn_anchor_generator = td.anchor_utils.AnchorGenerator(</span><br><span class="line">        anchor_sizes, aspect_ratios)</span><br><span class="line">    <span class="comment"># https://github.com/pytorch/vision/blob/master/torchvision/models/detection/faster_rcnn.py</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># faster rcnn，网络会再次对 图像数据进行重定义尺寸</span></span><br><span class="line">    <span class="comment"># https://github.com/pytorch/vision/blob/c2ab0c59f42babf9ad01aa616cd8a901daac86dd/torchvision/models/detection/transform.py#L64</span></span><br><span class="line">    detector = td.fasterrcnn_resnet50_fpn(</span><br><span class="line">        rpn_anchor_generator=rpn_anchor_generator, pretrained=<span class="literal">True</span>)</span><br><span class="line">    num_classes = num_class</span><br><span class="line">    <span class="comment"># ROI head 是 backbone 后，预测盒子和类别的位置</span></span><br><span class="line">    <span class="comment"># box_predictor 是 FastRCNNPredictor 类，cls_score 是类别分类器</span></span><br><span class="line">    <span class="comment"># in_features 是模型的输入特征</span></span><br><span class="line">    <span class="comment"># https://github.com/pytorch/vision/blob/5339e63148304ce32fd1cbd1e8bb74ea79458691/torchvision/models/detection/faster_rcnn.py#L263-L276</span></span><br><span class="line">    in_features = detector.roi_heads.box_predictor.cls_score.in_features</span><br><span class="line">    <span class="comment"># 设置要预测的新的分类数</span></span><br><span class="line">    detector.roi_heads.box_predictor = td.faster_rcnn.FastRCNNPredictor(</span><br><span class="line">        in_features, num_classes)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> detector</span><br></pre></td></tr></table></figure><h1 id="数据制作"><a href="#数据制作" class="headerlink" title="数据制作"></a>数据制作</h1><p>之前头铁自己写库解析 <code>anno_json</code> 去制作数据集，后来发现有它人最好的第三方工具，直接 <code>pip install pycocotools</code> 上车。然后去写自己的 <code>dataloader</code> 类就好了。注意，在构造函数中，只初始化一些工具，不要加载数据，否则会内存溢出；将数据加载推迟到 <code>__getitem__</code> 方法中，反正者里面只拿一小部分的数据。此时的核心代码为：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pycocotools.coco <span class="keyword">import</span> COCO</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">train_data_set</span><span class="params">(Dataset)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, image_dir, anno_path)</span>:</span></span><br><span class="line">        super().__init__()</span><br><span class="line">        self.image_dir = image_dir</span><br><span class="line">        <span class="comment"># COCO api class that loads COCO annotation file and prepare data structures</span></span><br><span class="line">        self.coco = COCO(anno_path)</span><br><span class="line">        <span class="comment"># 获取 image 的 id，字典转为 list</span></span><br><span class="line">        self.ids = list(self.coco.imgs.keys())</span><br><span class="line">        self.transform = transforms.Compose([</span><br><span class="line">            <span class="keyword">lambda</span> x: Image.open(x).convert(<span class="string">'RGB'</span>),</span><br><span class="line">            transforms.Resize((<span class="number">224</span>, <span class="number">224</span>)),</span><br><span class="line">            transforms.ToTensor(),</span><br><span class="line">            <span class="comment"># transforms.Normalize((0.485, 0.456, 0.406), (0.229, 0.224, 0.225))</span></span><br><span class="line">        ])</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 数据集很大时，要在 getitem 中获取</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__getitem__</span><span class="params">(self, idx)</span>:</span></span><br><span class="line">        <span class="comment"># 某个图片的 id</span></span><br><span class="line">        img_id = self.ids[idx]</span><br><span class="line">        <span class="comment"># 图片路径</span></span><br><span class="line">        path = self.image_dir + self.coco.loadImgs(img_id)[<span class="number">0</span>][<span class="string">'file_name'</span>]</span><br><span class="line">        <span class="comment"># 获取图片的 annotations 的 id</span></span><br><span class="line">        ann_ids = self.coco.getAnnIds(imgIds=img_id)</span><br><span class="line">        <span class="comment"># 根据 annotations 的 id 获取 annotions</span></span><br><span class="line">        target = self.coco.loadAnns(ann_ids)</span><br><span class="line">        <span class="keyword">return</span> self.transform(path), target</span><br></pre></td></tr></table></figure><p>但是，还没结束，这里还要改一些 <code>key</code>，因为提供的 <code>json</code> 文件中，目标区域的名字是 <code>bbox</code>，但 <code>FasterRCNN</code> 要求的 <code>key</code> 是 <code>boxes</code>。所以，这部分修改也在数据加载里面完成。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> d <span class="keyword">in</span> target:</span><br><span class="line">    v = &#123;&#125;</span><br><span class="line">    v[<span class="string">'boxes'</span>] = d.pop(<span class="string">'bbox'</span>)</span><br><span class="line">    d[<span class="string">'boxes'</span>] = v[<span class="string">'boxes'</span>]</span><br><span class="line">    x = []</span><br><span class="line">    <span class="keyword">for</span> y <span class="keyword">in</span> v[<span class="string">'boxes'</span>]:</span><br><span class="line">        x.append(y)</span><br><span class="line">    x[<span class="number">2</span>], x[<span class="number">3</span>] = x[<span class="number">0</span>] + x[<span class="number">2</span>], x[<span class="number">1</span>] + x[<span class="number">3</span>]</span><br><span class="line">    d[<span class="string">'boxes'</span>] = torch.tensor(x, dtype=torch.float32)</span><br><span class="line">    v = &#123;&#125;</span><br><span class="line">    v[<span class="string">'labels'</span>] = d.pop(<span class="string">'category_id'</span>)</span><br><span class="line">    d[<span class="string">'labels'</span>] = torch.tensor(v[<span class="string">'labels'</span>] - <span class="number">1</span>, dtype=torch.int64)</span><br></pre></td></tr></table></figure><ul><li><code>pycocotools</code> 做出来的 <code>boxes</code> 是一个列表，列表的每个元素是张量；但我们需要的是张量，张量的尺寸是 <code>[1, 4]</code>，所以有了上面的修改；此次任务不涉及 <code>mask</code>，所以没有考虑 <code>segementation</code> 的修改；</li><li>如果遇到 AssertionError: target boxes must of float type 此类错误，直接打开源代码：</li></ul><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">floating_point_types = (torch.float, torch.double, torch.half)</span><br><span class="line"><span class="keyword">assert</span> t[<span class="string">"boxes"</span>].dtype <span class="keyword">in</span> floating_point_types, <span class="string">'target boxes must of float type'</span></span><br><span class="line"><span class="keyword">assert</span> t[<span class="string">"labels"</span>].dtype == torch.int64, <span class="string">'target labels must of int64 type'</span></span><br></pre></td></tr></table></figure><p>一目了然，这并不是说 boxes 是浮点数，而是说，张量中的元素应该是 <code>torch.float32, torch.float64, torch.float16</code> 类型的，创建张量的时候要加上这个参数；</p><ul><li>如果遇到 ValueError: All bounding boxes should have positive height and width. 错误，并不是说盒子里面出现的负数，而是说，FasterRCNN 希望看到 <code>[xmin, ymin, xmax, ymax]</code> 这样的数据，而你传入的是 <code>[xmin, ymin, width, height]</code> 这样的数据。但在后面 <code>torchvision</code> 中修复了这个问题 <sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup>。</li><li>如果遇到 sampled_pos_inds_subset = torch.where(labels &gt; 0)[0] CUDA error: device-side assert triggered 这样的错误，也不要慌。有两种解决方案，选一个就好。<ul><li>这里出错的原因很简单，数据提供的类是从 1 开始编号的，假设是 <code>label = [1，2，3]</code>；在预测时，正确的标签是 3，但程序中 <code>label[3]</code> 会出错，所以，将标签改为 <code>[0,1,2]</code> 就可以了。</li><li>传入训练模型时，增加背景类。这里看自己，如果需要背景类，就第二种方案；如果不需要背景类，就第一种方案。</li></ul></li><li>而如果要追求多个 <code>batchsize</code>，只能重构 <code>json</code>，此部分程序在 <code>github</code> 中给出。</li></ul><h1 id="训练与推理"><a href="#训练与推理" class="headerlink" title="训练与推理"></a>训练与推理</h1><p>首先来看官方提供训练的例子（注意这个例子在某些版本上不一定能跑通）：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">model = torchvision.models.detection.fasterrcnn_resnet50_fpn(pretrained=<span class="literal">True</span>)</span><br><span class="line">images, boxes = torch.rand(<span class="number">4</span>, <span class="number">3</span>, <span class="number">600</span>, <span class="number">1200</span>), torch.rand(<span class="number">4</span>, <span class="number">11</span>, <span class="number">4</span>)</span><br><span class="line">labels = torch.randint(<span class="number">1</span>, <span class="number">91</span>, (<span class="number">4</span>, <span class="number">11</span>))</span><br><span class="line">images = list(image <span class="keyword">for</span> image <span class="keyword">in</span> images)</span><br><span class="line">targets = []</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(len(images)):</span><br><span class="line">    d = &#123;&#125;</span><br><span class="line">    d[<span class="string">'boxes'</span>] = boxes[i]</span><br><span class="line">    d[<span class="string">'labels'</span>] = labels[i]</span><br><span class="line">    targets.append(d)</span><br><span class="line">output = model(images, targets)</span><br><span class="line"><span class="comment"># For inference</span></span><br><span class="line">model.eval()</span><br><span class="line">x = [torch.rand(<span class="number">3</span>, <span class="number">300</span>, <span class="number">400</span>), torch.rand(<span class="number">3</span>, <span class="number">500</span>, <span class="number">400</span>)]</span><br><span class="line">predictions = model(x)</span><br></pre></td></tr></table></figure><p>所以，训练的时候，把 image 和 target 转成列表并输入就可以了。注意，列表中的每个元素的类型必须和模型参数的类型一致，别忘了 <code>to(device)</code>。我把最终训练和推理的程序放到 <code>engine</code> 程序中了：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">train_fn</span><span class="params">(train_dataloader, detector, optimizer, device, epoch, scheduler)</span>:</span></span><br><span class="line">    detector.train()</span><br><span class="line">    loss_value = <span class="number">0</span></span><br><span class="line">    cnt = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> images, target <span class="keyword">in</span> tqdm(train_dataloader):</span><br><span class="line">        cnt += <span class="number">1</span></span><br><span class="line">        images = list(image.to(device) <span class="keyword">for</span> image <span class="keyword">in</span> images)</span><br><span class="line">        <span class="comment"># it's key:value for t in targets.items</span></span><br><span class="line">        <span class="comment"># This is the format the fasterrcnn expects for targets</span></span><br><span class="line">        targets = []</span><br><span class="line">        <span class="keyword">for</span> l, b <span class="keyword">in</span> zip(target[<span class="string">'labels'</span>], target[<span class="string">'boxes'</span>]):</span><br><span class="line">            d = &#123;&#125;</span><br><span class="line">            d[<span class="string">'labels'</span>] = l.view(<span class="number">-1</span>).to(device)</span><br><span class="line">            d[<span class="string">'boxes'</span>] = b.view(<span class="number">-1</span>, <span class="number">4</span>).to(device)</span><br><span class="line">            targets.append(d)</span><br><span class="line">        loss_dict = detector(images, targets)</span><br><span class="line">        losses = sum(loss <span class="keyword">for</span> loss <span class="keyword">in</span> loss_dict.values())</span><br><span class="line">        loss_value = losses.item()</span><br><span class="line"></span><br><span class="line">        optimizer.zero_grad()</span><br><span class="line">        losses.backward()</span><br><span class="line">        optimizer.step()</span><br><span class="line"></span><br><span class="line">        scheduler.step()</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 训练数据过大，训练一部分就保存模型</span></span><br><span class="line">        <span class="keyword">if</span> cnt % <span class="number">1000</span> == <span class="number">999</span>:</span><br><span class="line">            cnt = <span class="number">0</span></span><br><span class="line">            utils.save_checkpoint_state(<span class="string">"model_tmp.pth"</span>, epoch, detector,</span><br><span class="line">                                        optimizer, scheduler)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> loss_value</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">predict</span><span class="params">(val_dataloader, detector, device)</span>:</span></span><br><span class="line">    results = []</span><br><span class="line">    <span class="keyword">for</span> images, image_names <span class="keyword">in</span> tqdm(val_dataloader):</span><br><span class="line">        images = list(image.to(device) <span class="keyword">for</span> image <span class="keyword">in</span> images)</span><br><span class="line">        model_time = time.time()</span><br><span class="line">        outputs = detector(images)</span><br><span class="line">        model_time = time.time() - model_time</span><br><span class="line">        <span class="keyword">for</span> i, image <span class="keyword">in</span> enumerate(images):</span><br><span class="line">            boxes = (outputs[i][<span class="string">"boxes"</span>].data.cpu().numpy().tolist())</span><br><span class="line">            scores = outputs[i][<span class="string">"scores"</span>].data.cpu().numpy()</span><br><span class="line">            labels = outputs[i][<span class="string">"labels"</span>].data.cpu().numpy()</span><br><span class="line">            image_id = image_names[i]</span><br><span class="line">            <span class="keyword">for</span> b, s, l <span class="keyword">in</span> zip(boxes, scores, labels):</span><br><span class="line">                <span class="keyword">if</span> s &gt; <span class="number">0.5</span>:</span><br><span class="line">                    result = &#123;</span><br><span class="line">                        <span class="string">"image_id"</span>: image_id,</span><br><span class="line">                        <span class="string">"boxes"</span>: b,</span><br><span class="line">                        <span class="string">"scores"</span>: s.astype(float),</span><br><span class="line">                        <span class="string">"labels"</span>: l.astype(float),</span><br><span class="line">                    &#125;</span><br><span class="line">                    results.append(result)</span><br><span class="line">    <span class="keyword">return</span> results</span><br></pre></td></tr></table></figure><h1 id="预测无输出"><a href="#预测无输出" class="headerlink" title="预测无输出"></a>预测无输出</h1><p>在推理时，网络对所有图片的输出都是：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">[&#123;<span class="string">'boxes'</span>: tensor([], size=(<span class="number">0</span>, <span class="number">4</span>)),</span><br><span class="line">  <span class="string">'labels'</span>: tensor([], dtype=torch.int64),</span><br><span class="line">  <span class="string">'scores'</span>: tensor([])&#125;]</span><br></pre></td></tr></table></figure><p>这个 <code>bug</code> 当时我也佷头疼，翻遍了全网，总结下可能导致此类错误的原因吧：</p><ul><li>类别数目没有加一，也就是没有考虑背景类。这个解决方案在上面已经给出。传入训练模型时，增加一个背景类</li><li><code>pretrained=False</code>。当时是我考虑少了，以为 <code>pretrained=False</code>的情况下，毕竟模型的 backbone 是经过预训练的，以为能应付简单的目标检测，后来发现是我草率了；<code>pretrained=True</code>，不仅 backbone，整个模型都是预训练的，这样才能检测到目标。</li></ul><h1 id="references"><a href="#references" class="headerlink" title="references"></a>references</h1><p><sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup>：<a href="https://github.com/pytorch/vision/blob/730c5e1eab130e2900c8e839ea08fa11f024516f/torchvision/models/detection/faster_rcnn.py#L23" target="_blank" rel="noopener">https://github.com/pytorch/vision/blob/730c5e1eab130e2900c8e839ea08fa11f024516f/torchvision/models/detection/faster_rcnn.py#L23</a><br><sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup>：<a href="https://github.com/pytorch/vision/issues/2740" target="_blank" rel="noopener">https://github.com/pytorch/vision/issues/2740</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;最早在天池玩耍的时候接触到了目标检测。当时真的啥都不知道，头铁，一点点的开坑造轮子。后来再看前几名开源的程序，发现有很多库可以使用，切图，目标检测等，比如数据增强、目标检测都有现成的工具箱。所以想着，就先用调库的形式写一个简单的 &lt;code&gt;baseline&lt;/code&gt;，下一个任务直接用现成的代码，省点事，所以写了一个这样的简单的平台。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="CV" scheme="https://muyuuuu.github.io/tags/CV/"/>
    
  </entry>
  
  <entry>
    <title>目标检测篇：FPN</title>
    <link href="https://muyuuuu.github.io/2021/05/01/FPN/"/>
    <id>https://muyuuuu.github.io/2021/05/01/FPN/</id>
    <published>2021-05-01T04:27:48.000Z</published>
    <updated>2021-05-23T05:55:03.994Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>在目标检测领域，很难保证所要检测目标的大小都是类似的，MNIST，cifar10，imageNet 等玩具数据集除外。实际场景中，往往目标大小不一致、长宽比例不一致、图片的大小也不一致。长宽比例不一致可以通过之前提到的 Faster R-CNN <sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup> 来解决。FPN 的全称是 Feature Pyramid Networks <sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup> ，本算法重点关注目标多尺度的问题。因为传统两阶段检测（区域提议、区域识别）算法基于特征图进行预测，通常来自网络骨干的最后一层，回导致小物体信息的丢失。</p><a id="more"></a><h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p><img data-src="https://z3.ax1x.com/2021/05/01/gVYavt.png" alt></p><p>来看一下传统的多尺度检测方法：</p><ul><li>图 a 中，一看就是常规的方法，将一张图片的多个尺度的特征进行预测。对某一输入图片我们通过压缩或放大从而形成不同尺寸的图片作为模型输入，使用同一模型对这些不同尺寸的图片分别处理后，最终再将这些分别得到的特征（feature maps）组合起来。此种方法缺点在于需要对同一图片在更改维度后输入处理多次，计算缓慢。</li><li>图 b 中，用单一尺寸的图片做为输入，然后经 CNN 模型处理后，拿最终一层的feature maps 作为最终的特征集。优点是计算简单，如大多数 R-CNN 系列目标检测方法所用，如 Faster R-CNN 等。因此最终这些模型对小维度的目标检测性能不是很好。我曾遇到过在 $8000\times 8000$ 的图像中检测 $10\times 10$ 目标的任务，使用普通的 backbone 去卷积时，一不小心由于 <code>stride</code> 过大，直接错过了目标，导致小目标检测的性能急剧下降。</li><li>图 c 中，用单一尺寸的图片做为输入，此方法除选取最后一层的特征外，选用稍靠下的反映图片 low level 信息的 feature maps。然后将这些不同层次的特征简单合并起来，用于最终的特征组合输出。但依然会忽略一些具有更低级别信息，对更小维度的目标检测效果就不大好。</li><li>图 d 中，用单一尺寸的图片作为输入，选取所有层的特征联合起来做为最终的特征输出。另外还对各层所反映的不同级别的特征信息进行了自上向下的整合，能更好检测目标。而此方法正是我们本文中要讲的 FPN CNN 特征提取方法。</li></ul><p>注意：</p><ul><li>对于卷积神经网络而言，不同深度对应着不同层次的语义特征，浅层网络分辨率高，学的更多是浅层特征，如细节、边缘等；深层网络分辨率低，学的更多是深层语义特征，如物体轮廓、类别等。</li></ul><h1 id="FPN"><a href="#FPN" class="headerlink" title="FPN"></a>FPN</h1><p>FPN 是传统 CNN 网络对图片信息进行表达的一种增强整合。目的是为了改进 CNN 网络的特征提取方式，从而可以使最终输出的特征更好地涵盖输入图片各个维度的信息。它包括两个基本过程：自下至上的通路，即计算不同尺寸的特征；自上至下的通路，即自上至下的特征补充。</p><h2 id="自下而上"><a href="#自下而上" class="headerlink" title="自下而上"></a>自下而上</h2><p>也就是网络的前向计算部分，而每层的输出都是上一层输出尺寸的 1/2，就完成了传统金字塔方法和 CNN 网络的名词的对应。</p><h2 id="自上而下"><a href="#自上而下" class="headerlink" title="自上而下"></a>自上而下</h2><p>将深层的有更强语义信息的 feature 经过上采样变成具有高分辨率特征图像的过程。然后再与下一层得到的 feature 经过侧边连接相加，进行增强。最底层的输出会有细节信息和高层特征，检测大目标的同时，也不会忽略小目标。</p><p>增强后的数据经过一个 <code>3×3</code> 卷积的处理，原文的意思是这个卷积能减少上采样导致混叠的不利影响，就可以作为网络的输出了。这个图看不懂的话，可以看下面的代码。代码看不懂的话，<del>我也没办法了</del>。</p><p>这里需要注意的是，<strong>每一层，也就是这三个 predict，输出的通道数是相同的。</strong></p><p><img data-src="https://z3.ax1x.com/2021/05/01/gVNOHK.png" alt></p><h1 id="与-RPN-结合"><a href="#与-RPN-结合" class="headerlink" title="与 RPN 结合"></a>与 RPN 结合</h1><p>FPN 将提取到的特征送到 RPN 中用于目标检测，RPN 再去用于用于预测边界框和前景、背景。因为 FPN 有多个层次的输出，所以在每一层的输出后面接入一个 <code>3×3</code> 卷积的处理，在共用两个并联的 <code>1x1</code> 的卷积层用于预测前景背景和目标框的位置。所以一般而言，称特征提取器为 backbone，FPN 为 neck，RPN 预测部分为 head。图片来源 <sup id="fnref:4"><a href="#fn:4" rel="footnote">4</a></sup>：</p><p><img data-src="https://miro.medium.com/1*Hi3mCsgTEjPLtWnRyXx47w.jpeg" alt></p><h1 id="与-Faster-RCNN-结合"><a href="#与-Faster-RCNN-结合" class="headerlink" title="与 Faster RCNN 结合"></a>与 Faster RCNN 结合</h1><p>这里，就参考这篇文章 <sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup> 吧，我就不照抄了。大体意思就是。在 backbone 中接入 FPN，生成不同尺寸的特征图。使用 RPN 生成不同的 ROI。根据 ROI 尺寸的不同，在不同尺寸的特征图中选择特征块，如下图所示 <sup id="fnref:4"><a href="#fn:4" rel="footnote">4</a></sup>： </p><p><img data-src="https://miro.medium.com/max/2000/1*Wvn0WG4XZ0w9Ed2fFYPrXw.jpeg" alt></p><h1 id="程序"><a href="#程序" class="headerlink" title="程序"></a>程序</h1><p>讲真，挺容易理解的。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"><span class="keyword">import</span> torch.nn.functional <span class="keyword">as</span> F</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 一个卷积残差块</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Bottleneck</span><span class="params">(nn.Module)</span>:</span></span><br><span class="line">    expansion = <span class="number">4</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, in_planes, planes, stride=<span class="number">1</span>)</span>:</span></span><br><span class="line">        super(Bottleneck, self).__init__()</span><br><span class="line">        self.conv1 = nn.Conv2d(in_channels=in_planes,</span><br><span class="line">                               out_channels=planes,</span><br><span class="line">                               kernel_size=<span class="number">1</span>,</span><br><span class="line">                               bias=<span class="literal">False</span>)</span><br><span class="line">        self.bn1 = nn.BatchNorm2d(planes)</span><br><span class="line"></span><br><span class="line">        self.conv2 = nn.Conv2d(in_channels=planes,</span><br><span class="line">                               out_channels=planes,</span><br><span class="line">                               kernel_size=<span class="number">3</span>,</span><br><span class="line">                               stride=stride,</span><br><span class="line">                               padding=<span class="number">1</span>,</span><br><span class="line">                               bias=<span class="literal">False</span>)</span><br><span class="line">        self.bn2 = nn.BatchNorm2d(planes)</span><br><span class="line"></span><br><span class="line">        self.conv3 = nn.Conv2d(in_channels=planes,</span><br><span class="line">                               out_channels=self.expansion * planes,</span><br><span class="line">                               kernel_size=<span class="number">1</span>,</span><br><span class="line">                               bias=<span class="literal">False</span>)</span><br><span class="line">        self.bn3 = nn.BatchNorm2d(num_features=self.expansion * planes)</span><br><span class="line"></span><br><span class="line">        self.shortcut = nn.Sequential()</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 步长不为 1 或者 输入特征不等于输出特征</span></span><br><span class="line">        <span class="keyword">if</span> stride != <span class="number">1</span> <span class="keyword">or</span> in_planes != self.expansion * planes:</span><br><span class="line">            <span class="comment"># 残差块</span></span><br><span class="line">            self.shortcut = nn.Sequential(</span><br><span class="line">                nn.Conv2d(in_channels=in_planes,</span><br><span class="line">                          out_channels=self.expansion * planes,</span><br><span class="line">                          kernel_size=<span class="number">1</span>,</span><br><span class="line">                          stride=stride,</span><br><span class="line">                          bias=<span class="literal">False</span>), nn.BatchNorm2d(self.expansion * planes))</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span><span class="params">(self, x)</span>:</span></span><br><span class="line">        out = F.relu(self.bn1(self.conv1(x)))</span><br><span class="line">        out = F.relu(self.bn2(self.conv2(out)))</span><br><span class="line">        out = self.bn3(self.conv3(out))</span><br><span class="line">        out += self.shortcut(x)</span><br><span class="line">        out = F.relu(out)</span><br><span class="line">        <span class="keyword">return</span> out</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">FPN</span><span class="params">(nn.Module)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, block, num_blocks)</span>:</span></span><br><span class="line">        super(FPN, self).__init__()</span><br><span class="line">        self.in_planes = <span class="number">64</span></span><br><span class="line"></span><br><span class="line">        self.conv1 = nn.Conv2d(in_channels=<span class="number">3</span>,</span><br><span class="line">                               out_channels=<span class="number">64</span>,</span><br><span class="line">                               kernel_size=<span class="number">7</span>,</span><br><span class="line">                               stride=<span class="number">2</span>,</span><br><span class="line">                               padding=<span class="number">3</span>,</span><br><span class="line">                               bias=<span class="literal">False</span>)</span><br><span class="line">        self.bn1 = nn.BatchNorm2d(<span class="number">64</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Bottom-up layers, backbone of the network</span></span><br><span class="line">        <span class="comment"># planes 是输出特征</span></span><br><span class="line">        <span class="comment"># channel 变化：3 -&gt; 64 -&gt; 64 -&gt; 256</span></span><br><span class="line">        self.layer1 = self._make_layer(block=block,</span><br><span class="line">                                       planes=<span class="number">64</span>,</span><br><span class="line">                                       num_blocks=num_blocks[<span class="number">0</span>],</span><br><span class="line">                                       stride=<span class="number">1</span>)</span><br><span class="line">        <span class="comment"># channel 变化：64*4 -&gt; 128 -&gt; 128 -&gt; 512</span></span><br><span class="line">        self.layer2 = self._make_layer(block=block,</span><br><span class="line">                                       planes=<span class="number">128</span>,</span><br><span class="line">                                       num_blocks=num_blocks[<span class="number">1</span>],</span><br><span class="line">                                       stride=<span class="number">2</span>)</span><br><span class="line">        <span class="comment"># out_channel: 1024</span></span><br><span class="line">        self.layer3 = self._make_layer(block=block,</span><br><span class="line">                                       planes=<span class="number">256</span>,</span><br><span class="line">                                       num_blocks=num_blocks[<span class="number">2</span>],</span><br><span class="line">                                       stride=<span class="number">2</span>)</span><br><span class="line">        <span class="comment"># out_channel 2048</span></span><br><span class="line">        self.layer4 = self._make_layer(block=block,</span><br><span class="line">                                       planes=<span class="number">512</span>,</span><br><span class="line">                                       num_blocks=num_blocks[<span class="number">3</span>],</span><br><span class="line">                                       stride=<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Top layer</span></span><br><span class="line">        <span class="comment"># layer4 后面接一个1x1, 256 conv，得到金字塔最顶端的feature</span></span><br><span class="line">        self.toplayer = nn.Conv2d(in_channels=<span class="number">2048</span>,</span><br><span class="line">                                  out_channels=<span class="number">256</span>,</span><br><span class="line">                                  kernel_size=<span class="number">1</span>,</span><br><span class="line">                                  stride=<span class="number">1</span>,</span><br><span class="line">                                  padding=<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Smooth layers</span></span><br><span class="line">        <span class="comment"># 这个是上面引文中提到的抗 『混叠』 的3x3卷积</span></span><br><span class="line">        <span class="comment"># 由于金字塔上的所有feature共享classifier和regressor</span></span><br><span class="line">        <span class="comment"># 要求它们的channel dimension必须一致</span></span><br><span class="line">        <span class="comment"># 这个用于多路预测</span></span><br><span class="line">        self.smooth1 = nn.Conv2d(in_channels=<span class="number">256</span>,</span><br><span class="line">                                 out_channels=<span class="number">256</span>,</span><br><span class="line">                                 kernel_size=<span class="number">3</span>,</span><br><span class="line">                                 stride=<span class="number">1</span>,</span><br><span class="line">                                 padding=<span class="number">1</span>)</span><br><span class="line">        self.smooth2 = nn.Conv2d(in_channels=<span class="number">256</span>,</span><br><span class="line">                                 out_channels=<span class="number">256</span>,</span><br><span class="line">                                 kernel_size=<span class="number">3</span>,</span><br><span class="line">                                 stride=<span class="number">1</span>,</span><br><span class="line">                                 padding=<span class="number">1</span>)</span><br><span class="line">        self.smooth3 = nn.Conv2d(in_channels=<span class="number">256</span>,</span><br><span class="line">                                 out_channels=<span class="number">256</span>,</span><br><span class="line">                                 kernel_size=<span class="number">3</span>,</span><br><span class="line">                                 stride=<span class="number">1</span>,</span><br><span class="line">                                 padding=<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Lateral layers</span></span><br><span class="line">        <span class="comment"># 为了匹配channel dimension引入的1x1卷积</span></span><br><span class="line">        <span class="comment"># 注意这些backbone之外的extra conv，输出都是256 channel</span></span><br><span class="line">        self.latlayer1 = nn.Conv2d(in_channels=<span class="number">1024</span>,</span><br><span class="line">                                   out_channels=<span class="number">256</span>,</span><br><span class="line">                                   kernel_size=<span class="number">1</span>,</span><br><span class="line">                                   stride=<span class="number">1</span>,</span><br><span class="line">                                   padding=<span class="number">0</span>)</span><br><span class="line">        self.latlayer2 = nn.Conv2d(in_channels=<span class="number">512</span>,</span><br><span class="line">                                   out_channels=<span class="number">256</span>,</span><br><span class="line">                                   kernel_size=<span class="number">1</span>,</span><br><span class="line">                                   stride=<span class="number">1</span>,</span><br><span class="line">                                   padding=<span class="number">0</span>)</span><br><span class="line">        self.latlayer3 = nn.Conv2d(in_channels=<span class="number">256</span>,</span><br><span class="line">                                   out_channels=<span class="number">256</span>,</span><br><span class="line">                                   kernel_size=<span class="number">1</span>,</span><br><span class="line">                                   stride=<span class="number">1</span>,</span><br><span class="line">                                   padding=<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">_make_layer</span><span class="params">(self, block, planes, num_blocks, stride)</span>:</span></span><br><span class="line">        strides = [stride] + [<span class="number">1</span>] * (num_blocks - <span class="number">1</span>)</span><br><span class="line">        layers = []</span><br><span class="line">        <span class="keyword">for</span> stride <span class="keyword">in</span> strides:</span><br><span class="line">            layers.append(block(self.in_planes, planes, stride))</span><br><span class="line">            self.in_planes = planes * block.expansion</span><br><span class="line">        <span class="keyword">return</span> nn.Sequential(*layers)</span><br><span class="line"></span><br><span class="line">    <span class="comment">## FPN的lateral connection部分: upsample以后，element-wise相加</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">_upsample_add</span><span class="params">(self, x, y)</span>:</span></span><br><span class="line">        _, _, H, W = y.size()</span><br><span class="line">        <span class="comment"># 上采样到指定尺寸</span></span><br><span class="line">        <span class="keyword">return</span> F.upsample(x, size=(H, W), mode=<span class="string">'bilinear'</span>) + y</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span><span class="params">(self, x)</span>:</span></span><br><span class="line">        <span class="comment"># Bottom-up</span></span><br><span class="line">        c1 = F.relu(self.bn1(self.conv1(x)))</span><br><span class="line">        c1 = F.max_pool2d(c1, kernel_size=<span class="number">3</span>, stride=<span class="number">2</span>, padding=<span class="number">1</span>)</span><br><span class="line">        c2 = self.layer1(c1)</span><br><span class="line">        c3 = self.layer2(c2)</span><br><span class="line">        c4 = self.layer3(c3)</span><br><span class="line">        c5 = self.layer4(c4)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Top-down</span></span><br><span class="line">        <span class="comment"># P5: 金字塔最顶上的feature 2048 -&gt; 256</span></span><br><span class="line">        p5 = self.toplayer(c5)</span><br><span class="line">        <span class="comment"># P4: 上一层 p5 + 侧边来的 c4</span></span><br><span class="line">        <span class="comment"># 其余同理</span></span><br><span class="line">        p4 = self._upsample_add(p5, self.latlayer1(c4))</span><br><span class="line">        p3 = self._upsample_add(p4, self.latlayer2(c3))</span><br><span class="line">        p2 = self._upsample_add(p3, self.latlayer3(c2))</span><br><span class="line"></span><br><span class="line">        p4 = self.smooth1(p4)</span><br><span class="line">        p3 = self.smooth2(p3)</span><br><span class="line">        p2 = self.smooth3(p2)</span><br><span class="line">        <span class="keyword">return</span> p2, p3, p4, p5</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">FPN101</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="comment"># 2 通过步长，控制上一层的图片尺寸是下一层图片尺寸的几倍，这里都是 2</span></span><br><span class="line">    <span class="keyword">return</span> FPN(Bottleneck, [<span class="number">2</span>, <span class="number">2</span>, <span class="number">2</span>, <span class="number">2</span>])</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">()</span>:</span></span><br><span class="line">    net = FPN101()</span><br><span class="line">    fms = net(torch.randn((<span class="number">1</span>, <span class="number">3</span>, <span class="number">600</span>, <span class="number">900</span>), requires_grad=<span class="literal">True</span>))</span><br><span class="line">    <span class="keyword">for</span> fm <span class="keyword">in</span> fms:</span><br><span class="line">        print(fm.size())</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">test()</span><br><span class="line"></span><br><span class="line"><span class="comment"># ref: https://github.com/kuangliu/pytorch-fpn/blob/master/fpn.py</span></span><br></pre></td></tr></table></figure><h1 id="references"><a href="#references" class="headerlink" title="references"></a>references</h1><p><sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup>：<a href="https://muyuuuu.github.io/2021/04/28/faster-rcnn/">Faster R-CNN</a><br><sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup>：<a href="https://arxiv.org/pdf/1612.03144.pdf" target="_blank" rel="noopener">FPN 论文</a><br><sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup>：<a href="https://xmfbit.github.io/2018/04/02/paper-fpn/" target="_blank" rel="noopener">FPN 用于 Faster R0CNN</a><br><sup id="fnref:4"><a href="#fn:4" rel="footnote">4</a></sup>：<a href="https://jonathan-hui.medium.com/understanding-feature-pyramid-networks-for-object-detection-fpn-45b227b9106c" target="_blank" rel="noopener">FPN与RPN结合</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在目标检测领域，很难保证所要检测目标的大小都是类似的，MNIST，cifar10，imageNet 等玩具数据集除外。实际场景中，往往目标大小不一致、长宽比例不一致、图片的大小也不一致。长宽比例不一致可以通过之前提到的 Faster R-CNN &lt;sup id=&quot;fnref:1&quot;&gt;&lt;a href=&quot;#fn:1&quot; rel=&quot;footnote&quot;&gt;1&lt;/a&gt;&lt;/sup&gt; 来解决。FPN 的全称是 Feature Pyramid Networks &lt;sup id=&quot;fnref:2&quot;&gt;&lt;a href=&quot;#fn:2&quot; rel=&quot;footnote&quot;&gt;2&lt;/a&gt;&lt;/sup&gt; ，本算法重点关注目标多尺度的问题。因为传统两阶段检测（区域提议、区域识别）算法基于特征图进行预测，通常来自网络骨干的最后一层，回导致小物体信息的丢失。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="CV" scheme="https://muyuuuu.github.io/tags/CV/"/>
    
  </entry>
  
  <entry>
    <title>目标检测篇：Faster R-CNN 系列</title>
    <link href="https://muyuuuu.github.io/2021/04/28/faster-rcnn/"/>
    <id>https://muyuuuu.github.io/2021/04/28/faster-rcnn/</id>
    <published>2021-04-28T07:01:05.000Z</published>
    <updated>2021-05-25T02:21:15.364Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>又来开计算机视觉的坑了，<strong>这次一定做好整理和记录，包括算法内容和程序</strong>。做好训练、保存和推理的框架，下次需要的时候直接用。做目标检测的东西，于是决定从 Fast R-CNN 重头来过了。其中，Fast R-CNN 只有理论，Faster-RCNN 部分含有代码，不过会在下一篇博客了。</p><p>既然是目标检测，就需要做两件事情，目标在哪，目标是什么。对于目标在哪的问题，可以让网络生成一个目标框，目标框有四部分组成：$(x,y,w,h)$，表示物体的起始点坐标和框的宽度与高度；对于目标是什么的问题，就是传统神经网络的多分类问题了。</p><a id="more"></a><h1 id="Fast-R-CNN"><a href="#Fast-R-CNN" class="headerlink" title="Fast R-CNN"></a>Fast R-CNN</h1><p>算法步骤与网络结构图<sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup>：</p><ol><li>首先使用 selective search 算法<sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup>搜索目标区域候选框，以下简称 SS 算法</li><li>将图像输入 backbone，一个超大规模的特征提取网络，得到一个特征图</li><li>将 SS 生成的候选框映射到特征图中，得到每个候选框的特征矩阵</li><li>将每个候选框的特征矩阵通过 ROI（region of interest）-pooling 层缩放到 $7\times 7$ 的矩阵</li><li>将矩阵展平，全连接，得到一系列的预测结果。包括预测概率和目标框，而后反向传播</li><li>这里需要注意的是，最后预测分类和预测目标边界框的两个全连接是<strong>并联</strong>的</li></ol><p><img data-src="https://z3.ax1x.com/2021/04/28/gP4CU1.png" alt></p><h2 id="详解"><a href="#详解" class="headerlink" title="详解"></a>详解</h2><p>对于算法步骤 3，特征图映射问题。是为了减少计算量，不用为每个候选框单独计算特征矩阵；而是计算一个特征矩阵后，直接选择候选框的内容。选择方式也很简单，矩阵映射就行。</p><h3 id="正样本与负样本"><a href="#正样本与负样本" class="headerlink" title="正样本与负样本"></a>正样本与负样本</h3><p>训练期间，并没有训练全部候选框的特征矩阵，只使用了其中的一小部分，比如只训练 64 个。正样本，表示含有待检测目标；负样本，表示背景区域。选用负样本的原因是，要让网络能正确识别背景，并不是所有图片都含有目标。</p><ul><li>正样本：候选框与真实框的 IOU 大于 0.5，选择其中的一部分进入网络</li><li>负样本：候选框与真实框的 IOU 在 0.1 到 0.5 之间</li></ul><p>论文中还提到了对数据做增强，当然这些并不是重点。IOU（Intersection over Union）的意思是，图像的交除以图像的并。就是蓝色区域的面积除以绿色区域的面积。</p><p><img data-src="https://z3.ax1x.com/2021/04/28/gPHdIJ.png" alt></p><h3 id="ROI-pooling"><a href="#ROI-pooling" class="headerlink" title="ROI-pooling"></a>ROI-pooling</h3><p>ROI-pooling 层的目的在于，不限制输入图像的大小。因为 ROI 后面的全连接层肯定是固定到某一个特殊维度的，而每个候选框对应特征矩阵的大小也不一致，所以，需要使用 ROI-pooling 将不同大小的特征矩阵映射到同一大小。具体方法如下，算了如图吧，如对任意大小的特征矩阵映射为 $2\times2$的特征矩阵，取其中的最大值或平均值。$7\times7$ 也是同一个道理。</p><p><img data-src="https://z3.ax1x.com/2021/04/28/gPIzDg.png" alt></p><h3 id="预测输出与反向传播"><a href="#预测输出与反向传播" class="headerlink" title="预测输出与反向传播"></a>预测输出与反向传播</h3><p>先来看概率预测。因为含有负样本，因此网络要识别 $N$ 的类的话，网络输出的概率就是 $N+1$，多一个背景类。ROI-pooling 后，经过两个全连接得到特征向量。特征向量经过第一个<strong>并联</strong>的全连接，softmax 后输出标签概率；经过第二个<strong>并联</strong>的全连接，输出目标框的位置参数。</p><ul><li>对于第一个并联的全连接而言，只需要和真实标签进行交叉熵<sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup>即可，<strong>分类损失</strong>记为 $L_1$；</li></ul><p>接下来重点讲输出目标边界框的部分，如何使输出的目标框越来越接近真实的目标框呢？对于边界框回归器，也就是第二个并联的全连接，输出 <code>N+1</code> 个类别候选边界框的回归参数 $(d_x,d_y,d_w,d_h)$，共输出 $4(N+1)$ 个参数。我们设当前 SS 算法得到候选框的<strong>中心点坐标</strong>和宽高为 $(P_x,P_y,P_w,P_h)$，最终预测的边界框的参数 $(G_x, G_y, G_w, G_h)$ 为：</p><p>\begin{equation}<br>    \begin{aligned}<br>        G_x &amp;= P_wd_x + P_x \\<br>        G_y &amp;= P_hd_y + P_y \\<br>        G_w &amp;= P_w \exp{(d_w)} \\<br>        G_h &amp;= P_h \exp{(d_h)}<br>    \end{aligned}<br>\end{equation}</p><p>可视化一下得到的最终预测的坐标 $(G_x, G_y, G_w, G_h)$ ，也就是，红色框是之前的候选框 $(P_x,P_y,P_w,P_h)$；绿色框是标注的候选框；经过神经网络的输出 $(d_x,d_y,d_w,d_h)$ 后，移动到蓝色框位置，也就是 $(G_x, G_y, G_w, G_h)$。</p><p><img data-src="https://z3.ax1x.com/2021/04/28/gipoqA.png" alt></p><p>在计算完候选框之后，现在就要计算损失反向传播了。假设此时真实的类别标签是 $u$，第 $u$ 个类别的目标边界框的输出为 $(d_x^u,d_y^u,d_w^u,d_h^u)$，真实目标边界框的参数为 $(v_x,v_y,v_w,v_h)$。</p><p><strong>边界框损失</strong>的损失为：</p><p>\begin{equation}<br>    \begin{aligned}<br>    L_2 &amp;= \sum_{i\in{x,y,w,h}}\text{smooth}(t_i^u-v_i) \\<br>    \text{smooth}(x) &amp;=<br>        \begin{cases}<br>        0.5 x^2 &amp; |x| &lt; 1 \\<br>        |x| - 0.5 &amp; \text{other}<br>        \end{cases}<br>    \end{aligned}<br>\end{equation}</p><p>这个损失在绝对差值大于1时不求平方，可以避免梯度爆炸。大部分回归问题都可以适用，尤其是数值比较大的时候。此时，最终的损失函数为：</p><p>\begin{equation}<br>L = L_1 + \lambda[\mu\geq 1]L_2<br>\end{equation}</p><p>$[u\geq 1]$ 表示爱佛森括号，意思是，当 $u\geq 1$ 时，$[u\geq 1] = 1$，否则 $[u\geq 1] = 0$；$u=0$时，表示当前是背景，背景是没有边界框的，也就没有边界框回归损失这一项。$\lambda$ 是权重。好滴，Fast R-CNN 记录完毕。Faster RCNN 就相对简单点了，只是在 Fast R-CNN 的基础上使用 RPN 替换了 SS 算法。</p><h1 id="Faster-RCNN"><a href="#Faster-RCNN" class="headerlink" title="Faster-RCNN"></a>Faster-RCNN</h1><p>Faster-RCNN = Fast R-CNN + RPN<sup id="fnref:4"><a href="#fn:4" rel="footnote">4</a></sup> 结构，用 RPN 生成特征候选框，将生成的候选框投影到特征图上得到特征矩阵，即代替了之前的 SS 操作，也就是，让 RPN 网络自己去寻找哪里可能有目标。</p><p>RPN 网络，输入为 backbone 后的 features map，用于生成 region proposal，该层为 features map 上每个像素生成若干个 anchors（9个），随后通过 softmax 判断每个 anchor 是属于 foreground（目标）或者 background（背景），再利用边界框回归器修正 anchors 获得精确的 proposal 位置。</p><p>而后，Roi Pooling 层则利用 RPN 提供的 proposals 以及 backbone 后的 feature maps，提取 proposal feature 送入后续全连接网络，再之后的操作和 Fast R-CNN 就一样了。所以重点来看 RPN，网络结构如下。</p><p><img data-src="https://z3.ax1x.com/2021/04/28/gif0ER.png" alt></p><h2 id="RPN-网络"><a href="#RPN-网络" class="headerlink" title="RPN 网络"></a>RPN 网络</h2><p>在 backbone 得到特征图后，使用一个卷积核继续提纯，在 features map 上进行滑动，且通过 padding 保证滑动前的特征矩阵和滑动后的特征矩阵尺寸一致。每滑动到一个位置就生成一个一维向量，图中 256 的意思是 channel，所以单个卷积核会生成一个一维向量。</p><p>而后一维向量进入两个并联的卷积层，使用 $2k$ 个卷积核输出 $2k$ 个概率得分（背景概率和前景概率）；另外一边使用 $4k$ 个卷积核输出 $4k$ 个边界框参数。$k$ 是 <code>anchor box</code> 的数量，卷积核的大小都是 $1\times1$。注意，这是一步卷积就生成这么多参数。</p><p><img data-src="https://z3.ax1x.com/2021/04/29/gFDsDx.png" alt></p><h2 id="anchor-box"><a href="#anchor-box" class="headerlink" title="anchor box"></a>anchor box</h2><p>首先在特征图上进行卷积的时候，计算当前卷积窗口中心点对应原图哪个点，然后以原图为中心，生成 $k$ 个 anchor box，anchor box 是提前给定的大小和长宽比例。如下图所示，从特征图一步步映射到原图。anchor box 里可能有目标，也可能没有目标。其中，每个 anchor 的形状不一样，因为目标大小不一样，长宽比也不一样。</p><p><img data-src="https://z3.ax1x.com/2021/04/28/giTcxP.png" alt></p><p>前面讲过，每个 anchor 会生成 2 个概率分数和 4 个预测框参数。如对于概率分数，<code>[0.3, 0.7]</code> 表示有 0.3 的概率是背景，0.7 的概率是前景。对于同一目标，有 $k$ 个 anchor，就会有 $2k$ 个概率分数和 $4k$ 个边界框参数。而 anchor 的大小和比例是提前设置的，如 $128\times 128，256\times 256，512\times 512$ 大小，对应 anchor 的面积；比例一般是三种，<code>1:1, 1:2, 2:1</code>，对应 anchor 的长宽比。因此，每个位置，也就是每个滑动窗口，都有 9 个 anchor。</p><p>如果感受野（也就是特征图上的$3\times 3$，可能对应原图的 $200\times 200$）的大小小于 anchor box 的大小，不要惊讶，因为目标可能没在图片内。就像根据直觉，看到一个脑袋，也能猜出剩下的身子应该是多大。意思是可以通过一个小的感受野，预测一个大的物体的边界框。</p><p><img data-src="https://z3.ax1x.com/2021/04/28/gi7azq.jpg" alt></p><p>在特征图上滑动时，步长为1，padding 为 1。所以每个位置都会有 anchor，舍弃掉超出原图边界框的 anchor。利用 RPN，将 anchor 根据生成的边界框回归参数，调整称为候选框，用非极大值一致筛掉一部分候选框，得到最终的候选框。</p><h2 id="正负样本"><a href="#正负样本" class="headerlink" title="正负样本"></a>正负样本</h2><p>并不是全部 anchor 都要训练，比如可以采样 256 个样本去训练，正负样本的比例是 1:1。如果正样本不到 128，那么用负样本进行补充。</p><ul><li>正样本：候选边界框与真实框的 IOU 大于 0.7；如果没有，就取与真实边界框有最大的 IOU 作为正样本（因为可能只有一小部分相交）</li><li>负样本：与所有真实边界框的 IOU 低于 0.3</li></ul><h2 id="RPN-损失函数"><a href="#RPN-损失函数" class="headerlink" title="RPN 损失函数"></a>RPN 损失函数</h2><p>第一部分是类别损失，这里的类别只有两种，前景或背景。所以损失函数表示为：</p><p>\begin{equation}<br>L_1 = \frac{1}{N}\sum_i(p_i, p_i^*)<br>\end{equation}</p><p>当前目标为前景时，$p_i^\star$ 为1；为背景时，$p_i^\star$ 为 0。</p><p>边界框回归损失和之前的 Fast R-CNN 是一样的。如果是背景，没有边界框损失函数；如果是目标，就有边界框损失函数。</p><h2 id="Faster-RCNN-1"><a href="#Faster-RCNN-1" class="headerlink" title="Faster-RCNN"></a>Faster-RCNN</h2><p>把 RPN（目标区域选择） 与 Fast R-CNN（目标检测） 组合起来，就是最终的 Faster-RCNN。至于两个网络的 loss 如何训练，这就取决于代码了，我这就去开坑写程序。在论文中：</p><ul><li>RPN 和 Fast R-CNN 可以理解为互惠共赢的东西，所以不希望分开训练，这个是我个人的观点</li><li>交替训练，先训练 RPN，能找到区域后，微调 Fast R-CNN 后用于初始化 RPN 网络参数，如此循环迭代。具体过程为：<ul><li>训练 RPN 网络，采用 ImageNet 预训练的模型进行初始化，并进行微调</li><li>利用 RPN 生成 proposal，由 Fast R-CNN 训练一个单独的检测网络，该网络同样由 ImageNet 预训练模型进行初始化。此时，两个网络还未共享卷积层</li><li>用检测网络初始化 RPN 训练，固定共享的卷积层，只微调 RPN 独有的层，现在两个网络实现了卷积层共享</li><li>保持共享的卷积层固定，微调 Fast R-CNN 的全连接层。这样，两个网络共享相同的卷积层，构成一个统一的网络</li></ul></li><li>近似联合训练，RPN 和 Fast R-CNN 整合到一个网络里一起训练。前向传递生成 region proposal，在训练 Fast R-CNN 检测器将这看作是固定的、预计算的 proposal。反向传播像往常一样进行，其中对于共享层，组合来自 RPN 损失和 Fast R-CNN 损失的反向传播信号。这个解决方案很容易实现。但是这个解决方案忽略了关于 proposal 边界框的坐标的导数，因此是近似的。由 RPN 预测的边界框也是输入的参数，Fast R-CNN 中的 RoI 池化层接受卷积特征以及预测的边界框作为输入，所以理论上有效的反向传播求解器也应该包括关于边界框坐标的梯度。在上述近似联合训练中，这些梯度被忽略。</li></ul><h1 id="扩展到-Cascade-RCNN"><a href="#扩展到-Cascade-RCNN" class="headerlink" title="扩展到 Cascade RCNN"></a>扩展到 Cascade RCNN</h1><p>至于经常听闻的 Cascade RCNN，只是 Faster RCNN 的一个变体，只是在 ROI head 预测部分增加了几个串联的网络，IOU 逐步提升，从不太 mismatch 的 0.5 开始，逐步提升目标框的质量。</p><ol><li>detector 在训练阶段和推理阶段面对的数据分布是不一样的，训练阶段 proposal 都是经过 IOU 采样的，正样本含有目标；推理阶段不知道哪个 proposal 为正样本，只能认为所有 proposal 都是正样本，但这样得到的 proposal 的 IOU 都很低，所以推理结果不太好；</li><li>如果使用低 IOU 重新训练网络，对噪音的识别较大；如果提高 IOU ，又会造成对正样本的过拟合；</li><li>所以可以在网络中使用不同的 IOU 逐步提纯。</li></ol><p>所以推荐几篇文章，就不详细解读了：</p><ol><li><a href="https://zhuanlan.zhihu.com/p/112828052" target="_blank" rel="noopener">https://zhuanlan.zhihu.com/p/112828052</a></li><li><a href="https://zhuanlan.zhihu.com/p/161530664" target="_blank" rel="noopener">https://zhuanlan.zhihu.com/p/161530664</a></li><li><a href="https://zhuanlan.zhihu.com/p/42553957" target="_blank" rel="noopener">https://zhuanlan.zhihu.com/p/42553957</a></li><li><a href="https://linzhenyuyuchen.github.io/2019/12/05/Cascade-R-CNN/" target="_blank" rel="noopener">https://linzhenyuyuchen.github.io/2019/12/05/Cascade-R-CNN/</a> （个人认为写的最好）</li></ol><h1 id="reference"><a href="#reference" class="headerlink" title="reference"></a>reference</h1><p><sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup>：<a href="https://zhuanlan.zhihu.com/p/39927488" target="_blank" rel="noopener">selective search 算法</a><br><sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup>：<a href="https://arxiv.org/pdf/1504.08083.pdf" target="_blank" rel="noopener">Fast R-CNN</a><br><sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup>：<a href="https://muyuuuu.github.io/2021/04/02/cross-entropy/">交叉熵损失</a><br><sup id="fnref:4"><a href="#fn:4" rel="footnote">4</a></sup>：<a href="https://arxiv.org/abs/1506.01497" target="_blank" rel="noopener">Faster RCNN</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;又来开计算机视觉的坑了，&lt;strong&gt;这次一定做好整理和记录，包括算法内容和程序&lt;/strong&gt;。做好训练、保存和推理的框架，下次需要的时候直接用。做目标检测的东西，于是决定从 Fast R-CNN 重头来过了。其中，Fast R-CNN 只有理论，Faster-RCNN 部分含有代码，不过会在下一篇博客了。&lt;/p&gt;
&lt;p&gt;既然是目标检测，就需要做两件事情，目标在哪，目标是什么。对于目标在哪的问题，可以让网络生成一个目标框，目标框有四部分组成：$(x,y,w,h)$，表示物体的起始点坐标和框的宽度与高度；对于目标是什么的问题，就是传统神经网络的多分类问题了。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="CV" scheme="https://muyuuuu.github.io/tags/CV/"/>
    
  </entry>
  
  <entry>
    <title>对抗攻击篇：FGSM 与 PGD 攻击算法</title>
    <link href="https://muyuuuu.github.io/2021/04/26/DNN-safe-basic/"/>
    <id>https://muyuuuu.github.io/2021/04/26/DNN-safe-basic/</id>
    <published>2021-04-26T06:42:03.000Z</published>
    <updated>2021-05-11T07:13:28.877Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>博客的进阶使用：在指定位置插入脚注<sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup>，这样就不用在文末列出参考文献，显得对不上号了。回归正题，如果不出什么特别大的意外，就应该去搞网络安全了。注：此网络安全非彼网络安全，指：对抗样本而非网络入侵。所以准备来开个坑，先整理基础知识，基础不劳，地动山摇。</p><a id="more"></a><h1 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h1><p>所谓对抗样本，即向原始样本中添加一些人眼无法察觉的噪声，这样的噪声不会影响人类的识别，但是却很容易欺骗 DNN <sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup>，使其作出与正确答案完全不同的判定。如下图所示，使用基于梯度<sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup>的攻击方式，很容易欺骗神经网络：</p><p><img data-src="https://z3.ax1x.com/2021/04/26/gpwOwd.png" alt></p><p>将肉眼不可见的细小扰动加入到原图中，网络识别出粗，甚至有 99.3% 的概率认为这是长臂猿。试想，如果犯罪分子将人烟稀少地区的交通信号牌改为立即停车，人眼不会觉得有什么不妥，但是无人驾驶系统却会乖乖停车，那么此时车上人员的安全何以保障？将禁止右转改为右转，造成交通事故就只在弹指一挥间了。究其本质原因，还是因为现有的 DNN 不能完全区分哪些特征是对分类有用的，哪些是可以忽略的。</p><h1 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h1><ul><li>白盒攻击：攻击者能够获知机器学习所使用的算法，以及算法所使用的参数。</li><li>黑盒攻击：攻击者并不知道机器学习所使用的算法和参数，但攻击者仍能与机器学习的系统有所交互，比如可以通过传入任意输入观察输出，判断输出。</li><li>无目标攻击：对于一张图片，生成一个对抗样本，使得标注系统在其上的标注与原标注无关，即只要攻击成功就好，对抗样本的最终属于哪一类不做限制。</li><li>有目标攻击：对于一张图片，生成一个对抗样本，使得标注系统在其上的标注与目标标注完全一致，即不仅要求攻击成功，还要求生成的对抗样本属于特定的类。</li></ul><h1 id="基于梯度的攻击算法"><a href="#基于梯度的攻击算法" class="headerlink" title="基于梯度的攻击算法"></a>基于梯度的攻击算法</h1><h2 id="FGSM-攻击算法"><a href="#FGSM-攻击算法" class="headerlink" title="FGSM 攻击算法"></a>FGSM 攻击算法</h2><p>设 $x$ 是原始样本，$x’$ 是对抗样本，其中：$x’ = x + \eta$，为了让对抗样本不被机器所识别，$\eta$ 应该足够小，这里使用无穷阶范数来表述 $\eta$ 足够小这一限制：$\Vert \eta \Vert_\inf &lt; \epsilon$。事实上，常说的 $l_\inf-\text{ball}$ 就是这个意思。</p><p>考虑输入层对对抗样本的线性变换：</p><p>\begin{equation}<br>w^Tx’=w^Tx+w^T\eta<br>\end{equation}</p><p>如果假设 $\eta = \text{sign} (w)$，$w$ 是一个 $n$ 维的矩阵，每一维的模长是 $m$，那么经过线性映射后，输入将增大 $\epsilon mn$。尽管 $\Vert \eta \Vert_\inf$ 不会变换，但随着维度的升高、矩阵权重的变换和非线性激活，回导致最终结果变化很大。这也就是为什么对抗样本与原始样本相差无几，但却产生了不同的输出。</p><p>因此，FGSM<sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup> 产生对抗样本的方式为：</p><p>\begin{equation}<br>\eta=\epsilon \text{sign} \big( \nabla_x J(\theta, x, y) \big)<br>\end{equation}</p><p>其中，$J$是分类损失函数，通过梯度上升，最大化损失函数，企图使得原始的 $x$ 不在属于 $y$ 类。<del>好坏啊</del>。torch 官方<sup id="fnref:5"><a href="#fn:5" rel="footnote">5</a></sup>也给出了对抗样本的程序：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="comment"># FGSM attack code</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">fgsm_attack</span><span class="params">(image, epsilon, data_grad)</span>:</span></span><br><span class="line">    <span class="comment"># Collect the element-wise sign of the data gradient</span></span><br><span class="line">    sign_data_grad = data_grad.sign()</span><br><span class="line">    <span class="comment"># Create the perturbed image by adjusting each pixel of the input image</span></span><br><span class="line">    perturbed_image = image + epsilon*sign_data_grad</span><br><span class="line">    <span class="comment"># Adding clipping to maintain [0,1] range</span></span><br><span class="line">    perturbed_image = torch.clamp(perturbed_image, <span class="number">0</span>, <span class="number">1</span>)</span><br><span class="line">    <span class="comment"># Return the perturbed image</span></span><br><span class="line">    <span class="keyword">return</span> perturbed_image</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span><span class="params">()</span>:</span></span><br><span class="line">    data_grad = data.grad.data</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Call FGSM Attack</span></span><br><span class="line">    perturbed_data = fgsm_attack(data, epsilon, data_grad)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Re-classify the perturbed image</span></span><br><span class="line">    output = model(perturbed_data)</span><br></pre></td></tr></table></figure><h2 id="PGD-攻击算法"><a href="#PGD-攻击算法" class="headerlink" title="PGD 攻击算法"></a>PGD 攻击算法</h2><p>PGD 攻击算法<sup id="fnref:6"><a href="#fn:6" rel="footnote">6</a></sup>是 FSGM 的变体，它即是产生对抗样本的攻击算法，也是对抗训练的防御算法。</p><p>论文先抛出了两个问题：</p><ol><li>如何产生强有效的对抗样本，只需要一点点扰动就可以欺骗神经网络？</li><li>如何训练模型，使得没有对抗样本轻易能欺骗网络？</li></ol><p>在论文中，为了防止网络只能抵御某一特定类型的攻击，应该保证模型有足够的对抗鲁棒性，接下来，使用一种方法来训练模型，达到这种保证。在传统的网络中，损失函数通常是优化网络参数 $\theta$，使得损失函数达到最小：$\mathbb{E}_{x\sim D}[L(x,y,\theta)]$。但是，对于对抗样本而言，就是要误导神经网络的输出。同样，这篇论文中，对抗样本的扰动范围也限制在 $l_\inf-\text{ball}$ 内。</p><p>但是在训练损失函数$\mathbb{E}_{x\sim D}[L]$之前，先考虑对抗样本的扰动，所以先不把干净样本 $x$ 输入到网络，而是用对抗样本作为网络的输入。所以论文的公式将攻击和防御放入统一框架内，对抗训练就是在一个马鞍面内求鞍点。</p><p>\begin{equation}<br>\min \mathbb{E}_{x\sim D} \big[ \max_{\sigma\in S} L(\theta, x, y) \big]<br>\end{equation}</p><p>其中的 $S$ 表示球面限制，即无穷范数不能超过 $S$。这个统一的框架包括了内部最大化和外部最小化，因此可以视为鞍点问题，也清晰的描述了鲁棒模型应该达到的目标。内部最大化的期望是，寻找最优的对抗样本达到很高的损失；外部最小化的期望是，寻找模型参数，能抵抗对抗样本的攻击。所以，最终训练得到的模型也具有稳定的对抗鲁棒性。</p><p>在攻击部分，对抗样本的制作方法为：</p><p>\begin{equation}<br>x+\epsilon \text{sign} \big( \nabla_x L(\theta, x, y) \big)<br>\end{equation}</p><p>这可以解释为内部期望最大化的单步攻击。但多步攻击会更加有效，因为 DNN 是复杂的非线性映射，不要企图一步到位。在数据点的 $l_\inf$ 球边界的许多点运行PGD，来探索损失函数中的大部分位置，即在这个球的范围内，探索到绝大多数地方，以能够找到最强的对抗样本。虽然在 $x_i+S$ 的范围内有许多局部最大值，但是他们的损失值往往相似，也就是说，解决了最大化内部非凹函数的问题。</p><p>\begin{equation}<br>x^{t+1}=\prod_{x+S}\bigg(x^t+\epsilon \text{sign} \big( \nabla_x L(\theta, x, y) \big)\bigg)<br>\end{equation}</p><p>$\prod_{x+S}$ 符号的意思是，先计算原图像的损失梯度得到对抗样本，对抗样本减去原图像得到扰动值，并通过 <code>torch.clamp</code> 限制在球面范围内，原图像加上扰动值就是最终的对抗样本。还是看代码吧：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">pgd_attack</span><span class="params">(model, images, labels, eps=<span class="number">0.3</span>, alpha=<span class="number">2</span>/<span class="number">255</span>, iters=<span class="number">40</span>)</span> :</span></span><br><span class="line">    images = images.to(device)</span><br><span class="line">    labels = labels.to(device)</span><br><span class="line">    loss = nn.CrossEntropyLoss()</span><br><span class="line">    <span class="comment"># 原图像</span></span><br><span class="line">    ori_images = images.data</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(iters) :</span><br><span class="line">        images.requires_grad = <span class="literal">True</span></span><br><span class="line">        outputs = model(images)</span><br><span class="line"></span><br><span class="line">        model.zero_grad()</span><br><span class="line">        cost = loss(outputs, labels).to(device)</span><br><span class="line">        cost.backward()</span><br><span class="line">        <span class="comment"># 图像 + 梯度得到对抗样本</span></span><br><span class="line">        adv_images = images + alpha*images.grad.sign()</span><br><span class="line">        <span class="comment"># 限制扰动范围</span></span><br><span class="line">        eta = torch.clamp(adv_images - ori_images, min=-eps, max=eps)</span><br><span class="line">        <span class="comment"># 进行下一轮对抗样本的生成。破坏之前的计算图</span></span><br><span class="line">        images = torch.clamp(ori_images + eta, min=<span class="number">0</span>, max=<span class="number">1</span>).detach_()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> images</span><br></pre></td></tr></table></figure><p>实验发现，通过PGD发现的局部最大值在正常训练的网络和对抗训练的网络中都有着相似的损失值；换句话说，PGD 训练对抗样本，和普通网络训练干净样本的损失是类似的；这也说明，只要能够防御住PGD，就会对所有的一阶攻击手段具有鲁棒性。</p><h3 id="补充"><a href="#补充" class="headerlink" title="补充"></a>补充</h3><p>剩下的这些都是论文中实验部分所提及的了，更多内容，还可以参考这里<sup id="fnref:7"><a href="#fn:7" rel="footnote">7</a></sup>的解读和这里<sup id="fnref:8"><a href="#fn:8" rel="footnote">8</a></sup>的代码。</p><ul><li>在模型容量较小时，模型对 FGSM 对抗样本产生了过拟合，这种行为被称为label leaking。而且，模型无法防御 PGD 攻击。</li><li>对抗样本的复杂性，是决策边界也变得复杂，因此模型需要更大的容量，才能抵御对抗样本。</li></ul><h1 id="常见防御算法"><a href="#常见防御算法" class="headerlink" title="常见防御算法"></a>常见防御算法</h1><ul><li>对抗训练：对抗训练旨在训练一个更加鲁棒的模型，其训练集由真实数据集和加入了对抗扰动的数据集组成，因此叫做对抗训练。</li><li>去噪模块：在输入模型进行判定之前，先对当前对抗样本进行去噪，剔除其中造成扰动的信息，使其不能对模型造成攻击。</li></ul><p>上述几种防御类型对对抗样本扰动都具有一定的防御能力，效果参差不齐。本来想都放到这篇博客里，后来发现任务量太大了，后续我会读一些论文，列举一些代表性算法。当然，也可以移步这里，有很多关于网络安全算法<sup id="fnref:4"><a href="#fn:4" rel="footnote">4</a></sup>的介绍。</p><h1 id="程序"><a href="#程序" class="headerlink" title="程序"></a>程序</h1><p>使用 <code>ResNet50</code> 制作对抗样本，攻击类型为黑盒攻击，使用 <code>VGG16</code> 作为目标模型，程序 <sup id="fnref:9"><a href="#fn:9" rel="footnote">9</a></sup> 放到了 Github。</p><div class="table-container"><table><thead><tr><th>攻击算法</th><th>目标模型</th><th>原始准确率</th><th>扰动0.05</th><th>扰动0.1</th><th>扰动0.15</th><th>扰动0.3</th></tr></thead><tbody><tr><td>FGSM</td><td>VGG16</td><td>94.27</td><td>52.52</td><td>47.37</td><td>42.86</td><td>32.24</td></tr><tr><td>PGD</td><td>VGG16</td><td>94.27</td><td>43.01</td><td>31.53</td><td>23.92</td><td>14.53</td></tr></tbody></table></div><h1 id="References"><a href="#References" class="headerlink" title="References"></a>References</h1><p><sup id="fnref:1"><a href="#fn:1" rel="footnote">1</a></sup>：<a href="https://guanqr.com/tech/website/hexo-theme-next-customization/#%E6%8F%92%E5%85%A5%E8%84%9A%E6%B3%A8" target="_blank" rel="noopener">如何加入脚注</a><br><sup id="fnref:2"><a href="#fn:2" rel="footnote">2</a></sup>：<a href="https://arxiv.org/pdf/1312.6199.pdf" target="_blank" rel="noopener">对抗样本</a><br><sup id="fnref:3"><a href="#fn:3" rel="footnote">3</a></sup>：<a href="https://arxiv.org/pdf/1412.6572.pdf" target="_blank" rel="noopener">FGSM攻击</a><br><sup id="fnref:4"><a href="#fn:4" rel="footnote">4</a></sup>：<a href="https://aisafety.readthedocs.io/zh_CN/latest/index.html" target="_blank" rel="noopener">网络安全系列算法</a><br><sup id="fnref:5"><a href="#fn:5" rel="footnote">5</a></sup>：<a href="https://pytorch.org/tutorials/beginner/fgsm_tutorial.html" target="_blank" rel="noopener">pytorch实现FSGM</a><br><sup id="fnref:6"><a href="#fn:6" rel="footnote">6</a></sup>：<a href="https://arxiv.org/pdf/1706.06083.pdf" target="_blank" rel="noopener">PGD攻击算法</a><br><sup id="fnref:7"><a href="#fn:7" rel="footnote">7</a></sup>：<a href="https://baidinghub.github.io/2020/04/03/%E5%AF%B9%E6%8A%97%E6%A0%B7%E6%9C%AC%EF%BC%88%E5%8D%81%E5%85%AB%EF%BC%89PGD%E5%8F%8A%E9%98%B2%E5%BE%A1/#5-1-Network-Capacity-and-Adversarial-Robustness" target="_blank" rel="noopener">PGD论文笔记</a><br><sup id="fnref:8"><a href="#fn:8" rel="footnote">8</a></sup>：<a href="https://github.com/Harry24k/PGD-pytorch/blob/master/PGD.ipynb" target="_blank" rel="noopener">PGD攻击程序</a><br><sup id="fnref:9"><a href="#fn:9" rel="footnote">9</a></sup>：<a href="https://github.com/muyuuuu/Adversarial-Attack" target="_blank" rel="noopener">PGD与FGSM的黑盒攻击</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;博客的进阶使用：在指定位置插入脚注&lt;sup id=&quot;fnref:1&quot;&gt;&lt;a href=&quot;#fn:1&quot; rel=&quot;footnote&quot;&gt;1&lt;/a&gt;&lt;/sup&gt;，这样就不用在文末列出参考文献，显得对不上号了。回归正题，如果不出什么特别大的意外，就应该去搞网络安全了。注：此网络安全非彼网络安全，指：对抗样本而非网络入侵。所以准备来开个坑，先整理基础知识，基础不劳，地动山摇。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="DeepLearning" scheme="https://muyuuuu.github.io/tags/DeepLearning/"/>
    
  </entry>
  
  <entry>
    <title>华山自由行之一日游</title>
    <link href="https://muyuuuu.github.io/2021/04/18/hua-shan-free-travel/"/>
    <id>https://muyuuuu.github.io/2021/04/18/hua-shan-free-travel/</id>
    <published>2021-04-18T04:00:04.000Z</published>
    <updated>2021-04-19T06:11:31.785Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>人在西安，离华山近。不想清明或五一去下饺子，想着周内去一趟，反正离得近。事实证明这个选择是正确的，华山这么险峻的地方下饺子太危险了。接下来前面部分算是旅行攻略吧，包括路线、时间、穿衣和花费等，因本人是学生党，所以都是精打细算，没有过高消费；后一部分是风景照什么的。</p><p>这是我目前去过最接近地府的景点，我只能这么评价。</p><a id="more"></a><h1 id="时间与路线安排"><a href="#时间与路线安排" class="headerlink" title="时间与路线安排"></a>时间与路线安排</h1><p>因同行人是三个青壮年，所以体力充沛，没有选择索道。据网上查阅，腿路的起点是玉泉院，所以我们的起点也就是玉泉院了，<strong>北上西下，腿上索道下</strong>，一天内爬完了华山，五峰全部登顶，时间还很充裕。之后用加粗字体表示重要路标和时间节点。</p><ol><li>学校在<strong>西安</strong>，门口就是地铁，所以买的是从<strong>西安北到华山北</strong>最早的高铁，是 <strong>7:28</strong> 开车，<strong>8:00</strong> 到达。所以5点起床洗漱，6点赶第一班地铁，6点40到了<strong>西安北</strong>，等高铁途中使劲吃面包和火腿。（普通火车没有上午的，不考虑；大巴晕车，可能堵车；不考虑）。</li><li>出了<strong>华山北</strong>高铁站后，有免费公交或出租可以乘坐；因公交需要等待且可能等不到，所以选择了出租到<strong>玉泉院</strong>，因为这是腿路的起点，共计19元，大概八点半到的。翻过前面的寺院，网上预约门票耽误了一会儿，<strong>9:00</strong> 开始登山。（疫情原因，不能线下购买，只能线上购买，刷身份证进）</li><li>前面的路是人工修的，之后到<strong>第一关</strong>，后面的山路就意味着正式开始登山，大约走了一个小时，也就是 <strong>10:00</strong> 正式开始登山。我们每个人的配置是：四瓶水，我单独带了三瓶酸奶，这个配置下，爬完山还剩一瓶水，如果带的水不够，立刻在山下补充。吃的就是面包之类。</li><li>之后会到<strong>回心石</strong>，意思就是现在放弃还来得及；过了<strong>千尺幢</strong>，开弓没有回头箭了。</li><li>华山自古一条路，旁边就是万丈悬崖。选择纠结症的福音，只能按着路线走，累了就休息下，景点就拍照留念。我们的路线是： 北峰（<strong>12:00</strong> 登顶），东峰（<strong>15:00</strong> 登顶），中峰（<strong>15:00</strong> 登顶，这俩离得很近很近），南峰(<strong>16:00</strong> 登顶)，西峰（<strong>17:00</strong> 登顶），没有去鹞子翻身，长空栈道等阴间景点，太太太危险了。西峰逗留整顿了一会儿，坐索道下山了。</li><li>而后从<strong>西峰索道</strong>下山，西峰索道真的天下一绝，<del>我直接吓瘫在地上</del>，<strong>18:30</strong> 坐上了从西峰脚下去游客中心的大巴，七点到的游客中心。门口叫滴滴就行；门口的出租太黑了，不打表，直接要钱，很贵，反正我们当时是这样。</li><li>县城里吃了顿肉补一补，据当地人说那里的特产是大刀面，用铡刀做的，可惜没赶上；之后乘坐 <strong>21:17</strong> 的高铁从华山北回西安北了。</li></ol><h1 id="花费"><a href="#花费" class="headerlink" title="花费"></a>花费</h1><h2 id="必要消费"><a href="#必要消费" class="headerlink" title="必要消费"></a>必要消费</h2><ol><li>高铁单程是 54.5 元，往返 109 元</li><li>出租去玉泉院 19 元</li><li>门票，我们是旺季，是 160 元</li><li>西峰索道 140 元，腿路下山伤膝盖</li><li>下山大巴 40 元，二十公里山路呢，坐车吧</li><li>因为大巴的下车点不是玉泉院，所以去华山北出租是 10 元左右</li></ol><h2 id="其它消费"><a href="#其它消费" class="headerlink" title="其它消费"></a>其它消费</h2><ol><li>纪念品华山香石，苍龙岭后面，不是很坑，也就10元</li><li>吃的喝的，自己看着买吧</li></ol><h1 id="装备"><a href="#装备" class="headerlink" title="装备"></a>装备</h1><ol><li>我们是 4 月中旬去的，早晚天气很凉，尤其是汗水干了，风一吹，个人建议带冲锋衣。山顶有穿背心的，有穿羽绒服的，我就穿的一个外套加一件秋衣；秋裤自己看这穿吧，我穿了，同行的人没穿，都行。</li><li>帽子，我们那天有风，风还凉，吹的头疼。</li><li>护膝，下山极度伤膝盖，上山没事。如何选择护膝或该不该带护膝，请知乎搜索。</li><li>手套，我感觉不太必要。</li></ol><h1 id="其它注意事项"><a href="#其它注意事项" class="headerlink" title="其它注意事项"></a>其它注意事项</h1><ol><li>山路极度危险，不要着急，慢慢走。走路不看景，看景不走路。</li><li>在攀登危险的山路前，做好心里和体力准备，不然会佷仓促。上也上不去，还不能下去；后面的人还会催你，只能硬着头皮继续上。</li><li>不要在人极度拥挤的危险道路拍照，后面的人都没法走，但总有 SB 这么做，毕竟旅游的人越来越多，啥人都有。</li><li>珍爱生命，远离 SB，不要和爱表现、哗众取宠的人一起出去。在某个坡度为 90 度直角的阶梯上，我正拉着铁链往上走，一个 SB 为了在女生面前表现，猛的往外甩铁链，故意把身体甩到外侧，把我拉铁链的手也震开了，得亏另一个手拉着铁链，不然真的从 90 度的阶梯上摔下去了，那就祝你早日摔死吧。</li><li>山上厕所比较少，遇到厕所就去一趟。</li><li>华山以险峻著称，如果恐高，慎来此地。</li><li>山上围栏的铁链都比较结实，这个可以放心。</li><li>山顶能俯瞰到关中沃野，这在古代，我也得打一仗。</li><li><del>不要带女生或体力不太行的人去，女生可以带男朋友去看看他到底爱不爱你。</del></li><li>我常年锻炼，第二天醒来腿居然不疼。</li></ol><h1 id="一些照片-超清原图，流量预警"><a href="#一些照片-超清原图，流量预警" class="headerlink" title="一些照片(超清原图，流量预警)"></a>一些照片(超清原图，流量预警)</h1><p><img data-src="https://z3.ax1x.com/2021/04/18/cIKszV.jpg" alt></p><h2 id="俯瞰关中沃野"><a href="#俯瞰关中沃野" class="headerlink" title="俯瞰关中沃野"></a>俯瞰关中沃野</h2><p><img data-src="https://z3.ax1x.com/2021/04/18/cIKrR0.jpg" alt></p><h2 id="直上直下的路"><a href="#直上直下的路" class="headerlink" title="直上直下的路"></a>直上直下的路</h2><p><img data-src="https://z3.ax1x.com/2021/04/18/cIKBin.jpg" alt></p><p><img data-src="https://z3.ax1x.com/2021/04/18/cIKgLF.jpg" alt></p><p><img data-src="https://z3.ax1x.com/2021/04/18/cIKWdJ.jpg" alt></p><h2 id="中华龙脉"><a href="#中华龙脉" class="headerlink" title="中华龙脉"></a>中华龙脉</h2><p>据说韩愈在这里被吓哭了，路陡，旁边是悬崖。</p><p><img data-src="https://z3.ax1x.com/2021/04/18/cIKcsU.jpg" alt></p><h2 id="华山论剑"><a href="#华山论剑" class="headerlink" title="华山论剑"></a>华山论剑</h2><p><img data-src="https://z3.ax1x.com/2021/04/18/cIKRZ4.jpg" alt></p><h2 id="悬崖绝壁"><a href="#悬崖绝壁" class="headerlink" title="悬崖绝壁"></a>悬崖绝壁</h2><p><img data-src="https://z3.ax1x.com/2021/04/18/cIKwIs.jpg" alt></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;人在西安，离华山近。不想清明或五一去下饺子，想着周内去一趟，反正离得近。事实证明这个选择是正确的，华山这么险峻的地方下饺子太危险了。接下来前面部分算是旅行攻略吧，包括路线、时间、穿衣和花费等，因本人是学生党，所以都是精打细算，没有过高消费；后一部分是风景照什么的。&lt;/p&gt;
&lt;p&gt;这是我目前去过最接近地府的景点，我只能这么评价。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="Travel" scheme="https://muyuuuu.github.io/tags/Travel/"/>
    
  </entry>
  
  <entry>
    <title>操作系统『五』：CPU 上下文切换</title>
    <link href="https://muyuuuu.github.io/2021/04/16/CPU-context-switch/"/>
    <id>https://muyuuuu.github.io/2021/04/16/CPU-context-switch/</id>
    <published>2021-04-16T13:52:58.000Z</published>
    <updated>2021-04-16T14:48:22.212Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>进程 CPU 调度和资源分配的单位，是一个程序在一个数据集上的一次执行；线程是CPU上运行和调度的基本执行单位，进程内的一个代码片段可创建为线程，线程由进程创建，寄生在进程身上。</p><p>CPU 从一个<strong>任务</strong>切换到另一个<strong>任务</strong>，俗称上下文切换。理论上而言，进程负载了程序运行所有内容，开销较大。线程较轻量，和父进程共享各类资源，上下文切换的成本较低。可实际情况中具体如何？今天一探究竟。</p><a id="more"></a><h1 id="多任务系统与上下文切换"><a href="#多任务系统与上下文切换" class="headerlink" title="多任务系统与上下文切换"></a>多任务系统与上下文切换</h1><p>我们都知道，现代操作系统是一个多任务操作系统，它支持远大于 CPU 数量的任务同时运行。当然，这些任务实际上并不是真的在同时运行，而是因为系统在很短的时间内，将 CPU 轮流分配给它们，每秒可能发生几百次上下文切换，造成多任务同时运行的错觉。</p><p>其中上下文的意思是指：某一时刻，寄存器内的数据和程序计数器的值；寄存器保留了CPU的指令，程序计数器表明了指令所在的地址，即当前指令之后立即执行的指令的地址。它们都是 CPU 在运行任何任务前，必须的依赖环境。</p><p>而在每个任务运行前，CPU 都需要知道任务从哪里加载、又从哪里开始运行，也就是说，需要系统事先帮它设置好 CPU 寄存器和程序计数器，也就是上下文。涉及到具体的切换，就是先把前一个任务的 CPU 上下文（也就是 CPU 寄存器和程序计数器）保存起来，然后加载新任务的上下文到这些寄存器和程序计数器，最后再跳转到程序计数器所指的位置，运行新任务。而这些保存下来的上下文，会存储在系统内核中，并在任务重新调度执行时再次加载进来。这样就能保证任务原来的状态不受影响，让任务看起来还是连续运行。</p><p>从上面的描述中可以知道，CPU 上下文切换无非就是更新了一些寄存器的值嘛，但这些寄存器，本身就是为了快速运行任务而设计的，为什么会影响系统的性能呢？在回答这个问题前，先得搞清楚操作系统管理的这些『任务』到底是什么呢？</p><p>进程和线程正是最常见的任务。但是除此之外，硬件通过触发信号，会导致中断处理程序的调用，也是一种常见的任务。所以，根据任务的不同，CPU上下文切换可分为几种场景：</p><ul><li>进程上下文切换</li><li>线程上下文切换</li><li>中断上下文切换</li><li>系统调用上下文切换</li></ul><h1 id="系统调用"><a href="#系统调用" class="headerlink" title="系统调用"></a>系统调用</h1><p>现代操作系统按照特权等级，把进程的运行空间分为内核空间和用户空间，内核模式运行特权指令，用户程序在用户模式执行，主要是为了保护系统不受一些故障程序的影响。进程既可以在用户空间运行，又可以在内核空间中运行。进程在用户空间运行时，被称为进程的用户态，而陷入内核空间的时候，被称为进程的内核态。</p><p>因为特权指令只能在内核模式下运行，所以无法在用户模式运行特权指令。只能在用户模式下发出特权指令的系统调用，此时用户程序暂停，转换为内核模式执行系统调用，得到结果反馈给用户，在转换为用户模式，进程继续执行。具体流程为：</p><ol><li>保存 CPU 寄存器里原来用户态的指令位</li><li>为了执行内核态代码，CPU 寄存器需要更新为内核态指令的新位置。</li><li>跳转到内核态运行内核任务。</li><li>当系统调用结束后，CPU 寄存器需要恢复原来保存的用户态，然后再切换到用户空间，继续运行进程。</li></ol><p>所以，一次系统调用的过程，其实是发生了两次 CPU 上下文切换。（用户态 <code>-&gt;</code> 内核态 <code>-&gt;</code> 用户态）</p><p>不过，需要注意的是，系统调用过程中，并不会涉及到虚拟内存等进程用户态的资源，也不会切换进程。这跟我们通常所说的进程上下文切换是不一样的：进程上下文切换，是指从一个进程切换到另一个进程运行；而系统调用过程中一直是同一个进程在运行。所以，系统调用过程通常称为特权模式切换，而不是上下文切换，系统调用属于同进程内的 CPU 上下文切换。</p><h1 id="进程上下文切换与系统调用"><a href="#进程上下文切换与系统调用" class="headerlink" title="进程上下文切换与系统调用"></a>进程上下文切换与系统调用</h1><p>首先，进程是由内核来管理和调度的，进程的切换只能发生在内核态。所以，进程的上下文不仅包括了内核堆栈、寄存器等内核空间的状态，还包括了虚拟内存、栈、全局变量等用户空间的资源。</p><p>因此，进程的上下文切换就比系统调用时多了一步：在保存内核态资源（当前进程的内核状态和 CPU 寄存器）之前，需要先把该进程的用户态资源（虚拟内存、栈等）保存下来；而加载了下一进程的内核态后，还需要刷新进程的虚拟内存和用户栈。如下图所示，保存上下文和恢复上下文的过程开销是很大的，此外，考虑到进程之间的相互的保护，只有操作系统能操作其它进程和了解其它进程的状态，因此这个过程需要在内核态完成。</p><p><img data-src="https://z3.ax1x.com/2021/04/16/chAMcR.png" alt></p><p>另外，我们知道，现代操作系统通过 TLB（Translation Lookaside Buffer）来管理虚拟内存到物理内存的映射关系。如果发生运行时动态链接（进入系统态）、内存紧缩等情况，即虚拟内存更新后，TLB 也需要刷新，内存的访问也会随之变慢，不过这是进程上下文切换带来的副作用了。</p><p>发生进程上下文切换的场景</p><ol><li>基于时间片的操作系统，CPU 时间被划分为一段段的时间片，这些时间片再被轮流分配给各个进程。这样，当某个进程的时间片耗尽了，就会被系统挂起，切换到其它正在等待 CPU 的进程运行。</li><li>进程在系统资源不足（比如内存不足）时，要等到资源满足后才可以运行，这个时候进程也会被挂起，并由系统调度其他进程运行。</li><li>当进程通过睡眠函数 sleep 这样的方法将自己主动挂起时，自然也会重新调度。</li><li>当有优先级更高的进程运行时，为了保证高优先级进程的运行，当前进程会被挂起，由高优先级进程来运行</li><li>发生硬件中断时，CPU 上的进程会被中断挂起，转而执行内核中的中断服务程序。</li></ol><h1 id="线程上下文切换"><a href="#线程上下文切换" class="headerlink" title="线程上下文切换"></a>线程上下文切换</h1><p>线程与进程最大的区别在于：线程是调度的基本单位，而进程则是资源拥有的基本单位。说白了，所谓内核中的任务调度，实际上的调度对象是线程；而进程只是给线程提供了虚拟内存、全局变量等资源。线程也有自己的私有数据，比如栈和寄存器等，这些在上下文切换时也是需要保存的。</p><p>发生线程上下文切换的场景：</p><ul><li>前后两个线程属于不同进程。此时，因为资源不共享，所以切换过程就跟进程上下文切换是一样。切换线程，CPU 的各种寄存器都要重新刷一遍，从这个角度而言，你可以把进程和线程当作一种东西，只是共享度不同，其他没区别的。</li><li>前后两个线程属于同一个进程。此时，因为虚拟内存是共享的，所以在切换时，虚拟内存这些资源就保持不动，内存命中率会高一些，只需要切换线程的私有数据、寄存器等不共享的数据。</li></ul><p>虽然同为上下文切换，但同进程内的线程切换，要比多进程间的切换消耗更少的资源，这也正是多线程代替多进程的一个优势。</p><h1 id="中断上下文切换"><a href="#中断上下文切换" class="headerlink" title="中断上下文切换"></a>中断上下文切换</h1><p>中断处理程序运行于内核态。中断发生时CPU可能处于内核态（如执行系统调用的过程中）也可能处于用户态（执行应用空间代码）。所以前者不涉及特权级转换，后者涉及。</p><p>不涉及特权级转换的情况：</p><ul><li>压入寄存器现场、错误代码等</li><li>执行中断处理程序</li><li>恢复寄存器现场</li></ul><p>可以看到这里并没有发生堆栈的切换——因为本来就运行在内核栈上嘛！中断处理程序借用了应用程序的内核栈。说『借用』是因为进程的内核栈是给进程执行内核空间代码使用的（通常就是系统调用），由于中断并不一定和正在运行的进程有什么关联。</p><p>但是对于后者，也就是用户态中被中断，有一个用户 <code>-&gt;</code> 内核 <code>-&gt;</code> 用户的切换过程，伴随着相关栈的切换。具体过程：</p><ul><li>找到内核栈</li><li>压入寄存器现场、错误代码</li><li>转入中断处理程序</li><li>恢复第二步保存的现场</li><li>切换换回用户栈</li></ul><p>为了快速响应硬件的事件，中断处理会打断进程的正常调度和执行，转而调用中断处理程序，响应设备事件，如断电和设备损坏等。而在打断其他进程时，就需要将进程当前的状态保存下来，这样在中断结束后，进程仍然可以从原来的状态恢复运行。</p><p>中断上下文，其实只包括内核态中断服务程序执行所必需的状态，包括 CPU 寄存器、内核堆栈、硬件中断参数等。跟进程上下文不同，中断上下文切换的东西不需要涉及到用户态。所以，即便中断过程打断了一个正处在用户态的进程，也不需要保存和恢复这个进程的虚拟内存、全局变量等用户态资源；只需在内核态记录：当前进程的寄存器和程序计数器。如喜闻乐见的，硬盘传输文件时突然拔掉硬盘，传输文件进程的虚拟内存、全局变量等用户态资源都没有被保存，相当于白传输了。<del>例子有点生硬。</del></p><p>由于中断会打断正常进程的调度和执行，所以大部分中断处理程序都短小精悍，以便尽可能快的执行结束。另外，跟进程上下文切换一样，中断上下文切换也需要消耗 CPU，切换次数过多也会耗费大量的 CPU，甚至严重降低系统的整体性能。所以，当你发现中断次数过多时，就需要注意去排查它是否会给你的系统带来严重的性能问题。</p><h1 id="一些讨论"><a href="#一些讨论" class="headerlink" title="一些讨论"></a>一些讨论</h1><ul><li>CPU 上下文切换，是保证现代系统正常工作的核心功能之一，一般情况下不需要我们特别关注。根据 <a href="https://blog.tsunanet.net/2010/11/how-long-does-it-take-to-make-context.html" target="_blank" rel="noopener">Tsuna</a> 的测试报告，每次上下文切换都需要几十纳秒到数微秒的 CPU 时间。这个时间还是相当可观的，特别是在进程上下文切换次数较多的情况下，会把 CPU 时间消耗在寄存器、内核栈以及虚拟内存等数据的保存和恢复上，从而缩短进程真正运行的时间，导致系统的整体性能大幅下降。</li><li>CPU 执行的最小逻辑单元是线程，并不是一个 CPU 核心。所以切换进程，只是切换一个进程里的一个线程到另一个进程里的一个线程，说白了还是线程切换。如果对比的是单个进程和进程内线程的切换，线程共享资源，cache命中率会高很多；进程切换，不共享资源，cache命中率低。</li><li>上下文切换说的是 CPU 寄存器的切换，跟 cache 和内存没啥关系，后者只是上下文切换带来的副作用。也许你会有疑问，当切换到新的进程，进程所需的资源不在内存里，这不就开始调度了，调度的开销呢？但实际是，新建进程不会分配 CPU，进程就绪后等等待分配处理器，此时资源已经进内存了。</li><li>切换进程可能要刷 TLB，进程内线程切换不需要。进程切换要切页表，所以可能同时要刷 TLB，这个根据实现定。</li></ul><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><ol><li><a href="https://yang-docs.readthedocs.io/en/latest/linux/contextswitch.html" target="_blank" rel="noopener">本文大量参考，CPU切换</a></li><li><a href="https://whatis.techtarget.com/definition/context-switch" target="_blank" rel="noopener">什么是上下文</a></li><li><a href="https://freemandealer.github.io/2015/07/14/context-switch/" target="_blank" rel="noopener">中断上下文</a></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;进程 CPU 调度和资源分配的单位，是一个程序在一个数据集上的一次执行；线程是CPU上运行和调度的基本执行单位，进程内的一个代码片段可创建为线程，线程由进程创建，寄生在进程身上。&lt;/p&gt;
&lt;p&gt;CPU 从一个&lt;strong&gt;任务&lt;/strong&gt;切换到另一个&lt;strong&gt;任务&lt;/strong&gt;，俗称上下文切换。理论上而言，进程负载了程序运行所有内容，开销较大。线程较轻量，和父进程共享各类资源，上下文切换的成本较低。可实际情况中具体如何？今天一探究竟。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="OS" scheme="https://muyuuuu.github.io/tags/OS/"/>
    
  </entry>
  
  <entry>
    <title>PyQt5 显示超清高分辨率图片</title>
    <link href="https://muyuuuu.github.io/2021/04/10/pyqt5-load-huge-image/"/>
    <id>https://muyuuuu.github.io/2021/04/10/pyqt5-load-huge-image/</id>
    <published>2021-04-10T03:25:20.000Z</published>
    <updated>2021-04-14T07:59:38.934Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>昨天写程序遇到一个问题，<code>pyqt5</code> 加载常规的图片完全可以显示。可当加载超清的高分辨率图片时，只能显示一个小角落。可我就想把一张 <code>3840x2160</code> 的图片加载到一个 <code>800x600</code> 的标签里该怎么办呢，如何自适应放缩尺寸？国内社区众所周知大多是抄袭，没什么解决方案，<del>就像本文刚发布两天就被抄走了</del>；外网站搜了一下也没找到现成的解决方案，我知道又到了我开坑的时候了。<del>博客能火全靠pyqt5，以后请叫我pyqt5之父（不是</del></p><a id="more"></a><h1 id="常规加载"><a href="#常规加载" class="headerlink" title="常规加载"></a>常规加载</h1><p>先来看一下，如何借助 <code>QLabel</code> 和 <code>QFileDialog</code> 加载低分辨率的图片，这时候时能正常显示的。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> sys</span><br><span class="line"><span class="keyword">from</span> PyQt5.QtWidgets <span class="keyword">import</span> (QMainWindow, QWidget, QHBoxLayout, QApplication, </span><br><span class="line">                             QPushButton, QLabel, QFileDialog, QVBoxLayout, </span><br><span class="line">                             QLineEdit)</span><br><span class="line"><span class="keyword">from</span> PyQt5.QtGui <span class="keyword">import</span> QPixmap</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">mainwindow</span><span class="params">(QMainWindow)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        super(mainwindow, self).__init__()</span><br><span class="line"></span><br><span class="line">        layout = QVBoxLayout()</span><br><span class="line">        w = QWidget()</span><br><span class="line">        w.setLayout(layout)</span><br><span class="line">        self.setCentralWidget(w)</span><br><span class="line"></span><br><span class="line">        self.image_label = QLabel()</span><br><span class="line">        self.image_label.setFixedSize(<span class="number">800</span>, <span class="number">500</span>)</span><br><span class="line">        layout.addWidget(self.image_label)</span><br><span class="line"></span><br><span class="line">        tmp_layout = QHBoxLayout()</span><br><span class="line">        btn = QPushButton(<span class="string">"选择图片路径"</span>)</span><br><span class="line">        tmp_layout.addWidget(btn)</span><br><span class="line">        btn.clicked.connect(self.load_image)</span><br><span class="line"></span><br><span class="line">        self.result = QLineEdit()</span><br><span class="line">        self.result.setPlaceholderText(<span class="string">"车牌展示"</span>)</span><br><span class="line">        self.result.setReadOnly(<span class="literal">True</span>)</span><br><span class="line">        tmp_layout.addWidget(self.result)</span><br><span class="line">        layout.addLayout(tmp_layout)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">load_image</span><span class="params">(self)</span>:</span></span><br><span class="line">        fname, _ = QFileDialog.getOpenFileName(self, <span class="string">'Open File'</span>, </span><br><span class="line">                    <span class="string">'C://'</span>, <span class="string">"Image files (*.jpg *.png)"</span>)</span><br><span class="line">        <span class="keyword">if</span> fname <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">            pixmap = QPixmap(fname)</span><br><span class="line">            self.image_label.setPixmap(pixmap)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    app = QApplication([])</span><br><span class="line">    m = mainwindow()</span><br><span class="line">    m.show()</span><br><span class="line">    sys.exit(app.exec())</span><br></pre></td></tr></table></figure><p>上述代码中，点击『选择图片路径』按钮就会调用文件对话框，选择图片后就会打开。步骤为：</p><ol><li>第一步，<code>QFileDialog</code> 选择文件路径</li><li>第二步，将文件路径传入 <code>QPixmap</code> 类，通过重载构造一个对象，文档原话：Constructs a pixmap from the file with the given fileName. If the file does not exist or is of an unknown format, the pixmap becomes a null pixmap.</li><li>第三步，将 <code>QPixmap</code> 对象传给标签的 <code>setPixmap</code> 方法，就完成了图片的显示。</li></ol><p>对于低分辨率图片，加载是没问题的：</p><p><img data-src="https://z3.ax1x.com/2021/04/10/calOyQ.png" alt></p><p>但高分辨率的图片，只能显示一个角落，也就是蓝色框那一部分：</p><p><img data-src="https://z3.ax1x.com/2021/04/10/ca81Zd.png" alt></p><p>如何解决呢？既然国内外都没有现成的解决方案，只能掏出万能的官方文档了。</p><h1 id="QImageReader-类"><a href="#QImageReader-类" class="headerlink" title="QImageReader 类"></a>QImageReader 类</h1><p>需要注意的是官方文档的语言是 <code>C++</code>，<del>还好我会C++。</del>打开文档，映入眼帘的就四句话：</p><ul><li><code>QImageReader reader(&quot;large.jpeg&quot;);</code> 读取图片</li><li><code>reader.size();</code> 图片尺寸</li><li><code>reader.setClipRect(myRect);</code> 图片裁剪</li><li><code>reader.setScaledSize(mySize);</code> 设置图片尺寸，文档原话：Another common function is to show a smaller version of the image. Loading a very large image and then scaling it down to the approriate size can be a very memory consuming operation. By calling the QImageReader::setScaledSize function, you can set the size that you want your resulting image to be.</li></ul><p>剩下的任务就很简单了，读图片，设置尺寸，显示。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> sys, time</span><br><span class="line"><span class="keyword">from</span> PyQt5.QtWidgets <span class="keyword">import</span> (QMainWindow, QWidget, QHBoxLayout, QApplication, </span><br><span class="line">                             QPushButton, QLabel, QFileDialog, QVBoxLayout, </span><br><span class="line">                             QLineEdit)</span><br><span class="line"><span class="keyword">from</span> PyQt5.QtGui <span class="keyword">import</span> QPixmap, QFont</span><br><span class="line"><span class="keyword">from</span> PyQt5.Qt <span class="keyword">import</span> QSize, QImageReader</span><br><span class="line"><span class="keyword">import</span> qdarkstyle</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">mainwindow</span><span class="params">(QMainWindow)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        super(mainwindow, self).__init__()</span><br><span class="line"></span><br><span class="line">        layout = QVBoxLayout()</span><br><span class="line">        w = QWidget()</span><br><span class="line">        w.setLayout(layout)</span><br><span class="line">        self.setCentralWidget(w)</span><br><span class="line"></span><br><span class="line">        self.image_label = QLabel()</span><br><span class="line">        self.image_label.setFixedSize(<span class="number">800</span>, <span class="number">500</span>)</span><br><span class="line">        layout.addWidget(self.image_label)</span><br><span class="line"></span><br><span class="line">        tmp_layout = QHBoxLayout()</span><br><span class="line">        btn = QPushButton(<span class="string">"选择图片路径"</span>)</span><br><span class="line">        tmp_layout.addWidget(btn)</span><br><span class="line">        btn.clicked.connect(self.load_image)</span><br><span class="line"></span><br><span class="line">        self.result = QLineEdit()</span><br><span class="line">        self.result.setPlaceholderText(<span class="string">"车牌展示"</span>)</span><br><span class="line">        self.result.setReadOnly(<span class="literal">True</span>)</span><br><span class="line">        tmp_layout.addWidget(self.result)</span><br><span class="line">        layout.addLayout(tmp_layout)</span><br><span class="line"></span><br><span class="line">        self.setStyleSheet(qdarkstyle.load_stylesheet_pyqt5())</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">load_image</span><span class="params">(self)</span>:</span></span><br><span class="line">        fname, _ = QFileDialog.getOpenFileName(self, <span class="string">'Open File'</span>, </span><br><span class="line">                   <span class="string">'C://'</span>, <span class="string">"Image files (*.jpg *.png)"</span>)</span><br><span class="line">        <span class="keyword">if</span> fname <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">            <span class="comment"># 还需要对图片进行重新调整大小</span></span><br><span class="line">            img = QImageReader(fname)</span><br><span class="line">            scale = <span class="number">800</span> / img.size().width()</span><br><span class="line">            height = int(img.size().height() * scale)</span><br><span class="line">            img.setScaledSize(QSize(<span class="number">800</span>, height))</span><br><span class="line">            img = img.read()</span><br><span class="line">            <span class="comment"># 打开设置好的图片</span></span><br><span class="line">            pixmap = QPixmap(img)</span><br><span class="line">            self.image_label.setPixmap(pixmap)</span><br><span class="line">            self.result.setText(<span class="string">"车牌号放到这里"</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    app = QApplication([])</span><br><span class="line">    font = QFont()</span><br><span class="line">    font.setFamily(<span class="string">"SimHei"</span>)</span><br><span class="line">    font.setPointSize(<span class="number">14</span>)</span><br><span class="line">    app.setFont(font)</span><br><span class="line">    m = mainwindow()</span><br><span class="line">    m.show()</span><br><span class="line">    sys.exit(app.exec())</span><br></pre></td></tr></table></figure><p>考虑到可能会加载超清图像，为了方便对图片进行控制，不要采用 <code>QImage</code> 或 <code>QPixmap</code>，而是使用 <code>QImageReader</code></p><p>代码解析：</p><ol><li>创建 <code>QImageReader</code> 对象，方便对图片进行更多的操作</li><li>自适应伸缩，将宽度限定为 800，自适应计算高度应该是多少，而后设置要缩放的大小</li><li>将设置好的图像读入为 <code>QImage</code> 类型，而后程序里将其转为 <code>QPixmap</code> 类型</li><li>正常方法设置即可，超清图像完美被加载</li></ol><p><img data-src="https://z3.ax1x.com/2021/04/10/caJGgf.png" alt></p><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><ol><li><a href="https://wiki.qt.io/Loading_Large_Images" target="_blank" rel="noopener">QImageReader官方文档</a></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;昨天写程序遇到一个问题，&lt;code&gt;pyqt5&lt;/code&gt; 加载常规的图片完全可以显示。可当加载超清的高分辨率图片时，只能显示一个小角落。可我就想把一张 &lt;code&gt;3840x2160&lt;/code&gt; 的图片加载到一个 &lt;code&gt;800x600&lt;/code&gt; 的标签里该怎么办呢，如何自适应放缩尺寸？国内社区众所周知大多是抄袭，没什么解决方案，&lt;del&gt;就像本文刚发布两天就被抄走了&lt;/del&gt;；外网站搜了一下也没找到现成的解决方案，我知道又到了我开坑的时候了。&lt;del&gt;博客能火全靠pyqt5，以后请叫我pyqt5之父（不是&lt;/del&gt;&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="GUI" scheme="https://muyuuuu.github.io/tags/GUI/"/>
    
  </entry>
  
  <entry>
    <title>交叉熵优缺点分析</title>
    <link href="https://muyuuuu.github.io/2021/04/02/cross-entropy/"/>
    <id>https://muyuuuu.github.io/2021/04/02/cross-entropy/</id>
    <published>2021-04-02T13:49:37.000Z</published>
    <updated>2021-04-02T14:46:49.365Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>继上次了解完<a href="https://muyuuuu.github.io/2020/05/09/entropy/">信息熵</a>后，没想到在对抗训练中又回到了损失函数的分析上。<code>torch</code>实现的有点迷惑，正好趁这次机会，连着代码和公式好好推导下。这种损失的优缺点日后会放上来，主要取决论文进度。</p><a id="more"></a><h1 id="一个简单实例"><a href="#一个简单实例" class="headerlink" title="一个简单实例"></a>一个简单实例</h1><p>交叉熵的公式定义耳熟能详了：</p><p>\begin{equation}\label{ce}<br>H(p,q) = -\sum_{x\in X} p(x) \log q(x)<br>\end{equation}</p><p>这里带入一个实例算一下：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="comment"># example of calculating cross entropy</span></span><br><span class="line"><span class="keyword">from</span> math <span class="keyword">import</span> log2</span><br><span class="line"></span><br><span class="line"><span class="comment"># calculate cross entropy</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">cross_entropy</span><span class="params">(p, q)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> -sum([p[i] * log2(q[i]) <span class="keyword">for</span> i <span class="keyword">in</span> range(len(p))])</span><br><span class="line"></span><br><span class="line"><span class="comment"># define data</span></span><br><span class="line">p = [<span class="number">0.10</span>, <span class="number">0.40</span>, <span class="number">0.50</span>]</span><br><span class="line">q = [<span class="number">0.80</span>, <span class="number">0.15</span>, <span class="number">0.05</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment"># calculate cross entropy H(P, Q)</span></span><br><span class="line">ce_pq = cross_entropy(p, q)</span><br><span class="line">print(<span class="string">'H(P, Q): %.3f bits'</span> % ce_pq)  <span class="comment"># H(P, Q): 3.288 bits</span></span><br><span class="line"><span class="comment"># calculate cross entropy H(Q, P)</span></span><br><span class="line">ce_qp = cross_entropy(q, p)</span><br><span class="line">print(<span class="string">'H(Q, P): %.3f bits'</span> % ce_qp)  <span class="comment"># H(Q, P): 2.906 bits</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 3 bit 的信息差异</span></span><br></pre></td></tr></table></figure><p>需要注意的是，$H(p,q) \neq H(q,p)$。那么再来看深度学习中的定义，其实都一样，只是深度学习中要求真实标签不能是 $\log$ 那一项，因为 $\log$ 的定义域不能取 0。</p><p>\begin{equation}<br>H = -\sum_{c=1}^M y \log \hat{y}<br>\end{equation}</p><p>其中，$y$ 是真实的标签，$M$ 是类别数量，$\hat{y}$ 是自己预测的结果。如果是三分类问题，假设 $y=[1,0,0]$，$\hat{y}=[0.4,0.1,0.5]$，那么此时的交叉熵是 $-\log 0.4$。<strong>因为其它标签取值是 0，即不参与运算</strong>，换句话说，交叉熵的损失只取决于被正确分类的概率，具体缺陷可以公式推导一下。带入上述代码，得到一致的结果。</p><h1 id="torch-实现"><a href="#torch-实现" class="headerlink" title="torch 实现"></a>torch 实现</h1><p>用过 tensorflow 和 keras 之后，愈发的发现 pytorch 真好用。<code>torch</code> 实现的交叉熵有一点需要注意，是通过 <code>logsoftmaax + NLLLoss</code> 实现的，实际代码中要注意这里，不要操作失误。 </p><ol><li>当获取网络的输出后，这里设网络的输出是 $O$，那么直接将 $O$ 扔进 <code>logsoftmax</code> 为交叉熵做准备，不需要激活。相当于计算公式 $\eqref{ce}$ 中的 $\log$ 那一项。</li><li>而后 <code>logsoftmax</code> 的输出进入 <code>NLLLoss</code>，计算真正的交叉熵损失。计算方法为，如果是三分类的任务，<code>logsoftmax</code> 的输出是 $-0.5,-0.1,-0.4$，如果当前类别是第一个类，那么损失就是 0.5，具体原因参考上述公式推导。</li></ol><p>如果你感觉哪里错了，可以看文末的参考链接中，有人用各种方法试验了 <code>torch</code> 的交叉熵，验证了 <code>torch</code> 的实现是正确的。</p><h1 id="优缺点分析"><a href="#优缺点分析" class="headerlink" title="优缺点分析"></a>优缺点分析</h1><ol><li>优点：对于 softmax 函数，常用的 MSE 误差越大，下降越慢。但交叉熵能很好改善这个问题，当误差较大时，梯度也大，下降的较快；也避免了某些情况下激活函数进入饱和区，梯度消失的问题。详情参考文末的公式推导。</li><li>缺点：论文要用，等哪天论文尘埃落定，我在回来填坑。<del>你可以手推公式，推着推着就发现了。</del></li></ol><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><ol><li><a href="https://ml-cheatsheet.readthedocs.io/en/latest/loss_functions.html" target="_blank" rel="noopener">交叉熵定义</a></li><li><a href="https://zhuanlan.zhihu.com/p/159477597" target="_blank" rel="noopener">torch 交叉熵代码验证</a></li><li><a href="https://byjiang.com/2017/07/16/Cross_Entropy_Cost_Function/" target="_blank" rel="noopener">交叉熵优点及公式推导</a></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;继上次了解完&lt;a href=&quot;https://muyuuuu.github.io/2020/05/09/entropy/&quot;&gt;信息熵&lt;/a&gt;后，没想到在对抗训练中又回到了损失函数的分析上。&lt;code&gt;torch&lt;/code&gt;实现的有点迷惑，正好趁这次机会，连着代码和公式好好推导下。这种损失的优缺点日后会放上来，主要取决论文进度。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="Algorithm" scheme="https://muyuuuu.github.io/tags/Algorithm/"/>
    
  </entry>
  
  <entry>
    <title>记一次重返唐山</title>
    <link href="https://muyuuuu.github.io/2021/03/31/back-to-TangShan/"/>
    <id>https://muyuuuu.github.io/2021/03/31/back-to-TangShan/</id>
    <published>2021-03-31T15:01:40.000Z</published>
    <updated>2021-04-02T14:50:55.588Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>好久不更新生活类的博客了，正好最近该忙的都忙完了，下一波 deadline 还早，<del>其实也就一周</del>，忙里偷闲写篇博客。</p><a id="more"></a><p>16 年考上唐山的大学的时候落差很大，所以对唐山也没什么特别的印象，除了大地震。2020 年毕业的时候，却发现对唐山，对理工，对实验室，对朋友，对老师都有了很深的感情，魂牵梦绕。</p><p>20 年因一个 SB 吃了蝙蝠，导致我毕业季只给了一天的返校时间。那天回学校的时候，才发现一切是如此亲切，才发现这竟然是我见的最后一面。比赛没拿奖没关系，朋友的感情和师生情谊这些更重要，也知道来日方长还有机会。回家的时候，发现来日不长了，送走了舍友，队友以后再也见不到了，老师也见不到了，朋友也见不到了，我都没好好告别，越想越不舍。在车上哭成了一条狗，口罩都湿透了，也不敢动作太大让旁边的人看见。</p><p>周六日接到了同学结婚的邀请，正好同学留在了唐山，婚礼也在唐山。我也回唐山好好看看，算是弥补一年前没好好告别的遗憾吧，哪怕是聊聊闲天，哪怕是拍个照片。上午决定要去，买票就走了，就是这么果断。</p><ol><li>收到了同学婚礼邀请，想着大学毕业太匆忙了，很多人没见一面。正好天南海北的同学都来，见上一面，以后见面也不容易了。晚上联系了老师，老师听说我们都回来了，打车过来和我们一起玩，聊一聊，折腾到了凌晨两三点。虽然我在写流水帐，但真的挺感动的。</li><li>第二天婚礼，新郎追新娘从初二到研二，十年青春啊，快节奏的生活下真的不容易。</li><li>婚礼，新娘挽着父亲，一位质朴的农民，沧桑的脸颊，杂乱的头发。说『我闺女今天就正式交给你了』，沙哑的声音却掷地有声。新娘湿红了双眼，新郎嘴角微抿。婚礼的其它流程都像排练好的，唯独这段情节不像。我在台下泪目，可能最诚挚的亲情、爱情都浓缩在那一句话里吧。</li><li>这次婚礼来了很多人，从2011级到2017级，铁打的实验室，流水的人。很多人只听说过名字，不认识真人。男生见面：你代码怎么样，你项目怎么样，有论文了吗？有了女生，聊天内容逐步转变，这个真好吃，你能不能吃慢点，你在哪要饭等等，酒席之间推杯换盏打打闹闹，原来这 TM 才是生活。</li><li>见到了熟悉的面孔，胡乱的扯一扯，算是给生活充充电吧。研一过的太混乱了，自己也很难受，混的很差。回首才发现原来他们依然对我充满了期待，无论走到哪里，无论身在何方，依然是同学、老师眼中的骄傲。哪怕是为了他们，我也该依旧热情下去，依旧努力。</li><li>分别的时候，好好告别了，和我的恩师，和我最好的队友，没那么仓促。也有女生提出了要拥抱，可能下次见面真的是很多年后了，不舍吧。与君离别意，同是宦游人，初中不懂的诗，现在懂了。</li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;好久不更新生活类的博客了，正好最近该忙的都忙完了，下一波 deadline 还早，&lt;del&gt;其实也就一周&lt;/del&gt;，忙里偷闲写篇博客。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="Life" scheme="https://muyuuuu.github.io/tags/Life/"/>
    
  </entry>
  
  <entry>
    <title>集群资源监控与任务调度</title>
    <link href="https://muyuuuu.github.io/2021/03/26/cluster-monitor-and-taskAssignment/"/>
    <id>https://muyuuuu.github.io/2021/03/26/cluster-monitor-and-taskAssignment/</id>
    <published>2021-03-26T14:36:11.000Z</published>
    <updated>2021-04-16T05:15:36.726Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>前阵子写了个集群计算资源监控程序，我觉得架构设计的还行，于是来整理成一篇博客。内容只有架构，不含代码，因为自己的实验室要用。<del>利益相关，人在美国，刚下飞机，懂的自然懂，匿了匿了。</del></p><a id="more"></a><h1 id="集群架构"><a href="#集群架构" class="headerlink" title="集群架构"></a>集群架构</h1><p>首先来看一下集群的结构，也就是计算节点如何连接，以及每个计算节点配备的计算资源如何，方便编写后续程序。</p><p><img data-src="https://z3.ax1x.com/2021/03/31/ck5RyR.png" alt></p><p>首先 <code>ssh</code> 到登录节点，而后可以由登录节点前往各个计算节点，假设这里一共由 <code>N</code> 个计算节点。其中，第 <code>M</code> 个计算节点配备了 <code>n</code> 个 GPU。这里就假设要监控 GPU 资源了，如果要监控 CPU 资源，一个道理。</p><h1 id="多线程工作模式"><a href="#多线程工作模式" class="headerlink" title="多线程工作模式"></a>多线程工作模式</h1><p>既然要监控这么多 GPU 的占用情况，轮询显然是一种最差的做法，毕竟 ssh 过去有网络延迟，且<strong>同步阻塞</strong>调用的方式效率很是低下。首先通过多线程的形式，将阻塞调用改为<strong>异步非阻塞调用</strong>。</p><p>这里，我们创建一个子线程去完成 GPU 负载信息的查询任务，使其不影响主进程的执行，这实现了<strong>异步</strong>；在子线程查询 GPU 负载信息时，再次创建多个子线程并发的完成查询，对于结果的返回选择<strong>非阻塞</strong>的方式，啥时候结果执行完了反馈回来就行；但多个子线程<strong>阻塞</strong>了子线程的执行，因为毕竟要等到多个子线程拿到结果才能返回，不然白执行一趟。理论上这里也应该非阻塞，但非阻塞也不需要去执行其他任务，索性让它阻塞在这里。</p><p>当然，我这里计算节点很少，可以为每个节点创建一个线程。当面临成百上千个计算节点时，可以分组创建线程，一次性创建这么多线程，维护、切换之类的开销太大了。</p><p>此时多线程的调用结构图如下所示：</p><p><img data-src="https://z3.ax1x.com/2021/03/31/ckLviq.png" alt></p><p>主进程核心代码如下：<br><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 主进程创建子线程定期轮询</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">monitor</span><span class="params">(compute_num, shell_path, timeout)</span>:</span></span><br><span class="line">    set_gpu_infoes_list(</span><br><span class="line">        get_all_gpu_node_info(compute_num, shell_path, timeout))</span><br><span class="line">    threading.Timer(interval, monitor, (compute_num, shell_path, timeout)).start()</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line">    th = threading.Timer(interval, monitor, (compute_num, shell_path, timeout))</span><br><span class="line">    <span class="comment"># 子线程去访问GPU信息，主进程干其他的</span></span><br><span class="line">    th.start()</span><br></pre></td></tr></table></figure></p><p>子线程核心代码如下：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 子线程创建多个子线程去查询</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_all_gpu_node_info</span><span class="params">(compute_num, shell_path, timeout)</span>:</span></span><br><span class="line">    task_list = []</span><br><span class="line">    result = []</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1</span>, compute_num+<span class="number">1</span>):</span><br><span class="line">        params = &#123;&#125;</span><br><span class="line">        params[<span class="string">'hostname'</span>] = <span class="string">'gpu0&#123;&#125;'</span>.format(i)</span><br><span class="line">        params[<span class="string">'shell_path'</span>] = shell_path</span><br><span class="line">        params[<span class="string">'timeout'</span>] = timeout</span><br><span class="line">        <span class="comment"># 创建线程</span></span><br><span class="line">        task = athread.AThread(get_single_gpu_node_info, args=params)</span><br><span class="line">        task_list.append(task)</span><br><span class="line">        task.start()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> task <span class="keyword">in</span> task_list:</span><br><span class="line">        <span class="comment"># 等待每个线程结束</span></span><br><span class="line">        task.join()</span><br><span class="line">        result.append(task.get_result())</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> result</span><br></pre></td></tr></table></figure><h1 id="log-写出"><a href="#log-写出" class="headerlink" title="log 写出"></a>log 写出</h1><p>这个是为了记录计算资源的历史使用信息准备的，每个计算节点一个 log 文件，每个 log 文件限长 1000 行，超过 1000 行后就先入先出处理掉。</p><h1 id="socket-通信"><a href="#socket-通信" class="headerlink" title="socket 通信"></a>socket 通信</h1><h1 id="消息队列"><a href="#消息队列" class="headerlink" title="消息队列"></a>消息队列</h1><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><ol><li><a href="https://juejin.cn/post/6844903817348136968" target="_blank" rel="noopener">什么是消息队列</a></li><li><a href="https://myblog.huajianjiang.com/post/detail/python-%E4%B8%AD%E5%AE%9E%E7%8E%B0%E7%AE%80%E5%8D%95%E7%9A%84%E5%9F%BA%E4%BA%8E-redis-%E5%90%8E%E7%AB%AF%E7%9A%84%E4%BB%BB%E5%8A%A1%E6%B6%88%E6%81%AF%E9%98%9F%E5%88%97/" target="_blank" rel="noopener">消息队列实现</a></li><li><a href="https://zhuanlan.zhihu.com/p/52845869" target="_blank" rel="noopener">上下文切换</a></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;前阵子写了个集群计算资源监控程序，我觉得架构设计的还行，于是来整理成一篇博客。内容只有架构，不含代码，因为自己的实验室要用。&lt;del&gt;利益相关，人在美国，刚下飞机，懂的自然懂，匿了匿了。&lt;/del&gt;&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="Design" scheme="https://muyuuuu.github.io/tags/Design/"/>
    
  </entry>
  
  <entry>
    <title>pagerank 算法实现</title>
    <link href="https://muyuuuu.github.io/2021/03/19/pagerank/"/>
    <id>https://muyuuuu.github.io/2021/03/19/pagerank/</id>
    <published>2021-03-19T04:00:32.000Z</published>
    <updated>2021-03-19T05:33:38.538Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>一个好的搜索引擎是现如今面向互联网编程的重要工具。而<code>pagerank</code>是曾经谷歌用来确定搜索引擎网页排名的算法，算法足够简洁且实用，且采用集群进行分布式计算而不是小型机的方法，给当时的网页排名带来了革命性突破。如果你实在看不懂理论部分如何处理的，就想当初的我一样。建议好好看一下代码，一目了然。</p><a id="more"></a><h1 id="算法思想"><a href="#算法思想" class="headerlink" title="算法思想"></a>算法思想</h1><p>这个算法十分简单，核心思想是『用投票的方法来确定哪个网页的质量好』，可以画几张图就描述清楚。</p><p><img data-src="https://s3.ax1x.com/2021/03/19/6WVjv4.png" alt></p><ul><li>如上图所示，当三个评分为0.2的网页指向一个网页时，那个网页的评分就是0.6；</li><li>当一个0.4评分的网页指向两个网页时，那两个网页平分分数，都是0.2。</li></ul><p>算法将整个互联网，看做一张有向图，网页是图上的点，链接是图上的有向边。每个网页都有一个分数，称作 PageRank，可以把它当做是一种「投票权」，将每一个链接作为一次「投票」，每个网页的 PageRank 等于所有指向该网页的网页的 PageRank 的求和。如果一个网页被指向的次数很多，且分数很大，那么就认为这个网页是被投票选出来的高质量网页；反之是广告等流氓网页。</p><h2 id="环形处理"><a href="#环形处理" class="headerlink" title="环形处理"></a>环形处理</h2><p><img data-src="https://s3.ax1x.com/2021/03/19/6WZ0iV.png" alt></p><p>如果是这样的网页，1号网页指向2号网页，2号网页指向3号网页，3号网页又指回1号网页。这个环路会一直循环，分数会持续叠加。这样的结果肯定不正确。为了处理环路，论文中指出：当用户在浏览网页时，有$\alpha$的概率沿着链接去访问下一个网页；$1-\alpha$的概率直接关闭网页去看其他网页。那么问题就迎刃而解了，设置随机数，来确定沿着链接访问网页还是访问其他的网页。处理环路的代码如下：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">pagerank</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, alpha=<span class="number">0.15</span>, num=<span class="number">1000</span>)</span>:</span></span><br><span class="line">        self.__alpha = alpha</span><br><span class="line">        self.__num = num</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="comment"># 邻接矩阵</span></span><br><span class="line">        adj = np.array(([<span class="number">0</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>], [<span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">0</span>], </span><br><span class="line">                        [<span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>], [<span class="number">0</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>]))</span><br><span class="line">        cnt = [<span class="number">0</span> <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">4</span>)]</span><br><span class="line">        <span class="comment"># 最开始访问的页面</span></span><br><span class="line">        page = np.random.randint(<span class="number">0</span>, <span class="number">4</span>, <span class="number">1</span>)</span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> range(self.__num):</span><br><span class="line">            tmp = np.random.random()</span><br><span class="line">            <span class="comment"># 页面内选择</span></span><br><span class="line">            <span class="keyword">if</span> tmp &gt; self.__alpha:</span><br><span class="line">                page = np.where(adj[page] == <span class="number">1</span>)[<span class="number">0</span>][<span class="number">0</span>]</span><br><span class="line">                cnt[page] += <span class="number">1</span></span><br><span class="line">            <span class="comment"># 随机游走</span></span><br><span class="line">            <span class="keyword">if</span> tmp &lt;= self.__alpha:</span><br><span class="line">                page = int(np.random.randint(<span class="number">0</span>, <span class="number">4</span>, <span class="number">1</span>))</span><br><span class="line">                cnt[page] += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> cnt</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">alpha, num = <span class="number">0.15</span>, <span class="number">1000</span></span><br><span class="line">page_num = pagerank(num=num, alpha=alpha)</span><br><span class="line">cnt = page_num.run()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 百分比表示某个时间点查看某个页面的概率</span></span><br><span class="line">page_pro = [str(i / num * <span class="number">100</span>) + <span class="string">'%'</span> <span class="keyword">for</span> i <span class="keyword">in</span> cnt]</span><br><span class="line">print(page_pro)</span><br></pre></td></tr></table></figure><p>而最后用百分比来表示某个页面在某一个时间段内被查看的概率，就可以按照概率的大小来确定网页排名了。结果大概是：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="number">0</span>: <span class="number">0.04</span></span><br><span class="line"><span class="number">1</span>: <span class="number">0.33</span></span><br><span class="line"><span class="number">2</span>: <span class="number">0.32</span></span><br><span class="line"><span class="number">3</span>: <span class="number">0.3</span></span><br></pre></td></tr></table></figure><h2 id="一般化"><a href="#一般化" class="headerlink" title="一般化"></a>一般化</h2><p>Step1：假设得到$N$个网页，所有的网站的初始排名是$B_0=(\frac{1}{N},\frac{1}{N},…,\frac{1}{N})^T$。</p><p>Step2：矩阵$A$是网页之间的链接数目，得到这个矩阵并不难，只要有个爬虫机器人就好了。</p><p>Step3: 更新网页排名分数：<script type="math/tex">B_{i+1}=\alpha A B_i + (1−\alpha)B_0</script> 。公式第一项为其他页面沿着链接访问该页面的分数，第二项为直接跳转到该页面的分数。</p><p>Step4：迭代计算直至$|B_{i+1}−B_i|&lt;\epsilon$，或者制定迭代轮数，可以得到最终结果。（选排名前几的就可以了）</p><p><img data-src="https://s3.ax1x.com/2021/03/19/6WezA1.png" alt></p><p>对于上图，一个完整的 <code>pagerank</code> 算法代码如下所示：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">pagerank</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, adj, alpha=<span class="number">0.15</span>, num=<span class="number">1000</span>)</span>:</span></span><br><span class="line">        self.__adj = adj</span><br><span class="line">        self.__alpha = alpha</span><br><span class="line">        self.__num = num</span><br><span class="line">        self.__page_num = self.__adj.shape[<span class="number">0</span>]</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="comment"># 均匀设置分数</span></span><br><span class="line">        score = [<span class="number">1</span> / self.__page_num <span class="keyword">for</span> i <span class="keyword">in</span> range(self.__page_num)]</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 看看有哪些页面指向自己</span></span><br><span class="line">        point_to_self = [</span><br><span class="line">            list(np.where(self.__adj[:, i] == <span class="number">1</span>)[<span class="number">0</span>])</span><br><span class="line">            <span class="keyword">for</span> i <span class="keyword">in</span> range(self.__page_num)</span><br><span class="line">        ]</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 如果四个页面，3号页面指向1号页面和2号页面，那么转移到任何一个页面的概率要平分</span></span><br><span class="line">        point_to_other = [</span><br><span class="line">            s / np.where(self.__adj[i, :] == <span class="number">1</span>)[<span class="number">0</span>].size</span><br><span class="line">            <span class="keyword">for</span> i, s <span class="keyword">in</span> enumerate(score)</span><br><span class="line">        ]</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 均匀跳转概率</span></span><br><span class="line">        jump_pro = <span class="number">1</span> / self.__page_num</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 网页访问 num 次，计算每个页面的概率</span></span><br><span class="line">        <span class="keyword">for</span> num <span class="keyword">in</span> range(self.__num):</span><br><span class="line">            <span class="keyword">for</span> i <span class="keyword">in</span> range(self.__page_num):</span><br><span class="line">                s = sum(point_to_other[j] <span class="keyword">for</span> j <span class="keyword">in</span> point_to_self[i])</span><br><span class="line">                <span class="comment"># 其它页面指向自己，加上跳转概率，就是自己被访问的概率</span></span><br><span class="line">                score[i] = s * (<span class="number">1</span> - self.__alpha) + self.__alpha * jump_pro</span><br><span class="line">            <span class="comment"># 访问一次后，指向其它页面的概率更新，也就是再次均匀划分</span></span><br><span class="line">            point_to_other = [</span><br><span class="line">                s / np.where(self.__adj[i, :] == <span class="number">1</span>)[<span class="number">0</span>].size</span><br><span class="line">                <span class="keyword">for</span> i, s <span class="keyword">in</span> enumerate(score)</span><br><span class="line">            ]</span><br><span class="line">        <span class="keyword">return</span> score</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 邻接矩阵</span></span><br><span class="line">    adj = np.array([[<span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">1</span>], [<span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">1</span>], [<span class="number">1</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>], [<span class="number">0</span>, <span class="number">1</span>, <span class="number">1</span>, <span class="number">0</span>]])</span><br><span class="line">    <span class="keyword">assert</span> adj.shape[<span class="number">0</span>] == adj.shape[<span class="number">1</span>]</span><br><span class="line">    alpha = <span class="number">0.15</span></span><br><span class="line">    num = <span class="number">1000</span></span><br><span class="line"></span><br><span class="line">    p = pagerank(adj, alpha=alpha, num=num)</span><br><span class="line">    score = p.run()</span><br><span class="line"></span><br><span class="line">    page_pro = [str(round(i * <span class="number">100</span>, <span class="number">2</span>)) + <span class="string">'%'</span> <span class="keyword">for</span> i <span class="keyword">in</span> score]</span><br><span class="line">    print(page_pro)</span><br></pre></td></tr></table></figure><h1 id="完整代码"><a href="#完整代码" class="headerlink" title="完整代码"></a>完整代码</h1><ul><li><a href="https://course.ccs.neu.edu/cs6200f14/wt2g_inlinks" target="_blank" rel="noopener">数据集</a>（把数据集直接复制粘贴保存为<code>link.data</code>）</li><li>数据集说明：<a href="https://amslaurea.unibo.it/10469/1/Temporal_PageRank.pdf" target="_blank" rel="noopener">https://amslaurea.unibo.it/10469/1/Temporal_PageRank.pdf</a></li><li>代码链接：<a href="https://github.com/muyuuuu/Spark-learn/blob/main/pagerank-demo/main.py" target="_blank" rel="noopener">https://github.com/muyuuuu/Spark-learn/blob/main/pagerank-demo/main.py</a></li></ul><p>我做出来的结果是下面这样的，和网上公布的大多结果保持一致。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 节点数量</span></span><br><span class="line">vertex number <span class="keyword">is</span> : <span class="number">183811</span></span><br><span class="line"><span class="comment"># 边的数量</span></span><br><span class="line">edge number <span class="keyword">is</span> : <span class="number">641727</span></span><br><span class="line"><span class="comment"># 迭代 100 轮执行时间</span></span><br><span class="line"><span class="number">33.12673735618591</span></span><br><span class="line"><span class="comment"># 排名前几的结果</span></span><br><span class="line">WT21-B37<span class="number">-76</span> <span class="number">0.0014513384131756468</span></span><br><span class="line">WT21-B37<span class="number">-75</span> <span class="number">0.0008265334385338406</span></span><br><span class="line">WT25-B39<span class="number">-116</span> <span class="number">0.0007959759004498566</span></span><br><span class="line">WT23-B21<span class="number">-53</span> <span class="number">0.000743342282644727</span></span><br><span class="line">WT24-B40<span class="number">-171</span> <span class="number">0.0006743921148099952</span></span><br><span class="line">WT23-B39<span class="number">-340</span> <span class="number">0.000671900553656238</span></span><br><span class="line">WT23-B37<span class="number">-134</span> <span class="number">0.0006528246305109245</span></span><br><span class="line">WT08-B18<span class="number">-400</span> <span class="number">0.000619414607912892</span></span><br><span class="line">WT13-B06<span class="number">-284</span> <span class="number">0.0006092497992106295</span></span><br><span class="line">WT24-B26<span class="number">-46</span> <span class="number">0.0005877279600700914</span></span><br><span class="line">WT13-B06<span class="number">-273</span> <span class="number">0.0005658734074429803</span></span><br><span class="line">WT01-B18<span class="number">-225</span> <span class="number">0.0005354041130099478</span></span><br><span class="line">WT04-B27<span class="number">-720</span> <span class="number">0.0005072144885359412</span></span><br><span class="line">WT23-B19<span class="number">-156</span> <span class="number">0.0004843699050803331</span></span><br></pre></td></tr></table></figure><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p><a href="https://liam.page/2017/04/04/Python-100-lines-of-PageRank/" target="_blank" rel="noopener">https://liam.page/2017/04/04/Python-100-lines-of-PageRank/</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;一个好的搜索引擎是现如今面向互联网编程的重要工具。而&lt;code&gt;pagerank&lt;/code&gt;是曾经谷歌用来确定搜索引擎网页排名的算法，算法足够简洁且实用，且采用集群进行分布式计算而不是小型机的方法，给当时的网页排名带来了革命性突破。如果你实在看不懂理论部分如何处理的，就想当初的我一样。建议好好看一下代码，一目了然。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="Algorithm" scheme="https://muyuuuu.github.io/tags/Algorithm/"/>
    
  </entry>
  
  <entry>
    <title>数据结构与算法笔记『一』哈希</title>
    <link href="https://muyuuuu.github.io/2021/03/14/hash/"/>
    <id>https://muyuuuu.github.io/2021/03/14/hash/</id>
    <published>2021-03-14T04:00:32.000Z</published>
    <updated>2021-03-19T04:48:24.563Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>每天一点点算法开始了，<del>为什么我就写不出很爽的代码</del>。今天被邀请去水了一场类似ACM的程序设计竞赛，他说是期末考试难度，结果那个难度……题目只做出了一半，剩下的实在是…….出题人的语文水平太差了，没交代清楚题要干什么，很多细节描述的模棱两可。如果你考我程序设计，我没问题；通过模糊的信息表达来考察语文理解能力就没意思了，为啥不多打几个字把问题交代清楚呢？<strong>为节省空间，本文压缩了某些代码，实际工程切勿模仿。</strong></p><a id="more"></a><h1 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h1><p>哈希是一种典型的利用空间换时间的算法，通过申请大量的空间，来达到在$O(1)$的时间复杂度内快速查询元素的目的。如最简单的，假设输入序列是<code>0,1,2,3,4,5</code>，申请一个数组作为哈希表。那么此时的哈希计算公式就可以是：<code>H[i] = i % size</code>。<code>size</code>是哈希表的长度，或者说是数组的长度。那么0存储在0号位置，5存储在5号位置。元素插入哈希表和查询哈希表内元素都可以在内完成。</p><p>假设表长为5，如果要插入的序列是<code>0,2,4,7</code>，就不能用上面的映射公式了，因为<code>7%5 == 2%5</code>，也就是2号位置会存储2或7，导致了冲突(collision)。这个时候就需要用探测的方法来确定7该存在哪里，常用的方法有：<a href="https://en.wikipedia.org/wiki/Linear_probing" target="_blank" rel="noopener">线性探测法</a>、<a href="https://en.wikipedia.org/wiki/Quadratic_probing" target="_blank" rel="noopener">平方探测法</a>。</p><p>道理就是这么个道理，有很多方法可以实现哈希，如数组、map等。</p><h1 id="哈希插入"><a href="#哈希插入" class="headerlink" title="哈希插入"></a>哈希插入</h1><p>现在手动模拟一下用平方探测法（只考虑正数项）来模拟下哈希插入的过程。经验而言，哈希表的长度最好是素数。第一行中，首先输入表长，若表长不为素数，则增加表长转换为最近的素数，而后输入元素的数量$N$。在下一行输入$N$个数据，插入到哈希表中，并输出插入的位置；若无法插入，则输出<code>-</code>。</p><p>样例输入：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="number">4</span> <span class="number">4</span></span><br><span class="line"><span class="number">10</span> <span class="number">6</span> <span class="number">4</span> <span class="number">15</span></span><br></pre></td></tr></table></figure></p><p>样例输出：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="number">0</span> <span class="number">1</span> <span class="number">4</span> -</span><br></pre></td></tr></table></figure></p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;array&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="comment">// 申请这么长的数组作为哈希表</span></span><br><span class="line"><span class="built_in">array</span>&lt;<span class="keyword">int</span>, 10005&gt; arr;</span><br><span class="line"><span class="keyword">int</span> m, n;</span><br><span class="line"><span class="comment">// 判断是否为素数</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">isprime</span><span class="params">(<span class="keyword">int</span> num)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(num == <span class="number">1</span>) </span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">2</span>; i * i &lt;= num; i++)</span><br><span class="line">        <span class="keyword">if</span>(num % i == <span class="number">0</span>) </span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 二次探测法插入元素</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">insert</span><span class="params">(<span class="keyword">int</span> a)</span></span>&#123;</span><br><span class="line">    <span class="comment">// i 是步长，m 是表长</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; m; i++)&#123;</span><br><span class="line">        <span class="keyword">int</span> idx = (a + i * i) % m;</span><br><span class="line">        <span class="comment">// 如果为空，插入，并输出索引位置</span></span><br><span class="line">        <span class="keyword">if</span> (arr[idx] == <span class="number">0</span>)&#123;</span><br><span class="line">            arr[idx] = <span class="number">1</span>;</span><br><span class="line">            <span class="built_in">cout</span> &lt;&lt; idx;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 否则表示插入失败</span></span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="string">"-"</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; m &gt;&gt; n;</span><br><span class="line">    <span class="comment">// 逐步叠加，找到素数的表长</span></span><br><span class="line">    <span class="keyword">while</span> (!isprime(m)) </span><br><span class="line">        m += <span class="number">1</span>;</span><br><span class="line">    <span class="comment">// 开始插入元素</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)&#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; a;</span><br><span class="line">        <span class="keyword">if</span> (i != <span class="number">0</span>) </span><br><span class="line">            <span class="built_in">cout</span> &lt;&lt; <span class="string">" "</span>;</span><br><span class="line">        insert(a);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="哈希查找"><a href="#哈希查找" class="headerlink" title="哈希查找"></a>哈希查找</h1><p>第二种常见应用是先把一个序列的元素插入到哈希表中；而后查询另一个序列的元素有没有在哈希表中，计算总共的查找次数。和上面不同的是，插入元素时不能单纯的将哈希表的数值设为1了，而应该是具体的数值，来看一个实例。在第一行输入三个数字，第一个数字是哈希表长度，和上述问题一样需要处理为素数；第二个为输入序列的长度$N$；第三为查询序列长度是$M$。第二行给出$N$个元素，用二次探测法插入到哈希表中，如果不能插入，打印插入失败信息；第三行给出$M$个元素，在哈希表中查询这些元素，记录查询次数。</p><p>输入样例：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="number">4</span> <span class="number">5</span> <span class="number">4</span></span><br><span class="line"><span class="number">10</span> <span class="number">6</span> <span class="number">4</span> <span class="number">15</span> <span class="number">11</span></span><br><span class="line"><span class="number">11</span> <span class="number">4</span> <span class="number">15</span> <span class="number">2</span></span><br></pre></td></tr></table></figure></p><p>输出样例：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="number">15</span> cannot be inserted.</span><br><span class="line"><span class="number">2.5</span></span><br></pre></td></tr></table></figure></p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;array&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="built_in">array</span>&lt;<span class="keyword">int</span>, 10005&gt; arr;</span><br><span class="line"><span class="keyword">int</span> size_, n, m;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">isPrime</span><span class="params">(<span class="keyword">int</span> m)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (m == <span class="number">1</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">2</span>; i * i &lt;= m; i++)</span><br><span class="line">        <span class="keyword">if</span> (m % i == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">insert</span><span class="params">(<span class="keyword">int</span> val)</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; size_; i++)&#123;</span><br><span class="line">        <span class="keyword">int</span> idx = (val + i * i) % size_;</span><br><span class="line">        <span class="comment">// 插入元素为对应的数值</span></span><br><span class="line">        <span class="keyword">if</span> (arr[idx] == <span class="number">0</span>)&#123;</span><br><span class="line">            arr[idx] = val;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; val &lt;&lt; <span class="string">" cannot be inserted."</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; size_ &gt;&gt; n &gt;&gt; m;</span><br><span class="line">    <span class="keyword">while</span> (!isPrime(size_))</span><br><span class="line">        size_ ++;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; size_ &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">int</span> t;</span><br><span class="line">    <span class="keyword">double</span> cnt&#123;<span class="number">0</span>&#125;;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)&#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; t;</span><br><span class="line">        insert(t);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; m; i++)&#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; t;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; size_; j++)&#123;</span><br><span class="line">            cnt++;</span><br><span class="line">            <span class="keyword">int</span> idx = (t + j * j) % size_;</span><br><span class="line">            <span class="comment">// 如果和查询元素相等，表示查找到，退出</span></span><br><span class="line">            <span class="comment">// 如果查找为 0，表示序列中无此元素，查找结束，退出</span></span><br><span class="line">            <span class="keyword">if</span> (arr[idx] == t || arr[idx] == <span class="number">0</span>)</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%0.1f"</span>, cnt / m);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="哈希应用"><a href="#哈希应用" class="headerlink" title="哈希应用"></a>哈希应用</h1><p>哈希在算法领域还是很常见的一种技巧，有很高的应用价值。接下来看几道例题，比单纯的哈希插入、查找要有意思的多。</p><h2 id="映射唯一"><a href="#映射唯一" class="headerlink" title="映射唯一"></a>映射唯一</h2><p>一种最简单的实例，通过数组实现哈希。先给出一个整数$N$，随后给出$N$行的人员信息，格式为：<code>队伍编号-队员编号 成绩</code>，以队伍编号为单位，计算得分最多的单位。思路就是：以每个队伍的编号为数组的下标，下标对应的数组元素为队伍的分数。这里能这么做的原因是，在哈希的过程中，队伍编号是唯一的。</p><p>输入样例:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="number">6</span></span><br><span class="line"><span class="number">3</span><span class="number">-10</span> <span class="number">99</span></span><br><span class="line"><span class="number">11</span><span class="number">-5</span> <span class="number">87</span></span><br><span class="line"><span class="number">102</span><span class="number">-1</span> <span class="number">0</span></span><br><span class="line"><span class="number">102</span><span class="number">-3</span> <span class="number">100</span></span><br><span class="line"><span class="number">11</span><span class="number">-9</span> <span class="number">89</span></span><br><span class="line"><span class="number">3</span><span class="number">-2</span> <span class="number">61</span></span><br></pre></td></tr></table></figure></p><p>输出样例:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="number">11</span> <span class="number">176</span></span><br></pre></td></tr></table></figure></p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;array&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="built_in">array</span>&lt;<span class="keyword">int</span>, 1005&gt; arr = &#123;<span class="number">0</span>&#125;;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> n, g, p, s, maxn&#123;<span class="number">-1</span>&#125;, id&#123;<span class="number">-1</span>&#125;;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; n;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)&#123;</span><br><span class="line">        <span class="built_in">scanf</span>(<span class="string">"%d-%d %d"</span>, &amp;g, &amp;p, &amp;s);</span><br><span class="line">        <span class="comment">// 队伍加分</span></span><br><span class="line">        arr[g] += s;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 输出最大</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">1005</span>; i++)&#123;</span><br><span class="line">        <span class="keyword">if</span> (arr[i] &gt; maxn)&#123;</span><br><span class="line">            maxn = arr[i];</span><br><span class="line">            id = i;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; id &lt;&lt; <span class="string">" "</span> &lt;&lt; maxn;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="map-实现哈希"><a href="#map-实现哈希" class="headerlink" title="map 实现哈希"></a>map 实现哈希</h2><p>map数据结构是什么请移步<a href="https://muyuuuu.github.io/2019/06/02/OJ-Cpp-cumulative/">此处</a>。假设开学要查抄学生携带的违规物品，输入第一行给出两个正整数$N$和$M$，分别是学生人数和需要被查缴的物品种类数。第二行给出$M$个需要被查缴的物品编号，其中编号为 4 位数字。随后$N$行，每行给出一位学生的姓名缩写（由 1-4 个大写英文字母组成）、个人物品数量$K$、以及$K$个物品的编号，顺次检查每个学生携带的物品，如果有需要被查缴的物品存在，则按以下格式输出该生的信息和其需要被查缴的物品的信息（注意行末不得有多余空格）。</p><p>输入样例：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="number">4</span> <span class="number">2</span></span><br><span class="line"><span class="number">2333</span> <span class="number">6666</span></span><br><span class="line">CYLL <span class="number">3</span> <span class="number">1234</span> <span class="number">2345</span> <span class="number">3456</span></span><br><span class="line">U <span class="number">4</span> <span class="number">9966</span> <span class="number">6666</span> <span class="number">8888</span> <span class="number">6666</span></span><br><span class="line">GG <span class="number">2</span> <span class="number">2333</span> <span class="number">7777</span></span><br><span class="line">JJ <span class="number">3</span> <span class="number">0012</span> <span class="number">6666</span> <span class="number">2333</span></span><br></pre></td></tr></table></figure></p><p>输出样例：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">U: <span class="number">6666</span> <span class="number">6666</span></span><br><span class="line">GG: <span class="number">2333</span></span><br><span class="line">JJ: <span class="number">6666</span> <span class="number">2333</span></span><br><span class="line"><span class="number">3</span> <span class="number">5</span></span><br></pre></td></tr></table></figure></p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;map&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> n, m, k, cnt1&#123;<span class="number">0</span>&#125;, cnt2&#123;<span class="number">0</span>&#125;;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; n &gt;&gt; m;</span><br><span class="line">    <span class="built_in">string</span> s, name;</span><br><span class="line">    <span class="comment">// 将违规物品映射为1</span></span><br><span class="line">    <span class="built_in">map</span> &lt;<span class="built_in">string</span>, <span class="keyword">int</span>&gt; check;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; m; i++)&#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; s;</span><br><span class="line">        check[s] = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)&#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; name &gt;&gt; k;</span><br><span class="line">        <span class="keyword">int</span> flag&#123;<span class="number">0</span>&#125;;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; k; j++)&#123;</span><br><span class="line">            <span class="built_in">cin</span> &gt;&gt; s;</span><br><span class="line">            <span class="comment">// 如果查询到就说明违规，违规物品记录多次</span></span><br><span class="line">            <span class="keyword">if</span> (check[s] == <span class="number">1</span>)&#123;</span><br><span class="line">                cnt2++;</span><br><span class="line">                <span class="comment">// 只记录一次违规人数</span></span><br><span class="line">                <span class="keyword">if</span> (flag == <span class="number">0</span>)&#123;</span><br><span class="line">                    <span class="built_in">cout</span> &lt;&lt; name &lt;&lt; <span class="string">":"</span>;</span><br><span class="line">                    flag = <span class="number">1</span>;</span><br><span class="line">                    cnt1 ++;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="built_in">cout</span> &lt;&lt; <span class="string">" "</span> &lt;&lt; s;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (flag == <span class="number">1</span>)</span><br><span class="line">            <span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; cnt1 &lt;&lt; <span class="string">" "</span> &lt;&lt; cnt2;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="字符串过滤"><a href="#字符串过滤" class="headerlink" title="字符串过滤"></a>字符串过滤</h2><p>给定两个字符串，删除第一个字符串中所包含的第二个字符串的字符，实现两个字符串相减。如果每次输出字符都判断一下这个字符是否在第二个字符串中，复杂度约为$O(n^2)$，必然会超时。如果使用哈希，开辟一块内存空间，就能大规模减少时间复杂度到$O(n)$。</p><p>样例输入：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">They are students.</span><br><span class="line">aeiou</span><br></pre></td></tr></table></figure></p><p>样例输出：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Thy r stdnts.</span><br></pre></td></tr></table></figure></p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">string</span> str1, str2;</span><br><span class="line">    getline(<span class="built_in">cin</span>, str1);</span><br><span class="line">    getline(<span class="built_in">cin</span>, str2);</span><br><span class="line">    <span class="keyword">bool</span> flag[<span class="number">256</span>] = &#123;<span class="literal">false</span>&#125;;</span><br><span class="line">    <span class="comment">// 遍历第一个字符串，全部设为 true</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; str1.length(); i++)</span><br><span class="line">        flag[str1[i]] = <span class="literal">true</span>;</span><br><span class="line">    <span class="comment">// 遍历第二个字符串，设为 false</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; str2.length(); i++)</span><br><span class="line">        flag[str2[i]] = <span class="literal">false</span>;</span><br><span class="line">    <span class="comment">// 再次遍历第一个，只输出 true，false的会被屏蔽</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; str1.length(); i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (flag[str1[i]] == <span class="literal">true</span>)</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">"%c"</span>, str1[i]);</span><br><span class="line">    &#125;  </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="减少计算量"><a href="#减少计算量" class="headerlink" title="减少计算量"></a>减少计算量</h2><p>$3n+1$猜想的步骤是：如果一个数是偶数，那将它除以2；如果一个数是奇数，那么将它乘以3后加1，经过有限步骤一定可以取值为1。为了节省计算，可以记录递推过程中遇到的每一个数。如计算$n=3$时，需要计算<code>3、5、8、4、2、1</code>，当下一次遇到<code>5、8、4、2</code>的时候，就不用计算了，可以直接判断得到结果。称<code>5、8、4、2</code>是3的覆盖数。我们称一个序列中的某几个数是关键数，如果这几个关键数不被其他数字所覆盖。现在给出一个序列，序列中元素的最大值为100，最小值为2，计算其中的关键数，并从大到小输出。</p><p>这就是一个典型的哈希问题，创建一个数组，以序列元素为数组的下标进行查询；数组的取值表示是否为关键数，这里设取值为1表示关键数。如果序列中的值对应数组元素的取值是1，则输出。因为序列中最大值为100，所以能设计的最大取值范围是99*3+1=298。凑个整，直接申请300大小的数组。那么来编写代码。</p><p>输入样例：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="number">6</span></span><br><span class="line"><span class="number">3</span> <span class="number">5</span> <span class="number">6</span> <span class="number">7</span> <span class="number">8</span> <span class="number">11</span></span><br></pre></td></tr></table></figure></p><p>输出样例：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="number">7</span> <span class="number">6</span></span><br></pre></td></tr></table></figure></p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;array&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="built_in">array</span>&lt;<span class="keyword">int</span>, 300&gt; arr = &#123;<span class="number">0</span>&#125;;</span><br><span class="line"><span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; v;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">cmp</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b)</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a &gt; b ? <span class="literal">true</span> : <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> k, tmp, flag&#123;<span class="number">0</span>&#125;;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; k;</span><br><span class="line">    v.resize(k);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; k; i++)&#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; tmp;</span><br><span class="line">        v[i] = tmp;</span><br><span class="line">        <span class="keyword">while</span> (tmp != <span class="number">1</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (tmp % <span class="number">2</span> != <span class="number">0</span>)</span><br><span class="line">                tmp = tmp * <span class="number">3</span> + <span class="number">1</span>;</span><br><span class="line">            tmp = tmp / <span class="number">2</span>;</span><br><span class="line">            <span class="comment">// 如果已经是非关键数了，那么之后的肯定是非关键数</span></span><br><span class="line">            <span class="keyword">if</span> (arr[tmp] == <span class="number">1</span>)</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="comment">// 非关键数全部记录为 1</span></span><br><span class="line">            arr[tmp] = <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 对序列从大到小排序</span></span><br><span class="line">    sort(v.<span class="built_in">begin</span>(), v.<span class="built_in">end</span>(), cmp);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; v.<span class="built_in">size</span>(); i++)&#123;</span><br><span class="line">        <span class="comment">// 如果序列对应的哈希值不是1，那么就是非关键数，输出</span></span><br><span class="line">        <span class="keyword">if</span> (arr[v[i]] == <span class="number">0</span>)&#123;</span><br><span class="line">            <span class="keyword">if</span> (flag == <span class="number">0</span>)&#123;</span><br><span class="line">                <span class="built_in">cout</span> &lt;&lt; v[i];</span><br><span class="line">                flag = <span class="number">1</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="built_in">cout</span> &lt;&lt; <span class="string">" "</span> &lt;&lt; v[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h1><p>如果后续遇到了其他类型的不错题目，会继续追加到这里。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;每天一点点算法开始了，&lt;del&gt;为什么我就写不出很爽的代码&lt;/del&gt;。今天被邀请去水了一场类似ACM的程序设计竞赛，他说是期末考试难度，结果那个难度……题目只做出了一半，剩下的实在是…….出题人的语文水平太差了，没交代清楚题要干什么，很多细节描述的模棱两可。如果你考我程序设计，我没问题；通过模糊的信息表达来考察语文理解能力就没意思了，为啥不多打几个字把问题交代清楚呢？&lt;strong&gt;为节省空间，本文压缩了某些代码，实际工程切勿模仿。&lt;/strong&gt;&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="DataStructure" scheme="https://muyuuuu.github.io/tags/DataStructure/"/>
    
  </entry>
  
  <entry>
    <title>因果扩张卷积</title>
    <link href="https://muyuuuu.github.io/2021/03/10/causal-dilated-conv/"/>
    <id>https://muyuuuu.github.io/2021/03/10/causal-dilated-conv/</id>
    <published>2021-03-10T06:30:05.000Z</published>
    <updated>2021-03-11T13:47:37.219Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>之前一直在看他们用『<strong>因果扩张卷积</strong>』这个东西，当时我只看到了一张图，大概了解了那是个什么东西。今天，在某次比赛中用到了这个模型，顺手看了下源代码，来做个整理。</p><a id="more"></a><p>在时序序列的预测问题中，很容易想到LSTM等循环神经网络。给定序列数据，预测下一时刻的输出，如翻译、问答等。</p><p>\begin{equation}<br>p(x)=\prod_{t=1}^T P(x_t|x_{t-1},…,x_1)<br>\end{equation}</p><p>当然也可以有隐藏状态：</p><p>\begin{equation}<br>p(x|h)=\prod_{t=1}^T P(x_t|x_{t-1},…,x_1, h)<br>\end{equation}</p><p>时至今日，也可以用 CNN 来代替这一过程，卷积的输入是历史数据，产生一个时刻的输出。且，相对而言 CNN 的参数要少很多，也可以叠加 CNN 层。需要注意的是，因果扩张卷积包括因果卷积和扩张卷积两部分。</p><h1 id="扩张卷积"><a href="#扩张卷积" class="headerlink" title="扩张卷积"></a>扩张卷积</h1><p>扩张卷积是为了增大卷积核感受野而设计的一种东西，看图就知道了：</p><p><img data-src="https://s3.ax1x.com/2021/03/10/6JPvCt.gif" alt></p><p>卷积核与图像计算时填充了间隙，在移动相同步伐的情况下，能卷积更多的数据，这样就能扩大视感受野。这里的扩张步是2。代码实现也很简单，指定参数即可：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">model = torch.nn.Conv2d(in_channels, out_channels, kernel_size, dilation=<span class="number">1</span>)</span><br></pre></td></tr></table></figure><p>默认情况下<code>dilation</code>取值为1，也就是没有扩张。</p><h1 id="因果卷积"><a href="#因果卷积" class="headerlink" title="因果卷积"></a>因果卷积</h1><p>因果卷积的重点突出在<strong>因果</strong>两个字上，输入的历史数据是因，输出的数据是果。来看一下模型结构图，这里有三层卷积网络。</p><p><img data-src="/2021/03/10/causal-dilated-conv/1.png" alt></p><p>第一层的卷积块读取几个历史数据，经过后几层的卷积块后输出一个数据，达到预测的目的。</p><p>但是，在逐层卷积的同时，每步只卷积两个模块。这就导致了视野域比较小。所以增加了扩张卷积，来增大网络的感受野，即：每次预测时能比之前看到更多的数据，做出更稳定的决策。此时的网络结构为：</p><p><img data-src="https://s3.ax1x.com/2021/03/10/6Jc9hT.png" alt></p><p>可以看到网络在预测输出时，能看到16个数据的信息，比之前的5个数据的信息要多出三倍，做出的决策也会更加稳定。在某种意义上，也就能像LSTM那样处理序列数据了。</p><p>也会你会有疑问，<code>1</code>号节点有历史数据，假设<code>1</code>号节点已经是输出序列的最后一个元素了，那<code>2</code>号节点和<code>3</code>号节点呢，他们的输入从哪里来？看了代码我才明白，填充即可，也就是<code>conv</code>里面常用的<code>padding</code>。卷积过程保证输入和输出的长度不变，填充多少，</p><p>至于残差连接或加入条件信息，这些都是可以的，在<a href="https://arxiv.org/pdf/1609.03499.pdf" target="_blank" rel="noopener"> casual dilated conv </a>首次被提出的论文中都有这些内容。</p><h1 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h1><p>读懂一个算法精髓最好的方式是读<a href="https://github.com/muyuuuu/Algorithm/blob/master/causal-dilated-conv/casualCNN.py" target="_blank" rel="noopener">代码</a>，理解一个算法精髓最好的方式是尝试去理解它为什么这么做，而不是这么做有什么好处。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;之前一直在看他们用『&lt;strong&gt;因果扩张卷积&lt;/strong&gt;』这个东西，当时我只看到了一张图，大概了解了那是个什么东西。今天，在某次比赛中用到了这个模型，顺手看了下源代码，来做个整理。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="DeepLearning" scheme="https://muyuuuu.github.io/tags/DeepLearning/"/>
    
  </entry>
  
  <entry>
    <title>pytorch并行训练</title>
    <link href="https://muyuuuu.github.io/2021/02/23/torch-parallel/"/>
    <id>https://muyuuuu.github.io/2021/02/23/torch-parallel/</id>
    <published>2021-02-23T14:22:20.000Z</published>
    <updated>2021-04-16T13:59:22.637Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>本文以 pytorch 为框架，分别从数据并行训练的方法(DataParallel, DistributedDataParallel)、并行加载数据两个角度，阐述了本次实验中并行算法的设计思想，并对并行实现的原理进行了解析，包括多进程的工作流程和通信方法等。<del>语言这么正规的原因是：这是从大作业里面摘出来的。</del>神经网络那部分我就不写了，跑题，最后直接给代码。</p><a id="more"></a><h1 id="环境"><a href="#环境" class="headerlink" title="环境"></a>环境</h1><h2 id="硬件设备"><a href="#硬件设备" class="headerlink" title="硬件设备"></a>硬件设备</h2><ul><li>CPU：2个Intel(R) Xeon(R) Gold 5115 CPU \@ 2.40GHz，10核心20线程。</li><li>GPU：4路Tesla P40，每路显存容量22GB，共88GB。</li><li>内存：128GB。</li><li>外存：520TB可用，已用15TB。</li></ul><h2 id="软件环境与版本"><a href="#软件环境与版本" class="headerlink" title="软件环境与版本"></a>软件环境与版本</h2><ul><li>运行程序的系统为 CentOS Linux release 7.3.1611。</li><li>开发程序的系统为 Arch 5.9.6。</li><li>python：3.8.2：作为开发语言。</li><li>pytorch：1.6.0：作为模型实现的工具，借助其提供的API实现并行。</li><li>ssh：OpenSSH_8.3p1, OpenSSL 1.1.1h，实现远程登录功能，即本地登录到服务器。</li><li>scp：实现文件传输功能，将本地文件传输至服务器，或将服务器文件传输至本地。</li></ul><h1 id="并行加载数据"><a href="#并行加载数据" class="headerlink" title="并行加载数据"></a>并行加载数据</h1><p>在加载数据时，可以使用多进程来加载数据。不使用多线程的目的是：python 的多线程因为 GIL（Global Interpreter Lock）的存在，只能在单核运行。不能发挥多核处理器的优势，因此只适合 I/O 型任务，不适合密集计算型任务。</p><p>首先以 torch.utils.data.Dataset 为父类，创建一个加载数据集的子类：MiniImagenet。在 MiniImagenet中，设置__getitem__方法来支持数据集的切片访问，即批量加载数据。</p><p>在训练阶段，实例化加载数据集的类，得到一个实例对象。在 torch.utils.data.DataLoader 中传入实例对象来加载数据，并设置其他参数来提升加载数据的速度和数量。设置 num_workers=8 可以使用 8 进程来加载数据，设置 batch_size=batchsz 对应 MiniImagenet 类的 __getitem__ 切片访问，即一次获取的训练数据的大小。</p><p>在创建 num_workers 个进程之后，多个进程共享一份数据集，将指定 batch（一批待加载的目标数据）分配给指定 worker（一个进程），worker 将对应 batch 的数据加载进内存。因此增大 num_workers 的数量，内存的占用率也会增加。在第一个 batch 的数据加载完成后，会等待主进程将该 batch 取走并汇总，而后此 worker 开始加载下一个 batch，不断迭代。</p><p>主进程采集完最后一个 worker 的数据后，此时需要返回并采集第一个 worker 加载的第二个 batch。如果第一个 worker 此时没有加载完，主线程将阻塞等待当前 worker 的数据加载完毕。最后所有数据加载完毕后，汇总到一起供用户使用。</p><p>当 num_worker 设置的很大时，优点时可以快速的寻找目标 batch；缺点是内存开销大，也加重了 CPU 维护进程的开销，如创建、初始化、通信、分配任务、接受任务反馈和销毁进程等。num_workers 的经验设置值是服务器的 CPU 核心数，如果 CPU 性能强大且内存充足，可以设置为较大的数值。</p><h1 id="DataParallel"><a href="#DataParallel" class="headerlink" title="DataParallel"></a>DataParallel</h1><p>DataParallel 并行方式实现较为简单。首先将模型放到主 GPU 中，一般是 GPU-0；而后将模型复制得到副本，将模型副本放到另外的$n$张 GPU 中，将输入的batchsz大小的数据平均分为$n$份依次作为每个模型副本的输入。因此要求 batchsz 的大小要大于 GPU 的数量，否则数据无法分配，会导致有的模型副本得不到数据。</p><p>每个模型副本独立的进行前向计算，得到各自的 loss值。在$n$个GPU完成计算后，将 loss 汇总到 GPU-0 中，由 GPU-0 中的模型进行反向传播和更新参数。更新好参数后，由 GPU-0 将参数分发至其他$n$个 GPU 卡，开始新一轮的计算。DataParallel 并行结构示意图如图所示：</p><p><img data-src="https://s3.ax1x.com/2021/02/23/yLsmwD.png" alt></p><p>DataParallel基于单进程多线程实现，因为python的多线程无法利用多核处理器，只能在单核上进行任务调度和参数更新的通信，所以此种方式并行效率较低，且0号卡负担较大。</p><h1 id="DistributedDataParallel"><a href="#DistributedDataParallel" class="headerlink" title="DistributedDataParallel"></a>DistributedDataParallel</h1><p>DistributedDataParallel 并行方式实现较为复杂，且相对于 DataParallel 需要更大的显存空间。DistributedDataParallel 使用的进程数量一般和GPU数量相等，对每个进程创建一个 DistributedDataParallel 实例，通过进程通信来同步梯度，通信方式为 AllReduce 。</p><p>在任务初始化阶段，即并行程序执行之前首先要创建通信群组，通信群组中的每一个进程创建一个 DistributedDataParallel 实例。DistributedDataParallel 通过rank0（进程ID）将当前模型的结构和参数等状态通过广播通信的形式发送到进程组的所有进程中，确保其他进程中的模型副本和当前模型保持一致。而后，每个进程创建一个本地的Reducer，用于反向传播阶段同步参数的梯度。因此相比于DataParallel， DistributedDataParallel 需要更大的显存容量。为了提升通信效率，Reducer将参数放到多个桶中，每次以桶为单位进行通信。</p><p>模型参数需要额外附加 unready 和 ready 两种状态，默认为 unready 状态。若参数在反向传播阶段计算了梯度，则参数由 unready 状态变为 ready 状态，表示这个参数可以同步到其他模型；否则保持 unready 状态。当模型完成一次训练后，参数由 ready 状态变为 unready 状态，为下一次训练做准备。</p><p>因为在反向传播阶段，DistributedDataParallel 只会等待 unready 状态的参数更新，所以在前向计算阶段首先要分析计算图，将不进行梯度更新的参数状态永久设为 ready 状态，防止这些参数影响反向传播。DistributedDataParallel 在获取输入数据后，将输入数据分发到每个 GPU 中，模型副本开始前向计算得到各自的 loss 值，而后开始反向传播。</p><p>DistributedDataParallel 在初始化阶段会为每一个可求导的参数申请一个自动求导的钩子，来发射同步梯度的信号。在反向传播阶段，当一个参数计算梯度后会为 ready 状态，对应梯度的钩子就会发射信号，DistributedDataParallel 会标记该参数的状态，表示该参数的梯度可以用于同步。当一个桶内的参数全部由 unready 状态变为 ready 状态后，Reducer通过AllReduce的通信方式来更新桶内的参数，来计算所有模型副本中参数梯度的平均值。</p><p>以4个GPU为例，其 AllReduce 通信方式如下图所示。</p><p><img data-src="https://s3.ax1x.com/2021/02/23/yLsc0U.png" alt></p><p>在上图中，rank0 将自己模型副本的梯度 D0 发送给 rank1，rank2和rank3，其他进程同理。因此在 AllReduce 通信后，所有的模型副本的梯度值保持一致。</p><p>当所有的桶更新完毕后，Reducer 会阻塞等待 AllReduce 的所有操作结束，如重新将参数设为 unready 状态。因为所有模型副本的梯度值相同，所以在梯度更新阶段，只需更新本卡中的模型参数。因为所有的模型副本来自相同的初始状态，且具有相同的平均梯度值，所以在梯度更新后所的模型副本状态都会保持一致，以此来实现并行。</p><h1 id="对比"><a href="#对比" class="headerlink" title="对比"></a>对比</h1><p>DataParallel 和 DistributedDataParallel 都属于数据并行的方法，在分析完各自的实现原理后，可以得到二者的对比。对比情况如下表所示：</p><div class="table-container"><table><thead><tr><th>参数</th><th>DataParallel</th><th>DistributedParallel</th></tr></thead><tbody><tr><td>机器支持</td><td>只支持单机</td><td>可支持多机</td></tr><tr><td>模型支持</td><td>不支持拆分模型</td><td>可以将模型放到多张GPU中</td></tr><tr><td>通信方式</td><td>scatter input 和 gather output</td><td>scatter input 和 AllReduce</td></tr><tr><td>并行支持</td><td>只支持数据并行</td><td>支持数据并行和模型并行</td></tr><tr><td>进程与线程</td><td>单进程、多线程</td><td>多进程</td></tr></tbody></table></div><p>这里需要注意的是，如果将batchsz太小，由此可见，AllReduce在数据量很小的情况下，每一次梯度更新带来的进程通信、任务调度与分配的开销占比会增大，而用于数据通信的开销占比却很小，导致并行效率没有明显提升。因此AllReduce不适合数据量较小情况下的通信。这里我尝试过。</p><h1 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h1><p><a href="https://github.com/muyuuuu/Algorithm/tree/master/meta-learning/Metric-based/Relation-Netowrk" target="_blank" rel="noopener">https://github.com/muyuuuu/Algorithm/tree/master/meta-learning/Metric-based/Relation-Netowrk</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本文以 pytorch 为框架，分别从数据并行训练的方法(DataParallel, DistributedDataParallel)、并行加载数据两个角度，阐述了本次实验中并行算法的设计思想，并对并行实现的原理进行了解析，包括多进程的工作流程和通信方法等。&lt;del&gt;语言这么正规的原因是：这是从大作业里面摘出来的。&lt;/del&gt;神经网络那部分我就不写了，跑题，最后直接给代码。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="DeepLearning" scheme="https://muyuuuu.github.io/tags/DeepLearning/"/>
    
  </entry>
  
</feed>
