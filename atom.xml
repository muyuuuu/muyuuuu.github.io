<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Just for Life.</title>
  
  <subtitle>明月更几时</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://muyuuuu.github.io/"/>
  <updated>2020-05-12T15:45:33.828Z</updated>
  <id>https://muyuuuu.github.io/</id>
  
  <author>
    <name>兰铃</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>倒车雷达(四)：PyQt5的多线程</title>
    <link href="https://muyuuuu.github.io/2020/05/12/reverse-radar-multi-thread/"/>
    <id>https://muyuuuu.github.io/2020/05/12/reverse-radar-multi-thread/</id>
    <published>2020-05-12T15:00:42.000Z</published>
    <updated>2020-05-12T15:45:33.828Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>这可能也是我毕业设计的最终章了，由PyQt5创建两个线程，数据读取的线程负责读取Arduino发送的距离信息，主线程负责倒车雷达动画的实时显示。还是要认真一点，也算是给大学毕业一个交代。<del>然而还不开学</del></p><a id="more"></a><h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>根据前几章内容，我们已经实现了倒车雷达的：硬件搭建与编程、PC端上位机的动画制作两部分。结合前两篇文章的基础，在PC端的动画部分添加<strong>指令发送</strong>的界面，将Arduino硬件层的测量距离返回PC端上位机，并用距离和发送的指令来调节动画的移动，就大功告成了。</p><h1 id="多线程"><a href="#多线程" class="headerlink" title="多线程"></a>多线程</h1><p>举一个最通俗的例子，我们想实现以下的功能：在一个界面中，有一个按钮和一个计数器，点击按钮后，计数器每秒加一。就是这么简单的功能，却必须要使用多线程来实现。因为PyQt5运行后，界面所处的线程是显示线程，显示线程不断循环带保证界面的正常显示，但点击按钮后显示线程进入了每秒加一的循环且没有退出条件，所以显示线程会因长时间没响应而导致界面的卡死。</p><p>因此，必须要新建一个线程，即现在有两个线程。使得显示线程来显示界面，计算线程进入每秒加一的循环，互不干扰，互不卡死。至于为何不用进程，<a href="https://muyuuuu.github.io/2020/03/16/multi-process-thread/">是因为多进程之间不共享变量，不便操作</a>。</p><p>而倒车雷达也是如上的道理，需要两个线程，第一个显示线程来维护界面的显示，第二个串口线程来不断的读取距离数据和发送控制指令。因此，创建和串口进行交互的类（基于pyserial这个库）。因为需要将读取的距离信息返回主程序，所以在这个类内将读取的距离数据通过<strong>pyqt信号</strong>的形式发送。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 读取串口数据的类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Com</span><span class="params">(QThread)</span>:</span></span><br><span class="line">    <span class="comment"># 信号的返回</span></span><br><span class="line">    my_signal = pyqtSignal(str)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        super(Com, self).__init__()</span><br><span class="line">        self.data = <span class="string">""</span></span><br><span class="line">        self.is_on = <span class="literal">True</span> </span><br><span class="line">        <span class="comment"># 创建连接实例</span></span><br><span class="line">        self.s = serial.Serial(port=<span class="string">'COM6'</span>,</span><br><span class="line">                                baudrate=<span class="number">9600</span>,</span><br><span class="line">                                stopbits=serial.STOPBITS_ONE)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 接收数据</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">recv</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            self.sleep(<span class="number">1</span>)</span><br><span class="line">            data = self.s.readline()</span><br><span class="line">            <span class="keyword">if</span> data == <span class="string">''</span>:</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line">        <span class="keyword">return</span> data</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 是否打开成功</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">while</span> self.is_on:</span><br><span class="line">            <span class="keyword">if</span> self.s.isOpen():</span><br><span class="line">                print(<span class="string">"open success"</span>)</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                print(<span class="string">"open failed"</span>)</span><br><span class="line"></span><br><span class="line">            <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">                self.data = self.recv()</span><br><span class="line">                <span class="comment"># 将读取的数据返回</span></span><br><span class="line">                self.my_signal.emit(str(self.data))    </span><br><span class="line"></span><br><span class="line">    <span class="comment"># 发送各种指令</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">stop</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.s.write(<span class="string">b"2"</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.s.write(<span class="string">b"0"</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">back</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.s.write(<span class="string">b"1"</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">left</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.s.write(<span class="string">b"3"</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">right</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.s.write(<span class="string">b"4"</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">begin</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.s.write(<span class="string">b"5"</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">lift</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.s.write(<span class="string">b"6"</span>)</span><br></pre></td></tr></table></figure><p>在主程序中实例化这个线程的类，并通过start方法来启动。因为这个类会不断的将距离信息作为信号通过emit方法进行反馈。因此我们需要将这个信号连接至一个函数，那么这个函数就可以读取返回的距离信息。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">self.com = Com()</span><br><span class="line">self.com.my_signal.connect(self.run)</span><br><span class="line"><span class="comment"># 启动线程</span></span><br><span class="line">self.com.start()</span><br><span class="line"></span><br><span class="line"><span class="comment"># distance 就是 my_signal emit 返回的距离信息</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self, distance)</span>:</span></span><br><span class="line">    self.dis = distance</span><br></pre></td></tr></table></figure><h1 id="指令发送"><a href="#指令发送" class="headerlink" title="指令发送"></a>指令发送</h1><p>指令的发送主要负责控制车辆的前进、后退等移动。其实嘛，指令的发送很简单，我们只需要做几个按钮，点击按钮发送对应的指令就好了。在上一章节已经实现了接收Arduino发来的数据的功能，且预留了形如<code>def lift()</code>等发送指令的接口，此时仅需要将发送指令的按钮和那些接口相对应即可，代码如下：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">btn_start = QPushButton(<span class="string">"倒车"</span>)</span><br><span class="line">btn_start.setFixedSize(<span class="number">100</span>, <span class="number">80</span>)</span><br><span class="line"></span><br><span class="line">btn_forward = QPushButton(<span class="string">"前进"</span>)</span><br><span class="line">btn_forward.setFixedSize(<span class="number">100</span>, <span class="number">80</span>)</span><br><span class="line"></span><br><span class="line">btn_left = QPushButton(<span class="string">"左转"</span>)</span><br><span class="line">btn_left.setFixedSize(<span class="number">100</span>, <span class="number">80</span>)</span><br><span class="line"></span><br><span class="line">btn_stop = QPushButton(<span class="string">"停车"</span>)</span><br><span class="line">btn_stop.setFixedSize(<span class="number">100</span>, <span class="number">80</span>)</span><br><span class="line"></span><br><span class="line">btn_right = QPushButton(<span class="string">"右转"</span>)</span><br><span class="line">btn_right.setFixedSize(<span class="number">100</span>, <span class="number">80</span>)</span><br><span class="line"></span><br><span class="line">exit_btn = QPushButton(<span class="string">"退出"</span>)</span><br><span class="line">exit_btn.setFixedSize(<span class="number">100</span>, <span class="number">80</span>)</span><br><span class="line"></span><br><span class="line">btn_back = QPushButton(<span class="string">"后退"</span>)</span><br><span class="line">btn_back.setFixedSize(<span class="number">100</span>, <span class="number">80</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 将按钮与发送数据相关联</span></span><br><span class="line">btn_start.clicked.connect(self.run)</span><br><span class="line">exit_btn.clicked.connect(self.lift)</span><br><span class="line">btn_stop.clicked.connect(self.stop)</span><br><span class="line">btn_left.clicked.connect(self.left)</span><br><span class="line">btn_right.clicked.connect(self.right)</span><br><span class="line">btn_forward.clicked.connect(self.forward)</span><br><span class="line">btn_back.clicked.connect(self.back)</span><br><span class="line"></span><br><span class="line">self.com = Com()</span><br><span class="line">self.com.my_signal.connect(self.run)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">lift</span><span class="params">(self)</span>:</span></span><br><span class="line">    self.com.lift()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">back</span><span class="params">(self)</span>:</span></span><br><span class="line">    self.com.back()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">left</span><span class="params">(self)</span>:</span></span><br><span class="line">    self.com.left()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">right</span><span class="params">(self)</span>:</span></span><br><span class="line">    self.com.right()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">stop</span><span class="params">(self)</span>:</span></span><br><span class="line">    self.com.stop()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">forward</span><span class="params">(self)</span>:</span></span><br><span class="line">    self.com.forward()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self, distance)</span>:</span></span><br><span class="line">    self.com.start()</span><br><span class="line">    self.com.begin()</span><br></pre></td></tr></table></figure><h1 id="实时影像"><a href="#实时影像" class="headerlink" title="实时影像"></a>实时影像</h1><p>其实，将以上两部分的内容整合起来并与前几章的内容(如车身的旋转)在结合一下，就是全部的倒车雷达的代码了。</p><p>完整代码如下：</p><p><a href="https://github.com/muyuuuu/Reversing-radar-on-Arduino/blob/master/code/Reverse-radar/view.py" target="_blank" rel="noopener">https://github.com/muyuuuu/Reversing-radar-on-Arduino/blob/master/code/Reverse-radar/view.py</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;这可能也是我毕业设计的最终章了，由PyQt5创建两个线程，数据读取的线程负责读取Arduino发送的距离信息，主线程负责倒车雷达动画的实时显示。还是要认真一点，也算是给大学毕业一个交代。&lt;del&gt;然而还不开学&lt;/del&gt;&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="GUI" scheme="https://muyuuuu.github.io/tags/GUI/"/>
    
      <category term="microcomputer" scheme="https://muyuuuu.github.io/tags/microcomputer/"/>
    
  </entry>
  
  <entry>
    <title>图解反向传播与梯度下降</title>
    <link href="https://muyuuuu.github.io/2020/05/09/back-propagation-and-gradient-descent/"/>
    <id>https://muyuuuu.github.io/2020/05/09/back-propagation-and-gradient-descent/</id>
    <published>2020-05-09T15:31:33.000Z</published>
    <updated>2020-05-10T09:29:22.269Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>在之前的学习深度学习的过程中，以MLP为例，仅仅是知道了梯度下降和反向传播能很好的降低预测误差(本文暂且不谈论梯度消失和爆炸的问题)，使得训练出来的模型能够对未知的数据进行预测或者分类，以此来达到智能的目的。</p><p>时至今日，意识到不求甚解释不好的行为，于是下决心搞懂梯度下降和反向传播的原理。结果上网一搜，呵，大部分tm的是各处偷来的截图，截图里还有三四个水印，最后胡乱的堆一些数学公式，讲解的并不够彻底，也不通俗易懂。于是本文决定图解反向传播和梯度下降，并配备必要的数学计算，使读者一目了然。</p><a id="more"></a><h1 id="基础知识"><a href="#基础知识" class="headerlink" title="基础知识"></a>基础知识</h1><p>求偏导的概念得会一下(懂概念即可，不用自己求)，激活函数如何工作得会，网络的连接得会(搞明白参数在哪里工作就好了)。</p><h1 id="梯度下降"><a href="#梯度下降" class="headerlink" title="梯度下降"></a>梯度下降</h1><p>我们先设一个简单的函数$y=3x^2+2x+1$。假设此时$x=10$，那么$y=321$，我们想优化这个函数，即利用梯度下降的方式来求得$y$的最小值和对应的$x$的值。<del>为什么不求导直接算$y$的最小值？因为现实中的函数维度极度复杂，在复杂的函数空间内可能求不出来极小值点，这里只是用简单函数举例。</del>可以求出目标函数的导函数为：$y’=6x+2$。万事具备，再来回顾下导数的意义<del>非严谨数学定义，不要较真</del>：</p><p><img data-src="/2020/05/09/back-propagation-and-gradient-descent/2.png" alt></p><p>如上图所示，无限放大曲线的一小部分，在$y$轴方向截取一段$dy$，向$x$轴方向截取一段$dx$，则$f’(x)=dy/dx$即为这一小段曲线的斜率，也为导数值。当然，具体到实际问题，并不是取一段曲线而是以一个具体的点计算，这里只是距离。如$f(x)=2x^2$在$x=2$处的导数值为8。</p><p>梯度的本意是一个向量（矢量），即函数在该点处沿着该方向（此梯度的方向）变化最快，变化率最大，显而易见，对一元函数而言就是求导(多元函数求偏导并含有方向)。继续推导，可以得到这样的式子：$f’(x)dx=dy$。在详细一点，当前$x=10$，我们取一个固定的$dx=0.2$，沿着导数的方向，让$x$逐渐的变化，此时的$y$也逐渐变小，是不是就能逐步得到目标函数的最小值呢？数学语言如下：</p><ol><li>初始化$x$</li><li>开始迭代：<ul><li>求$f(x)$的导数$f’(x)$</li><li>$x_{i+1} = x_i - dx * f’(x)$</li><li>$i ++$</li></ul></li></ol><p>即迭代示意图如下：</p><p><img data-src="/2020/05/09/back-propagation-and-gradient-descent/3.png" alt></p><ul><li>$x_1-f’_1dx=x_2$，$f’_1$为$x_1$处的导数值</li><li>$x_2-f’_2dx=x_3$，$f’_2$为$x_2$处的导数值</li></ul><p>当然，$dx$也可以换成$\alpha$表示，含义为学习率，即每次行走的步幅。直到行走到最低点结束，代码如下：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">dx = <span class="number">0.2</span></span><br><span class="line">n_eproch = <span class="number">1000</span></span><br><span class="line">x = <span class="number">-10</span> </span><br><span class="line">ls = []</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> _ <span class="keyword">in</span> range(n_eproch):  </span><br><span class="line">    y = <span class="number">3</span> * x ** <span class="number">2</span> + <span class="number">2</span> * x + <span class="number">1</span></span><br><span class="line">    ls.append(y)</span><br><span class="line">    x = x - (<span class="number">6</span> * x + <span class="number">2</span>) * dx</span><br><span class="line"></span><br><span class="line">print(ls[<span class="number">-1</span>])</span><br><span class="line"><span class="comment"># 0.67</span></span><br></pre></td></tr></table></figure><p>可见在迭代1000次后，$y$的最终取值为0.67，和函数实际的最小值不谋而合<del>利用中学知识求一下即可</del>，这就是梯度下降的力量。当然，对于复杂的多元函数求偏导，或者求导的链式法则，还是借助<code>Tensorflow</code>这种专业工具更加省事。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line"></span><br><span class="line">X = tf.constant([<span class="number">0.</span>, <span class="number">0.25</span>, <span class="number">0.5</span>, <span class="number">0.74</span>, <span class="number">1.</span>])</span><br><span class="line">y = tf.constant([<span class="number">0.</span>, <span class="number">0.36</span>, <span class="number">0.54</span>, <span class="number">0.82</span>, <span class="number">1.</span>])</span><br><span class="line"></span><br><span class="line">a = tf.Variable(initial_value=<span class="number">0.</span>)</span><br><span class="line">b = tf.Variable(initial_value=<span class="number">0.</span>)</span><br><span class="line">variables = [a, b]</span><br><span class="line"></span><br><span class="line">num_epoch = <span class="number">100</span></span><br><span class="line">optimizer = tf.keras.optimizers.SGD(learning_rate=<span class="number">5e-4</span>)</span><br><span class="line"><span class="keyword">for</span> e <span class="keyword">in</span> range(num_epoch):</span><br><span class="line">    <span class="comment"># 使用tf.GradientTape()记录损失函数的梯度信息</span></span><br><span class="line">    <span class="keyword">with</span> tf.GradientTape() <span class="keyword">as</span> tape:</span><br><span class="line">        y_pred = a * X + b</span><br><span class="line">        loss = tf.reduce_sum(tf.square(y_pred - y))</span><br><span class="line">    <span class="comment"># TensorFlow自动计算损失函数关于自变量（模型参数）的梯度</span></span><br><span class="line">    grads = tape.gradient(loss, variables)</span><br><span class="line">    <span class="comment"># TensorFlow自动根据梯度更新参数</span></span><br><span class="line">    optimizer.apply_gradients(grads_and_vars=zip(grads, variables))</span><br><span class="line"></span><br><span class="line">print(a, b)</span><br></pre></td></tr></table></figure><h1 id="反向传播"><a href="#反向传播" class="headerlink" title="反向传播"></a>反向传播</h1><p>反向传播是一种以梯度下降为基础，对误差进行反馈，并调节相关参数的方法，因网络文章普遍都是公式较为晦涩，因此这里以数学计算为主，给出反向传播的实例，供读者理解。(因此期待看数学公式的可以移步了)。以MLP为例，首先构建结构如下图所示的网络：</p><p><img data-src="/2020/05/09/back-propagation-and-gradient-descent/4.png" alt></p><p>然后完成参数的初始化，且激活函数均采用Sigmoid($f(x)=\frac{1}{1 + e ^{-x}}$)</p><p>$x=2,w_{11}=1,w_{12}=-1,b_1=3,w_{21}=2,w{22}=1,b_2=0,y_{true}=1,\alpha=0.2$</p><h2 id="前向计算"><a href="#前向计算" class="headerlink" title="前向计算"></a>前向计算</h2><ol><li>隐藏层第一个神经元：$x\times w_{11} + b_1 = 5$，激活后的输出为0.99</li><li>隐藏层第二个神经元：$x\times w_{12} + b_1 = 1$，激活后的输出为0.73</li><li>输出层的预测输出为：$y_{temp}=0.99\times w_{21} + 0.73\times w_{22} + b_2=2.71$，激活后的输出为0.93</li><li>计算Loss：$E=\frac{1}{2}(y_{true}-y_{pred})^2$ (选用这类型是是因为求导方便，绝对值函数不一定可导)</li></ol><h2 id="反向传播-1"><a href="#反向传播-1" class="headerlink" title="反向传播"></a>反向传播</h2><ol><li>补充一下对$\text{Sigmoid}$函数求导的结果为：$\frac{1}{1+e^{-x}}’=\frac{e^x}{e^{2x}+2e^x+1}$</li><li>$E$对输出层求导：$\frac{\partial E}{\partial y_{pred}}=y_{pred}-y_{true}=-0.07$</li><li>而$y_{pred}=\text{Sigmoid}(y_{temp})$，$\frac{\partial y_{pred}}{\partial y_{temp}}=\frac{e^{2.71}}{e^{5.42}+2e^{2.71}+1}=0.058$</li><li>而计算$y_{temp}$对$w_{21}$这个参数的偏导值：$\frac{y_{temp}}{w_{21}}=0.99$ (求偏导时无关变量视为常量)</li><li>通过链式法则求误差$E$对参数$w_{21}$的梯度：$\frac{\partial E}{\partial w_{21}}=\frac{\partial E}{\partial y_{pred}}\times \frac{\partial y_{pred}}{\partial y_{temp}} \times \frac{\partial y_{temp}}{\partial w_{21}}=-0.004$</li><li>$w_{21}$沿着梯度下降即可：$w_{21}=w_{21} + \alpha \times -0.004=2+0.2\times -0.004=1.9992$，这样，参数$w_{21}$就得到了更新。(同上文的梯度下降方法)</li></ol><p>以此类推，通过不断的迭代，参数会逐渐修改，最终的结果是模型能有效的对输入进行预测或者分类。其他参数的更新同理。对于经常变成的小伙伴，会发现训练过程经常使用batch，此时最终的误差为这个bacth的总误差，需要对总误差求均值或求和等操作：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">loss = tf.keras.losses.sparse_categorical_crossentropy(y_true=y, y_pred=y_pred)</span><br><span class="line">loss = tf.reduce_mean(loss)</span><br></pre></td></tr></table></figure><h1 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h1><ol><li>总算搞明白了梯度下降和反向传播的工作原理。其实，这么手推一下也就对梯度爆炸和梯度消失有了大概的了解。</li><li>学习率$\alpha$不是一成不变的，可以根据学习状态进行更新，参考这里：<br><a href="https://muyuuuu.github.io/2019/02/20/gradient-function/">https://muyuuuu.github.io/2019/02/20/gradient-function/</a></li><li>与大部分网文不同的是，本文尽力用通俗的语言讲解清楚了这两个东西，同时也会成为我的学习笔记。所以我没有给图片打水印的打算，只是受到网页浏览的限制，并没有上传高清图。如果你感觉本文还不错，且想分享，可以随意拷贝走本文的图片，嫌清晰度低可以在评论区找我要原图。</li><li>如果你对本文的某些细节描述还是不懂，可以评论区见，我也尽力做一个用大白话讲明白<del>高深</del>原理的人。</li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在之前的学习深度学习的过程中，以MLP为例，仅仅是知道了梯度下降和反向传播能很好的降低预测误差(本文暂且不谈论梯度消失和爆炸的问题)，使得训练出来的模型能够对未知的数据进行预测或者分类，以此来达到智能的目的。&lt;/p&gt;
&lt;p&gt;时至今日，意识到不求甚解释不好的行为，于是下决心搞懂梯度下降和反向传播的原理。结果上网一搜，呵，大部分tm的是各处偷来的截图，截图里还有三四个水印，最后胡乱的堆一些数学公式，讲解的并不够彻底，也不通俗易懂。于是本文决定图解反向传播和梯度下降，并配备必要的数学计算，使读者一目了然。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="DeepLearning" scheme="https://muyuuuu.github.io/tags/DeepLearning/"/>
    
  </entry>
  
  <entry>
    <title>更通俗的理解熵</title>
    <link href="https://muyuuuu.github.io/2020/05/09/entropy/"/>
    <id>https://muyuuuu.github.io/2020/05/09/entropy/</id>
    <published>2020-05-08T23:11:45.000Z</published>
    <updated>2020-05-09T15:30:28.097Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>时至今日，早已忘记高中化学里面的熵代表什么，还好化学里的熵和我要研究的熵并非同一性质。在进坑DL中，遇到了许多和熵有关的概念，于是今天就彻底来扒一扒熵的有关概念和常用领域。包括：</p><ol><li>什么是熵</li><li>条件熵</li><li>KL散度</li><li>交叉熵</li></ol><p>注：本篇文章默认读者有一定的求期望、计算联合概率分布、计算条件概率分布和知道什么是概率密度的基础知识。</p><a id="more"></a><h1 id="什么是熵"><a href="#什么是熵" class="headerlink" title="什么是熵"></a>什么是熵</h1><p>首先，仅需要知道熵能够衡量信息的不确定性即可。一条信息的价值和他的不确定性有密切的关系，如：明天下雨的概率是1%和明天下雨的概率是99%这两条信息的价值是不一样的。不清楚事物的不确定性时，决策时出错的概率就越大；相反，通过自己逐步抽丝剥茧搞清了一个事物的本质，那么他的不确定性就为0，决策就不可能出错。那么如何衡量事物的不确定性呢？答案就是熵。</p><p>假设此时有32个盒子，31个盒子里是白球，1个盒子里是黑球。假设让人<strong>用二分法</strong>来猜测黑球在哪个盒子里，猜一次1块钱，只需要5次就能猜中$(\log_232=5)$，那么这个信息值5块钱。当然信息的不确定性不能用钱来衡量，香农给出的定义是：<br>\begin{equation}<br>H = -(p_1\log p_1 + p_2\log p_2 + \cdots+p_n\log p_n), n=32<br>\end{equation}</p><p>$p_i$为黑球在第$i$个盒子里的概率，$H$即为信息熵，且此时的熵恰好为5。在通俗一点，对于一个随机变量$X$，它的熵定义如下：</p><p>\begin{equation}<br>H = \sum_{x\in X}P(x)\log P(x)<br>\end{equation}</p><p>在通俗一点，对于序列<code>0, 0, 0, 1, 1, 1</code>，它的熵为2，单位是bit，因为序列里不是1就是0，2，即需要几个bit能够表示序列的全部的信息。所以也可以得到<strong>变量的不确定性越大，熵也就越大</strong>这样的结论。</p><h1 id="条件熵"><a href="#条件熵" class="headerlink" title="条件熵"></a>条件熵</h1><p>信息的作用在于消除不确定性，根据相关的信息来减少另外信息的不确定性。如：A想明天邀请B来做客，但不知道B有没有时间，此时的信息由不确定性因素，但A此时得到了一条消息：B出门旅游了，那么A就不必在明天邀请B反而可以去做一些别的事情。</p><p>这就是利用其他信息来减少不确定性，知道的相关信息越多，一件事的不确定性就越小。那么如何用数学语言描述呢？</p><p>假设两个随机变量$X, Y$，$X$是我们了解的，可以根据$X$的分布来求出$X$的熵：</p><p>\begin{equation}<br>H(X) = -\sum_{x\in X}P(x)\log P(x)<br>\end{equation}</p><p>而此时恰好知道$X,Y$的联合分布$p(x,y)$和条件概率分布$p(x|y)$，定义$X$在$Y$下的熵为：</p><p>\begin{equation}<br>H(X|Y) = -\sum_{x\in X, y \in Y}P(x, y)\log P(x|y)<br>\end{equation}</p><p>此时便可以得到互信息$I(X;Y)$:</p><p>\begin{equation}<br>H(X)-H(X|Y) = I(X;Y) = H(Y)-H(Y|X) = H(X)+H(Y)-H(X,Y)<br>\end{equation}</p><p>\begin{equation}<br>I(X;Y) = \sum_{x\in X, y\in Y}P(x,y)\log{\frac{P(x,y)}{P(x)P(y)}}<br>\end{equation}</p><p>互信息一定是大于等于0，当互信息为0时，表明$\frac{P(x,y)}{P(x)P(y)} = 1$，此时$P(x,y)=P(x)P(y)$，表明了随机变量$X,Y$相互独立。可见，只有两个信息相关时，才可以有效降低当前信息的不确定性。如明天是否下雨这个信息和小明今晚出去购物这两条信息是没有任何相关性的，他们的互信息为0。</p><p>借鉴这个思路，在2019年美国大学生数学建模竞赛的C题中，成功分析了主要是哪些人群在吸毒，合理提出建议。即吸毒人口的熵减去各种身份吸毒人口的熵，单亲家庭、残疾人员等，得到互信息，互信息越大，说明相关性越高，互信息越小则说明两者完全无关。</p><h1 id="KL散度"><a href="#KL散度" class="headerlink" title="KL散度"></a>KL散度</h1><p>KL 散度可以用来衡量两个分布的差异。在深度学习中，我们经常会将一个复杂的分布用一个简单的近似分布来代替。KL 散度可以帮助我们测量在选择一个近似分布时丢失的信息量。</p><p>对于离散型随机变量$X$，假设原概率分布为$P(x)$，近似概率分布为$Q(x)$，则使用 KL 散度衡量这两个分布的差异：</p><p>\begin{equation}<br>KL(P||Q)=\sum_{x\in X}P(x)\log (P(x)-Q(x))<br>\end{equation}</p><p>对于$P(x)-Q(x)$这一项，可以理解为如果我们使用 $Q$ 的编码来表示 $P$ 额外所需的编码长度。当然，也许你一眼就看出来了，这不就是在求期望嘛，改写一下看起来更直观：</p><p>\begin{equation}<br>KL(P||Q)=E_{x\sim P}[\log (P(x)-Q(x))]<br>\end{equation}</p><p>对于连续型变量，我们对概率密度函数求积分即可(假设随机变量$X$的取值范围是$[-\infty, +\infty]$)：</p><p>\begin{equation}<br>KL(P||Q)=\int_{-\infty}^{+\infty}p(x)\log{\frac{p(x)}{q(x)}}dx<br>\end{equation}</p><h1 id="交叉熵"><a href="#交叉熵" class="headerlink" title="交叉熵"></a>交叉熵</h1><p>交叉熵(也有人叫相对熵)也可以用来衡量两个分布的差异。以离散型变量 $x$ 为例，交叉熵$H(P,Q)$的定义如下：</p><p>\begin{equation}<br>H(P,Q)=-\sum_{x\in X} P(x)\log Q(x)<br>\end{equation}</p><p>当然也可以写成期望的形式：$-E_{x\sim P}\log Q(x)$，也许你善于观察，肯快发现了交叉熵与KL散的联系(公式推导较为简单，不在展示)：</p><p>\begin{equation}<br>H(P,Q) = H(P) + KL(P||Q)<br>\end{equation}</p><p>在机器学习评价两个分布之间的差异时，由于分布 $P$ 会是给定的（如测试样本的标签值），所以此时 KL 散度和交叉熵的作用其实是一样的，而且因为交叉熵少算一项，更加简单，所以选择交叉熵会更好。交叉熵也有以下的性质：</p><ol><li>对于两个完全相同的函数，它们的交叉熵等于零。</li><li>交叉熵越大，两个函数差异越大；反之，交叉熵越小，两个函数差异越小。</li></ol><h1 id="举例"><a href="#举例" class="headerlink" title="举例"></a>举例</h1><p>也许说了一大堆你还是不明白，那么在这里举例说明。</p><h2 id="信息熵"><a href="#信息熵" class="headerlink" title="信息熵"></a>信息熵</h2><p>还是那个例子，32个箱子，31个箱子里是白球，1个箱子里是黑球，你用二分法去摸球，直到摸到黑球位置，此时摸球次数的随机变量$X$为：</p><script type="math/tex; mode=display">H(X)=-32\times \frac{1}{32} \times \log_2(\frac{1}{32})=5</script><h2 id="条件熵-1"><a href="#条件熵-1" class="headerlink" title="条件熵"></a>条件熵</h2><p>根据以下的表格，对于第一列上班族，上班族的随机变量为$X$，且1表示上班，那么上班族的熵$H(X)=0.589$。对于第二列上班族的年龄，年龄在区间内$[0,20]$为0，年龄在区间$[20, 100]$内为1，那么年龄这列的熵$H(Y)$为0.589。互信息$I(X;Y)$(自行手动按照公式计算即可)为0.42，表明两者上班的人和上班的人的年龄之间有一定的相关性。</p><div class="table-container"><table><thead><tr><th>上班族$X$</th><th>上班族的年龄$Y$</th></tr></thead><tbody><tr><td>1</td><td>1</td></tr><tr><td>1</td><td>0</td></tr><tr><td>0</td><td>0</td></tr><tr><td>1</td><td>1</td></tr><tr><td>0</td><td>0</td></tr></tbody></table></div><h2 id="交叉熵-1"><a href="#交叉熵-1" class="headerlink" title="交叉熵"></a>交叉熵</h2><p>对于两个分布：$P(x)$为<code>0, 1, 0, 1, 0, 1</code>，$Q(x)$为<code>0, 1, 0, 1, 0, 0</code>，两者的交叉熵为</p><script type="math/tex; mode=display">H(P,Q)=-(P(X=1)\log Q(X=1) + P(X=0) \log Q(X=0))=\frac{1}{2}\log \frac{9}{2}</script><p>而$Q(x)$为<code>1, 2, 2, 1, 2, 0</code>时的交叉熵为$H(P,Q)=\frac{1}{2}\log 18$，自然是前者较小，表明了$Q(x)$的第一个分布和原始分布$P(x)$较为接近。KL的计算同理，不再过多展示。</p><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p>站在巨人的肩膀上。</p><ol><li>《数学之美(第二版)》第六章：信息的度量和作用</li><li>互信息公式推导：<a href="https://www.cnblogs.com/gatherstars/p/6004075.html" target="_blank" rel="noopener">https://www.cnblogs.com/gatherstars/p/6004075.html</a></li><li>KL散度与交叉熵：<a href="https://www.cnblogs.com/wuliytTaotao/p/9713038.html" target="_blank" rel="noopener">https://www.cnblogs.com/wuliytTaotao/p/9713038.html</a></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;时至今日，早已忘记高中化学里面的熵代表什么，还好化学里的熵和我要研究的熵并非同一性质。在进坑DL中，遇到了许多和熵有关的概念，于是今天就彻底来扒一扒熵的有关概念和常用领域。包括：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;什么是熵&lt;/li&gt;
&lt;li&gt;条件熵&lt;/li&gt;
&lt;li&gt;KL散度&lt;/li&gt;
&lt;li&gt;交叉熵&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;注：本篇文章默认读者有一定的求期望、计算联合概率分布、计算条件概率分布和知道什么是概率密度的基础知识。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="Algorithm" scheme="https://muyuuuu.github.io/tags/Algorithm/"/>
    
  </entry>
  
  <entry>
    <title>Github灵魂：pull-request</title>
    <link href="https://muyuuuu.github.io/2020/05/02/pull-request/"/>
    <id>https://muyuuuu.github.io/2020/05/02/pull-request/</id>
    <published>2020-05-02T08:13:38.000Z</published>
    <updated>2020-05-02T08:25:25.382Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p><a href="https://muyuuuu。github。io/2019/11/16/git-use/">承接上文</a>，人傻就要多学习系列，在记录完<code>git</code>的常用方法后，又学习了<code>github</code>的灵魂功能<code>pull request</code>，是团队开发和开源必不可少的工具。</p><p>同样，本文没有任何截图，都是文字阐述，更像是我的个人笔记。对纯小白不是很友好，若不喜欢这类形式可以选择性关闭。</p><a id="more"></a><h1 id="Pull-Request"><a href="#Pull-Request" class="headerlink" title="Pull Request"></a>Pull Request</h1><p><code>Pull Request</code>(以下简称<code>pr</code>)是自己修改源代码后，请求对方仓库采纳该修改方式时的一种行为，能更好的实现多人开发。</p><p>如发现了一个软件的<code>BUG</code>并修改后，在<code>github</code>上发送了<code>pr</code>，接收方的仓库会创建一个附带源代码的<code>issue</code>，在这个<code>issue</code>里详细记录内容。发送过去的<code>pr</code>是否被采纳，要由接收方的管理者进行判断。如果被接收，<code>pr</code>的发起者就会变成这个项目的<code>contributor</code>，也就变成了开源软件的贡献者。</p><h2 id="发送pr"><a href="#发送pr" class="headerlink" title="发送pr"></a>发送<code>pr</code></h2><h3 id="发送前的准备"><a href="#发送前的准备" class="headerlink" title="发送前的准备"></a>发送前的准备</h3><ul><li><code>fork</code>:在对方的仓库中点击<code>fork</code>按钮，然后会发现自己的仓库中也有了对应的仓库。(当然也可以不用进行<code>fork</code>，假设团队的成员对仓库都有编辑的权限时，可以免去<code>fork</code>这一步骤)</li><li><code>clone</code>:复制自己<code>fork</code>仓库的<code>URL</code>，建议以<code>SSH</code>协议将仓库克隆到本地仓库中。此时修改仓库内容并<code>push</code>，只会修改自己的仓库，不会修改他人的仓库，这也是<code>github</code>的一大优势。</li><li><code>branch</code>:创建特性分支然后在分支中修改代码，这是个好习惯。因为在给对方仓库<code>pr</code>时都是发送特性分支，这样对方仓库就能根据分支来更好的理解代码的意图。如修改错别字分支，那么就<code>git branch typo</code>。</li><li><code>git branch -a</code>查看分支，开头带有<code>remotes/origin/</code>的是<code>github</code>端仓库的分支。</li><li>在修改完代码后，提交修改。<code>git add filename</code> 还有<code>git commit -m &quot;comment&quot;</code>。</li><li>要从<code>github</code> 发送<code>pr</code>，<code>github</code>仓库端必须有一个包含了修改代码后的分支，所以就创建本地<code>typo</code>分支的远程分支，<code>git push origin typo</code>即可。</li></ul><h3 id="发送期间"><a href="#发送期间" class="headerlink" title="发送期间"></a>发送期间</h3><p>登录<code>github</code>并切换到<code>typo</code>分支，确认发送的内容准确无误后，点击<code>create pull request</code>，然后在表单中填写本次<code>pr</code>的理由和改进即可。</p><p>再次确认无误后，点击<code>send pull request</code>，这样，<code>pr</code>的目标仓库就会新建<code>pr</code>和<code>issue</code>，至于对方仓库接收与否，还得看仓库原作者的想法了。</p><h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h3><p>在程序的开发过程中，<code>pr</code>是对软件实现和功能探讨的好方法。当然更建议的是不要写完代码在<code>pr</code>，而是可以尽早的<code>pr</code>获得反馈，统一大家的开发思路，提高代码质量。否则一声不吭的写完并<code>pr</code>后，会导致一个功能开发完毕后才能得到修正，甚至要大幅度改变代码，很浪费经历。</p><p>所以，只想要发起讨论的时候<code>pr</code>即可，不必等代码有所实现。即使对某个正在开发的功能提供简短的伪代码让大家有个印象，也能获得不少反馈。如被他人指正自己没注意到的小错误，或功能的修改意见，或已经有他人实现的代码等，能让自己避免浪费不必要的精力。这种边审查边开发的过程，比开发完在审查高效的多。</p><p>但需要注意的是，在发送的一个<code>pr</code>或者<code>issue</code>中，不要讨论无关的东西，当需要讨论另外的内容时，重新开一个<code>pr</code>即可。</p><h3 id="仓库的维护"><a href="#仓库的维护" class="headerlink" title="仓库的维护"></a>仓库的维护</h3><p>当然在<code>fork</code>和<code>clone</code>后想修改代码，但因某些不可避免的原因搁置了。而远程仓库一直在开发，则此时本地的仓库和远程的最新仓库难免有很多不同之处，所以要让本地的仓库保持最新的状态:</p><ul><li>首先给目标仓库设置一个名称，这里叫上游<code>upstream</code>，<code>git remote add upstream git://github.com/user/repo.git</code>作为远程仓库。</li><li>然后<code>git fetch upstream</code>就能从远程仓库获取最新的代码，将<code>upstream/master</code>分支与当前分支合并，以此来获取远程仓库的最新状态。</li></ul><h2 id="接收pr"><a href="#接收pr" class="headerlink" title="接收pr"></a>接收<code>pr</code></h2><p>接收到他人发送的<code>pr</code>后，点击即可查看详细内容，在检测完他人发送<code>pr</code>的代码准确无误后，点击<code>merge pull request</code>按钮就会将内容自动合并到仓库的主分支。</p><h3 id="接收前的准备"><a href="#接收前的准备" class="headerlink" title="接收前的准备"></a>接收前的准备</h3><ul><li>审查代码，对代码作出必要的注释和评论，点击代码左侧的<code>+</code>号即可。</li><li>将<code>pr</code><strong>接收方</strong>的仓库<code>clone</code>到本地(不要操作原有的仓库)，如果已经克隆，可以<code>git pull</code>获取最新状态。</li><li>然后获取<code>pr</code><strong>发送方</strong>的仓库，设置为本地仓库的远程仓库:<code>git remote add pr-sender git@github.com:pr-sender/repo.git</code>，并<code>git fetch</code>合并。</li><li>创建用于检测的分支<code>test</code>，将<code>fetch</code>完的分支和<code>test</code>分支合并，这样<code>test</code>分支就捕获了他人发送<code>pr</code>的分支。</li><li>检查代码无误后<code>git branch -D test</code>删除此分支即可。</li><li>确认无误后，进入浏览器，点击<code>merge pull request</code>即可，自动合并仓库，仓库的<code>pr</code>会自动从<code>open</code>变为<code>close</code>。</li></ul><h3 id="注意事项-1"><a href="#注意事项-1" class="headerlink" title="注意事项"></a>注意事项</h3><ul><li>无法运行的代码不要合并到仓库;</li><li>不要发布错误的不能运行的代码;</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://muyuuuu。github。io/2019/11/16/git-use/&quot;&gt;承接上文&lt;/a&gt;，人傻就要多学习系列，在记录完&lt;code&gt;git&lt;/code&gt;的常用方法后，又学习了&lt;code&gt;github&lt;/code&gt;的灵魂功能&lt;code&gt;pull request&lt;/code&gt;，是团队开发和开源必不可少的工具。&lt;/p&gt;
&lt;p&gt;同样，本文没有任何截图，都是文字阐述，更像是我的个人笔记。对纯小白不是很友好，若不喜欢这类形式可以选择性关闭。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="Computer" scheme="https://muyuuuu.github.io/tags/Computer/"/>
    
  </entry>
  
  <entry>
    <title>倒车雷达(三)：动画制作</title>
    <link href="https://muyuuuu.github.io/2020/04/27/reverse-radar-software-animation/"/>
    <id>https://muyuuuu.github.io/2020/04/27/reverse-radar-software-animation/</id>
    <published>2020-04-27T06:51:39.000Z</published>
    <updated>2020-04-27T08:03:47.515Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>在PC端绘制倒车的动画，达到实时显示车辆的倒车情况的目的。如车辆倒退时，动画中的车辆也倒退，车辆左转时，动画也实时变化。使用的工具为QT(python)，做了一个如上形式的可视化界面。</p><a id="more"></a><ul><li>选择的工具为：PyQt5，QGraphicsView，QGraphicsRectItem；</li><li>与以往的GUI设计不同，这次的GUI开发为场景和实体的运动，而不是控件的使用；前者更偏向游戏设计，场景移动和物体的碰撞检测，后者更像使用的软件，点击按钮输入密码等；</li></ul><h1 id="设置物体"><a href="#设置物体" class="headerlink" title="设置物体"></a>设置物体</h1><p>首先创建车辆的实体，这里以方块代表车辆：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">RectItem</span><span class="params">(QGraphicsRectItem)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, rect=QRectF<span class="params">()</span>)</span>:</span></span><br><span class="line">        super(RectItem, self).__init__(rect)</span><br></pre></td></tr></table></figure><p>而后创建场景，场景中包括车辆：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 负责绘制车辆的类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GraphicsView</span><span class="params">(QGraphicsView)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        super(GraphicsView, self).__init__()</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 创建图形容器</span></span><br><span class="line">        self.setFixedSize(<span class="number">460</span>, <span class="number">560</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 将车库实体化为 宽度450 长度600 大小</span></span><br><span class="line">        self.scene = QGraphicsScene()</span><br><span class="line">        self.scene.setSceneRect(<span class="number">0</span>, <span class="number">0</span>, <span class="number">450</span>, <span class="number">550</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 创建车辆实例</span></span><br><span class="line">        self.rect = RectItem()</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 初始化位置为 0 30 车辆宽度 250 长度 400</span></span><br><span class="line">        self.rect.setRect(<span class="number">0</span>, <span class="number">0</span>, <span class="number">240</span>, <span class="number">300</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 创建颜色刷子</span></span><br><span class="line">        brush1 = QBrush(Qt.SolidPattern)</span><br><span class="line">        brush1.setColor(QColor(<span class="number">124</span>, <span class="number">214</span>, <span class="number">175</span>))</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 给车身上色</span></span><br><span class="line">        self.rect.setBrush(brush1)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 将车身添加到容器中 即放到车库里</span></span><br><span class="line">        self.scene.addItem(self.rect)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 设置当前场景</span></span><br><span class="line">        self.setScene(self.scene)</span><br></pre></td></tr></table></figure><p>之后，再将场景添加到窗口中即可，伪代码如下：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">g = GraphicsView()</span><br><span class="line"><span class="comment"># g.setFixedSize(450, 550)</span></span><br><span class="line">self.car = g.rect</span><br><span class="line">self.scene = g.scene</span><br><span class="line"></span><br><span class="line"><span class="comment"># print(self.scene.x())</span></span><br><span class="line">layout.addWidget(g)</span><br></pre></td></tr></table></figure><p>因初始化位置为(0, 0)，所以车辆在左上角，效果如下所示。<a href="https://github.com/muyuuuu/Reversing-radar-on-Arduino/tree/master/code/Reverse-radar" target="_blank" rel="noopener">完整代码，car_test.py</a></p><p><img data-src="/2020/04/27/reverse-radar-software-animation/1.jpg" alt></p><h1 id="物体移动"><a href="#物体移动" class="headerlink" title="物体移动"></a>物体移动</h1><p>最初的想法是：设定一组位置，通过time.sleep()设置移动速度来达到缓慢移动的效果，但这并不是可行的思路，错误的伪代码如下：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">x, y = <span class="number">100</span>, <span class="number">100</span></span><br><span class="line">i, j = <span class="number">0</span>, <span class="number">0</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">move</span><span class="params">(self)</span>:</span></span><br><span class="line">    time.sleep(<span class="number">1</span>)</span><br><span class="line">    car.setpos(i, j)</span><br><span class="line">    <span class="keyword">if</span> i &lt; x <span class="keyword">and</span> j &lt; y:</span><br><span class="line">        i += <span class="number">10</span>, j += <span class="number">10</span></span><br></pre></td></tr></table></figure><p>但实际是，物体会直接移动到(100, 100)，并没有中间的过程。在Qt的开发中，一定不能使用time.sleep这种方法，因为它会阻塞事件的循环，导致窗口冻结，从而阻止了GUI的重新绘制。所以在Qt中，必须使用事件来执行操作，如：分为显示事件和工作事件，显示事件负责GUI的显示，工作事件负责刷新物体的位置。如：每次触发计时器的计时时，计算物体的位置，而后显示事件负责更新。</p><p>此时修改车辆实体的类，增加光滑移动的方法：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">RectItem</span><span class="params">(QGraphicsRectItem)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, rect=QRectF<span class="params">()</span>)</span>:</span></span><br><span class="line">        super(RectItem, self).__init__(rect)</span><br><span class="line">        self.setFlag(QGraphicsItem.ItemIsSelectable, <span class="literal">True</span>)</span><br><span class="line">        self.setFlag(QGraphicsItem.ItemSendsGeometryChanges, <span class="literal">True</span>)</span><br><span class="line">        self._pos_animation = QVariantAnimation()</span><br><span class="line">        self._pos_animation.valueChanged.connect(self.setPos)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">move_smooth</span><span class="params">(self, end, duration)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> self._pos_animation.state() == QAbstractAnimation.Running:</span><br><span class="line">            self._pos_animation.stop()</span><br><span class="line">        self._pos_animation.setDuration(duration)</span><br><span class="line">        self._pos_animation.setStartValue(self.pos())</span><br><span class="line">        self._pos_animation.setEndValue(end)</span><br><span class="line">        self._pos_animation.start()</span><br></pre></td></tr></table></figure><p>而后设置计时器及时触发即可：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">btn_start.clicked.connect(self.run)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">move_pos</span><span class="params">(self, scene)</span>:</span></span><br><span class="line">    Left = [<span class="number">40</span>]</span><br><span class="line">    Center = [<span class="number">40</span>]</span><br><span class="line">    <span class="keyword">for</span> it <span class="keyword">in</span> scene.items():</span><br><span class="line">        self.item = it</span><br><span class="line">        <span class="keyword">for</span> left, center <span class="keyword">in</span> zip(Left, Center):</span><br><span class="line">            pos = QPointF(left, center)</span><br><span class="line">            <span class="keyword">if</span> hasattr(it, <span class="string">'move_smooth'</span>):</span><br><span class="line">                it.move_smooth(pos, <span class="number">500</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self, distance)</span>:</span></span><br><span class="line">    <span class="comment"># 偏函数传入函数和参数</span></span><br><span class="line">    wrapper = partial(self.move_pos, self.scene)</span><br><span class="line">    timer = QTimer(interval=<span class="number">5000</span>, timeout=wrapper)</span><br><span class="line">    timer.start()</span><br><span class="line">    wrapper()</span><br></pre></td></tr></table></figure><p>实现效果如下，此时可以完美的时间光滑移动。<a href="https://github.com/muyuuuu/Reversing-radar-on-Arduino/blob/master/code/Reverse-radar/remove_test.py" target="_blank" rel="noopener">完整代码</a></p><p><img data-src="/2020/04/27/reverse-radar-software-animation/2.gif" alt></p><h1 id="物体旋转"><a href="#物体旋转" class="headerlink" title="物体旋转"></a>物体旋转</h1><p>我摸索了两种旋转方案，如下图所示：</p><p><img data-src="/2020/04/27/reverse-radar-software-animation/3.png" alt></p><ul><li>第一种旋转方案是以左上角的顶点为中心，逆时针旋转</li><li>第二种旋转方案是以矩形的中心为旋转点，逆时针旋转</li></ul><p>经仿真模拟，第二种方案在操作难度和展示效果两个方面均取得不错的效果。</p><p>那么，以此来设置旋转方案：</p><ul><li>设立向左旋转的角度self.left_angle和向右旋转的角度self.right_angle两个参数，均初始化为5度</li><li>向左旋转时，self.left_angle += 5, self.right_angle -= 5</li><li>同理，向右旋转时，self.left_angle -= 5, self.right_angle += 5</li><li>PyQt5默认的旋转方式是逆时针，所以向左旋转时，角度为(360 - self.left_angle)</li><li>scenePos()方法可以捕获当前物体左上角的坐标，因此坐标加上车身宽度的一半就是旋转点的横坐标，加上车身长度的一半就是旋转点的纵坐标</li><li>更严谨一点可以加上旋转角度来计算旋转点坐标的位置，因为实际的车库倒车中不会频繁的左右转动，但我选择了忽略这些误差。</li></ul><p>因此，不难得到旋转的代码：(旋转点的计算不够严谨)</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">self.angle_left = <span class="number">5</span></span><br><span class="line">self.angle_right = <span class="number">5</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 以车辆中心为旋转点</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">rorate_right</span><span class="params">(self)</span>:</span></span><br><span class="line">    pos = self.car.scenePos()</span><br><span class="line">    x = pos.x() + <span class="number">120</span></span><br><span class="line">    y = pos.y() + <span class="number">150</span></span><br><span class="line">    <span class="comment"># 设置旋转点</span></span><br><span class="line">    self.car.setTransformOriginPoint(QPointF(x, y))</span><br><span class="line">    self.car.setRotation(<span class="number">360</span> - self.angle_right)</span><br><span class="line">    self.angle_right += <span class="number">5</span></span><br><span class="line">    self.angle_left -= <span class="number">5</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">rorate_left</span><span class="params">(self)</span>:</span></span><br><span class="line">    pos = self.car.scenePos()</span><br><span class="line">    x = pos.x() + <span class="number">120</span></span><br><span class="line">    y = pos.y() + <span class="number">150</span></span><br><span class="line">    self.car.setTransformOriginPoint(QPointF(x, y))</span><br><span class="line">    self.car.setRotation(self.angle_left)</span><br><span class="line">    self.angle_left += <span class="number">5</span></span><br><span class="line">    self.angle_right -= <span class="number">5</span></span><br></pre></td></tr></table></figure><p>实现的效果如下。<a href="https://github.com/muyuuuu/Reversing-radar-on-Arduino/blob/master/code/Reverse-radar/rotation_test.py" target="_blank" rel="noopener">完整代码</a></p><p><img data-src="/2020/04/27/reverse-radar-software-animation/4.gif" alt></p><h1 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h1><p>而接下来的就是读取车辆倒车时与后方障碍物的距离，根据距离信息更新动画中车辆的位置。这涉及了多线程的东西，将在下文给出实现方案。</p><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p>站在巨人的肩膀上，我们能更好的前行：</p><ol><li><a href="https://stackoverflow.com/questions/55277098/move-qgraphicsitem-continuously-in-qgraphicsscene-and-check-collision" target="_blank" rel="noopener">https://stackoverflow.com/questions/55277098/move-qgraphicsitem-continuously-in-qgraphicsscene-and-check-collision</a></li><li><a href="https://stackoverflow.com/questions/11147443/rotate-qgraphicspixmapitem-with-mouse" target="_blank" rel="noopener">https://stackoverflow.com/questions/11147443/rotate-qgraphicspixmapitem-with-mouse</a></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在PC端绘制倒车的动画，达到实时显示车辆的倒车情况的目的。如车辆倒退时，动画中的车辆也倒退，车辆左转时，动画也实时变化。使用的工具为QT(python)，做了一个如上形式的可视化界面。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="GUI" scheme="https://muyuuuu.github.io/tags/GUI/"/>
    
      <category term="microcomputer" scheme="https://muyuuuu.github.io/tags/microcomputer/"/>
    
  </entry>
  
  <entry>
    <title>倒车雷达(二)：硬件搭建</title>
    <link href="https://muyuuuu.github.io/2020/04/23/reverse-radar-hardware/"/>
    <id>https://muyuuuu.github.io/2020/04/23/reverse-radar-hardware/</id>
    <published>2020-04-23T08:03:31.000Z</published>
    <updated>2020-04-23T11:22:32.029Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>硬件是软件运行的基础，因此有必要建立良好的硬件系统供软件的运行。因此，针对通信功能的蓝牙模块、制动功能的电机模块和测距功能的超声波模块三大模块，建立电路连接，提供电路连接图，并编写程序。</p><p>本文也提供部分代码，代码虽然都是核心代码<strong>但不能够独立运行</strong>。程序都附注释，有一丢丢开发基础都能看懂。希望看官能有所思考后取得所需代码，而不是无脑的复制粘贴。完整代码参考<a href="https://github.com/muyuuuu/Reversing-radar-on-Arduino/blob/master/code/Reverse-radar/Reverse-radar.ino" target="_blank" rel="noopener">这里</a>。且代码仅以我的接线方式为标准。</p><a id="more"></a><h1 id="通信功能"><a href="#通信功能" class="headerlink" title="通信功能"></a>通信功能</h1><p>通信功能选取了HC-05蓝牙模块，某购物平台25RMB左右。对于Arduino而言，还是能很轻易的做到Arduino与HC-05蓝牙模块的连接。连接的电路图如下所示：</p><p><img data-src="/2020/04/23/reverse-radar-hardware/1.jpg" alt></p><ul><li>5V连接VCC，GND相互连接构成回路</li><li>HC-05的发射端TXD连接Arduino的接收端RXD</li><li>HC-05的接收端RXD连接Arduino的发射端TXD</li><li>以此来构成通信的回路。</li></ul><p>但需要注意的是，在给Arduino烧录程序时，需要断开Arduino与HC-05的RXD和TXD连接。否则Arduni的TXD和RXD处于连接状态而非悬空状态时，程序无法烧录。(也可能是我用Vscode做IDE的原因)</p><h2 id="PC向Arduino发送数据"><a href="#PC向Arduino发送数据" class="headerlink" title="PC向Arduino发送数据"></a>PC向Arduino发送数据</h2><p>而在完成电路的连接后，需要编写程序来测试连接是否正确。首先Arduino处于接收端，电脑处于发送端。接通电路，电脑开启蓝牙并连接。对于接收端，当available()返回值大于0时，表明串口有数据发送到Arduino，此时调用read方法读取即可。</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setup</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 设置硬件串口通信的波特率</span></span><br><span class="line">    Serial.begin(<span class="number">9600</span>);</span><br><span class="line">    <span class="comment">// 先清空串口缓存</span></span><br><span class="line">    <span class="keyword">while</span>(Serial.available())</span><br><span class="line">        Serial.read();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">loop</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 读取串口数据 </span></span><br><span class="line">    <span class="keyword">if</span> (Serial.available() &gt; <span class="number">0</span>)</span><br><span class="line">    &#123; </span><br><span class="line">        delay(<span class="number">500</span>);</span><br><span class="line">        <span class="keyword">char</span> val;</span><br><span class="line">        <span class="comment">// 读取数据</span></span><br><span class="line">        val = Serial.read();</span><br><span class="line">        <span class="comment">// 进入倒车模式</span></span><br><span class="line">        <span class="keyword">if</span> (val == <span class="string">'5'</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            flag = <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (val == <span class="string">'6'</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            flag = <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>而在发送端，选择了使用python作为发射信号和读取信号的工具。借助pyserial串口，很容易实现这个功能。(pip install pyserial)</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> serial</span><br><span class="line"><span class="comment"># 读取串口数据的类</span></span><br><span class="line"></span><br><span class="line">s = serial.Serial(port=<span class="string">'COM6'</span>,</span><br><span class="line">                baudrate=<span class="number">9600</span>,</span><br><span class="line">                stopbits=serial.STOPBITS_ONE)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 发送数据</span></span><br><span class="line">s.write(<span class="string">b"5"</span>)</span><br><span class="line">s.write(<span class="string">b"6"</span>)</span><br></pre></td></tr></table></figure><p>至于为什么要用<code>b&quot;&quot;</code>这样的形式，因为serial这个库<strong>发送和读取</strong>的数据都是字节类型。蓝牙HC-05的使用是我经历的最难的一部分，主要的坑是：只有在程序连接蓝牙时才算连接，而仅仅在Win10蓝牙中连接是不够的。</p><h2 id="Arduino向PC发送数据"><a href="#Arduino向PC发送数据" class="headerlink" title="Arduino向PC发送数据"></a>Arduino向PC发送数据</h2><p>首先来看Arduino端的程序，一般是调用println方法完成发送，调用flush方法等待数据的发送完毕。</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setup</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 设置硬件串口通信的波特率</span></span><br><span class="line">    Serial.begin(<span class="number">9600</span>);</span><br><span class="line">    <span class="comment">// 先清空串口缓存</span></span><br><span class="line">    <span class="keyword">while</span>(Serial.available())</span><br><span class="line">        Serial.read();  </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">loop</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 设置发送数据</span></span><br><span class="line">    <span class="built_in">string</span> str = <span class="string">"asd"</span></span><br><span class="line">    <span class="comment">// 参数可以是任何类型 不一定都是字符串 </span></span><br><span class="line">    Serial.println(str);</span><br><span class="line">    <span class="comment">// 等待串口发送完毕</span></span><br><span class="line">    Serial.flush();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>而在python端完成读取数据的功能：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> serial</span><br><span class="line"><span class="comment"># 读取串口数据的类</span></span><br><span class="line"></span><br><span class="line">s = serial.Serial(port=<span class="string">'COM6'</span>,</span><br><span class="line">                baudrate=<span class="number">9600</span>,</span><br><span class="line">                stopbits=serial.STOPBITS_ONE)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">recv</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="comment"># 读取数据</span></span><br><span class="line">    data = s.readline()</span><br><span class="line">    <span class="keyword">if</span> data == <span class="string">''</span>:</span><br><span class="line">        <span class="keyword">continue</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line">    <span class="keyword">return</span> data</span><br></pre></td></tr></table></figure><p>同样读取的数据为字节类型，通常为这样的格式：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="string">b'1\r\n'</span></span><br><span class="line"><span class="string">b'1\r\n'</span></span><br><span class="line"><span class="string">b'1\r\n'</span></span><br><span class="line"><span class="string">b'1\r\n'</span></span><br><span class="line"><span class="string">b'1\r\n'</span></span><br><span class="line"><span class="string">b'1\r\n'</span></span><br><span class="line"><span class="string">b'1\r\n'</span></span><br></pre></td></tr></table></figure><p>所以可以str(data[2:-5])这样取出其中的数值部分。</p><h1 id="测距功能"><a href="#测距功能" class="headerlink" title="测距功能"></a>测距功能</h1><p>选用了HC-SR04模块作为超声测距模块，购物平台5RMB左右。超声波测距的原理较为简单，首先发送特定频率的声波，在接收对应频率的声波。记录声波传输的时间，再结合声波传输的速度，很容易得到当前与障碍物的距离。测距原理图如下所示：</p><p><img data-src="/2020/04/23/reverse-radar-hardware/2.png" alt></p><p>而具体到硬件而言，测距方法如下。HC-SR04有两个喇叭，Trig发送声波，Echo接收声波。Trig是启动信号引脚，使用的时候先给他10us以上的高电平，再拉低，SR04就会发送八个40khz的超声波。此时对Echo拉低。然后就开始等待Echo啥时候变高了我们就开始计时 <strong>(开始接收到反馈的声波)</strong>， 直到他变低为止 <strong>(声波接收完毕)</strong>，当它变低的时候，我们就可以根据这个时间计算距离了。</p><p>这个时间是声波在空气中传播的时间，因为是往返的，所以计算过程需要除以2，变成单程的时间，再将这个单程时间乘以声速即340米/秒，得到的就是从当前位置到对面障碍物的距离。因此，得到的电路连接方式如下：</p><p><img data-src="/2020/04/23/reverse-radar-hardware/3.jpg" alt></p><p>也很容易得到相应的程序，但也有相关的注意事项：</p><ul><li>使用pulseIn函数记录Echo持续高电平的时间即可。</li><li>在完成一次测距后一定要延时一秒左右。因为程序的执行速度和硬件的执行速度是不一样的，程序执行完了要缓一缓，等待硬件执行完。不延时的情况下HC-SR04是不能工作的。</li></ul><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> trigBack = <span class="number">2</span>;</span><br><span class="line"><span class="keyword">int</span> echoBack = A0;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setup</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 定义超声波的输入输出模式 -----------------------------</span></span><br><span class="line">    pinMode(trigBack, OUTPUT);</span><br><span class="line">    pinMode(echoBack, INPUT);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">loop</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 先拉底做初始化</span></span><br><span class="line">    digitalWrite(trigBack, LOW);</span><br><span class="line">    delayMicroseconds(<span class="number">5</span>);</span><br><span class="line">    <span class="comment">// 拉高十毫秒</span></span><br><span class="line">    digitalWrite(trigBack, HIGH);</span><br><span class="line">    delayMicroseconds(<span class="number">10</span>);</span><br><span class="line">    <span class="comment">// 在拉低</span></span><br><span class="line">    digitalWrite(trigBack, LOW);</span><br><span class="line">    <span class="comment">// 根据高电平持续的时间计算与障碍物的距离</span></span><br><span class="line">    cmB = (pulseIn(echoBack, HIGH) / <span class="number">2</span>) / <span class="number">29.1</span>;</span><br><span class="line">    Serial.println(cmB);</span><br><span class="line">    <span class="comment">// 一定要有延时</span></span><br><span class="line">    delay(<span class="number">2000</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="制动功能"><a href="#制动功能" class="headerlink" title="制动功能"></a>制动功能</h1><p>车辆最基本的功能还是实现前进、后退、左转和右转，而这些功能的实现离不开电机模块。选用的电机模块是HC02-48，便宜到忘记了它的价格。但是Arduino不能直接驱动HC02-48电机完成工作，因为驱动电机需要较大电流，而Arduino的作用在于高低电平的控制而不是电流的输出。Arduino的IO口输出电流在40mA左右，无法驱动最低要求150mA的电机。因此需要借助电机驱动模块L298N来放大驱动电流。</p><p>L298N驱动直流电机时可选择任何额定电压在4.8~35V和电流小于2A范围内的电机。而HC02-48电机的工作电压在3-6V，且L298N的输出电流恰好能驱动电机模块，因此在硬件层面可完成驱动。这个好像也很便宜，大约10RMB左右。</p><p>将L298N的四个入口引脚IN1，IN2，IN3和IN4接Arduino的控制引脚，此时Out1，Out2连接一个电机，Out3，Out4出口引脚连接另外的电机。通过对4个入口引脚赋值来更改出口引脚的电平输出，进而控制电机的正反转，完成前进、后退和转向的功能。此外可以通过总开关ENA，ENB两个使能端控制两个电机能否转动和转速。电路连接方式下：</p><p><img data-src="/2020/04/23/reverse-radar-hardware/4.jpg" alt></p><p>而L298N的+12V和GND接口，需要单独的电池供电。正如上文所说，Arduino的核心功能是控制电平的输出而不是提供电压，因此仅将Arduino的IO口和L298N的使能端连接即可，L298N的供电部分需要另外的电池。</p><h2 id="转速调节"><a href="#转速调节" class="headerlink" title="转速调节"></a>转速调节</h2><p>在倒车的过程中，车辆不能够以过高的速度倒车，否则容易发生碰撞。因此，需要PWM调制的手段将车速给降下来。想必在做的看官应该不是自动化专业的，所以我准备通俗的解释PWM的含义：</p><ul><li>在一个时间段内，一直给电机供电，那么电机会持续转动，此时是满电的输出；</li><li>在一个时间段内，一半的时间供电，一半的时间不供电，那么电机只会在一半的时间内转动，此时的速度会低于满电输出；</li><li>在一个时间段内，75%的时间不供电，25%的时间供电，那么电机会在1/4的时间内转动，此时速度会更低；</li><li>如果将一个时间段取极限无限的减小，那么电机就是时而供电时而没电，此时便会减速。如下图所示：</li></ul><p><img data-src="/2020/04/23/reverse-radar-hardware/5.png" alt></p><p>而在Arduino实现这个并不是很难，只需要调用analogWrite方法对L298N的使能端进行PWM调制即可。其中，analogWrite方法接收一个参数，参数的取值范围是[0, 255]，取值越大，输出电压越大，电机转速越快。代码如下：</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 定义两个电机驱动的使能端</span></span><br><span class="line"><span class="keyword">int</span> en1 = <span class="number">9</span>;</span><br><span class="line"><span class="keyword">int</span> en2 = <span class="number">6</span>;</span><br><span class="line"><span class="comment">// 定义行驶速度</span></span><br><span class="line"><span class="keyword">int</span> speed = <span class="number">95</span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setup</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 使能端为输出模式</span></span><br><span class="line">    pinMode(en1, OUTPUT);</span><br><span class="line">    pinMode(en2, OUTPUT);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">loop</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// speed 就是行走的速度</span></span><br><span class="line">    analogWrite(en1, speed);</span><br><span class="line">    analogWrite(en2, speed);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>需要注意的是：电机的实际转速不仅和参数speed有关，还和供电的电压有关。即使speed的取值为255，但电池没电了，电机还是会转的很慢。</p><h2 id="电机转动"><a href="#电机转动" class="headerlink" title="电机转动"></a>电机转动</h2><p>在能够对电机的转速进行调节后，下一步就是实现电机的转动了：</p><ul><li>四个电机都向前转，那么车辆会向前行进；</li><li>四个电机都向后转，那么车辆会向后行进；</li><li>左侧的电机向前转，右侧的电机向后转，那么车辆会向右行进；</li><li>左侧的电机向后转，右侧的电机向前转，那么车辆会向左行进；</li></ul><p>仅需要digitalWrite方法对电机两侧输入不同的电平即可完成电机的转动，因此代码如下：</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 右前轮 -----------------------------------------</span></span><br><span class="line"><span class="comment">// 定义 uno 的 pin 8 向 input1 输出</span></span><br><span class="line"><span class="keyword">int</span> input1 = <span class="number">8</span>; </span><br><span class="line"><span class="comment">// 定义 uno 的 pin 12 向 input2 输出</span></span><br><span class="line"><span class="keyword">int</span> input2 = <span class="number">10</span>; </span><br><span class="line"><span class="comment">// 左前轮 -----------------------------------------</span></span><br><span class="line"><span class="comment">// 定义 uno 的 pin 11 向 input3 输出</span></span><br><span class="line"><span class="keyword">int</span> input3 = <span class="number">11</span>; </span><br><span class="line"><span class="comment">// 定义 uno 的 pin 12 向 input4 输出</span></span><br><span class="line"><span class="keyword">int</span> input4 = <span class="number">12</span>; </span><br><span class="line"><span class="comment">// 右后轮 -----------------------------------------</span></span><br><span class="line"><span class="comment">// 定义 uno 的 pin 3 向 input5 输出</span></span><br><span class="line"><span class="keyword">int</span> input5 = <span class="number">3</span>; </span><br><span class="line"><span class="comment">// 定义 uno 的 pin 4 向 input6 输出</span></span><br><span class="line"><span class="keyword">int</span> input6 = <span class="number">4</span>; </span><br><span class="line"><span class="comment">// 左后轮 -----------------------------------------</span></span><br><span class="line"><span class="comment">// 定义 uno 的 pin 5 向 input7 输出</span></span><br><span class="line"><span class="keyword">int</span> input7 = <span class="number">5</span>; </span><br><span class="line"><span class="comment">// 定义 uno 的 pin 6 向 input8 输出</span></span><br><span class="line"><span class="keyword">int</span> input8 = <span class="number">7</span>; </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setup</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 使能端</span></span><br><span class="line">    pinMode(en1, OUTPUT);</span><br><span class="line">    pinMode(en2, OUTPUT);</span><br><span class="line">    <span class="comment">// 右前</span></span><br><span class="line">    pinMode(input1, OUTPUT);</span><br><span class="line">    pinMode(input2, OUTPUT);</span><br><span class="line">    <span class="comment">// 左前  </span></span><br><span class="line">    pinMode(input3, OUTPUT);</span><br><span class="line">    pinMode(input4, OUTPUT);</span><br><span class="line">    <span class="comment">// 右后</span></span><br><span class="line">    pinMode(input5, OUTPUT);</span><br><span class="line">    pinMode(input6, OUTPUT);</span><br><span class="line">    <span class="comment">// 左后  </span></span><br><span class="line">    pinMode(input7, OUTPUT);</span><br><span class="line">    pinMode(input8, OUTPUT);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 前进函数 ------------------------------------------</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">forward</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    analogWrite(en1, speed);</span><br><span class="line">    analogWrite(en2, speed);</span><br><span class="line">    <span class="comment">// 右前轮</span></span><br><span class="line">    digitalWrite(input1, LOW); </span><br><span class="line">    digitalWrite(input2, HIGH);   </span><br><span class="line">    <span class="comment">// 右后轮</span></span><br><span class="line">    digitalWrite(input5, HIGH);</span><br><span class="line">    digitalWrite(input6, LOW);</span><br><span class="line">    <span class="comment">// 左后轮</span></span><br><span class="line">    digitalWrite(input7, HIGH);</span><br><span class="line">    digitalWrite(input8, LOW);</span><br><span class="line">    <span class="comment">// 左前轮 </span></span><br><span class="line">    digitalWrite(input3, LOW); </span><br><span class="line">    digitalWrite(input4, HIGH);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 停止函数 ------------------------------------------</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">stop</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 左前轮</span></span><br><span class="line">    digitalWrite(input1, LOW); </span><br><span class="line">    digitalWrite(input2, LOW);  </span><br><span class="line">    <span class="comment">// 左后轮</span></span><br><span class="line">    digitalWrite(input7, LOW);</span><br><span class="line">    digitalWrite(input8, LOW);</span><br><span class="line">    <span class="comment">// 右前轮 </span></span><br><span class="line">    digitalWrite(input3, LOW); </span><br><span class="line">    digitalWrite(input4, LOW);</span><br><span class="line">    <span class="comment">// 右后轮</span></span><br><span class="line">    digitalWrite(input5, LOW);</span><br><span class="line">    digitalWrite(input6, LOW);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 后退函数 ------------------------------------------</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">back</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    analogWrite(en1, speed);</span><br><span class="line">    analogWrite(en2, speed);</span><br><span class="line">    <span class="comment">// 右前轮</span></span><br><span class="line">    digitalWrite(input1, HIGH); </span><br><span class="line">    digitalWrite(input2, LOW);  </span><br><span class="line">    <span class="comment">// 右后轮</span></span><br><span class="line">    digitalWrite(input5, LOW);</span><br><span class="line">    digitalWrite(input6, HIGH);</span><br><span class="line">    <span class="comment">// 左后轮</span></span><br><span class="line">    digitalWrite(input7, LOW);</span><br><span class="line">    digitalWrite(input8, HIGH);</span><br><span class="line">    <span class="comment">// 左前轮 </span></span><br><span class="line">    digitalWrite(input3, HIGH); </span><br><span class="line">    digitalWrite(input4, LOW);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 左转函数 ------------------------------------------</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">left</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    analogWrite(en1, speed);</span><br><span class="line">    analogWrite(en2, speed);</span><br><span class="line">    <span class="comment">// 左后轮</span></span><br><span class="line">    digitalWrite(input7, LOW);</span><br><span class="line">    digitalWrite(input8, HIGH);</span><br><span class="line">    <span class="comment">// 左前轮 </span></span><br><span class="line">    digitalWrite(input3, HIGH); </span><br><span class="line">    digitalWrite(input4, LOW);</span><br><span class="line">    <span class="comment">// 右前轮</span></span><br><span class="line">    digitalWrite(input1, LOW); </span><br><span class="line">    digitalWrite(input2, HIGH);  </span><br><span class="line">    <span class="comment">// 右后轮</span></span><br><span class="line">    digitalWrite(input5, HIGH);</span><br><span class="line">    digitalWrite(input6, LOW);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 右转函数 ------------------------------------------</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">right</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    analogWrite(en1, speed);</span><br><span class="line">    analogWrite(en2, speed);</span><br><span class="line">    <span class="comment">// 左后轮</span></span><br><span class="line">    digitalWrite(input7, HIGH);</span><br><span class="line">    digitalWrite(input8, LOW);</span><br><span class="line">    <span class="comment">// 左前轮 </span></span><br><span class="line">    digitalWrite(input3, LOW); </span><br><span class="line">    digitalWrite(input4, HIGH);</span><br><span class="line">    <span class="comment">// 右前轮</span></span><br><span class="line">    digitalWrite(input1, HIGH); </span><br><span class="line">    digitalWrite(input2, LOW);  </span><br><span class="line">    <span class="comment">// 右后轮</span></span><br><span class="line">    digitalWrite(input5, LOW);</span><br><span class="line">    digitalWrite(input6, HIGH);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">loop</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    delay(<span class="number">3000</span>);</span><br><span class="line">    forward();</span><br><span class="line">    delay(<span class="number">3000</span>);</span><br><span class="line">    back();</span><br><span class="line">    delay(<span class="number">3000</span>);</span><br><span class="line">    stop();</span><br><span class="line">    delay(<span class="number">3000</span>);</span><br><span class="line">    right();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当然，电平高低的输出需要根据实际的接线情况来判断。</p><h1 id="供电系统"><a href="#供电系统" class="headerlink" title="供电系统"></a>供电系统</h1><p>全部系统的供电系统分为两部分，一个是给Arduino供电，一个是给L298N电机供电。</p><ul><li><p>对于给Arduino供电，我选择了使用电源接口为Arduino供电。通过此方法为Arduino开发板供电时，直流电源电压为9V ~ 12V，买一个9V电池和转接线即可。使用低于9V的电源电压可能导致Arduino工作不稳定，使用高于12V电源电压存在着毁坏Arduino开发板的风险。<img data-src="/2020/04/23/reverse-radar-hardware/6.jpg" alt></p></li><li><p>对于给L298N供电，选择的是3.7V 18650可充电锂电池，在配一个电池盒即可。电池盒要求是串联的，而不是并联，否则电压不够。将电池盒自带的红色线与L298N的+12V连接，将黑色线与L298N的GND连接即可完成电机部分的供电。</p></li></ul><h1 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h1><p>接线并不是很复杂，我都是用的杜邦线接起来的，大胆的去尝试就好了。有问题也欢迎在下方留言，毕竟计算机搞这些可能也是一头雾水，上文都是我一点点摸索出来的经验。</p><p><img data-src="/2020/04/23/reverse-radar-hardware/7.jpg" alt></p><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><ol><li><a href="http://www.taichi-maker.com/homepage/arduino-projects-index/arduino-power-supply/" target="_blank" rel="noopener">http://www.taichi-maker.com/homepage/arduino-projects-index/arduino-power-supply/</a></li><li><a href="http://www.taichi-maker.com/homepage/reference-index/arduino-code-reference/" target="_blank" rel="noopener">http://www.taichi-maker.com/homepage/reference-index/arduino-code-reference/</a></li><li><a href="http://www.taichi-maker.com/?s=HC-SR04" target="_blank" rel="noopener">http://www.taichi-maker.com/?s=HC-SR04</a></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;硬件是软件运行的基础，因此有必要建立良好的硬件系统供软件的运行。因此，针对通信功能的蓝牙模块、制动功能的电机模块和测距功能的超声波模块三大模块，建立电路连接，提供电路连接图，并编写程序。&lt;/p&gt;
&lt;p&gt;本文也提供部分代码，代码虽然都是核心代码&lt;strong&gt;但不能够独立运行&lt;/strong&gt;。程序都附注释，有一丢丢开发基础都能看懂。希望看官能有所思考后取得所需代码，而不是无脑的复制粘贴。完整代码参考&lt;a href=&quot;https://github.com/muyuuuu/Reversing-radar-on-Arduino/blob/master/code/Reverse-radar/Reverse-radar.ino&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;这里&lt;/a&gt;。且代码仅以我的接线方式为标准。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="microcomputer" scheme="https://muyuuuu.github.io/tags/microcomputer/"/>
    
  </entry>
  
  <entry>
    <title>倒车雷达（一）：介绍</title>
    <link href="https://muyuuuu.github.io/2020/04/18/reverse-radar-begin/"/>
    <id>https://muyuuuu.github.io/2020/04/18/reverse-radar-begin/</id>
    <published>2020-04-18T13:01:02.000Z</published>
    <updated>2020-04-23T10:34:58.112Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>问题背景说来话长，在去年选毕设的时候，计算机的毕设95%都是游戏开发和后端开发(后端数据库配前端页面展示)，这些东西我实在不会，唯一会的就是其中为数不多的单片机开发。而唯一的深度学习项目难度过大当作研究生毕设也不为过，后期老师提供的一些python项目也没及时看到，阴差阳错的选了这个毕设。</p><a id="more"></a><h1 id="设计思路"><a href="#设计思路" class="headerlink" title="设计思路"></a>设计思路</h1><p>大概的要求是：基于Arduino实现倒车雷达，并在电脑端绘制实时的倒车影像。这个产品其实已经存在于市面上，也有幸见过一次倒车雷达，因此对倒车雷达的基本概念有了初步了解。<br>所以很容易得到这个设计的思路：</p><ol><li>按毕设要求，车辆的主控芯片选择Arduino；</li><li>使用超声波模块测量距离，当倒车过程中距离小于某个安全值时采取紧急制动；</li><li>因需要将距离数据发送给PC端用于绘制实时影象，因此数据的发送借助蓝牙模块实现；</li><li>既然Arduino将距离发送回PC，那么就在PC端写一个图形化的上位机，用于发送控制指令，如前进、后退等。</li></ol><p>以及相关注意事项：</p><ol><li>在正常开车时不能开启倒车雷达。因为在堵车时，后面的车离自己很近，但此时不能制动，防止车祸发生；</li><li>实时影象可以考虑Wifi图传，但价格我承受不起，所以选择非实际影象，而是制作动画代替；</li><li>可以考虑在车的两侧添加超声波模块，用于反馈倒车时车身两侧的距离。但IO口不够，因此只添加车身后侧的超声波；</li><li>PCB难以绘制，Altium Designer学到一半就放弃。实在搞不懂什么时候放电阻、电容、电感等。确切的说电路等相关知识并不是计算机专业必须要会和要求掌握的东西，所以选择面包板偷工减料。</li></ol><h1 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h1><p><img data-src="/2020/04/18/reverse-radar-begin/1.jpg" alt></p><p><a href="https://muyuuuu.github.io/2018/05/22/C51/">左图是两年前做的</a>，包括：蓝牙、电机、液晶、加速减速、避障、巡迹等常见功能；右图是今年的做的，只有雷达、电机和蓝牙，更多的是PC端的上位机。</p><p>在这两周的开发中，也遇到了各种难以解决的bug，好在我<del>智勇双全</del>(文档丰富)。不过，也正因如此，我也选择了彻底放弃开发单片机的幻想。在三年前大一结束转专业的时候，我大概花了一个晚上去分析我到底是转专业去计算机呢，还是去电子信息呢，还是去自动化专业呢？毕竟以当时的成绩还是能想去哪里就去哪里的。</p><p>想去计算机是因为发现代码很有趣，大一自学了C语言and看着别人的python，都挺好玩的样子；想去电子信息是因为在大一的时候自学了C51单片机，发现单片机做出来的诸如：四旋翼、飞思卡尔智能车等东西也很有趣。当时以极低的分数考上大学，大一的专业都是调剂的，好不容易有了能凭自己兴趣选专业的机会不能错过。</p><p>大概在斟酌了专业的发展前景和研究方向后，还是选择了计算机专业，<del>当时单纯的以为自动化等专业还得去车间，像个电焊工一样带着护目镜作业</del>。事实证明，这个选择终究是正确的。经过这几天的开发，总结了我认为的电子方向的缺陷(仅仅是我片面的观点，电子专业的看管求放过)：</p><ol><li>程序执行不便，每次执行程序都是：修改ino、接线烧录、拔线、启动PC端、观察结果，某次执行不对还得重复这个流程。</li><li>bug难以调试。当软件出问题时，可以很方便的查文档、debug去解决；但硬件层面的bug并不是能轻易解决的，如某个模块内部的一个电路被击穿了，供电电压不够导致模块不稳定而烧坏，或者是不小心把某个线路弄坏了。这种硬件问题对计算机专业的人来说很难准确定位和解决。如上次调试了好久的bug，以为是驱动问题，代码问题，库依赖问题，最后才发现只是那个蓝牙模块坏了。</li><li>烧钱。承接第一个缺陷，蓝牙模块坏了是个大问题，根本不知道如何维修别人集成好的电路。因质量问题、电子元件寿命、错误操作等多个原因都可能导致模块损坏，甚至是快递的途中颠簸坏了，都会导致金钱白白的流失。而软件开发则不要钱(当然是选择免费工具)，且不会因误操作和寿命问题导致软件损坏，大不了更新嘛。</li><li>也是最重要的一点，我没学过电路、模电、电磁场等专业课。所以我只适合开发软件，硬件对我来说到处都是知识盲区。</li></ol><p>因此，尽管我能用所学知识做出相应的产品，但我也不会在去选择单片机开发这条路，也算是了却了一件心事吧。（单纯的开发不涉及任何硬件的软件除外）</p><p>当然我不是在贬低电子这个专业，只是我不适合。这个世界离不开电子这个专业，从常见的手机、充电宝，到大街上的电子灯，到一般的蓝牙耳机，包括我写这篇文章用的键盘、鼠标和电脑，到不常见的机器人控制芯片，这都离不开电子这个专业。</p><p>等彻底完结了再去发后面的设计过程。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;问题背景说来话长，在去年选毕设的时候，计算机的毕设95%都是游戏开发和后端开发(后端数据库配前端页面展示)，这些东西我实在不会，唯一会的就是其中为数不多的单片机开发。而唯一的深度学习项目难度过大当作研究生毕设也不为过，后期老师提供的一些python项目也没及时看到，阴差阳错的选了这个毕设。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="microcomputer" scheme="https://muyuuuu.github.io/tags/microcomputer/"/>
    
  </entry>
  
  <entry>
    <title>动态规划问题系列</title>
    <link href="https://muyuuuu.github.io/2020/04/11/dynamic-programming/"/>
    <id>https://muyuuuu.github.io/2020/04/11/dynamic-programming/</id>
    <published>2020-04-11T04:28:02.000Z</published>
    <updated>2020-04-11T07:35:22.218Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>实在没兴趣去新开一个标签或分类，就准备把刷题期间遇到的所有动态规划问题放在这里了。还是老样子，思路分析，算法求解，代码实现。但动态规划问题多种多样，我暂时遇到的也不是很多，愿本文能一直保持收录。</p><a id="more"></a><h1 id="上楼梯问题"><a href="#上楼梯问题" class="headerlink" title="上楼梯问题"></a>上楼梯问题</h1><p>这也许是最经典的动态规划问题，想必很多人都遇到过：一共十层楼梯，一次可以走一个阶梯，两个阶梯或三个阶梯。问走完十层阶梯有几种方法。那么走一百层楼梯有多少种方法呢？</p><p>求解方案：在第七、第八、第九层楼梯时，可以到达第十层，那么到达第十层的方案数量就取决于到达第七、第八、第九层的方案数量，以此类推至最开始的第一层。代码如下。</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 走 100 层楼梯</span></span><br><span class="line"><span class="keyword">int</span> num = <span class="number">100</span>;</span><br><span class="line">dp[<span class="number">1</span>] = <span class="number">1</span>; dp[<span class="number">2</span>] = <span class="number">2</span>; dp[<span class="number">3</span>] = <span class="number">4</span>;</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">4</span>; i &lt;= num; i++)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 第8层的方案取决于5、6、7层的方案</span></span><br><span class="line">    <span class="comment">// 依次递归类推到第三层开始</span></span><br><span class="line">    dp[i] = dp[i<span class="number">-1</span>] + dp[i<span class="number">-2</span>] + dp[i<span class="number">-3</span>];</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">cout</span> &lt;&lt; dp[num] &lt;&lt; <span class="built_in">endl</span>;</span><br></pre></td></tr></table></figure><h2 id="楼梯坏了"><a href="#楼梯坏了" class="headerlink" title="楼梯坏了"></a>楼梯坏了</h2><p>​假设此时的部分楼梯坏了不能走，那么一共有几种走法呢？输入数据共两行，第一行包含两个自然数 $n (1≤n≤100)$ 和 $k (0≤k&lt;n)$，第二行包含 $k$ 个自然数 $X_i$ $(1≤X_i≤n)$，数字之间用一个空格隔开，表示损坏的台阶的序号（从楼梯底部到楼梯顶部，台阶序号依次为 $1$ 到 $n$）。</p><p>​输出数据仅包含一个整数，表示所有可行走法的总数。当有的楼梯坏了，又该怎么走呢？</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> method = <span class="number">0</span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">up</span><span class="params">(<span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;   <span class="comment">// N表示有N个楼梯</span></span><br><span class="line">    <span class="keyword">if</span> (n == N)&#123;</span><br><span class="line">        method ++;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 当还没有走到终点且楼梯能走时</span></span><br><span class="line">    <span class="keyword">if</span> (n &lt; N &amp;&amp; !bad[n])&#123;</span><br><span class="line">        <span class="comment">// 有这么多种走法</span></span><br><span class="line">        up(n + <span class="number">1</span>);</span><br><span class="line">        up(n + <span class="number">2</span>);</span><br><span class="line">        up(n + <span class="number">3</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> k;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; N &gt;&gt; k;</span><br><span class="line">    <span class="keyword">int</span> a;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; k; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; a;</span><br><span class="line">        <span class="comment">// 记录坏的楼梯 </span></span><br><span class="line">        bad[a] = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 从第一个阶梯开始走</span></span><br><span class="line">    up(<span class="number">0</span>);</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; method;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="和最大的子序列"><a href="#和最大的子序列" class="headerlink" title="和最大的子序列"></a>和最大的子序列</h1><p>这也是一个比较经典的问题，对于给定的$k$个元素的序列，求其中的一个连续的子序列，使子序列的所有元素相加的和最大。示例如下：</p><ul><li>Sample input: -10 1 3 4 -2 6 </li><li>Sample output:  12 1 6 （第一个数是最大和，第二个和第三个是起始位置和终止为止）</li></ul><p>​甲级1007 。<a href="https://pintia.cn/problem-sets/994805342720868352/problems/994805514284679168" target="_blank" rel="noopener">Maximum Subsequence Sum</a></p><p>对于此类问题，因为随时可能在相加的过程中出现负数，且负数后面可能还有更大的正数。因此，可能拿不准何时继续加，何时放弃。因为对于-9， 10这样的序列，应该继续向后加，因为加到10相当于赚了1；但对于-9， 1这样的序列，到-9时就该放弃，后面的1没有加的必要。所以应该从以下两个角度出发，考虑元素的取舍问题：</p><ol><li>记录元素的累积和，作为判断取舍的标准；</li><li>如果累积和小于零，就放弃当前元素，考虑下一个元素；</li><li>如果累积和大于上一次的和，就开始标记左右边界，此时左右边界内的元素和是最大的；</li><li>累积和小于上一次的和但大于零，相当于加了一个负数，此时仍然继续向后加，但不标记左右边界。一来可以继续探索后面的元素是否能更大，二来保证之前左右边界内的元素和是最大的。</li></ol><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 输入序列元素</span></span><br><span class="line"><span class="built_in">cin</span> &gt;&gt; k;</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; k; i++)</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; v[i];</span><br><span class="line"><span class="keyword">int</span> sum = <span class="number">-1</span>; </span><br><span class="line"><span class="comment">// 可能全是0，所以不能 int sum = 0; 判断sum &lt;= 0。</span></span><br><span class="line"><span class="keyword">int</span> flag = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; k ; i++)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 对于和加一次后开始减小，无关紧要，因为下一个可能会更大</span></span><br><span class="line">    <span class="comment">// 如 3 -2 6。所以和减小的情况不予考虑 </span></span><br><span class="line">    temp = temp + v[i];</span><br><span class="line">    <span class="comment">// 和小于0，就重新开始左定位</span></span><br><span class="line">    <span class="keyword">if</span> (temp &lt; <span class="number">0</span>)         </span><br><span class="line">    &#123;</span><br><span class="line">        temp = <span class="number">0</span>;</span><br><span class="line">        flag = i + <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 和大于之前的和，开始标记，越加越小不予考虑</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (temp &gt; sum)   </span><br><span class="line">    &#123;</span><br><span class="line">        sum = temp;</span><br><span class="line">        left = flag;</span><br><span class="line">        right = i;</span><br><span class="line">    &#125;   </span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span> (sum &lt; <span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="number">0</span> &lt;&lt; <span class="string">" "</span> &lt;&lt; v[<span class="number">0</span>] &lt;&lt; <span class="string">" "</span> &lt;&lt; v[k<span class="number">-1</span>];</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; sum &lt;&lt; <span class="string">" "</span> &lt;&lt; v[left] &lt;&lt; <span class="string">" "</span> &lt;&lt; v[right];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="和为固定值的子序列"><a href="#和为固定值的子序列" class="headerlink" title="和为固定值的子序列"></a>和为固定值的子序列</h2><p>和上文类似，只是增加了额外的要求。大概意思是：给定一个序列，求序列中连续的子序列，使子序列的和为目标值，或者子序列的和大于目标值，但与目标值的相差最小。即此时不要求和最大，而是增加了目标值这样一个范围。<a href="https://pintia.cn/problem-sets/994805342720868352/problems/994805439202443264" target="_blank" rel="noopener">甲级1044</a>。</p><p>举例说明：对于序列3, 2, 1, 5, 4, 6, 8, 7，当要求的目标值为15时，有三种选择方法：</p><ol><li>from the position 1 to 5 (with values 3 + 2 + 1 + 5 + 4 = 15).</li><li>from the position 4 to 6 (with values 5 + 4 + 6 = 15).</li><li>from the position 7 to 8 (with values 8 + 7 = 15).</li></ol><p>对于序列2 4 5 7 9，当目标值为13时，有两种选择方法：</p><ol><li>from the position 2 to 4 (with values 5 + 7 + 9 = 21).</li><li>from the position 4 to 5 (with values 7 + 9 = 16).</li></ol><p>思路如下：还没研究明白，改日吧。</p><h1 id="对称子串问题"><a href="#对称子串问题" class="headerlink" title="对称子串问题"></a>对称子串问题</h1><p>给定一个字符串，求其中包含的最长的对称子串的长度。如<code>0000000</code>的长度是7(7个0)，<code>baaq</code>的长度是2(两个a)，<code>Is PAT&amp;TAP symmetric?</code>的长度是11(s PAT$TAP s这个字符串是对称的)。即将问题归纳为：给定的字符串中是否含有对称字符串？如果有，长度是多少？<a href="https://pintia.cn/problem-sets/994805342720868352/problems/994805446102073344" target="_blank" rel="noopener">甲级1040</a>。分析：</p><ol><li>设立数组$dp[i][j]$，其取值只有0和1，表示$s[i]$到$s[j]$所表示的字串是否是回文字串。如当s[2] == s[4]时，d[2][4] == 1，否则为0。</li><li><p>那么很容易得出：单个长度的字符串肯定是对称的，长度为1；字符串中有两个连续的字符，肯定有对称字符串，且此时长度为2。如序列<code>daaq</code>，有两个连续的<code>a</code>，所以含有对称的字符串<code>aa</code>，且长度为2。这是最简单的情况。​首先初始化$dp[i][i] = 1, dp[i][i+1]$，把长度为1和2的都初始化好，然后在判断长度为3一直到长度为$N$。这是边界情况，判断方程为：$dp[i][i] = 1$, $dp[i][i+1] = (s[i] == s[i+1]) ? 1 : 0$，初始化部分程序下：</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">string</span> s;</span><br><span class="line">getline(<span class="built_in">cin</span>, s);</span><br><span class="line"><span class="keyword">int</span> len = s.length(), ans = <span class="number">1</span>;</span><br><span class="line"><span class="comment">// 初始化长度为2的序列</span></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; len; i++) &#123;</span><br><span class="line">    dp[i][i] = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">if</span>(i &lt; len - <span class="number">1</span> &amp;&amp; s[i] == s[i+<span class="number">1</span>]) &#123;</span><br><span class="line">        dp[i][i+<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">        <span class="comment">// 当存在连续的字符时，对称字符的长度最小为2</span></span><br><span class="line">        ans = <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>在判断完长度为2的子串后，下一步要判断长度为3的字串。因此初始化步长$L=3$，且步长逐逐渐增加。要判断的左边界字符为s[i]，右边界字符为s[j]，且$i$从0开始一直递增，$j$的取值为$i+L-1$。</p></li><li>当$s[i] != s[j]$ 时，$dp[i][j] = 0$，表示[i, j]中的子串不是对称的；</li><li>当$s[i] == s[j]$ 时，$dp[i][j] = dp[i+1][j-1]$，当前子串对称与否的情况取决于上次的子串是否对称，得到递推方程。如果上次的小子串s[i+1][j-1]对称，那么这次的大子串s[i][j]也对称，将对称字串的长度赋值为$L$。即本次的状态取决于上次的状态，以此类推。而最开始的情况$dp[i][i] = 1, dp[i][i+1]$已经初始化。</li></ol><p>​因为$i, j$按照从小到大的顺序来枚举，因此必须保证更新$dp[i][j]$的时候$dp[i+1][j-1]$已经被计算过。因此第一遍先计算长度为1和长度为2的字串，第三遍将长度为3的子串的$dp$的值全部求出，第四遍通过第三遍结果计算出长度为4的子串的$dp$的值，这样就可以避免状态无法转移的问题。这部分伪代码如下：<br><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> L = <span class="number">3</span>; L &lt;= len; L++) &#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i + L - <span class="number">1</span> &lt; len; i++) &#123;</span><br><span class="line">        <span class="keyword">int</span> j = i + L <span class="number">-1</span>;</span><br><span class="line">        <span class="keyword">if</span>(s[i] == s[j] &amp;&amp; dp[i + <span class="number">1</span>][j - <span class="number">1</span>] == <span class="number">1</span>) &#123;</span><br><span class="line">            dp[i][j] = <span class="number">1</span>;</span><br><span class="line">            ans = L;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>为了防止看官头晕，这里以字符串<code>Is PAT&amp;TAP symmetric?</code>为例：</p><ol><li>初始化$dp[i][i] = 1$，ans = 1；</li><li>L = 3, i = 5, j = 7 时，s[5] == s[7]均为T，且根据第一次的状态d[6][6] = 1，得到此时ans为3，并将d[5][7]赋值为1，求的对称子串为<code>T&amp;T</code>；</li><li>L = 5, i = 4, j = 8 时，s[4] == s[8]均为A，且根据上一次的状态d[5][7] = 1，得到此时ans为5，并将d[4][8]赋值为1，求得对称子串为<code>AT$TA</code>；</li><li>以此标准类推即可。由小的子串逐步扩大到大的子串，当前子串计算完毕后更改当前状态，因为下一状态取决于当前状态，逐步类推，完整代码如下：</li></ol><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">int</span> dp[<span class="number">1010</span>][<span class="number">1010</span>];</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">string</span> s;</span><br><span class="line">    getline(<span class="built_in">cin</span>, s);</span><br><span class="line">    <span class="keyword">int</span> len = s.length(), ans = <span class="number">1</span>;</span><br><span class="line">    <span class="comment">// 初始化长度为2的序列</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; len; i++) </span><br><span class="line">    &#123;</span><br><span class="line">        dp[i][i] = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span>(i &lt; len - <span class="number">1</span> &amp;&amp; s[i] == s[i+<span class="number">1</span>]) </span><br><span class="line">        &#123;</span><br><span class="line">            dp[i][i+<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">            <span class="comment">// 当存在连续的字符时，对称字符的长度最小为2</span></span><br><span class="line">            ans = <span class="number">2</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// i-&gt;j的差距为3,4,5逐渐递增，先算小的，保证计算大的时候小的已经更新</span></span><br><span class="line">    <span class="comment">// 即计算d[i][j] 时候，d[i+1][j-1]需要已经更新</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> L = <span class="number">3</span>; L &lt;= len; L++) </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i + L - <span class="number">1</span> &lt; len; i++) </span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">int</span> j = i + L <span class="number">-1</span>;</span><br><span class="line">            <span class="keyword">if</span>(s[i] == s[j] &amp;&amp; dp[i + <span class="number">1</span>][j - <span class="number">1</span>] == <span class="number">1</span>) </span><br><span class="line">            &#123;</span><br><span class="line">                dp[i][j] = <span class="number">1</span>;</span><br><span class="line">                ans = L;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%d"</span>, ans);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;实在没兴趣去新开一个标签或分类，就准备把刷题期间遇到的所有动态规划问题放在这里了。还是老样子，思路分析，算法求解，代码实现。但动态规划问题多种多样，我暂时遇到的也不是很多，愿本文能一直保持收录。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="DataStructure" scheme="https://muyuuuu.github.io/tags/DataStructure/"/>
    
  </entry>
  
  <entry>
    <title>提升方法（上）——基于AdaBoost：SAMME</title>
    <link href="https://muyuuuu.github.io/2020/04/02/adaboost/"/>
    <id>https://muyuuuu.github.io/2020/04/02/adaboost/</id>
    <published>2020-04-02T07:47:55.000Z</published>
    <updated>2020-04-07T12:22:33.503Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>在多年前的某次任务中，看到了别人使用提升树等算法，取得了比较好的效果。今日眼馋，特来学习提升方法，并记录于此。提升方法是一种统计学习方法。在分类问题中，通过改变训练样本的权重，学习多个分类器，将多个分类器进行线性组合，以提升分类的性能。</p><p><img data-src="/2020/04/02/adaboost/error_rate.png" alt></p><a id="more"></a><p>本文收录：</p><ol><li>具有代表性的提升方法：AdaBoost算法；</li><li>算法原理</li><li>算法推导</li><li>算法解释</li><li>举例说明</li><li>代码实现</li></ol><h1 id="算法原理"><a href="#算法原理" class="headerlink" title="算法原理"></a>算法原理</h1><p>提升算法的思想很简单：对于一个分类任务，一个专家说的结果可能不太可靠，但一群专家说的结果总是大体可靠的。即：多个专家说的话比一个专家说的话可信，多个分类器共同分类的结果要好于一个分类器分类的结果。</p><p>而训练一个弱分类器总比训练一个强分类器方便的多。那么针对一个训练集，提升方法就是先学习一个弱分类器，从弱分类器出发，改变训练数据的权重，反复学习到一系列的弱分类器，将这些弱分类器组合成一个强分类器。</p><p>这样，就导出了两个问题：</p><ol><li>反复训练时如何更新数据的权重；</li><li>如何将多个弱分类器组成一个强分类器；</li></ol><p>对于第一个问题：提高被前一轮分类器分类错误样本的权重，这样这些样本会在下一轮的弱分类器中受到更大的关注，于是分类问题将被一系列的弱分类器分而治之。</p><p>对于第二个问题：AdaBoost采用了加权多数表决的方法，即：加大分类误差率小的分类器的权重，降低分类误差率大的分类器的权重。使正确率高的分类器在表决的过程中起到更加关键的作用。</p><h1 id="算法推导"><a href="#算法推导" class="headerlink" title="算法推导"></a>算法推导</h1><p>输入：训练数据集$T\{(x_1, y_1), (x_n, y_n), \cdots ,(x_n, y_n)\}$，假设为二分类任务，标签为$\{-1, +1\}$。输出：最终的分类器 $G(x)$。</p><ol><li>初始换训练数据的权重，均匀分布权重。<script type="math/tex; mode=display">D=(w_{11}, w_{1i}, \cdots, w_{1n}), w_{1i}=1/N,i=1, 2, ..., N</script></li><li>设有$M$个弱分类器，对于$m=1, 2, …, M$：</li><li>使用权重 $D_m$ 的分类器训练数据，得到基本分类器 $G_m$</li><li>计算$G_m$ 在训练数据集合中的误差，误差率是分类错误样本的权重和。<script type="math/tex; mode=display">e_m=\sum_{i=1}^nP(G_m(x_i)\neq y_i)=\sum_{G_m{x_i}\neq y_i} w_{mi}</script></li><li>计算$G_m(x)$这个分类器的权重：$\alpha_m=\frac{1}{2}\log \frac{1-e_m}{e_m}$，不难得到$\log \frac{1-x}{x}$为减函数，误差越小，权重越大</li><li><p>更新数据集的权重分布，有点softmax的意思，确定所有样本的权重和为1且为正数：</p><script type="math/tex; mode=display">D_{m+1}=(w_{m+1, 1}, w_{m+1, i}, \cdots, w_{m+1, N})</script><script type="math/tex; mode=display">w_{m+1, i}=\frac{w_{m, i}}{Z_m}\exp (-\alpha_m y_i G_m(x_i))</script><script type="math/tex; mode=display">Z_m=\sum_{i=1}^Nw_{m, i}\exp (-\alpha_m y_i G_m(x_i))</script><p>当分类正确时，$y_i G_m(x_i)$同号，权重被减小；当分类错误时，权重放大，受到下一轮分类器的关注。</p></li><li><p>构建线性组合器：$f(x)=\sum_{i=1}^N\alpha_mG_m(x)$</p></li><li><p>得到最终分类器：$G(x)=\text{sign}(f(x))$，结果由$M$个分类器加权表决。</p></li></ol><h1 id="算法解释"><a href="#算法解释" class="headerlink" title="算法解释"></a>算法解释</h1><p>其实，AdaBoost本质上为一种前向加法模型。即每次只优化当前的一步，后一步根据前一步的结果继续优化，得到最终的分类器。</p><h2 id="前向分布算法"><a href="#前向分布算法" class="headerlink" title="前向分布算法"></a>前向分布算法</h2><p>考虑加法模型</p><script type="math/tex; mode=display">\begin{equation}f(x)=\sum_{m=1}^M\beta_mb(x;\theta_m)\end{equation}</script><p>$b(x;\theta_m)$为基函数，$\theta_m$为函数的参数，$\beta_m$为基函数的权重。在给定训练数据和损失函数的条件下，学习加法模型$f(x)$成为损失函数极小化的问题：</p><script type="math/tex; mode=display">\begin{equation}\min_{\beta_m, \theta_m}\sum_{i=1}^N(y_i, \sum_{m=1}^M\beta_mb(x;\theta_m))\end{equation}</script><p>这是一个复杂的优化问题，前向步算法求解此类问题的思路是：每一次只优化一个基函数和系数，逐步逼近优化目标，那么就可以简化优化的复杂度。因此每一步的极小化损失函数为：</p><script type="math/tex; mode=display">\begin{equation}(\beta_m, \theta_m)=\text{argmin}_{\beta_m, \theta_m}\sum_{i=1}^NL(y_i, f_{m-1}(x_i)+\beta_{m-1}b(x_i;\theta_{m-1}))\end{equation}</script><p>得到新的参数$\theta_m, \beta_m$，更新当前加法模型为</p><script type="math/tex; mode=display">\begin{equation}f_m(x)=f_{m-1}(x)+\beta_mb(x;\theta_m)\end{equation}</script><p>得到最终的加法模型：</p><script type="math/tex; mode=display">\begin{equation}f(x)=f_M(x)=\sum_{m=1}^M\beta_mb(x;\theta_m)\end{equation}</script><h1 id="举例说明"><a href="#举例说明" class="headerlink" title="举例说明"></a>举例说明</h1><p>也许你公式看的很枯燥，但看个例子也就明白了，训练数据如下。目标是训练三个分类器：</p><div class="table-container"><table><thead><tr><th>$x$</th><th>0</th><th>1</th><th>2</th><th>3</th><th>4</th><th>5</th><th>6</th><th>7</th><th>8</th><th>9</th></tr></thead><tbody><tr><td>$y$</td><td>1</td><td>1</td><td>1</td><td>-1</td><td>-1</td><td>-1</td><td>1</td><td>1</td><td>1</td><td>-1</td></tr></tbody></table></div><ol><li>第一个分类器：初始化每个样本的权重为0.1，分类误差率最低时，阈值$v_1=2.5$。得到的分类器为：当$x&gt;2.5$时，$y=-1$，当$x&lt;2.5$时，$y=1$，类似一个深度与1的决策树。计算得到误差率$e_1=0.3$，跟新第一个分类器的权重$\alpha$为$\frac{1}{2}\log\frac{1-0.3}{0.3}=0.4$，更新权重分布$D_1$（太长了就不一一列举了）。</li><li>第二个分类器：利用第一轮的权重结果$D_1$，更新数据集。此时分类错误率最低时，阈值$v_2=8.5$，且$x$小于8.5时判断为1。计算误差$e_2$，计算第二个分类器的权重$\alpha_2$，计算第三轮数据的权重$D_2$。因要求是训练三个分类器，这也是最后一次更新权重。</li><li>第三个分类器：按照第二轮的权重$D_2$，再次训练，分类错误率最低时的阈值为$v_3=5.5$，且$x$大于5.5时判断为1。而后计算误差，计算第三个分类器的权重。</li></ol><p>也许你会问为什么有的时候大于有的时候小于？脑补决策树，具体是大于还是小于不是固定的。</p><p>三轮过后，得到的分类器为：</p><script type="math/tex; mode=display">\begin{equation}G(x)=\text{sign}[f_3(x)]=\text{sign}[0.4236G_1(x)+0.6496G_2(x)+0.7514G_3(x)]\end{equation}</script><p>假设，输入的$x$为8，$x&gt;v_1$第一个分类器$G_1(x)$的判断结果为-1，同理，第二个分类器的判断结果为1，第三个分类器的判断结果为1。带入$G(x)$，得到$-0.4236+0.6496+0.7514=0.97$，经过$\text{sign}$函数后取值为1，分到1那一类，且分类正确。</p><h1 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h1><p>终于到了激动人心的代码实现了，只是简单的完成二分类任务，借助sklearn实现。</p><ul><li>首先通过sklearn创建训练数据；</li><li>其次设置不同的分类器的数量，观察分类的准确性；</li><li>打印分类器的误差和权重；</li><li>记录训练时间；</li><li>我看了源代码，样本权重封装的很好，想调出来不是很容易，就不调了；</li><li>绘制图像：随着分类器数量的增加误差逐渐减小；</li><li>更多操作请参考sklearn的官网，解释都挺详细的。</li></ul><h2 id="baseline-版本"><a href="#baseline-版本" class="headerlink" title="baseline 版本"></a>baseline 版本</h2><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> AdaBoostClassifier</span><br><span class="line"><span class="keyword">from</span> sklearn.datasets <span class="keyword">import</span> make_hastie_10_2</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建8000数据 4个特征 2个类</span></span><br><span class="line">X, Y = make_hastie_10_2(n_samples=<span class="number">12000</span>, random_state=<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">n_split = <span class="number">2000</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 分类器的个数</span></span><br><span class="line">num_estimators = [<span class="number">1</span>, <span class="number">5</span>, <span class="number">20</span>, <span class="number">50</span>, <span class="number">100</span>, <span class="number">200</span>]</span><br><span class="line"></span><br><span class="line">X_train, X_test = X[n_split:], X[:n_split]</span><br><span class="line">Y_train, Y_test = Y[n_split:], Y[:n_split]</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> num <span class="keyword">in</span> num_estimators:</span><br><span class="line">    clf = AdaBoostClassifier(n_estimators=num,</span><br><span class="line">                             random_state=<span class="number">0</span>,</span><br><span class="line">                             algorithm=<span class="string">'SAMME'</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 记录算法的训练时间</span></span><br><span class="line">    start = time.time()</span><br><span class="line">    clf.fit(X_train, Y_train)</span><br><span class="line">    end = time.time()</span><br><span class="line"></span><br><span class="line">    score = clf.score(X_train, Y_train)</span><br><span class="line"></span><br><span class="line">    print(<span class="string">'Estimators ===&gt;'</span>, num, <span class="string">' Accuracy ===&gt;'</span>, score, <span class="string">'Time ===&gt;'</span>, round(end - start, <span class="number">2</span>), <span class="string">'s'</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (num == <span class="number">5</span>):</span><br><span class="line">        print(<span class="string">'Errors '</span>, clf.estimator_errors_)</span><br><span class="line">        print(<span class="string">'Weights '</span>, clf.estimator_weights_)</span><br><span class="line">        print(clf.base_estimator_)</span><br></pre></td></tr></table></figure><p>输出结果如下：<br><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Estimators ===&gt; 1  Accuracy ===&gt; 0.5513 Time ===&gt; 0.01 s</span><br><span class="line">Estimators ===&gt; 5  Accuracy ===&gt; 0.6194 Time ===&gt; 0.06 s</span><br><span class="line">Estimators ===&gt; 20  Accuracy ===&gt; 0.7108 Time ===&gt; 0.23 s</span><br><span class="line">Estimators ===&gt; 50  Accuracy ===&gt; 0.8166 Time ===&gt; 0.55 s</span><br><span class="line">Estimators ===&gt; 100  Accuracy ===&gt; 0.8614 Time ===&gt; 1.1 s</span><br><span class="line">Estimators ===&gt; 200  Accuracy ===&gt; 0.8871 Time ===&gt; 2.23 s</span><br></pre></td></tr></table></figure></p><p>可见，对于较多的分类器，准确性有所提升。且，能在很短的时间内执行处不错的分类结果。当只有5个分类器时，我们打印一下每个分类器的误差和权重，并打印当前分类器，即选择了什么分类器。输出如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Errors  [0.4487     0.46195913 0.45847958 0.46397641 0.45167916]</span><br><span class="line">Weights  [0.20592461 0.15245809 0.16646502 0.14434448 0.19388849]</span><br><span class="line">DecisionTreeClassifier(ccp_alpha=0.0, class_weight=None, criterion=&apos;gini&apos;,</span><br><span class="line">                       max_depth=1, max_features=None, max_leaf_nodes=None,</span><br><span class="line">                       min_impurity_decrease=0.0, min_impurity_split=None,</span><br><span class="line">                       min_samples_leaf=1, min_samples_split=2,</span><br><span class="line">                       min_weight_fraction_leaf=0.0, presort=&apos;deprecated&apos;,</span><br><span class="line">                       random_state=None, splitter=&apos;best&apos;)</span><br></pre></td></tr></table></figure><p>由上不难看出选择的分类器为：深度为一的选用基尼系数进行决策的决策树。</p><h2 id="决策树版本"><a href="#决策树版本" class="headerlink" title="决策树版本"></a>决策树版本</h2><p>既然选用了决策树，何不增加一下决策树的深度？以此来提高分类的准确性。代码如下：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> AdaBoostClassifier</span><br><span class="line"><span class="keyword">from</span> sklearn.tree <span class="keyword">import</span> DecisionTreeClassifier</span><br><span class="line"><span class="keyword">from</span> sklearn.datasets <span class="keyword">import</span> make_hastie_10_2</span><br><span class="line"></span><br><span class="line"><span class="comment"># Construct dataset</span></span><br><span class="line"><span class="comment"># 创建8000数据 4个特征 2个类</span></span><br><span class="line">X, Y = make_hastie_10_2(n_samples=<span class="number">12000</span>, random_state=<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">n_split = <span class="number">2000</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 分类器的个数</span></span><br><span class="line">num_estimators = [<span class="number">1</span>, <span class="number">5</span>, <span class="number">20</span>, <span class="number">50</span>, <span class="number">100</span>]</span><br><span class="line"></span><br><span class="line">X_train, X_test = X[n_split:], X[:n_split]</span><br><span class="line">Y_train, Y_test = Y[n_split:], Y[:n_split]</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> num <span class="keyword">in</span> num_estimators:</span><br><span class="line">    <span class="comment"># Create and fit an AdaBoosted decision tree</span></span><br><span class="line">    bdt = AdaBoostClassifier(DecisionTreeClassifier(max_depth=<span class="number">4</span>),</span><br><span class="line">                            algorithm=<span class="string">"SAMME"</span>,</span><br><span class="line">                            n_estimators=num)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 记录算法的训练时间</span></span><br><span class="line">    start = time.time()</span><br><span class="line">    bdt.fit(X_train, Y_train)</span><br><span class="line">    end = time.time()</span><br><span class="line"></span><br><span class="line">    score = bdt.score(X_test, Y_test)</span><br><span class="line"></span><br><span class="line">    print(<span class="string">'Estimators ===&gt;'</span>, num, <span class="string">' Accuracy ===&gt;'</span>, score, <span class="string">'Time ===&gt;'</span>, round(end - start, <span class="number">2</span>), <span class="string">'s'</span>)</span><br><span class="line"></span><br><span class="line">print(bdt.base_estimator_)</span><br></pre></td></tr></table></figure><p>输出如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Estimators ===&gt; 1  Accuracy ===&gt; 0.6295 Time ===&gt; 0.03 s</span><br><span class="line">Estimators ===&gt; 5  Accuracy ===&gt; 0.741 Time ===&gt; 0.2 s</span><br><span class="line">Estimators ===&gt; 20  Accuracy ===&gt; 0.841 Time ===&gt; 0.77 s</span><br><span class="line">Estimators ===&gt; 50  Accuracy ===&gt; 0.9105 Time ===&gt; 1.92 s</span><br><span class="line">Estimators ===&gt; 100  Accuracy ===&gt; 0.937 Time ===&gt; 3.86 s</span><br><span class="line">DecisionTreeClassifier(ccp_alpha=0.0, class_weight=None, criterion=&apos;gini&apos;,</span><br><span class="line">                       max_depth=4, max_features=None, max_leaf_nodes=None,</span><br><span class="line">                       min_impurity_decrease=0.0, min_impurity_split=None,</span><br><span class="line">                       min_samples_leaf=1, min_samples_split=2,</span><br><span class="line">                       min_weight_fraction_leaf=0.0, presort=&apos;deprecated&apos;,</span><br><span class="line">                       random_state=None, splitter=&apos;best&apos;)</span><br></pre></td></tr></table></figure><p>可见，在增加决策树深度后，准确性有了明显提升。</p><h2 id="误差分析"><a href="#误差分析" class="headerlink" title="误差分析"></a>误差分析</h2><p><img data-src="/2020/04/02/adaboost/error_rate.png" alt></p><p>如图所示，随着分类器数量的增加，误差率逐渐下降并稳定在一个区间内。因为提升算法是有训练误差的界限的，其训练误差为：</p><script type="math/tex; mode=display">\begin{equation}\frac{1}{N}\sum_{i=1}^NI(G(x_i) != y_i) \leq \frac{1}{N}\sum_{i=1}\exp (-y_iG(x_i)) = \frac{1}{N}\sum_{i=1}\exp(-\sum_{m=1}^M\alpha_my_iG_m(x_i))\end{equation}</script><p>上述误差式的取值也可以表示为：$\prod_mZ_m$。绘图的代码如下：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> AdaBoostClassifier</span><br><span class="line"><span class="keyword">from</span> sklearn.tree <span class="keyword">import</span> DecisionTreeClassifier</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> zero_one_loss</span><br><span class="line"><span class="keyword">from</span> sklearn <span class="keyword">import</span> datasets</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line"><span class="comment"># Construct dataset</span></span><br><span class="line"><span class="comment"># 创建8000数据 4个特征 2个类</span></span><br><span class="line">X, Y = datasets.make_hastie_10_2(n_samples=<span class="number">12000</span>, random_state=<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">n_split = <span class="number">2000</span></span><br><span class="line"></span><br><span class="line">X_train, X_test = X[n_split:], X[:n_split]</span><br><span class="line">Y_train, Y_test = Y[n_split:], Y[:n_split]</span><br><span class="line"></span><br><span class="line">train_errors = []</span><br><span class="line">test_errors = []</span><br><span class="line"></span><br><span class="line">num = <span class="number">350</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Create and fit an AdaBoosted decision tree</span></span><br><span class="line">bdt = AdaBoostClassifier(DecisionTreeClassifier(max_depth=<span class="number">3</span>),</span><br><span class="line">                        algorithm=<span class="string">"SAMME"</span>,</span><br><span class="line">                        n_estimators=num)</span><br><span class="line"></span><br><span class="line">bdt.fit(X_train, Y_train)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> train_error, test_error <span class="keyword">in</span> zip(bdt.staged_predict(X_train), bdt.staged_predict(X_test)):</span><br><span class="line">    train_errors.append(zero_one_loss(train_error, Y_train))</span><br><span class="line">    test_errors.append(zero_one_loss(test_error, Y_test))</span><br><span class="line"></span><br><span class="line">x = np.linspace(<span class="number">1</span>, num, num)</span><br><span class="line">plt.style.use(<span class="string">'ggplot'</span>)</span><br><span class="line">plt.plot(x, train_errors, <span class="string">'r'</span>, label=<span class="string">'SAMME Train Error'</span>)</span><br><span class="line">plt.plot(x, test_errors, <span class="string">'b'</span>, label=<span class="string">'SAMME Test Error'</span>)</span><br><span class="line">plt.legend(loc=<span class="string">'upper right'</span>, fancybox=<span class="literal">True</span>)</span><br><span class="line">plt.xlabel(<span class="string">'Eestimator Numbers'</span>)</span><br><span class="line">plt.ylabel(<span class="string">'Error Rate'</span>)</span><br><span class="line"><span class="comment"># plt.show()</span></span><br><span class="line">plt.savefig(<span class="string">'error_rate.png'</span>, dpi=<span class="number">250</span>)</span><br></pre></td></tr></table></figure><h1 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h1><p>提升方法还有更多的应用空间，如提升树，回归树。梯度提升等。即可以在一个分类器中建立更多的层数，而不只是分为简单的$x&gt;v_1$时和$x&lt;v_1$时，用更多的层数来提高分类的准确性。</p><p>甚至可以和神经网络相结合，给不同的网络赋予不同的权重，最终决策。</p><p>为什么不在这里写？因为我忘了决策树里的一些东西，也忘记了回归树如何实现，等下次再写吧。</p><p>还有一点，本文提到的算法已经过时，本文的是<code>SAMME</code>版本的算法，目前已经发布<code>SAMME.R</code>版本的算法且准确率更高，也等下次再写吧。</p><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p>站在巨人的肩膀上，我们能走得更好：</p><ol><li><a href="https://scikit-learn.org/stable/modules/generated/sklearn.ensemble.AdaBoostClassifier.html" target="_blank" rel="noopener">https://scikit-learn.org/stable/modules/generated/sklearn.ensemble.AdaBoostClassifier.html</a></li><li><a href="https://scikit-learn.org/stable/modules/generated/sklearn.datasets.make_classification.html" target="_blank" rel="noopener">https://scikit-learn.org/stable/modules/generated/sklearn.datasets.make_classification.html</a></li><li><a href="https://stackoverflow.com/questions/31981453/why-estimator-weight-in-samme-r-adaboost-algorithm-is-set-to-1" target="_blank" rel="noopener">https://stackoverflow.com/questions/31981453/why-estimator-weight-in-samme-r-adaboost-algorithm-is-set-to-1</a></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在多年前的某次任务中，看到了别人使用提升树等算法，取得了比较好的效果。今日眼馋，特来学习提升方法，并记录于此。提升方法是一种统计学习方法。在分类问题中，通过改变训练样本的权重，学习多个分类器，将多个分类器进行线性组合，以提升分类的性能。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/2020/04/02/adaboost/error_rate.png&quot; alt&gt;&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="Algorithm" scheme="https://muyuuuu.github.io/tags/Algorithm/"/>
    
  </entry>
  
  <entry>
    <title>核密度聚类</title>
    <link href="https://muyuuuu.github.io/2020/03/30/KDE-cluster/"/>
    <id>https://muyuuuu.github.io/2020/03/30/KDE-cluster/</id>
    <published>2020-03-30T10:11:13.000Z</published>
    <updated>2020-03-30T14:15:09.806Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>当问题需要自动地确定聚类数目时，传统的KMeans等聚类方法不在适用。因此，使用“核概率密度估计”的思路自行设计了两种聚类方法。本文收录：</p><ul><li>核是什么</li><li>核密度估计</li><li>基于核密度估计的两种聚类方法</li><li>代码实现</li></ul><a id="more"></a><h1 id="核函数"><a href="#核函数" class="headerlink" title="核函数"></a>核函数</h1><p>有一些数据，想“看看”它长什么样，基于高中的知识，我们一般会画频率分布直方图（Histogram）。但基于大学的知识，此时也可以用核密度估计，因为之前的知识水平让我们默认为频率等于概率，但实际情况不一定如此。</p><p>这里的“核”是一个函数，用来提供权重。例如高斯函数 (Gaussian) 就是一个常用的核函数。举个例子，假设我们现在想买房，钱不够要找亲戚朋友借，我们用一个数组来表示 5 个亲戚的财产状况： [8, 2, 5, 6, 4]。我们是中间这个数 5。“核”是一个权重函数，假设这个核函数的取值为[0.1, 0.4, 1, 0.3, 0.2]，以此来代表同的亲戚朋友亲疏有别。</p><p>在借钱的时候，关系好的朋友出力多，关系不好的朋友出力少，于是我们可以用权重来表示。总共能借到的钱是： $8\times 0.1 + 2\times 0.4 + 5\times 1 + 6\times 0.3 + 4\times 0.2 = 9.2$。</p><p>那么“核”的作用就是用来决定权重，例如高斯函数（即正态分布），呈现出距离中心越远，权重越小；反之，距离中心越近，权重越大的特点。核函数还有以下重要的性质：</p><ul><li>$\int_{-\infty}^{+\infty}K(x)\text{d}x=1$</li><li>$K(-x)=K(x)$</li></ul><h1 id="核密度估计"><a href="#核密度估计" class="headerlink" title="核密度估计"></a>核密度估计</h1><p>如果我们画频率分布直方图，其实目的是画出“概率密度函数”，因为统计直方图的组距无限接近于0时，得到的函数就是概率密度函数。</p><p>但直方图本质上是认为频率等于概率，但这种假设不是必然的。</p><p>核密度估计方法是一种非参数估计方法，非参数估计法指：如果一个估计问题所涉及的分布未知或不能用有穷参数来刻划，称这种估计为非参数估计。非参数估计是一种“平滑(smooth)”的手段：设 $(x_1,x_2,\cdots,x_n)$ 是独立同分布的 $n$ 个样本点，于是我们的估计：</p><script type="math/tex; mode=display">\hat{p}(x)=\frac{1}{n}\sum_{i=1}^nK_h(x-x_i)=\frac{1}{nh}\sum_{i=1}^nK(\frac{x-x_i}{h})</script><p>上面式子中 $x−x_i$ 要提前归一化到 $[-1, 1]$ 之间。</p><p>这个公式的意思相当于是<strong>“我说的你可能不信，一起看看别人怎么说我的你就信了”</strong>。别人是指除自己外的其他样本点，距离越远的样本点权重越小，说话没有分量；距离样本点越近则权重越大，说话的可信度也就越高。当然权重的大小取决于实际采用的核函数，也可能距离越远权重越大，这里只是举个例子。</p><p>其中距离的远近通过$x-x_i$的大小来衡量。相当于加权平均后，就能更好地了解我了，于是乎得到新的函数值作为这一点概率的取值。$h$ 是人为指定的，代表“朋友圈”的大小，正式的叫法是“带宽”(bandwidth)，且带宽越大曲线越平滑。</p><p>看一下核密度估计和频率分布直方图就能更好的理解了，蓝色的线为概率密度估计，橙色的柱状图为数据出现的频率统计。</p><p><img data-src="/2020/03/30/KDE-cluster/1.png" alt></p><h1 id="核密度聚类"><a href="#核密度聚类" class="headerlink" title="核密度聚类"></a>核密度聚类</h1><p>当然文章到这里还没有结束，要是到这里结束我就真成了抄袭文章的了。这组数据看上去很有聚类的趋势不是么，有两种聚类方法：</p><ol><li>第一种方法。取曲线中的极大值和极小值分成几个区间，将数据的出现频率完成分类，此时会将出现频率相近的数据聚合在一起。按出现频率的高低分为四个居间A,B,C,D共四个类。则A类的数据出现频率最高，D类的数据出现频率最低。如下图所示：<img data-src="/2020/03/30/KDE-cluster/2.png" alt></li><li>第二种方法。既然曲线有多个极大值和极小值，那么何不以极大值极小值为分界点进行划分区间？按极值点的横坐标取值将数据分为四个居间A,B,C,D共四个类。那么此时对数据的含义也有了要求，即：每个数据段都有不同的含义，不能以数据出现的频率进行笼统划分。如下图所示：<img data-src="/2020/03/30/KDE-cluster/3.png" alt>举个例子：图片的灰度值的取值范围是[0,255]，假设[0,50]区间数据的出现频率和[200,255]区间数据的出现频率相近，但一定不能把这两组数据放在一起，因为前者代表黑色图层，后者代表白色图层。此时应以极大值、极小值等极值点的横坐标的取值划分区间，而后进行聚类更为合适。</li><li>无论哪种方法，都能自动确定聚类数量。</li></ol><h2 id="核密度估计-1"><a href="#核密度估计-1" class="headerlink" title="核密度估计"></a>核密度估计</h2><p>本文着重实现第二种聚类方法，第一种其实同理。</p><p>首先产生一组随机数：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">N = <span class="number">1000</span></span><br><span class="line">np.random.seed(<span class="number">1</span>)</span><br><span class="line"><span class="comment"># concatenate 用来组合两种数据，第一组数据均值0方差1，数量占 30% </span></span><br><span class="line"><span class="comment"># 第二组数据均值 5 方差1 数量占 70%</span></span><br><span class="line"><span class="comment"># [:, np.newaxis] 把数据维度更新为二维一列的数据</span></span><br><span class="line">X = np.concatenate((np.random.normal(<span class="number">0</span>, <span class="number">1</span>, int(<span class="number">0.3</span> * N)), np.random.normal(<span class="number">5</span>, <span class="number">1</span>, int(<span class="number">0.7</span> * N))))[:, np.newaxis]</span><br></pre></td></tr></table></figure><p>其次调用sklearn这个库实现核密度估计即可：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">X_plot = np.linspace(<span class="number">-5</span>, <span class="number">10</span>, <span class="number">1000</span>)[:, np.newaxis]</span><br><span class="line"></span><br><span class="line">colors = [<span class="string">'blue'</span>]</span><br><span class="line">kernels = [<span class="string">'gaussian'</span>]</span><br><span class="line">lw = <span class="number">2</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> color, kernel <span class="keyword">in</span> zip(colors, kernels):</span><br><span class="line">    <span class="comment"># Fit the Kernel Density model on the data.</span></span><br><span class="line">    kde = KernelDensity(kernel=kernel, bandwidth=<span class="number">0.5</span>).fit(X)</span><br><span class="line">    <span class="comment"># Evaluate the log density model on the data. </span></span><br><span class="line">    log_dens = kde.score_samples(X_plot)</span><br><span class="line">    plt.plot(X_plot[:, <span class="number">0</span>], np.exp(log_dens), color=color, lw=lw,</span><br><span class="line">            linestyle=<span class="string">'-'</span>, label=<span class="string">"kernel = '&#123;0&#125;'"</span>.format(kernel))</span><br><span class="line">    arr = np.exp(log_dens)</span><br><span class="line">    np.save(<span class="string">'data.npy'</span>, arr)</span><br></pre></td></tr></table></figure><ul><li>X_plot ：An array of points to query；</li><li>kde = KernelDensity(kernel=kernel, bandwidth=0.5).fit(X) 完成训练工作，计算核密度；</li><li>log_dens = kde.score_samples(X_plot) 评估这点的概率值该是多少。画图是连续的曲线，曲线上的点不一定在样本中，所以要弥补一些缺失的点；</li><li>np.exp 是因为kde.score_samples反馈结果的概率值取了ln(可参看源码实现)，所以计算真实概率值要exp一下；</li><li>保存数据用于后期聚类处理。</li></ul><p>完整代码如下：<br><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">from</span> sklearn.neighbors <span class="keyword">import</span> KernelDensity</span><br><span class="line"><span class="keyword">from</span> scipy.stats <span class="keyword">import</span> norm</span><br><span class="line"></span><br><span class="line">N = <span class="number">1000</span></span><br><span class="line">np.random.seed(<span class="number">1</span>)</span><br><span class="line">X = np.concatenate((np.random.normal(<span class="number">0</span>, <span class="number">1</span>, int(<span class="number">0.3</span> * N)), np.random.normal(<span class="number">5</span>, <span class="number">1</span>, int(<span class="number">0.7</span> * N))))[:, np.newaxis]</span><br><span class="line"></span><br><span class="line">X_plot = np.linspace(<span class="number">-5</span>, <span class="number">10</span>, <span class="number">1000</span>)[:, np.newaxis]</span><br><span class="line"></span><br><span class="line">colors = [<span class="string">'blue'</span>]</span><br><span class="line">kernels = [<span class="string">'gaussian'</span>]</span><br><span class="line">lw = <span class="number">2</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> color, kernel <span class="keyword">in</span> zip(colors, kernels):</span><br><span class="line">    kde = KernelDensity(kernel=kernel, bandwidth=<span class="number">0.5</span>).fit(X)</span><br><span class="line">    log_dens = kde.score_samples(X_plot)</span><br><span class="line">    plt.plot(X_plot[:, <span class="number">0</span>], np.exp(log_dens), color=color, lw=lw,</span><br><span class="line">            linestyle=<span class="string">'-'</span>, label=<span class="string">"kernel = '&#123;0&#125;'"</span>.format(kernel))</span><br><span class="line">    arr = np.exp(log_dens)</span><br><span class="line">    np.save(<span class="string">'data.npy'</span>, arr)</span><br><span class="line"></span><br><span class="line">plt.legend(loc=<span class="string">'upper left'</span>)</span><br><span class="line">plt.hist(X, <span class="number">70</span>, normed = <span class="number">1</span>, histtype=<span class="string">'bar'</span>, facecolor=<span class="string">'salmon'</span>, rwidth=<span class="number">0.9</span>)  </span><br><span class="line"><span class="comment"># plt.set_xlim(-4, 9)</span></span><br><span class="line"><span class="comment"># plt.set_ylim(-0.02, 0.4)</span></span><br><span class="line"></span><br><span class="line">plt.title(<span class="string">"$N$ = 1000 points"</span>, fontsize=<span class="number">16</span>)</span><br><span class="line">plt.xlabel(<span class="string">"Data"</span>, fontsize=<span class="number">14</span>)</span><br><span class="line">plt.ylabel(<span class="string">"Density Function"</span>, fontsize=<span class="number">14</span>)</span><br><span class="line">plt.savefig(<span class="string">'test.png'</span>, dpi=<span class="number">300</span>)</span><br></pre></td></tr></table></figure></p><h2 id="求极值以聚类"><a href="#求极值以聚类" class="headerlink" title="求极值以聚类"></a>求极值以聚类</h2><p>之后借助scipy求出核密度估计的点中的几个极大值和极小值（极值并不唯一）</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">from</span> scipy.signal <span class="keyword">import</span> argrelextrema</span><br><span class="line"><span class="comment"># 加载数据</span></span><br><span class="line">data = np.load(<span class="string">'data.npy'</span>)</span><br><span class="line"><span class="comment"># 求极大值</span></span><br><span class="line">maxn = argrelextrema(data, np.greater)</span><br><span class="line"><span class="comment"># 求极小值</span></span><br><span class="line">minn = argrelextrema(data, np.less)</span><br><span class="line"><span class="comment"># 打印极大值的索引</span></span><br><span class="line">print(maxn)</span><br><span class="line"><span class="comment"># 打印极小值的索引</span></span><br><span class="line">print(minn)</span><br></pre></td></tr></table></figure><p>输出结果如下：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">(array([<span class="number">340</span>, <span class="number">667</span>], dtype=int64),)</span><br><span class="line">(array([<span class="number">480</span>], dtype=int64),)</span><br></pre></td></tr></table></figure><p>得到的结果显示有两个极大值两个极小值，与概率密度估计图象相吻合。按照返回的极值索引访问极值即可。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">print(data[minn])</span><br></pre></td></tr></table></figure><p>进而按照极值划分数据，即可完成聚类。</p><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p>站在巨人的肩膀上，我们能更好的前行：</p><ol><li><a href="https://lotabout.me/2018/kernel-density-estimation/" target="_blank" rel="noopener">https://lotabout.me/2018/kernel-density-estimation/</a></li><li><a href="https://spaces.ac.cn/archives/3785" target="_blank" rel="noopener">https://spaces.ac.cn/archives/3785</a></li><li><a href="https://scikit-learn.org/stable/auto_examples/neighbors/plot_kde_1d.html" target="_blank" rel="noopener">https://scikit-learn.org/stable/auto_examples/neighbors/plot_kde_1d.html</a></li><li><a href="https://scikit-learn.org/stable/modules/generated/sklearn.neighbors.KernelDensity.html" target="_blank" rel="noopener">https://scikit-learn.org/stable/modules/generated/sklearn.neighbors.KernelDensity.html</a></li><li><a href="https://docs.scipy.org/doc/scipy/referenc/generated/scipy.signal.argrelextrema.html" target="_blank" rel="noopener">https://docs.scipy.org/doc/scipy/referenc/generated/scipy.signal.argrelextrema.html</a></li><li><a href="https://docs.scipy.org/doc/numpy/reference/generated/numpy.concatenate.html" target="_blank" rel="noopener">https://docs.scipy.org/doc/numpy/reference/generated/numpy.concatenate.html</a></li><li><a href="https://docs.scipy.org/doc/numpy/reference/generated/numpy.expand_dims.html" target="_blank" rel="noopener">https://docs.scipy.org/doc/numpy/reference/generated/numpy.expand_dims.html</a></li></ol><p>你也可以看到我查看翻阅了大量的官方文档，其实对于这种技术东西，官方文档的解释往往是最好的，不懂的问题就多动动手多思考嘛，哪能不懂就问。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;当问题需要自动地确定聚类数目时，传统的KMeans等聚类方法不在适用。因此，使用“核概率密度估计”的思路自行设计了两种聚类方法。本文收录：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;核是什么&lt;/li&gt;
&lt;li&gt;核密度估计&lt;/li&gt;
&lt;li&gt;基于核密度估计的两种聚类方法&lt;/li&gt;
&lt;li&gt;代码实现&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
    
    
      <category term="Algorithm" scheme="https://muyuuuu.github.io/tags/Algorithm/"/>
    
  </entry>
  
  <entry>
    <title>栈的应用——判断pop序列是否合理</title>
    <link href="https://muyuuuu.github.io/2020/03/28/stack-pop-sequence/"/>
    <id>https://muyuuuu.github.io/2020/03/28/stack-pop-sequence/</id>
    <published>2020-03-28T09:26:55.000Z</published>
    <updated>2020-03-28T10:26:36.626Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>栈有着广泛的应用，比如逆波兰表达式，表达式求值，迷宫问题求解，程序调用等，不过这些问题今天都不涉及哈哈哈哈。关于程序执行期间，各个函数的相互调用使用的调用栈，可以<a href="https://muyuuuu.github.io/2019/09/22/stack/">参考这里</a>。</p><p>也许在上数据结构这门课的时候，经常遇到一个问题，一个栈压入一个序列，随缘pop，判断哪个是合理的pop，哪个是不合理的pop，之前都是脑内演算。很巧的今天我在刷题期间也遇到了，记录一下如何用代码判断pop序列是否合理。</p><a id="more"></a><h1 id="题目"><a href="#题目" class="headerlink" title="题目"></a>题目</h1><p>Given a stack which can keep $M$ numbers at most. Push $N$ numbers in the order of 1, 2, 3, …, $N$ and pop randomly. You are supposed to tell if a given sequence of numbers is a possible pop sequence of the stack. </p><p>Each input file contains one test case. For each case, the first line contains 3 numbers (all no more than 1000): $M$ (the maximum capacity of the stack), $N$ (the length of push sequence), and $K$ (the number of pop sequences to be checked). Then $K$ lines follow, each contains a pop sequence of $N$ numbers. All the numbers in a line are separated by a space.</p><p>For each pop sequence, print in one line “YES” if it is indeed a possible pop sequence of the stack, or “NO” if not.</p><h2 id="示例输入"><a href="#示例输入" class="headerlink" title="示例输入"></a>示例输入</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">5 7 5</span><br><span class="line">1 2 3 4 5 6 7</span><br><span class="line">3 2 1 7 5 6 4</span><br><span class="line">7 6 5 4 3 2 1</span><br><span class="line">5 6 4 3 7 2 1</span><br><span class="line">1 7 6 5 4 3 2</span><br></pre></td></tr></table></figure><h2 id="示例输出"><a href="#示例输出" class="headerlink" title="示例输出"></a>示例输出</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">YES</span><br><span class="line">NO</span><br><span class="line">NO</span><br><span class="line">YES</span><br><span class="line">NO</span><br></pre></td></tr></table></figure><p>众所周知，栈是先进先出的一种数据结构，进栈为push，出栈为pop。题目的大概意思就是给定栈的最大空间，压入的序列和待检查的pop序列，因为栈是随缘pop的，判断哪个pop序列是正确的，哪个pop序列是错误的。</p><h1 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h1><h2 id="脑内演算"><a href="#脑内演算" class="headerlink" title="脑内演算"></a>脑内演算</h2><ul><li>对于序列 1, 2, 3, 4, 5, 6, 7， 很容易得到栈是如何工作的，即压入一个元素立刻弹出即可。push 1，pop 1，push 2，pop 2，一直到push 7，pop 7。</li><li>对于序列 5, 6, 4, 3, 7, 2, 1，也可以得到栈的工作顺序为：push 1, 2, 3, 4, 5，而后pop 5，而后push 6 pop 6，pop 4，pop 3，push 7 pop 7，pop 2，pop 1。</li><li>对于序列 3, 2, 1, 7, 5, 6, 4，如果pop 7后，后面的顺序必然是 6，5，4而不可能是5，6，4。因此这个序列是错误的。</li><li>对于序列 7, 6, 5, 4, 3, 2, 1，在栈空间为5的情况下，不可能第一次就pop 7，因此这个序列是错误的。</li></ul><h2 id="一般化"><a href="#一般化" class="headerlink" title="一般化"></a>一般化</h2><p>在脑内演算后，也很容易发现题的难点在于不确定性的pop，可能在第一次pop，也可能在第三次pop，且每次pop几个元素也是不确定的。</p><p>我们实现知道了元素的进栈顺序，是1，2，3，4，…，$N$这样的顺序序列，所以，如果想办法<strong>按着它给出的序列模拟压栈和弹栈</strong>，如果能模拟出来，说明序列正确，模拟不出来说明序列错误。</p><ul><li><p>第一步，创建一个序列，读取题目示例输入的序列：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">"%d"</span>, &amp;arr[i]);</span><br></pre></td></tr></table></figure></li><li><p>第二步，创建一个栈，并按元素的输入顺序组织元素进栈，开始按读取序列元素的顺序进行压栈和弹栈。</p></li><li><p>对输入序列设立标志位，并初始化标志位为1。当刚进栈的元素和输入序列标志位的元素相等时，说明此时元素要弹栈，则执行弹栈操作，且标志位自增向后移动；若不相等，则继续组织进栈，直到进栈元素和输入序列中标志位的元素相等。若一直不相等，则表明此序列错误。因此核心算法程序如下：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 设立标志位</span></span><br><span class="line"><span class="keyword">int</span> pos = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">1</span>; j &lt;= n; j++) &#123;</span><br><span class="line">    <span class="comment">// 按元素顺序组织进栈</span></span><br><span class="line">    s.push(j);</span><br><span class="line">    <span class="comment">// 超过栈允许的最大空间</span></span><br><span class="line">    <span class="keyword">if</span>(s.<span class="built_in">size</span>() &gt; m) <span class="keyword">break</span>;</span><br><span class="line">    <span class="comment">// 按序列元素的弹栈顺序开始模拟</span></span><br><span class="line">    <span class="comment">// 栈非空且栈顶元素和当前标志位的元素相等</span></span><br><span class="line">    <span class="keyword">while</span>(!s.empty() &amp;&amp; s.top() == arr[pos]) &#123;</span><br><span class="line">        <span class="comment">// 弹栈</span></span><br><span class="line">        s.pop();</span><br><span class="line">        <span class="comment">// 标志位自增</span></span><br><span class="line">        pos++;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h1 id="程序"><a href="#程序" class="headerlink" title="程序"></a>程序</h1><p>得到最终的程序如下：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stack&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> arr[<span class="number">1002</span>];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> <span class="keyword">const</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> m, n, k;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; m &gt;&gt; n &gt;&gt; k;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; k; j++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">            <span class="built_in">scanf</span>(<span class="string">"%d"</span>, &amp;arr[i]);</span><br><span class="line">        <span class="keyword">int</span> flag = <span class="number">0</span>, pos = <span class="number">1</span>;</span><br><span class="line">        <span class="built_in">stack</span> &lt;<span class="keyword">int</span>&gt; s;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            s.push(i);</span><br><span class="line">            <span class="keyword">if</span> (s.<span class="built_in">size</span>() &gt; m) <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">while</span> (!s.empty() &amp;&amp; s.top() == arr[pos])</span><br><span class="line">            &#123;</span><br><span class="line">                s.pop();</span><br><span class="line">                pos += <span class="number">1</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (pos == n + <span class="number">1</span>) flag = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span> (flag == <span class="number">1</span>) <span class="built_in">printf</span>(<span class="string">"YES\n"</span>);</span><br><span class="line">        <span class="keyword">else</span> <span class="built_in">printf</span>(<span class="string">"NO\n"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img data-src="/2020/03/28/stack-pop-sequence/1.png" alt></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;栈有着广泛的应用，比如逆波兰表达式，表达式求值，迷宫问题求解，程序调用等，不过这些问题今天都不涉及哈哈哈哈。关于程序执行期间，各个函数的相互调用使用的调用栈，可以&lt;a href=&quot;https://muyuuuu.github.io/2019/09/22/stack/&quot;&gt;参考这里&lt;/a&gt;。&lt;/p&gt;
&lt;p&gt;也许在上数据结构这门课的时候，经常遇到一个问题，一个栈压入一个序列，随缘pop，判断哪个是合理的pop，哪个是不合理的pop，之前都是脑内演算。很巧的今天我在刷题期间也遇到了，记录一下如何用代码判断pop序列是否合理。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="DataStructure" scheme="https://muyuuuu.github.io/tags/DataStructure/"/>
    
  </entry>
  
  <entry>
    <title>大学毕业的前夕</title>
    <link href="https://muyuuuu.github.io/2020/03/20/college-graduation/"/>
    <id>https://muyuuuu.github.io/2020/03/20/college-graduation/</id>
    <published>2020-03-20T15:43:08.000Z</published>
    <updated>2020-04-12T08:11:28.202Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>想来也快要大学毕业了，2016年唐县一中毕业，2020年某不太行大学毕业，四年了。</p><a id="more"></a><p>在我眼里学校一直是二本水平，包括好多老师也这么说。也许你会反驳说学校不是二本啊，河北省明明一本招生，可在河北省外，肉眼可见的大多省都是二本招生。出去参加了几趟夏令营深有感悟，以我的专业为例，和外面其他一本学校的的教学水平、资源差得很远很远。</p><p>当然说这些也没用，每个人都懂一个道理，<strong>就是外界环境与自身水平关系不大</strong>。就好比，写代码水平的高低与程序员用的什么牌子的电脑没有必然联系。</p><p>老师通知的时间是5月中旬毕设答辩，6月份就可以走人，想来，也就俩月了，真他娘的快。</p><ul><li>谢师宴想去吗？不想去，水平可以且认真讲课的老师超不过那么几个，何况我会的只是都靠自学，也没有老师能给我关键性的、受用的指导和教育。都是走一个流程，有什么可谢的呢？大多老师都是完成学校任务，念PPT，考试，挣钱，下一轮回。</li><li>有想见的老师吗？有啊，那种能当朋友的老师，能随意聊天不push的老师，关键时刻在的老师，当然要去见一面。大学能让老师主动的记住一个学生的名字的确不是很容易。</li><li>有想见的人吗？可能没有，似乎真的没有，有什么值得留恋的呢？没有与其他人有过多交集，也不会谈恋爱，也没对谁说过我好喜欢你，撩过的小姐姐早已悄然无踪影。</li></ul><p>高考结束后，年级下降了三百名，想死又不敢，想复读又懒，勉强能走本一，比录取分数线多两分来了这所学校。不过大抵也怪我高中的心思都不在学习上，心猿意马，青春疼痛矫情文学描述过去的我可真是再合适不过。还好许多文字已经删除，无迹可寻。而我也意识到过去的众多错误：矫情、猜忌、暴躁、偏执、自私，自我为中心等等等等，只要是个贬义词，大概率的能用在我身上。对于太多不堪回首的东西我选择了失忆，不去回忆，就真的记不起来了。后来和朋友聊起，才发现高中的我大概率是抑郁症，终日目光涣散、懒散、双目无神、行尸走肉，到处都有压迫的窒息感，窒息到胸闷心慌，盯着书都看不下去两个字。不是因为懒惰而不学习，是身体真的不允许。</p><p>高中毕业后的几个月，也还是那样的多愁善感，沉浸在自己的世界，瘦了十多斤，和几个关系好的朋友吹了吹牛逼也就开启新的生活了。来了大学里没办法，除了学习and看书我不会其他，也别无出路。大一上半学期是过渡时期，慢慢熬呗，沉迷高数和经典名著，每天都去上自习，每天晚上操场跑步，也就不在想其他的了。大概也开始摆脱自私和矫情等青春疼痛文学等缺陷。逐步的能够控制自己的情绪，这比什么都好。不再低落，除了偶尔几次的牢骚，大多时期也挺过来了，现在想来，就这？能有啥。</p><p>《明天你好》——此处应有这首音乐。</p><p>逐步自学了C语言，参加了数学建模，对计算机产生了兴趣，转专业到了计算机。学着专业课，鼓捣自己感兴趣的东西，参加着比赛，一周四次跑步，偶尔读读课外书，隔几天看一次电影，无聊了打打游戏，考试也很简单，随便背背拿个高分不成问题。大学四年就这么过去了，平淡无奇。</p><p>如果非要说什么印象深刻的事情，第一次是看到了图书馆，通过书籍，我知道了原来外面还有这么精彩的世界；第二次是看到了实验室，吾辈虽置身沟渠但仍可仰望星空；第三次是从你眼睛里看到了星河，但又转瞬成空；第四次是看到了柳神的代码；第五次是出去走走看到了外面更广阔的世界。</p><ul><li>大一上学期过渡期间的迷茫时期，用半年的时间拯救了自己。我也实在不知道也去干些什么，就一直在从学校的图书馆借书看。大一时图书馆刚刚建立，只有一层，可山不在高有仙则名啊。大一上学期我看完了《简爱》，《假如给我三天光明》，《牧羊少年的奇幻之旅》，《追风筝的人》，《麦田守望者》，《激流男孩》，《一怒之下和DH劳伦斯的决斗》和《荒野猎人》。现在想来真是不可思议，也许当初课少吧。当然学业也没落下，每晚去上晚自习，高数20分钟交卷100分，出了考场奔去图书馆看完了《荒野猎人》。看过的书就像吃过的饭，吃过的饭融入你的血液，促使你长成了今天的体格；读过的书融入你的血液，让你成为这样的人，对待事物有了不同的想法和深度。据说读书能长气质，可我没有。《假如给我三天光明》是这里面我感觉最好的吧，世间竟然还有如此清澈的情感，的确能强力洗涤当初的我污秽不堪的情感和思想，至少，不再是青春疼痛文学。<blockquote class="blockquote-center">            <p>走过黑暗的那段没人拉我一把，我自己把自己带出了深渊。 </p>          </blockquote></li><li>实验室，的确是个神奇的地方，认识了好多人，跨专业跨学院跨性别跨年级。这里并不像学生会那样，各种开会，各种活动，回复收到。好在我天生不喜欢去学生会那种地方，也丝毫不想去学生会所谓的锻炼自己，更不想参加XX举办的XX活动而后发个动态说自己多不容易或显示自己多有水平，对我的成长来说没有丝毫帮助。<strong>还是选择了实验室。</strong>每个人都放的很开，也玩的来，毕竟都是来这里交流学习和技术，平时约饭and一起玩耍，也可以放心的交流学术，交流未来要走的路，当然更多的都是在交流读研，或是读研经验分享。你可以直呼打你三届四届学长的名字，甚至私底下直呼老师的名字，这里更多的像是朋友。比赛期间互相扯淡，考试周相继来实验室通宵，几个人就挤着睡在实验室的沙发和凳子拼凑的床上。比赛期间是绝对主力，玩起来也不输任何人。实验室这栋楼里，保研的人也占了理工科专业的一半以上，大约三分之二的样子，就像我去保研的那天，去保研的人我都认识，XX是对门实验室的，XX是楼上实验室的，XX和我在同一个机房一起比赛等等。好在学校也支持这些，所有的教学楼只有这里安装了中央空调，冬暖夏凉。也配备了一些门禁的教室，不过上次去那个教室是我们一群人假期去玩游戏了，打扑克，象棋五子棋，放下了手中的代码和电路板，其实我们都会玩。回归正题，我还是在这里学了很多知识的，要想当初实验室要每个人的课表，抽公共事件让学长学姐来给我们讲课，数学模型、代码编写、论文编写，最后考核上交作业，还真是到位，每天忙的不亦乐乎。不过这里只是提供一个门，门后更多的世界还要靠自己探索。后来又自学了更多东西，这些知识、解决问题的思路，将陪伴我走很长的路。自此，也彻底甩掉了之前的坏习惯，不在抱怨，也不再崩溃，也不存在还需要控制情绪这件事情。<blockquote class="blockquote-center">            <p>学习的痛苦是暂时的，学不到的痛苦是终身的。 </p>          </blockquote></li><li>自高中毕业后，高三的同桌也终于选择离我而去。差一个3分选择题我能去她的学校，同样，再错一个3分的选择题，我连本一都考不上，可能这就是命吧，终究要告别过去走向新的生活。经过这一段时间，也许是感情开始萌动了，你的眼里看到了星河？也许是吧。反正我到现在都不知道那是不是爱情，算不算单纯的喜欢。你的脾气性格都很好啊，我问可不可以追你，你没否认也没同意，我实在get不到点。在一次高烧期间，我迷迷糊糊的分析了一下，得到了你可能不想理我的八条理由，呵，我现在都想不起来那八条理由到底是什么。不过好在最终删了好友，最终都要走向自己的生活，也没有什么过不去。我去西安，我不知道你要去哪里，未来做何打算，虽然我知道你可能也不想见我，罢了。不过，此后我也学会了坦然以待，不以物喜不以己悲，离开了，终会有新的人来。也不会再为谁的离去而悲伤，但是不管谁来，我都会欣喜。</li><li>后来看到了柳神的代码，也是第一次见到如此精简但不失神奇的算法，也有幸和她通了一次邮件。她的学校比我的二本还要不好，她的学校的确是所三本，但她看过的书比我要多得多，写的算法也比我强百倍，项目接触的比我多得多，更不要提英语词汇量和学习成绩，没有一点能比上她。当然她现在去了哪里我也不知道，我只是个路过的小粉丝。我也看过985，211的学生的代码，也有幸和其中的几位聊天，无论是加好友还是群聊。我只想说有人浪费的985的资源，无论是学习还是实践水平都不咋地，有人借助很好的平台达到了更高的高度。所以，英雄各有见，何必问出处。但更可怕的是，虽然他比你聪明，但是他比你勤奋；虽然他出身名校，但他实力比你好得多。总要相信，有很多人站在一流的平台靠着天赋或勤奋，比我要努力的多。</li><li>出去走走说的就很笼统了，也许是操场的一次跑步，也许是上网的一次冲浪，也许是一次旅行。每次出去走走，都能看到各色各样的人，无论是端庄优雅的小姐姐，还是行走天涯的行者；各有千秋的风景，无论是温润婉约的江南水乡，还是一马平川万里飞沙的塞上西北；亦或是网上冲浪看到了不同人的自我介绍和展示，都会促使着我去看更加广阔的世界，认识更多的人，在这条路上走的更远，促使着我打开眼界，目光放长远，对我来说是一种积极的鼓励。</li></ul><p>当然，我也和大多数人一样，喜欢熬夜看电影，拖延症晚期，偶尔做错的决定会把生活搞得一团糟，像众多的大学生一样。只不过我不爱玩游戏，更不要说男生聚在一起玩游戏，我实在受不了这些，浪费大好年华。相反的爱好学习和折腾，爱好读书和跑步锻炼身体，就走到了今天吧。</p><p>一介书生，生于天地之间，受过教育，恰同学少年，当以『为天地立心，为生民立命，为往圣继绝学，为万世开太平』，这也是中国古代文人的风骨，也许我永远都达不到这个高度和水平。但我也很反感『仗义每多屠狗辈，无情最是读书人』这种想法，并不是每个学生都学傻了，都无情，都没有骨气，那为何这个社会的人不是挣前恐后的去做个屠夫之类？我至今怀疑这句诗的作者对仗义有所误解，对没有当大官的文化和水平而酸涩。如果说，朋友打个电话叫你过去填XX组织的外联，而我不去被理解为无情；关键时刻通过XX手段避免自己承担事件后果，把主要责任推到别人身上称为没有学傻；某人心情不好叫一堆朋友喝个酒，拍个照，发个动态，评论区在走个队形，以此来显示自己的”地位”和朋友的仗义。那么的确和我理解的仗义与无情有所不同，我倒也可以承认自己无情不够仗义，也学傻了，相形见绌，比不过。</p><p>也许是我的这种性格，三观不同导致了我不在和之前的同学联系。我也曾劝过我的朋友不要去考计算机二级，结果被挂在朋友圈骂了个狗血淋头，反过来教育我不懂计算机，而且还影响她的进步，最后被评论：你事真多。是啊，她的事情，我管的着吗，对于劝谏都听不进去的人，还有必要交朋友吗。</p><p>之前的同学，有的在不断的发展所谓的人际关系，以求生老病死时打个电话能有好多人来看望自己；有的逐步巴结讨好自己眼中的优等人而对自己眼中的低等人嗤之以鼻；有的认同学习无用论认为学习过多的知识没用，要去多参加学生会的活动来提升自己，最后企图只用学习成绩和社团活动来争取保研名额（二本院校，一个专业只有一个保研名额，竞争很激烈），一开始便把大学、读研这条路理解为了学习及格就行，项目比赛等多余的学习没用，还是要多做学生会活动，大肆的弘扬学习无用论，那你为何又来保研呢？思之令人发笑；有的在上学期间故意做一些出格之事来展示自己不同凡响，所以在偶尔学习时一定要突出自己所谓的”品学兼优”，以此来证明自己是坏学生里最能学的，是学习这群人里最放荡不羁还没有学傻的；有的开始挂科来完整自己的大学；有的开始考研；有的终日吹嘘自己在学习高大上的东西，学会了的好处，却不见一个成果；有的开始倒买倒卖论文；有的开始参加各种课外活动以显示自己的水平，XX国际班，XX国际组织，最后也只是一个人的成果在挂十个人的名字而已；有的开始游戏人生；有的逐步归于平凡；有的趁着大学的黄金学习时间不去上课反而出去当服务员挣一时外快。</p><p>我的高中并不是很好，学生质量可见一斑，所以我之前大多数同学的真实写照是这样也不足为奇。我也不敢评价他们的选择正确与否，不能因为和自己走的路不同就去否定他人，人各有志罢了，这也是我不再联系的原因。以上都是我个人的态度，也许你认同，也许你不喜欢，但都要认真的确定自己要走的路，然后认真的走下去。当然也可以悟已往之不谏，知来者之可追；实迷途其未远，觉今是而昨非。</p><p>至于我大学的朋友没有之前的同学这么千奇百怪的现象，这里的千奇百怪也只是我认为。因为我的圈子基本在实验室，同班同学都认不全（转专业过来的，人实在认不全），交友限制在了志同道合的人里面。小部分朋友保研成功，大部分朋友考研成功，剩下的对研究生无感的朋友也已找好了不错的工作准备就业。所以对我来说也就很难看到各种千奇百怪的人和三观不同的行为了，反正我也不想看。</p><p>不过还是要在毕业之前见几个好朋友，吃顿饭也好，聊天也罢，还是要好好告别。之前有个朋友对我说，研究生毕业就找个人嫁了。现如今大家都是恰同学少年，风华正茂，还是一起玩耍一起吃饭一起跑步的好朋友，想到三年后要嫁为人妻沦为人母，还是些许的难以接受。此去经年，也许三年五载，也许此生不见，莫愁前路无好友，西出阳关后，海内仍然存在其他好友，天涯若比邻。</p><p>满纸荒唐，一介狷狂。《曾经我也想过一了百了》——此处应有这首音乐。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;想来也快要大学毕业了，2016年唐县一中毕业，2020年某不太行大学毕业，四年了。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="Life" scheme="https://muyuuuu.github.io/tags/Life/"/>
    
  </entry>
  
  <entry>
    <title>爬虫系列（三）——多进程爬虫</title>
    <link href="https://muyuuuu.github.io/2020/03/18/multi-process/"/>
    <id>https://muyuuuu.github.io/2020/03/18/multi-process/</id>
    <published>2020-03-18T03:53:38.000Z</published>
    <updated>2020-03-20T15:33:30.546Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>承接上文，实现多任务的方法一般有三种，今日来实现其中的一种：多进程爬虫。而剩余的多线程并发和协程暂时没时间搞了，一方面是要先学习下Python的yield和send如何使用，其次要准备毕设的中期答辩和被老师安排了去读源代码，日后会补上的。但仅仅是多进程，<strong>就将IO密集的任务从1153秒提升到了105秒</strong>，且极高的提升了资源利用率。</p><p>还有一点，本文的代码可能只适用于Next主题，但方法思路是通解，只要改改参数适应你的主题，代码同样能用。结果展示：<a href="https://muyuuuu.github.io/hot/">本站热门</a></p><a id="more"></a><h1 id="爬虫"><a href="#爬虫" class="headerlink" title="爬虫"></a>爬虫</h1><p>如果对爬虫不太了解，可以参考我之前的几篇文章。如果你计算机功底还不错，那么实现爬虫入门，完成简单的爬虫任务还是可以的：</p><ul><li><a href="https://muyuuuu.github.io/2019/02/24/school-start1/">爬虫入门</a></li><li><a href="https://muyuuuu.github.io/2020/01/11/resptile/">借助自动化测试的爬虫</a></li></ul><p>这次的爬取任务很简单，之前看到了别人博客有<strong>『热榜』</strong>这个东西，很是眼馋，查询了一下发现实现热榜这个东西得经过很多的配置，但是我懒不想配置。于是准备自己动手爬虫，爬取每篇博客的访问量，以此作为博客每排文章的热度，说干就干，开工。</p><h2 id="爬虫思路"><a href="#爬虫思路" class="headerlink" title="爬虫思路"></a>爬虫思路</h2><ol><li><p>本次爬虫需要request、BeautiulSoup和selenium联合使用，只有其中的一方完不成任务。</p></li><li><p>因为每篇博客的访问量并不在外部显示，必须点击到文章内部才难加载，所以，这次爬虫必须使用selenium控制浏览器模拟点击，之后进入对应网页，以此来获取不蒜子统计的浏览数量。</p></li><li><p>首先打开网页的根域名，按<code>F12</code>发现每篇文章的链接的类是<code>post-title-link</code>，以此来获取第一篇文章的名称，点击，进入第一篇博客。部分代码如下所示：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 寻找第一篇文章</span></span><br><span class="line">first_page = body.find(<span class="string">'a'</span>, &#123;<span class="string">'class'</span>, <span class="string">'post-title-link'</span>&#125;)</span><br><span class="line"><span class="comment"># 记录第一篇文章的标题</span></span><br><span class="line">first = <span class="string">""</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> first_page:</span><br><span class="line">    first = i</span><br><span class="line"><span class="comment"># 进入第一篇文章</span></span><br><span class="line">driver.find_element_by_xpath(<span class="string">"//a[contains(text(),'&#123;&#125;')]"</span>.format(first)).click()</span><br></pre></td></tr></table></figure></li><li><p>本来想的是，进入网页后，使用requests库直接请求，然后解析出不蒜子统计那个标签的数量。但是，requests网页后发现不蒜子标签下数量根本不显示。意思是，仅仅依靠requests和beautifulsoup的爬虫是爬取不到不蒜子统计的。</p></li><li><p>所以，还得靠selenium。再次按一下<code>F12</code>，发现不蒜子统计的id是<code>busuanzi_value_page_pv</code>。利用selenium的id定位法，定位到不蒜子统计元素，然后输出定位元素里的文本。部分代码如下所示：</p> <figure class="highlight python"><table><tr><td class="code"><pre><span class="line">view = driver.find_element_by_id(<span class="string">"busuanzi_value_page_pv"</span>)</span><br><span class="line"><span class="comment"># num 就是每篇文章的访问量 </span></span><br><span class="line">num = int(view.text)</span><br></pre></td></tr></table></figure></li><li><p>而后，在每篇博客的末尾，都有指向下一篇博客的链接，点击既能进入下一篇博客。如下图所示：<img data-src="/2020/03/18/multi-process/1.jpg" alt></p></li><li><p>而selenium定位到链接的文字又很费劲，不如用selenium获取当前网页的URL，然后使用requests和beautifulsoup库跟去获取的URL，进一步解析出指向下一篇博客链接的文字。而后把解析出的文字传给selenium的链接定位法，让selenium按照文字去点击链接，进入下一篇博客。如此循环往复，直到最后一篇博客。</p></li><li><p>因某些博客的图片数量过多，导致加载过慢，如果5秒内加载不完，则直接进入下一篇文章。中间为防止爬虫过猛被屏蔽，其中使用了一些延时函数放缓进度。</p></li><li><p>最终的结果要把文章的名称、文章的网址和文章的访问量都记录下来。考虑使用字典这种结构，将文章名称和网址作为key，访问量作为value。</p></li><li><p>将字典保存为json文件，而后将json文件中的结果稍微清洗一下，做成markdown文件，放到博客里，热榜就做成了。</p></li><li><p>最后，对爬虫任务的整体时间进行记录，对比单进程和多进程的效率。</p></li></ol><h1 id="单进程"><a href="#单进程" class="headerlink" title="单进程"></a>单进程</h1><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">from</span> bs4 <span class="keyword">import</span> BeautifulSoup</span><br><span class="line"></span><br><span class="line">home_url = <span class="string">"https://muyuuuu.github.io/"</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 申请当前页面</span></span><br><span class="line">r = requests.get(home_url)</span><br><span class="line">soup = BeautifulSoup(r.text, <span class="string">"lxml"</span>)</span><br><span class="line">body = soup.body</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用selenium点开每个页面，统计访问人数和评论数</span></span><br><span class="line"><span class="keyword">from</span> selenium <span class="keyword">import</span> webdriver</span><br><span class="line">driver = webdriver.Chrome(executable_path=<span class="string">'/home/lanling/chromedriver_linux64/chromedriver'</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line"><span class="comment"># 记录文章的数量</span></span><br><span class="line">article = &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 先进入第一页</span></span><br><span class="line"><span class="comment"># 打开网页</span></span><br><span class="line">driver.get(home_url)</span><br><span class="line"><span class="comment"># 寻找第一篇文章</span></span><br><span class="line">first_page = body.find(<span class="string">'a'</span>, &#123;<span class="string">'class'</span>, <span class="string">'post-title-link'</span>&#125;)</span><br><span class="line"><span class="comment"># 记录第一篇文章的标题</span></span><br><span class="line">first = <span class="string">""</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> first_page:</span><br><span class="line">    first = i</span><br><span class="line"><span class="comment"># 进入第一篇文章</span></span><br><span class="line">driver.find_element_by_xpath(<span class="string">"//a[contains(text(),'&#123;&#125;')]"</span>.format(first)).click()</span><br><span class="line">time.sleep(<span class="number">3</span>)</span><br><span class="line"><span class="comment"># 记录浏览数</span></span><br><span class="line">dr = driver.find_element_by_id(<span class="string">"busuanzi_value_page_pv"</span>)</span><br><span class="line"><span class="comment"># 标题名和域名作为字典的 key</span></span><br><span class="line">string = <span class="string">"["</span> + first + <span class="string">"]"</span> + <span class="string">"("</span> + driver.current_url + <span class="string">")"</span></span><br><span class="line">article[string] = int(dr.text)</span><br><span class="line">print(article)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 而后点击 每篇博客末尾的指向下一篇文章的链接 直到遍历完所有文章</span></span><br><span class="line"><span class="comment"># 记录开始时间</span></span><br><span class="line">start = time.time()</span><br><span class="line"><span class="comment"># 捕获当前的 url</span></span><br><span class="line">url = driver.current_url</span><br><span class="line">data = requests.get(url)</span><br><span class="line">data = BeautifulSoup(data.text, <span class="string">"lxml"</span>)</span><br><span class="line"><span class="comment"># 解析当前页面，获取下一页的按钮</span></span><br><span class="line">next = data.find(<span class="string">'div'</span>, &#123;<span class="string">'class'</span>:<span class="string">'post-nav-next post-nav-item'</span>&#125;)</span><br><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    <span class="keyword">while</span> next:</span><br><span class="line">        time.sleep(<span class="number">1</span>)</span><br><span class="line">        <span class="comment"># 点击下一页的链接 进入 下一个网页</span></span><br><span class="line">        driver.find_element_by_link_text(next.text.strip()).click()</span><br><span class="line">        time.sleep(<span class="number">5</span>)</span><br><span class="line">        <span class="comment"># 查看浏览数，图片较多加载缓慢，5秒内加载不出来跳过</span></span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            view = driver.find_element_by_id(<span class="string">"busuanzi_value_page_pv"</span>)</span><br><span class="line">        <span class="keyword">except</span>:</span><br><span class="line">            <span class="keyword">pass</span></span><br><span class="line">        <span class="comment"># 获取当前页面的url</span></span><br><span class="line">        url = driver.current_url</span><br><span class="line">        time.sleep(<span class="number">1</span>)</span><br><span class="line">        data = requests.get(url)</span><br><span class="line">        <span class="comment"># 解析当前url</span></span><br><span class="line">        data = BeautifulSoup(data.text, <span class="string">"lxml"</span>)</span><br><span class="line">        <span class="comment"># 当前文章的标题和域名传入字典</span></span><br><span class="line">        string = <span class="string">"["</span> + data.title.text + <span class="string">"]"</span> + <span class="string">"("</span> + driver.current_url + <span class="string">")"</span></span><br><span class="line">        <span class="comment"># 如果没有加载到浏览数 就给一个负数</span></span><br><span class="line">        <span class="keyword">if</span> view.text == <span class="string">""</span>:</span><br><span class="line">            article[string] = <span class="number">-2</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            article[string] = int(view.text)</span><br><span class="line">            print(string, int(view.text))</span><br><span class="line">        time.sleep(<span class="number">1</span>)</span><br><span class="line">        <span class="comment"># 寻找下一篇博客的链接名，然后点击，直到最后一篇文章。</span></span><br><span class="line">        next = data.find(<span class="string">'div'</span>, &#123;<span class="string">'class'</span>:<span class="string">'post-nav-next post-nav-item'</span>&#125;)</span><br><span class="line"><span class="keyword">except</span>:</span><br><span class="line">    <span class="comment"># 记录结束时间</span></span><br><span class="line">    end = time.time()</span><br><span class="line"></span><br><span class="line">print(end - start)</span><br><span class="line">driver.quit()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 将博客按访问数排序</span></span><br><span class="line">article_save = sorted(article.items(), key = <span class="keyword">lambda</span> item:item[<span class="number">1</span>])</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> json</span><br><span class="line"><span class="comment"># encoding='utf-8'，用于确保写入中文不乱码</span></span><br><span class="line"><span class="keyword">with</span> open(<span class="string">'data.json'</span>,<span class="string">'w'</span>,encoding=<span class="string">'utf-8'</span>) <span class="keyword">as</span> f_obj:</span><br><span class="line">    <span class="comment"># ensure_ascii=False，用于确保写入json的中文不发生乱码</span></span><br><span class="line">    json.dump(article_save,f_obj,ensure_ascii=<span class="literal">False</span>, indent=<span class="number">4</span>)</span><br></pre></td></tr></table></figure><p>这个单进程的任务总共耗时<strong>1153</strong>秒左右（毕竟网络时延说不准），资源利用率很少，如下所示：<img data-src="/2020/03/18/multi-process/1.gif" alt></p><h1 id="多进程"><a href="#多进程" class="headerlink" title="多进程"></a>多进程</h1><p>多进程仅需在单进程的代码上做一点改动即可：（如果你不懂多进程，请参考我的上一篇文章，<a href="https://muyuuuu.github.io/2020/03/16/multi-process-thread/">如何理解多进程</a>）</p><ol><li><p>首先统计一下博客有几页，就创建几个进程。比如，我的博客目前有14页，就创建14个进程：<img data-src="/2020/03/18/multi-process/2.jpg" alt></p></li><li><p>进程扔到一个进程池里，等待所有进程执行完毕即可。</p></li><li><p>多进程之间不共享变量，所以字典的创建方法需要更改。</p></li><li><p>同样，防止爬虫过猛，中间同样适用延时函数放缓进度。</p></li><li><p>记录多进程的耗时，与单进程对比。代码如下所示：</p></li></ol><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">from</span> bs4 <span class="keyword">import</span> BeautifulSoup</span><br><span class="line"></span><br><span class="line">home_url = <span class="string">"https://muyuuuu.github.io/"</span></span><br><span class="line"></span><br><span class="line">r = requests.get(home_url)</span><br><span class="line">soup = BeautifulSoup(r.text, <span class="string">"lxml"</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 有几个页面就创建几个进程</span></span><br><span class="line">body = soup.body</span><br><span class="line">page_number = body.find(<span class="string">'nav'</span>, &#123;<span class="string">'class'</span> : <span class="string">'pagination'</span>&#125;)</span><br><span class="line">a = page_number.find_all(<span class="string">'a'</span>)</span><br><span class="line">max_page_number = <span class="number">0</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> a:</span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> i:</span><br><span class="line">        <span class="keyword">if</span> str(j).isdigit():</span><br><span class="line">            <span class="keyword">if</span> int(j) &gt; max_page_number:</span><br><span class="line">                max_page_number = int(j)</span><br><span class="line">print(max_page_number)</span><br><span class="line">process_num = max_page_number</span><br><span class="line"></span><br><span class="line"><span class="comment"># 扔进进程池统一管理 并计时</span></span><br><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Pool</span><br><span class="line"><span class="keyword">import</span> time, random</span><br><span class="line"><span class="keyword">from</span> selenium <span class="keyword">import</span> webdriver</span><br><span class="line"><span class="keyword">import</span> multiprocessing</span><br><span class="line"></span><br><span class="line"><span class="comment"># 进程的字典 </span></span><br><span class="line">article = multiprocessing.Manager().dict()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 记录每个进程爬几次，就是一个页面有几篇文章，我懒的爬取了，手动设置下</span></span><br><span class="line">per_page = <span class="number">7</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 每个进程的任务</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">respite_task</span><span class="params">(url_num)</span>:</span></span><br><span class="line">    num = <span class="number">1</span></span><br><span class="line">    sub_url = <span class="string">""</span></span><br><span class="line">    <span class="keyword">if</span> url_num == <span class="number">0</span>:</span><br><span class="line">        sub_url = <span class="string">"https://muyuuuu.github.io/"</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        sub_url = <span class="string">"https://muyuuuu.github.io/page/"</span> + str(url_num) + <span class="string">"/"</span></span><br><span class="line">    <span class="comment"># 每个进程启动一个 driver</span></span><br><span class="line">    print(<span class="string">"here"</span>)</span><br><span class="line">    driver = webdriver.Chrome(executable_path=<span class="string">'/home/lanling/chromedriver_linux64/chromedriver'</span>)</span><br><span class="line">    driver.get(sub_url)</span><br><span class="line">    time.sleep(random.randint(<span class="number">2</span>, <span class="number">6</span>))</span><br><span class="line">    r = requests.get(sub_url)</span><br><span class="line">    soup = BeautifulSoup(r.text, <span class="string">"lxml"</span>)</span><br><span class="line">    body = soup.body</span><br><span class="line">    <span class="comment"># 寻找第一篇文章</span></span><br><span class="line">    first_page = body.find(<span class="string">'a'</span>, &#123;<span class="string">'class'</span>, <span class="string">'post-title-link'</span>&#125;)</span><br><span class="line">    <span class="comment"># 记录第一篇文章的标题</span></span><br><span class="line">    first = <span class="string">""</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> first_page:</span><br><span class="line">        first = i</span><br><span class="line">    <span class="comment"># 进入第一篇文章</span></span><br><span class="line">    driver.find_element_by_xpath(<span class="string">"//a[contains(text(),'&#123;&#125;')]"</span>.format(first)).click()</span><br><span class="line">    time.sleep(<span class="number">5</span>)</span><br><span class="line">    <span class="comment"># 记录浏览数</span></span><br><span class="line">    <span class="keyword">try</span>: </span><br><span class="line">        view = driver.find_element_by_id(<span class="string">"busuanzi_value_page_pv"</span>)</span><br><span class="line">    <span class="keyword">except</span>:</span><br><span class="line">        <span class="keyword">pass</span></span><br><span class="line">    <span class="comment"># 标题名和域名作为字典的 key</span></span><br><span class="line">    string = <span class="string">"["</span> + first + <span class="string">"]"</span> + <span class="string">"("</span> + driver.current_url + <span class="string">")"</span></span><br><span class="line">    <span class="comment"># 如果没有加载到浏览数 就给一个负数</span></span><br><span class="line">    <span class="keyword">if</span> view.text == <span class="string">""</span>:</span><br><span class="line">        article[string] = <span class="number">-2</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        article[string] = int(view.text)</span><br><span class="line">        print(string, int(view.text))</span><br><span class="line">    <span class="comment"># 捕获当前的 url (已经进入了新页面)</span></span><br><span class="line">    url = driver.current_url</span><br><span class="line">    data = requests.get(url)</span><br><span class="line">    data = BeautifulSoup(data.text, <span class="string">"lxml"</span>)</span><br><span class="line">    <span class="comment"># 解析当前页面，获取下一页的按钮</span></span><br><span class="line">    next = data.find(<span class="string">'div'</span>, &#123;<span class="string">'class'</span>:<span class="string">'post-nav-next post-nav-item'</span>&#125;)</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">while</span> next <span class="keyword">and</span> num &lt; per_page:</span><br><span class="line">            num += <span class="number">1</span></span><br><span class="line">            time.sleep(<span class="number">1</span>)</span><br><span class="line">            <span class="comment"># 点击下一页的链接 进入 下一个网页</span></span><br><span class="line">            driver.find_element_by_link_text(next.text.strip()).click()</span><br><span class="line">            time.sleep(<span class="number">5</span>)</span><br><span class="line">            <span class="comment"># 查看浏览数，图片较多加载缓慢，5秒内加载不出来跳过</span></span><br><span class="line">            <span class="keyword">try</span>:</span><br><span class="line">                view = driver.find_element_by_id(<span class="string">"busuanzi_value_page_pv"</span>)</span><br><span class="line">            <span class="keyword">except</span>:</span><br><span class="line">                <span class="keyword">pass</span></span><br><span class="line">            <span class="comment"># 获取当前页面的url</span></span><br><span class="line">            url = driver.current_url</span><br><span class="line">            time.sleep(<span class="number">1</span>)</span><br><span class="line">            data = requests.get(url)</span><br><span class="line">            <span class="comment"># 解析当前url</span></span><br><span class="line">            data = BeautifulSoup(data.text, <span class="string">"lxml"</span>)</span><br><span class="line">            <span class="comment"># 当前文章的标题和域名传入字典</span></span><br><span class="line">            string = <span class="string">"["</span> + data.title.text + <span class="string">"]"</span> + <span class="string">"("</span> + driver.current_url + <span class="string">")"</span></span><br><span class="line">            <span class="comment"># 如果没有加载到浏览数 就给一个负数</span></span><br><span class="line">            <span class="keyword">if</span> view.text == <span class="string">""</span>:</span><br><span class="line">                article[string] = <span class="number">-2</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                article[string] = int(view.text)</span><br><span class="line">                print(string, int(view.text))</span><br><span class="line">            time.sleep(<span class="number">1</span>)</span><br><span class="line">            <span class="comment"># 寻找下一篇博客的链接名，然后点击，直到最后一篇文章。</span></span><br><span class="line">            next = data.find(<span class="string">'div'</span>, &#123;<span class="string">'class'</span>:<span class="string">'post-nav-next post-nav-item'</span>&#125;)</span><br><span class="line">    <span class="keyword">except</span>:</span><br><span class="line">        <span class="keyword">pass</span></span><br><span class="line">    <span class="keyword">finally</span>:</span><br><span class="line">        driver.quit()</span><br><span class="line">    </span><br><span class="line">p = Pool(process_num)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range (process_num + <span class="number">1</span>):</span><br><span class="line">    p.apply_async(respite_task, args=(i,))</span><br><span class="line"></span><br><span class="line">print(<span class="string">'waitting for all subprocess done'</span>)</span><br><span class="line">start = time.time()</span><br><span class="line">p.close()</span><br><span class="line">p.join()</span><br><span class="line">end = time.time()</span><br><span class="line">print(<span class="string">'All subprocesses done costs &#123;&#125; seconds'</span>.format(end - start))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 升序排列并保存</span></span><br><span class="line">article_save = sorted(article.items(), key = <span class="keyword">lambda</span> item:item[<span class="number">1</span>])</span><br><span class="line"><span class="keyword">import</span> json</span><br><span class="line"><span class="comment"># encoding='utf-8'，用于确保写入中文不乱码</span></span><br><span class="line"><span class="keyword">with</span> open(<span class="string">'data_multiprocess.json'</span>,<span class="string">'w'</span>,encoding=<span class="string">'utf-8'</span>) <span class="keyword">as</span> f_obj:</span><br><span class="line">    <span class="comment"># ensure_ascii=False，用于确保写入json的中文不发生乱码</span></span><br><span class="line">    json.dump(article_save,f_obj,ensure_ascii=<span class="literal">False</span>, indent=<span class="number">4</span>)</span><br></pre></td></tr></table></figure><p>最终的结果和单线程的结果保持一致，但仅仅用了<strong>105</strong>秒就执行完毕，足足提升了10倍。因为每个进程都有一个主线程，将主线程分配给多个核，所以导致了资源利用率比单线程要高的多：<img data-src="/2020/03/18/multi-process/2.gif" alt></p><h1 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h1><p>这好像不是传说中的并行计算，毕竟不是计算密集的任务，但这个几个进程的任务确实在并行执行，『知识就是力量』还是有道理的。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;承接上文，实现多任务的方法一般有三种，今日来实现其中的一种：多进程爬虫。而剩余的多线程并发和协程暂时没时间搞了，一方面是要先学习下Python的yield和send如何使用，其次要准备毕设的中期答辩和被老师安排了去读源代码，日后会补上的。但仅仅是多进程，&lt;strong&gt;就将IO密集的任务从1153秒提升到了105秒&lt;/strong&gt;，且极高的提升了资源利用率。&lt;/p&gt;
&lt;p&gt;还有一点，本文的代码可能只适用于Next主题，但方法思路是通解，只要改改参数适应你的主题，代码同样能用。结果展示：&lt;a href=&quot;https://muyuuuu.github.io/hot/&quot;&gt;本站热门&lt;/a&gt;&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="Resptile" scheme="https://muyuuuu.github.io/tags/Resptile/"/>
    
  </entry>
  
  <entry>
    <title>多线程与多进程, 基于Python实现</title>
    <link href="https://muyuuuu.github.io/2020/03/16/multi-process-thread/"/>
    <id>https://muyuuuu.github.io/2020/03/16/multi-process-thread/</id>
    <published>2020-03-16T12:53:42.000Z</published>
    <updated>2020-03-16T13:52:08.129Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>作为一个程序员，我发现我不会写多进程和多线程的程序，实在羞愧，在赶完毕业设计后准备开一个多线程编程的坑。本文是我这种小白从无到有的学习收获，内容较多，还请耐心观看。因疫情原因没在学校，手头没有一本靠谱的《操作系统》的书籍，大多东西为网上搜刮而来后整理，可能不严谨。本文收录：</p><ul><li>引入线程、进程的基本概念；</li><li>多进程编程：变量引用、性能分析、进程池和进程通信；</li><li>多线程编程：线程锁、线程的局部变量；</li><li>实际项目：暂时的目标是多线程爬虫，内容过多准备放下一个文章（同时也包括防止线程死锁、协程等）。</li></ul><a id="more"></a><h1 id="从物理机开始"><a href="#从物理机开始" class="headerlink" title="从物理机开始"></a>从物理机开始</h1><p>本文以Linux操作为主，Windows操作不太了解。首先先从物理机出发，了解物理CPU，CPU的核，线程是什么概念。物理CPU指实际存在的CPU处理器，安装在PC主板或服务器上。首先查看物理CPU个数，查看电脑有几个CPU：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">grep &apos;physical id&apos; /proc/CPUinfo | sort -u</span><br></pre></td></tr></table></figure><p>执行结果反馈如下：（后期解释为什么是12个id为0的记录）</p><p><img data-src="/2020/03/16/multi-process-thread/1.png" alt></p><p>其id均为0，表示我电脑有一个CPU（从0开始编号）。CPU中包含物理内核，比如多核CPU，单核CPU。这个多核或者单核已经集成在CPU内部了，那么来查看一下核心数量，就是买电脑的时候常听商家常说的4核CPU或者8核CPU：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">grep &apos;core id&apos; /proc/CPUinfo | sort -u | wc -l</span><br></pre></td></tr></table></figure><p>我这里输出的结果为6,表示我的是6核CPU。同时，你在买电脑的时候听说过，6核12线程，4核8线程的CPU，那么，再看一下自己的电脑支持几个线程：（说了半天终于到线程了）。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">grep &apos;processor&apos; /proc/CPUinfo | sort -u | wc -l</span><br></pre></td></tr></table></figure><p>输出的结果是12，表示我的电脑的处理器是一个CPU，且是6核心12线程的。所谓的6核12线程，6核指的是物理核心。用Intel的超线程技术(HT， Hyper-threading)将物理核虚拟而成的逻辑处理单元，现在大部分的主机的CPU都在使用HT技术，用一个物理核模拟两个虚拟核，即每个核两个线程，总数为12线程。所以在操作系统看来是12个核，但实际是一个物理CPU中的6个物理内核虚拟出来的12个线程：</p><p><img data-src="/2020/03/16/multi-process-thread/1.png" alt></p><p>此时我们需要知道：<strong>一个核心只能同时执行一个线程</strong>。</p><h1 id="进程与线程的概念"><a href="#进程与线程的概念" class="headerlink" title="进程与线程的概念"></a>进程与线程的概念</h1><h2 id="进程"><a href="#进程" class="headerlink" title="进程"></a>进程</h2><p>当写完一份程序，编译为二进制的可执行文件并执行时：可执行的.exe文件可以理解为一种程序，程序本身也是指令的集合，而进程才是程序(那些指令)的真正运行。进程本身不会运行，运行的程序叫进程。进程会标记程序要访问的地址、要执行的操作等，执行完毕后进程会被销毁。（如有兴趣请参考《操作系统》的进程控制块，我快忘光了）</p><p>若干进程有可能与同一个程序有关系，如<strong>循序和平行</strong>。进程<code>A</code>需要得到进程<code>B</code>的处理结果才能继续运行，所以进程<code>A</code>要等待进程<code>B</code>执行完，这叫<strong>循序</strong>；进程<code>C</code>和进程<code>D</code>没有关系，这俩可以同时进行，这叫<strong>平行</strong>。</p><p>举个生动的例子：某生产流水线，得先通过质量安检才能包装，所以质量检查和包装这两个进程不能同时进行，有明显的先后顺序，这叫<strong>循序</strong>；对于没有明显先后顺序的，比如电脑打开两个软件，一个负责聊天，一个负责放音乐，这两个进程毫不相干，所以同一时刻谁先执行谁后执行无所谓的，没有明显的先后顺序，这叫<strong>平行</strong>。</p><p>也许你会问：音乐软件和聊天软件这两个进程不是同一时刻在一起执行吗？其实不是的，计算机只能串行执行不同的进程，即执行进程<code>A</code>的时候不能执行进程<code>B</code>，所以同一时刻，计算机只能执行一个进程。你所谓的边听音乐边聊天，其实是计算机中断实现的。先执行聊天进程，在以极低的间隔切换到音乐进程，只是这个时间间隔短暂，短暂到肉眼无法察觉，你感受不到，所以你的体会是同时进行的，但实际是单独进行的。</p><p>而每一个进程都有一个主线程，一个CPU核心在同一时刻只能执行一个线程。所以，当系统内存在的多个进程的时候，操作系统会根据进程的优先级算法来决定先执行谁，后执行谁，<code>A</code>的优先级高，那就先执行<code>A</code>。确定进程的优先级有很多算法，详情参考《操作系统》。以我的电脑为例，启动系统，打开一堆软件，肯定不止12个进程，而多个进程以优先级的形式在仅12个核的CPU上轮番执行，保证系统的有条不紊。</p><p>进程也是线程的容器。最简单的比喻多线程就像火车的每一节车厢，而进程则是火车。车厢离开火车是无法跑动的，同理火车也不可能只有一节车厢，多线程的出现就是为了提高效率。</p><p>总结一下：进程是操作系统进行资源（包括CPU、内存、磁盘IO等）分配的最小单位，而进程中的线程是CPU调度和分配的基本单位。我们打开的聊天工具，浏览器都是一个进程。进程可能有多个子任务，比如聊天工具要接受消息，发送消息，这些子任务就是线程。</p><p>而资源分配给进程，且不同进程之间不共享资源，线程共享进程资源。</p><h2 id="线程"><a href="#线程" class="headerlink" title="线程"></a>线程</h2><p>线程是操作系统能够进行运算调度的最小单位。它被包涵在进程之中，一条线程指的是进程中一个单一顺序的控制流，一个进程中可以有多个线程，每条线程并行执行不同的任务，线程的运行中需要使用计算机的内存资源和CPU。</p><p>同一进程中的多条线程将共享该进程中的全部系统资源，如访问数据、地址空间等等。但同一进程中的多个线程有各自的调用栈(call stack)，自己的寄存器环境(register context)，自己的线程本地存储(thread-local storage)。</p><p>在多核或多CPU，或支持HT的CPU上使用多线程程序设计的好处是显而易见，即提高了程序的执行吞吐率。多核CPU在毕竟同一时刻跑多个任务，单核CPU同一时刻只能跑一个任务。</p><p>总结：进程是资源分配的最小单位，线程是CPU调度的最小单位。线程和进程的区别在于，子进程和父进程有不同的代码和数据空间，而多个线程则共享数据空间，每个线程有自己的执行堆栈和程序计数器为其执行上下文。</p><h2 id="串行、并行、并发"><a href="#串行、并行、并发" class="headerlink" title="串行、并行、并发"></a>串行、并行、并发</h2><p>串行：多个任务，执行时一个执行完再执行另一个。比喻：吃完饭再看视频。</p><p>并发：多个线程在单个核心运行，同一时间一个线程运行，系统不停切换线程，看起来像同时运行，实际上是线程不停切换。比喻： 一会跑去厨房吃饭，一会跑去客厅看视频。</p><p>并行：每个线程分配给独立的核心，线程同时运行。比喻：一边吃饭一边看视频。</p><p>在了解完串行、并行、并发的概念后，可以更多的理解多进程和多线程了：</p><h1 id="多进程"><a href="#多进程" class="headerlink" title="多进程"></a>多进程</h1><p>当你运行一个程序，你就启动了一个进程。在同一个时间里，同一个计算机系统中如果允许两个或两个以上的进程处于运行状态，这便是多任务。</p><p>现代的操作系统几乎都是多任务操作系统，能够同时管理多个进程的运行。多任务带来的好处是明显的，比如你可以边听mp3边上网，与此同时甚至可以将下载的文档打印出来，而这些任务之间丝毫不会相互干扰。如果这些任务同时在一个核上运行，这就是<strong>并发</strong>，如果这些任务分配到了多个核心，这就是<strong>并行</strong>。</p><p>如何实现并发？详情参考《操作系统》，可以粗暴的理解为利用系统中断，根据优先级确定先执行谁。执行A，中断一下去执行B，执行一会儿B，在中断一下返回去执行A，如此循环往复。</p><p>并行运行的效率显然高于并发运行，所以在多CPU的计算机中，多任务的效率比较高。同理，如果在多CPU计算机中只运行一个进程(线程)，就不能发挥多CPU的优势。</p><h1 id="多线程"><a href="#多线程" class="headerlink" title="多线程"></a>多线程</h1><p>在计算机编程中，一个基本的概念就是同时对多个任务加以控制。许多程序设计问题都要求程序能够停下手头的工作，改为处理其他一些问题，再返回主进程。最开始的时候，那些掌握机器低级语言的程序员编写一些<strong>中断服务例程</strong>，主进程的暂停是通过硬件级的中断实现的。</p><p>尽管这是一种有用的方法，但编出的程序很难移植，由此造成了另一类的代价高昂问题。中断对那些实时性很强的任务来说是很有必要的。</p><p>但对于其他许多问题，只要求将问题划分进入<strong>独立运行</strong>的程序片断中，程序在逻辑意义上被分割为数个线程，并将数个线程分配给多个核，使整个程序能更迅速地响应用户的请求。多线程是为了同步完成多项任务，即在同一时间需要完成多项任务的时候实现的。假如操作系统本身支持多个处理器，那么每个线程都可分配给一个不同的处理器，真正进入<strong>并行运算</strong>状态。</p><p>从程序设计语言的角度看，多线程操作最有价值的特性之一就是程序员不必关心到底使用了多少个处理器（处理器不够可以并发），可以创建100个线程一起来嘛。</p><h1 id="多进程编程"><a href="#多进程编程" class="headerlink" title="多进程编程"></a>多进程编程</h1><p>在了解了那么多的基础知识后，先开始进程的编程吧。</p><h2 id="进程不共享数据"><a href="#进程不共享数据" class="headerlink" title="进程不共享数据"></a>进程不共享数据</h2><p>可以考虑python的multiprocessing库来实现多进程。众所周知，进程之间是不共享数据的，那么一探究竟。本例程的任务是，创建两个进程实现两个计算任务，完成计算任务1后才能完成计算任务2，这是明显的<strong>循序</strong>。</p><p>首先，创建了两个子进程p和q，子进程p执行sum1函数，子进程q执行sum2函数。start()方法表示进程开始，join()方法表示等待进程结束。先执行完q进程，修改a和c的值，在执行p进程修改a的值，得到最后a的结果。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Process</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"></span><br><span class="line">a, b, c, d = <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 子进程要执行的代码</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sum1</span><span class="params">(name)</span>:</span></span><br><span class="line">    <span class="keyword">global</span> a, b, c</span><br><span class="line">    print(<span class="string">'Run child process %s (%s)...'</span> % (name, os.getpid()))</span><br><span class="line">    a = a + <span class="number">1</span> + c</span><br><span class="line">    print(a)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sum2</span><span class="params">(name)</span>:</span></span><br><span class="line">    <span class="keyword">global</span> a, c, d</span><br><span class="line">    print(<span class="string">'Run child process %s (%s)...'</span> % (name, os.getpid()))</span><br><span class="line">    a = a + <span class="number">2</span></span><br><span class="line">    c = c + d</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">'__main__'</span>:</span><br><span class="line">    print(<span class="string">'Parent process %s.'</span> % os.getpid())</span><br><span class="line">    p = Process(target=sum1, args=(<span class="string">'a + b'</span>,))</span><br><span class="line">    q = Process(target=sum2, args=(<span class="string">'c + d'</span>,))</span><br><span class="line">    print(<span class="string">'Child process will start.'</span>)</span><br><span class="line">    p.start()</span><br><span class="line">    q.start()</span><br><span class="line">    q.join()</span><br><span class="line">    p.join()</span><br><span class="line">    print(<span class="string">'Child process end.'</span>)</span><br><span class="line">    print(a)</span><br></pre></td></tr></table></figure><p>执行结果输出如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Parent process 23843.</span><br><span class="line">Child process will start.</span><br><span class="line">Run child process a + b (23844)...</span><br><span class="line">5</span><br><span class="line">Run child process c + d (23845)...</span><br><span class="line">Child process end.</span><br><span class="line">1</span><br></pre></td></tr></table></figure><p>最后一条的数据结果表明现在的a是1，即程序的主进程和创建的两个子进程之间不共享数据，证明了结论。即多进程中，同一个变量，各自有一份拷贝存在于每个进程中，互不影响。那如果就想让进程之间共享数据该如何操作呢？</p><h2 id="进程共享数据"><a href="#进程共享数据" class="headerlink" title="进程共享数据"></a>进程共享数据</h2><p>只需要特殊的声明变量即可，以下是数值类型的变量：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Process</span><br><span class="line"><span class="keyword">import</span> os, multiprocessing</span><br><span class="line"></span><br><span class="line">a = multiprocessing.Value(<span class="string">"d"</span>, <span class="number">1</span>)</span><br><span class="line">b = multiprocessing.Value(<span class="string">"d"</span>, <span class="number">2</span>)</span><br><span class="line">c = multiprocessing.Value(<span class="string">"d"</span>, <span class="number">3</span>)</span><br><span class="line">d = multiprocessing.Value(<span class="string">"d"</span>, <span class="number">4</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 子进程要执行的代码</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sum1</span><span class="params">(name)</span>:</span></span><br><span class="line">    <span class="keyword">global</span> a, b, c</span><br><span class="line">    print(<span class="string">'Run child process %s (%s)...'</span> % (name, os.getpid()))</span><br><span class="line">    a.value = a.value + <span class="number">1</span> + c.value</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sum2</span><span class="params">(name)</span>:</span></span><br><span class="line">    <span class="keyword">global</span> a, c, d</span><br><span class="line">    print(<span class="string">'Run child process %s (%s)...'</span> % (name, os.getpid()))</span><br><span class="line">    a.value = a.value + <span class="number">2</span></span><br><span class="line">    c.value = c.value + d.value</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">'__main__'</span>:</span><br><span class="line">    print(<span class="string">'Parent process %s.'</span> % os.getpid())</span><br><span class="line">    p = Process(target=sum1, args=(<span class="string">'a + b'</span>,))</span><br><span class="line">    q = Process(target=sum2, args=(<span class="string">'c + d'</span>,))</span><br><span class="line">    print(<span class="string">'Child process will start.'</span>)</span><br><span class="line">    p.start()</span><br><span class="line">    q.start()</span><br><span class="line">    q.join()</span><br><span class="line">    p.join()</span><br><span class="line">    print(<span class="string">'Child process end.'</span>)</span><br><span class="line">    print(a.value)</span><br></pre></td></tr></table></figure><p>输出结果如下，a的值是7,表明此时多进程以共享数据。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Parent process 23949.</span><br><span class="line">Child process will start.</span><br><span class="line">Run child process a + b (23950)...</span><br><span class="line">Run child process c + d (23951)...</span><br><span class="line">Child process end.</span><br><span class="line">7.0</span><br></pre></td></tr></table></figure><p>顺手给出其他类型的数据共享的方法：</p><ul><li>数组型：<code>num=multiprocessing.Array(&quot;i&quot;,[1,2,3,4,5])</code></li><li>字典：<code>mydict=multiprocessing.Manager().dict()</code></li><li>列表：<code>mylist=multiprocessing.Manager().list(range(5))</code></li></ul><h2 id="多进程性能记录"><a href="#多进程性能记录" class="headerlink" title="多进程性能记录"></a>多进程性能记录</h2><p>在前文提到，每个进程有一个主线程，两个进程便有两个主线程。将两个线程分配给两个CPU核心，有利于提升程序的执行效率，那么来一探究竟。</p><p>这也要求了函数是<strong>平行</strong>的，即函数的执行没有明显的顺序关系。首先，创建两个进程，在创建两个函数。分别对比顺序执行两个函数和用两个进程执行两个函数的时间，以此来观察多进程是否提升了效率。其中计时函数利用python的<code>time.time()</code>方法，且只记录函数的执行时间。</p><h3 id="顺序执行两个函数"><a href="#顺序执行两个函数" class="headerlink" title="顺序执行两个函数"></a>顺序执行两个函数</h3><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line">a, b, c, d = <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sum1</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">global</span> a, b, c</span><br><span class="line">    a = a + <span class="number">1</span> + c</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sum2</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">global</span> a, c, d</span><br><span class="line">    a = a + <span class="number">2</span></span><br><span class="line">    c = c + d</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">'__main__'</span>:</span><br><span class="line">    start = time.time()</span><br><span class="line">    sum1()</span><br><span class="line">    sum2()</span><br><span class="line">    end = time.time()</span><br><span class="line">    print(end - start)</span><br><span class="line">    print(a)</span><br></pre></td></tr></table></figure><h3 id="两个进程分别执行"><a href="#两个进程分别执行" class="headerlink" title="两个进程分别执行"></a>两个进程分别执行</h3><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Process</span><br><span class="line"><span class="keyword">import</span> os, multiprocessing</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line">a, b, c, d = <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 子进程要执行的代码</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sum1</span><span class="params">(name)</span>:</span></span><br><span class="line">    <span class="keyword">global</span> a, c</span><br><span class="line">    a = a + <span class="number">1</span> + c</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sum2</span><span class="params">(name)</span>:</span></span><br><span class="line">    <span class="keyword">global</span> a, c, d</span><br><span class="line">    a = a + <span class="number">2</span></span><br><span class="line">    c = c + d</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">'__main__'</span>:</span><br><span class="line">    print(<span class="string">'Parent process %s.'</span> % os.getpid())</span><br><span class="line">    p = Process(target=sum1, args=(<span class="string">'a + b'</span>,))</span><br><span class="line">    q = Process(target=sum2, args=(<span class="string">'c + d'</span>,))</span><br><span class="line">    print(<span class="string">'Child process will start.'</span>)</span><br><span class="line">    start = time.time()</span><br><span class="line">    p.start()</span><br><span class="line">    q.start()</span><br><span class="line">    q.join()</span><br><span class="line">    p.join()</span><br><span class="line">    end = time.time()</span><br><span class="line">    print(<span class="string">'Child process end.'</span>)</span><br><span class="line">    print(end - start)</span><br></pre></td></tr></table></figure><p>这两个程序不用对比了，顺序执行的比进程执行的要块的多，顺序执行大约0.00000009秒，线程执行大约0.002秒，根本不在一个数量级。</p><h2 id="修正"><a href="#修正" class="headerlink" title="修正"></a>修正</h2><p>也许你看到这里会疑问，为什么顺序执行会比多进程要快，多进程明明把两个进程分配给了两个核心，并行执行为啥还慢了好几个数量级？</p><p>因为，进程的创建、销毁也有额外的开销啊～当开销时间远远比执行时间少的时候，才能体现多进程的优势。既然这样，就额外增加程序的执行时间即可，让两个函数都延时两秒看看。</p><h3 id="延时的顺序执行"><a href="#延时的顺序执行" class="headerlink" title="延时的顺序执行"></a>延时的顺序执行</h3><p>通过time.sleep(2)方法给每个函数增加两秒的延时：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line">a, b, c, d = <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sum1</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">global</span> a, b, c</span><br><span class="line">    a = a + <span class="number">1</span> + c</span><br><span class="line">    time.sleep(<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sum2</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="keyword">global</span> a, c, d</span><br><span class="line">    a = a + <span class="number">2</span></span><br><span class="line">    c = c + d</span><br><span class="line">    time.sleep(<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">'__main__'</span>:</span><br><span class="line">    start = time.time()</span><br><span class="line">    sum1()</span><br><span class="line">    sum2()</span><br><span class="line">    end = time.time()</span><br><span class="line">    print(end - start)</span><br><span class="line">    print(a)</span><br></pre></td></tr></table></figure><h3 id="延时的多进程"><a href="#延时的多进程" class="headerlink" title="延时的多进程"></a>延时的多进程</h3><p>通过time.sleep(2)方法给每个函数增加两秒的延时：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Process</span><br><span class="line"><span class="keyword">import</span> os, multiprocessing</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line">a, b, c, d = <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 子进程要执行的代码</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sum1</span><span class="params">(name)</span>:</span></span><br><span class="line">    <span class="keyword">global</span> a, c</span><br><span class="line">    a = a + <span class="number">1</span> + c</span><br><span class="line">    time.sleep(<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sum2</span><span class="params">(name)</span>:</span></span><br><span class="line">    <span class="keyword">global</span> a, c, d</span><br><span class="line">    a = a + <span class="number">2</span></span><br><span class="line">    c = c + d</span><br><span class="line">    time.sleep(<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">'__main__'</span>:</span><br><span class="line">    print(<span class="string">'Parent process %s.'</span> % os.getpid())</span><br><span class="line">    p = Process(target=sum1, args=(<span class="string">'a + b'</span>,))</span><br><span class="line">    q = Process(target=sum2, args=(<span class="string">'c + d'</span>,))</span><br><span class="line">    print(<span class="string">'Child process will start.'</span>)</span><br><span class="line">    start = time.time()</span><br><span class="line">    p.start()</span><br><span class="line">    q.start()</span><br><span class="line">    q.join()</span><br><span class="line">    p.join()</span><br><span class="line">    end = time.time()</span><br><span class="line">    print(<span class="string">'Child process end.'</span>)</span><br><span class="line">    print(end - start)</span><br></pre></td></tr></table></figure><p>我这里的对比结果是：顺序执行的需要4秒，而多进程仅需2秒。证明了多进程的确能有效提高程序的执行效率。</p><h2 id="进程池"><a href="#进程池" class="headerlink" title="进程池"></a>进程池</h2><p>可以使用进程池实现对每个进程的管理，即：决定哪些进程可以在进程池内，等待进程池内的所有进程执行完毕才执行下一部分的程序。粗暴的理解：一个大池子，这个池子里有好多进程，通过这个池子实现对进程的统一管理。</p><p>为了体现进程的<strong>并发</strong>，我特意在12个核的CPU内创建了13个进程。意思是：会有一个进程要等待其他进程释放一个CPU资源才能执行。进程池的close()方法会阻止其他进程在进入进程池，join()方法会等待进程池全部的进程执行完毕。</p><p>创建13个进程，并使每个进程等待2秒观看执行效果：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Pool</span><br><span class="line"><span class="keyword">import</span> os, time</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">long_time_task</span><span class="params">(name)</span>:</span></span><br><span class="line">    print(<span class="string">'Run task %s (%s)...'</span> % (name, os.getpid()))</span><br><span class="line">    start = time.time()</span><br><span class="line">    time.sleep(<span class="number">2</span>)</span><br><span class="line">    end = time.time()</span><br><span class="line">    print(<span class="string">'Task %s runs %0.2f seconds.'</span> % (name, (end - start)))</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">'__main__'</span>:</span><br><span class="line">    print(<span class="string">'Parent process %s.'</span> % os.getpid())</span><br><span class="line">    p = Pool(<span class="number">13</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">14</span>):</span><br><span class="line">        p.apply_async(long_time_task, args=(i,))</span><br><span class="line">    print(<span class="string">'Waiting for all subprocesses done...'</span>)</span><br><span class="line">    <span class="comment"># 不能在增加其他进程</span></span><br><span class="line">    p.close()</span><br><span class="line">    p.join()</span><br><span class="line">    print(<span class="string">'All subprocesses done.'</span>)</span><br></pre></td></tr></table></figure><p><img data-src="/2020/03/16/multi-process-thread/2.png" alt></p><p>可以看到，第13个进程很晚才创建，在3, 2, 1, 12, 0, 10, 4, 7, 11, 8这几个进程执行完毕后，第13个进程才刚刚创建。</p><p>在这13个进程中，每个进程都有一个主线程。CPU给线程分配时间片(也就是分配给线程的时间)，执行完时间片后会切换都另一个线程。从保存线程A的状态到准备切换为线程B时，加载线程B的状态的这个过程就叫上下文切换，而上下切换时会消耗大量的CPU时间。因此，只有在特定的条件下开启多线程才会更合适。不可避免的线程开销包括：</p><ul><li><p>上下文切换消耗</p></li><li><p>线程创建和消亡的开销</p></li><li><p>线程需要保存维持线程本地栈，会消耗内存</p></li></ul><p>对于计算密集型的程序，程序主要为复杂的逻辑判断和复杂的运算，此时CPU的利用率高，不用开太多的线程，开太多线程反而会因为线程切换时切换上下文而浪费资源。计算密集型任务由于主要消耗CPU资源，因此，代码运行效率至关重要。Python这样的脚本语言运行效率很低，完全不适合计算密集型任务。对于计算密集型任务，最好用C语言编写。</p><p>对于IO密集型的程序，比如磁盘IO(读取文件)和网络IO(网络请求)。因为IO操作会阻塞线程，CPU利用率不高，可以开多点线程，一个线程阻塞时(网络延迟等)可以切换到其他就绪线程，提高CPU的利用率。这类任务的特点是CPU消耗很少，任务的大部分时间都在等待IO操作完成（因为IO的速度远远低于CPU和内存的速度）。IO密集型任务执行期间，99%的时间都花在IO上，花在CPU上的时间很少，因此，用运行速度极快的C语言替换用Python这样运行速度极低的脚本语言，完全无法提升运行效率。对于IO密集型任务，最合适的语言就是开发效率最高（代码量最少）的语言，脚本语言是首选，C语言最差。</p><h2 id="进程通信"><a href="#进程通信" class="headerlink" title="进程通信"></a>进程通信</h2><p>举个通俗但不严谨的例子：在输入法开启英文模式并向记事本中打字时，一边打字一边显示，这就涉及了打字进程和记事本的显示进程。记事本的显示进程一直读取打字进程的打字结果，讲打字结果读取并显示在屏幕上，这就设计了两个进程之间的通信。那么实现一个边写边读的例子：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> multiprocessing <span class="keyword">import</span> Process, Queue</span><br><span class="line"><span class="keyword">import</span> os, time, random</span><br><span class="line"></span><br><span class="line"><span class="comment"># 写数据进程执行的代码:</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">write</span><span class="params">(q)</span>:</span></span><br><span class="line">    print(<span class="string">'Process to write: %s'</span> % os.getpid())</span><br><span class="line">    <span class="keyword">for</span> value <span class="keyword">in</span> [<span class="string">'A'</span>, <span class="string">'B'</span>, <span class="string">'C'</span>]:</span><br><span class="line">        print(<span class="string">'Put %s to queue...'</span> % value)</span><br><span class="line">        q.put(value)</span><br><span class="line">        time.sleep(random.random())</span><br><span class="line"></span><br><span class="line"><span class="comment"># 读数据进程执行的代码:</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">read</span><span class="params">(q)</span>:</span></span><br><span class="line">    print(<span class="string">'Process to read: %s'</span> % os.getpid())</span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        value = q.get(<span class="literal">True</span>)</span><br><span class="line">        print(<span class="string">'Get %s from queue.'</span> % value)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__==<span class="string">'__main__'</span>:</span><br><span class="line">    <span class="comment"># 父进程创建Queue，并传给各个子进程：</span></span><br><span class="line">    q = Queue()</span><br><span class="line">    pw = Process(target=write, args=(q,))</span><br><span class="line">    pr = Process(target=read, args=(q,))</span><br><span class="line">    <span class="comment"># 启动子进程pw，写入:</span></span><br><span class="line">    pw.start()</span><br><span class="line">    <span class="comment"># 启动子进程pr，读取:</span></span><br><span class="line">    pr.start()</span><br><span class="line">    <span class="comment"># 等待pw结束:</span></span><br><span class="line">    pw.join()</span><br><span class="line">    <span class="comment"># pr进程里是死循环，无法等待其结束，只能强行终止:</span></span><br><span class="line">    pr.terminate()</span><br></pre></td></tr></table></figure><h1 id="多线程编程"><a href="#多线程编程" class="headerlink" title="多线程编程"></a>多线程编程</h1><p>假如机器本身安装了多个处理器，那么程序会运行得更快，毋需作出任何特殊的调校。多任务可以由多进程完成，也可以由一个进程内的多线程完成。</p><h2 id="创建线程"><a href="#创建线程" class="headerlink" title="创建线程"></a>创建线程</h2><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> time, threading</span><br><span class="line"></span><br><span class="line"><span class="comment"># 新线程执行的代码:</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">loop</span><span class="params">()</span>:</span></span><br><span class="line">    print(<span class="string">'thread %s is running...'</span> % threading.current_thread().name)</span><br><span class="line">    time.sleep(<span class="number">1</span>)</span><br><span class="line">    print(<span class="string">'thread %s ended.'</span> % threading.current_thread().name)</span><br><span class="line"></span><br><span class="line">print(<span class="string">'thread %s is running...'</span> % threading.current_thread().name)</span><br><span class="line">t = threading.Thread(target=loop, name=<span class="string">'Thread-1'</span>)</span><br><span class="line">t.start()</span><br><span class="line">t.join()</span><br><span class="line">print(<span class="string">'thread %s ended.'</span> % threading.current_thread().name)</span><br></pre></td></tr></table></figure><p>由于任何进程默认就会启动一个线程，我们把该线程称为主线程，主线程又可以启动新的线程。current_thread()函数，它永远返回当前线程的实例。主线程实例的名字叫MainThread，子线程的名字在创建时指定，我们用Thread-1命名子线程。输出结果如下（最后输出的是主进程的结束）：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">thread MainThread is running...</span><br><span class="line">thread LoopThread is running...</span><br><span class="line">thread LoopThread ended.</span><br><span class="line">thread MainThread ended.</span><br></pre></td></tr></table></figure><h2 id="线程锁"><a href="#线程锁" class="headerlink" title="线程锁"></a>线程锁</h2><p>使用多线程必须注意一个问题，多个线程之间共享资源！如果有多个线程同时运行，而且它们试图访问相同的资源，就会遇到各种问题。举个例子来说，两个线程不能同时读写文件，以最常见的$\LaTeX$为例，除summtrapdf外，其他pdf阅读器在观看pdf文件时，不能进行编译，在编译的时候要提前关闭打开的pdf。即一个线程要读，一个线程要写，读线程占据着资源，写线程得不到资源，自然就报错了。</p><p>多线程中，变量都由所有线程共享。所以，任何一个变量都可以被任何一个线程修改。如修改一个变量需要多条语句，在执行这几条语句时，线程可能中断，很可能把内容给改乱了。如下所示的代码，创建一个为0的变量，创建两个线程，并反复执行+1和-1的操作，最后的结果不一定是0。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">balance = <span class="number">0</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">change_it</span><span class="params">(n)</span>:</span></span><br><span class="line">    <span class="comment"># 先存后取，结果应该为0:</span></span><br><span class="line">    <span class="keyword">global</span> balance</span><br><span class="line">    balance = balance + n</span><br><span class="line">    balance = balance - n</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">run_thread</span><span class="params">(n)</span>:</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">10000000</span>):</span><br><span class="line">        change_it(n)</span><br><span class="line"></span><br><span class="line">t1 = threading.Thread(target=run_thread, args=(<span class="number">1</span>,))</span><br><span class="line">t2 = threading.Thread(target=run_thread, args=(<span class="number">1</span>,))</span><br><span class="line">t1.start()</span><br><span class="line">t2.start()</span><br><span class="line">t1.join()</span><br><span class="line">t2.join()</span><br><span class="line">print(balance)</span><br></pre></td></tr></table></figure><p>输出的结果是2。为什么呢？因为两个线程的调度是由操作系统决定的，是交替执行的。第一步计算balance + 1，存入临时变量中，第二步将临时变量的值赋给balance。那么执行顺序是这样时：(x1 和 x2 是临时变量)</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">初始值 balance = 0</span><br><span class="line"></span><br><span class="line">t1: x1 = balance + 1      # x1 = 0 + 1 = 1</span><br><span class="line">t2: x2 = balance + 1      # x2 = 0 + 1 = 1</span><br><span class="line"></span><br><span class="line">t2: balance = x2          # balance = 1</span><br><span class="line">t1: balance = x1          # balance = 1</span><br><span class="line"></span><br><span class="line">t1: x1 = balance - 1      # x1 = 1 - 1 = 0</span><br><span class="line">t1: balance = x1          # balance = 0</span><br><span class="line"></span><br><span class="line">t2: x2 = balance - 1      # x2 = 0 - 1 = -1</span><br><span class="line">t2: balance = x2          # balance = -1</span><br></pre></td></tr></table></figure><p>结果 balance = -1，不再是脑补出来的0。因此，线程之间共享数据最大的危险在于多个线程同时改一个变量。你也不想银行系统里改着改着你的存款成了负数，或者结婚系统里你的老婆成了别人的老婆，滑稽。</p><p>为了保证变量的正确，务必要给操作函数上一把锁子，姑且把操作的函数成为change_it()。当某个线程开始执行change_it()时，我们说，该线程因为获得了锁，因此<strong>其他线程不能同时执行</strong>change_it()，只能等待。直到锁被释放后，其他线程获得该锁以后才能继续操作。由于锁只有一个，无论多少线程，同一时刻最多只有一个线程持有该锁，所以，不会造成修改的冲突。创建一个锁就是通过threading.Lock()来实现：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">balance = <span class="number">0</span></span><br><span class="line">lock = threading.Lock()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">run_thread</span><span class="params">(n)</span>:</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">100000</span>):</span><br><span class="line">        <span class="comment"># 先要获取锁:</span></span><br><span class="line">        lock.acquire()</span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            <span class="comment"># 放心地改吧:</span></span><br><span class="line">            change_it(n)</span><br><span class="line">        <span class="keyword">finally</span>:</span><br><span class="line">            <span class="comment"># 改完了一定要释放锁:</span></span><br><span class="line">            lock.release()</span><br></pre></td></tr></table></figure><p>当多个线程同时执行lock.acquire()时，只有一个线程能成功地获取锁，然后继续执行代码，其他线程就继续等待锁释放，直到获得锁为止。获得锁的线程用完后一定要释放锁，否则那些苦苦等待锁的线程将永远等待下去，成为死线程。所以我们用try…finally来确保锁一定会被释放。</p><p>锁的好处就是确保了某段关键代码只能由一个线程从头到尾完整地执行，坏处当然也很多。首先是程序操作临界资源时，阻止了多线程<strong>并发</strong>执行，包含锁的某段代码实际上只能以单线程模式执行，效率就大大地下降了。临界资源举例：如打印机，同一时刻只能由一个线程控制打印，如写文件，同一时刻只能有一个线程在写。</p><p>其次，由于可以存在多个锁，不同的线程持有不同的锁，并试图获取对方持有的锁时，可能会造成死锁。如线程A等待线程B释放资源C，线程B要等待线程A释放资源D，两者均需要对方先释放，于是两者相互等待，无限套娃。在详细一点举个不恰当的例子，一个绑架犯和被绑架的亲属，绑架犯说：不给钱就不放人，绑架犯亲属说：不放人就不给钱。对于绑架犯而言，人质是获取的锁，想要获取钱这个锁；对于绑架犯亲属而言，钱是获取的锁，想要获取人质这个锁。但双方都不先松口，即不释放自己的资源，于是无限套娃，造成线程死锁，多个线程全部挂起，既不能执行，也无法结束，只能靠操作系统强制终止。（操作系统有一套判断死锁的方法，详情参考《操作系统》相关书籍），所以需要避免线程的死锁。</p><h2 id="多核线程"><a href="#多核线程" class="headerlink" title="多核线程"></a>多核线程</h2><p>如果你不幸拥有一个多核CPU，你肯定在想，多核应该可以同时执行多个线程。如果写一个死循环的话，会出现什么情况呢？我们可以监控到一个死循环线程会100%占用一个CPU。</p><p>如果有两个死循环线程，在多核CPU中，可以监控到会占用200%的CPU，也就是占用两个CPU核心。要想把N核CPU的核心全部跑满，就必须启动N个死循环线程。</p><p>但python启动与CPU核心数量相同的N个线程跑死循环，在4核CPU上可以监控到CPU占用率仅有102%，也就是仅使用了一核。但是用C、C++或Java来改写相同的死循环，直接可以把全部核心跑满，4核就跑到400%，8核就跑到800%，为什么Python不行呢？</p><p>因为Python的线程虽然是真正的线程，但解释器执行代码时，有一个GIL锁：Global Interpreter Lock。任何Python线程执行前，必须先获得GIL锁，然后，每执行100条字节码，解释器就自动释放GIL锁，让别的线程有机会执行。这个GIL全局锁实际上把所有线程的执行代码都给上了锁，所以，多线程在Python中只能交替执行，即使100个线程跑在100核CPU上，也只能用到1个核。</p><p>GIL最大的问题就是Python的多线程程序并不能利用多核CPU的优势 。在讨论普通的GIL之前，有一点要强调的是GIL只会影响到那些严重依赖CPU的程序（比如计算型的）。 如果你的程序大部分只会涉及到I/O，比如网络交互，那么使用多线程就很合适， 因为它们大部分时间都在等待。实际上，你完全可以放心的创建几千个Python线程， 现代操作系统运行这么多线程没有任何压力，没啥可担心的。</p><p>GIL是Python解释器设计的历史遗留问题，通常我们用的解释器是官方实现的CPython，要真正利用多核，除非重写一个不带GIL的解释器。所以，在Python中，可以使用多线程，但不要指望能有效利用多核。不过，也不用过于担心，Python虽然不能利用多线程实现多核任务，但可以通过多进程实现多核任务。多个Python进程有各自独立的GIL锁，互不影响。</p><h2 id="线程的变量"><a href="#线程的变量" class="headerlink" title="线程的变量"></a>线程的变量</h2><p>不同的线程调用函数，且需要处理一些局部变量时，也可以写成这样不断的传参，只是很麻烦罢了：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">process_student</span><span class="params">(name)</span>:</span></span><br><span class="line">    std = Student(name)</span><br><span class="line">    <span class="comment"># std是局部变量，但是每个函数都要用它，因此必须传进去：</span></span><br><span class="line">    do_task_1(std)</span><br><span class="line">    do_task_2(std)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">do_task_1</span><span class="params">(std)</span>:</span></span><br><span class="line">    do_subtask_1(std)</span><br><span class="line">    do_subtask_2(std)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">do_task_2</span><span class="params">(std)</span>:</span></span><br><span class="line">    do_subtask_2(std)</span><br><span class="line">    do_subtask_2(std)</span><br></pre></td></tr></table></figure><p>在多线程环境下，每个线程都有自己的数据。一个线程使用自己的局部变量比使用全局变量好（不是程序中的局部变量和全局变量，是线程里的局部变量和全局变量）。因为线程的局部变量只有线程自己能看见，不会影响其他线程。可以通过ThreadLocal实现避免局部变量的层层传递的问题：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> threading</span><br><span class="line">    </span><br><span class="line"><span class="comment"># 创建全局ThreadLocal对象:</span></span><br><span class="line">local_school = threading.local()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">process_student</span><span class="params">()</span>:</span></span><br><span class="line">    <span class="comment"># 获取当前线程关联的student:</span></span><br><span class="line">    std = local_school.student</span><br><span class="line">    print(<span class="string">'Hello, %s in %s'</span> % (std, threading.current_thread().name))</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">process_thread</span><span class="params">(string)</span>:</span></span><br><span class="line">    <span class="comment"># 绑定ThreadLocal的student:</span></span><br><span class="line">    local_school.student = string</span><br><span class="line">    process_student()</span><br><span class="line"></span><br><span class="line">t1 = threading.Thread(target = process_thread, args=(<span class="string">'Alice'</span>,), name=<span class="string">'Thread-A'</span>)</span><br><span class="line">t2 = threading.Thread(target = process_thread, args=(<span class="string">'Bob'</span>,), name=<span class="string">'Thread-B'</span>)</span><br><span class="line">t1.start()</span><br><span class="line">t2.start()</span><br><span class="line">t1.join()</span><br><span class="line">t2.join()</span><br></pre></td></tr></table></figure><p>输出结果如下：</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">Hello, Alice <span class="keyword">in</span> Thread-A</span><br><span class="line">Hello, Bob <span class="keyword">in</span> Thread-B</span><br></pre></td></tr></table></figure><p>全局变量local_school就是一个ThreadLocal对象，每个Thread对它都可以读写student属性，但每个线程都只能读写自己线程的独立副本，互不影响。你可以把local_school看成全局变量，但每个属性如local_school.student都是线程的局部变量，可以任意读写而互不干扰，也不用管理锁的问题。ThreadLocal解决了参数在一个线程中各个函数之间互相传递的问题。</p><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>我们介绍了多进程和多线程，这是实现多任务最常用的两种方式。现在，我们来讨论一下这两种方式的优缺点。</p><p>多进程模式最大的优点就是稳定性高，因为一个子进程崩溃了，不会影响主进程和其他子进程。（当然主进程挂了所有进程就全挂了，但是Master进程只负责分配任务，挂掉的概率低）。</p><p>多进程模式的缺点是创建进程的代价大，在Unix/Linux系统下，用fork调用还行，在Windows下创建进程开销巨大。</p><p>多线程模式致命的缺点就是任何一个线程挂掉都可能直接造成整个进程崩溃，因为所有线程共享进程的内存。在Windows上，如果一个线程执行的代码出了问题，你经常可以看到这样的提示：“该程序执行了非法操作，即将关闭”，其实往往是某个线程出了问题，但是操作系统会强制结束整个进程。</p><p>无论是多进程还是多线程，只要数量一多，效率肯定上不去，为什么呢？</p><p>因为进程或线程的切换是有代价的，操作系统在切换进程或者线程时需要先保存当前执行的现场环境（CPU寄存器状态、内存页等），然后，把新任务的执行环境准备好（恢复上次的寄存器状态，切换内存页等），才能开始执行。这个切换过程虽然很快，但是也需要耗费时间。如果有几千个任务同时进行，操作系统可能就主要忙着切换任务，根本没有多少时间去执行任务了，这种情况最常见的就是硬盘狂响，点窗口无反应，系统处于假死状态。</p><p>所以，多任务一旦多到一个限度，就会消耗掉系统所有的资源，结果效率急剧下降，所有任务都做不好。</p><h1 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h1><p>为下文的异步爬虫做准备。考虑到CPU和IO之间巨大的速度差异，一个任务在执行的过程中大部分时间都在等待IO操作，单进程单线程模型会导致别的任务无法并行执行，因此，我们才需要多进程模型或者多线程模型来支持多任务并发执行。</p><p>现代操作系统对IO操作已经做了巨大的改进，最大的特点就是支持异步IO。如果充分利用操作系统提供的异步IO支持，就可以用单进程单线程模型来执行多任务，这种全新的模型称为事件驱动模型，在单核CPU上采用单进程模型就可以高效地支持多任务。在多核CPU上，可以运行多个进程（数量与CPU核心数相同），充分利用多核CPU。由于系统总的进程数量十分有限，因此操作系统调度非常高效。</p><p>用异步IO编程模型来实现多任务是一个主要的趋势。对应到Python语言，单线程的异步编程模型称为协程，有了协程的支持，就可以基于事件驱动编写高效的多任务程序，我会在后面讨论如何编写协程。</p><p>至此，通过本文的学习我了解了三种实现多任务的方式：（没错我也是刚学，本文只是学习内容的整理）</p><ul><li>多进程</li><li>多线程并发</li><li>协称</li></ul><p>会在后文实际对比这三种方法的效率，敬请期待。</p><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p>站在巨人的肩膀上，我们能前行的更快。</p><p><a href="https://blog.csdn.net/zhengyshan/article/details/80641770" target="_blank" rel="noopener">https://blog.csdn.net/zhengyshan/article/details/80641770</a></p><p><a href="https://blog.csdn.net/SSIrreplaceable/article/details/53171785" target="_blank" rel="noopener">https://blog.csdn.net/SSIrreplaceable/article/details/53171785</a></p><p><a href="https://www.liaoxuefeng.com/wiki/1016959663602400/1017629247922688" target="_blank" rel="noopener">https://www.liaoxuefeng.com/wiki/1016959663602400/1017629247922688</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;作为一个程序员，我发现我不会写多进程和多线程的程序，实在羞愧，在赶完毕业设计后准备开一个多线程编程的坑。本文是我这种小白从无到有的学习收获，内容较多，还请耐心观看。因疫情原因没在学校，手头没有一本靠谱的《操作系统》的书籍，大多东西为网上搜刮而来后整理，可能不严谨。本文收录：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;引入线程、进程的基本概念；&lt;/li&gt;
&lt;li&gt;多进程编程：变量引用、性能分析、进程池和进程通信；&lt;/li&gt;
&lt;li&gt;多线程编程：线程锁、线程的局部变量；&lt;/li&gt;
&lt;li&gt;实际项目：暂时的目标是多线程爬虫，内容过多准备放下一个文章（同时也包括防止线程死锁、协程等）。&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
    
    
      <category term="Python" scheme="https://muyuuuu.github.io/tags/Python/"/>
    
      <category term="Computer" scheme="https://muyuuuu.github.io/tags/Computer/"/>
    
  </entry>
  
  <entry>
    <title>修复双系统切换导致V2Ray无法使用的问题</title>
    <link href="https://muyuuuu.github.io/2020/03/13/v2ray-date/"/>
    <id>https://muyuuuu.github.io/2020/03/13/v2ray-date/</id>
    <published>2020-03-13T12:40:12.000Z</published>
    <updated>2020-03-13T16:17:56.435Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>在给系统安装<code>Linux</code>后，也配置了<code>V2Ray</code>。但发现了一个问题：</p><ul><li>之前<code>windows</code>使用<code>V2Ray</code>是没有任何问题的；</li><li><code>Linux</code>使用<code>V2Ray</code>也是没有任何问题的；</li><li>但是，从<code>Linux</code>切换回<code>windows</code>，<code>windows</code>的<code>V2Ray</code>就不能用了，得关机、重启、过一段时间才能用。</li></ul><p>本文给出此类问题的修复方法。</p><a id="more"></a><h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><p>安装还请移步这里：<a href="https://toutyrater.github.io/" target="_blank" rel="noopener">https://toutyrater.github.io/</a></p><h1 id="修复"><a href="#修复" class="headerlink" title="修复"></a>修复</h1><p>对于我这种不太行的程序员而言，在没有接触过这个领域的编程知识时，要想解决问题，通常只能<strong>排除变量</strong>。而在多次对系统开机、关机、重启来回切换于两系统寻找<code>BUG</code>期间，并没有发现配置文件不对的问题。意思就是，两个系统的配置文件（包括<code>SwitchOmega</code>）都是正确的，那为什么<code>windows</code>不能用但是<code>Linux</code>可以使用呢？</p><p>经过不断的切换，终于发现了唯一的一个不同点，系统的时间。如果<code>Linux</code>系统的时间是正常的话，正常的定义是，现在是北京时间<code>23:01:52</code>，那么<code>Linux</code>的时间也会是<code>23:01:52</code>，而<code>windows</code>的时间却提前了10个小时，是<code>13:01:52</code>。也就是往西边跑了几个时区。那么再来回顾<code>V2Ray</code>的配置注意事项：</p><ol><li><code>V2Ray</code>对于时间有比较严格的要求，要求服务器和客户端时间差绝对值不能超过 2 分钟，所以一定要保证时间足够准确。</li><li><code>V2Ray</code>并不要求时区一致。比如说自己电脑上的时间是北京时间（东 <code>8</code> 区）<code>2017-07-31 12:08:31</code>，但是 <code>VPS</code> 上的时区是东 <code>9</code> 区，所以 <code>VPS</code> 上的时间应该是<code>2017-07-31 13:06:31</code> 到 <code>2017-07-31 13:10:31</code> 之间才能正常使用 <code>V2Ray</code>。当然，也可以自行改成自己想要的时区。</li></ol><p>读到这里，很容易发现是<code>windows</code>系统的时间不对导致的<code>V2Ray</code>无法使用，这时候只需要修改<code>windows</code>系统的时间即可。</p><p>键盘<code>windows+s</code>输入设置，打开<code>时间和语言</code>，点击 <code>立即同步</code> 即可完成时间的修改，即将时间修复为所在时区的时间。而后就可以正常使用了。</p><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p><a href="https://toutyrater.github.io/prep/start.html" target="_blank" rel="noopener">https://toutyrater.github.io/prep/start.html</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在给系统安装&lt;code&gt;Linux&lt;/code&gt;后，也配置了&lt;code&gt;V2Ray&lt;/code&gt;。但发现了一个问题：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;之前&lt;code&gt;windows&lt;/code&gt;使用&lt;code&gt;V2Ray&lt;/code&gt;是没有任何问题的；&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Linux&lt;/code&gt;使用&lt;code&gt;V2Ray&lt;/code&gt;也是没有任何问题的；&lt;/li&gt;
&lt;li&gt;但是，从&lt;code&gt;Linux&lt;/code&gt;切换回&lt;code&gt;windows&lt;/code&gt;，&lt;code&gt;windows&lt;/code&gt;的&lt;code&gt;V2Ray&lt;/code&gt;就不能用了，得关机、重启、过一段时间才能用。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;本文给出此类问题的修复方法。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="Computer" scheme="https://muyuuuu.github.io/tags/Computer/"/>
    
  </entry>
  
  <entry>
    <title>一些不常见的进阶博客优化</title>
    <link href="https://muyuuuu.github.io/2020/03/12/blog-optimize/"/>
    <id>https://muyuuuu.github.io/2020/03/12/blog-optimize/</id>
    <published>2020-03-12T06:47:08.000Z</published>
    <updated>2020-03-21T12:54:56.543Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>首先，如果你是想来搭建博客的小白，那么这篇文章不适合你，这篇文章写的优化博客是一些进阶操作。常见的：文章字体、打赏按钮、图片加载、数学公式、音乐链接、不蒜子统计等等等等，网上一搜一大把的我都认为是常规设置，本文在详细描述。而本文收录的进阶优化有：</p><ul><li>降低网页功耗</li><li>快速加载博客</li></ul><p>此外，本次优化仅针对<code>next</code>主题，但其他主题也可参考借鉴优化思路，只是操作方法不敢保证完全一致。</p><a id="more"></a><h1 id="降低网页功耗"><a href="#降低网页功耗" class="headerlink" title="降低网页功耗"></a>降低网页功耗</h1><p>起初，我还真没在意网页的功耗问题，首先举个例子明白功耗是干什么用的。假设，你打开电脑，不管是写<code>word</code>，听音乐还是看电影，只要电脑在工作就会产生功耗，单位是瓦(W)。一般打开网页和浏览网页的功耗是10W~15W左右(我亲手测过)，而你笔记本的电池是60Wh的，那么你笔记本在满电、不充电的情况下，浏览网页的时长大约为6小时（$60\text{Wh} \div 10\text{W} = 6\text{h}$）。而浏览网页的同时，如果再有一些写<code>word</code>，听音乐或者跑程序的操作，那么电脑的续航时长也就4小时左右。</p><p>这就是笔记本续航的粗暴计算方法。但是很遗憾，市面上常见轻薄本的电池均为60Wh左右，但都在宣传自己笔记本续航高达10小时以上，我实在不能理解这是怎么计算出来的。所以，买电脑的时候不要看它吹嘘的续航时间多长，应该看电池是多少Wh的。</p><p>扯远了，回归正题，如何降低博客的功耗呢？我第一次发现问题是在<code>Linux</code>系统中，打开我的博客后发现，电脑风扇在飞速旋转，键盘也开始发烫。嗯？这是为何，我也没载入杀病毒啊。找了一个查看功耗的命令显示下现在的功耗。右上角为查看<code>CPU</code>，内存等利用率，右下角为功耗，单位W。可以看到，我博客当初的功耗为55W。</p><p><img data-src="/2020/03/12/blog-optimize/1.png" alt></p><p>经过一段时间的排查变量实锤了一个现象：观看我的博客，功耗飞速上升到55W，CPU利用率<code>10%</code>，然后关闭我的博客，功耗降低到1W左右，极度真实。55W的功耗，意味着，我笔记本在看我博客的时候只能保证一个小时的续航，岂不给看我博客的用户带来极度不良体验？我寻思我就看个网页不至于啊，那么看一下别人同款<code>next</code>主题的博客，什么？5W？<code>CPU</code>利用率从<code>10%</code>降到了<code>0%</code>，见鬼了？不行，得优化。</p><p><img data-src="/2020/03/12/blog-optimize/2.png" alt></p><p>废话扯多了，优化的方案无非就是排除变量，（毕竟我不会前端无法确定哪个插件的功耗影响有多大）。索性打开了主题文件夹（非站点文件夹）下的<code>_config.yml</code>文件，大不了一个一个改。</p><p>我看到了其中的<code>minify, lazyload, quicklink, pjax, jquery, SEO, baidu_push</code>等等一些列配置，发现，问题丝毫没在这些上面，且与我魔改主题的字体，颜色都无关。不绕弯子了，问题出在<code>live2d</code>这个看板娘上，就是他导致的过高的功耗。注意，不仅要在博客里关闭看板娘，还要删除安装在站点文件夹下<code>node_modules</code>里面的<code>live2d</code>的配置文件。<del>（因为我最开始也只是关闭，结果发现看板娘还在，索性在<code>node_modules</code>文件夹里面搜索名称含有<code>live2d</code>的文件，全部删除，<code>live2d</code>才正式在我博客里消失）</del>。后续就正常很多了，功耗也降低了下来，希望看我博客的人能有一个好的体验感。</p><h1 id="优化博客访问速度"><a href="#优化博客访问速度" class="headerlink" title="优化博客访问速度"></a>优化博客访问速度</h1><p>如果你不幸的读到了这里，我先引入<code>next</code>官方的一句话吧：<strong>DO NOT EDIT THE FOLLOWING SETTINGS UNLESS YOU KNOW WHAT YOU ARE DOING</strong>，意思是：不要改以下的配置，除非你知道自己在干什么。</p><p>很高兴，我不知道我在干什么，但我想改改看。（当然，如果你迫切想优化博客，可以跟随我的步伐一起魔改，否则本文对你来说就到此结束了）。但是<code>next</code>官方对每个选项都给了注释，仔细读一读，也不是很难改。</p><p><code>pjax</code>是一个<code>jQuery</code>插件，它提供了极速的浏览体验，并且保持了真实的地址、网页标题，浏览器的后退（前进）按钮也可以正常使用。没学过前端的我只注意到了<code>pjax</code>能提速，其他一概不懂。好，那就直接开启设置。以下所有操作均在主题文件夹下的<code>_config.yml</code>文件里进行。</p><p>第一步，将<code>pjax: false</code>改为<code>pjax: true</code>，先开启，而后，在<code>vendors</code>选项中，将注释取消，即导入这么一句话，指定<code>CDN</code>的链接即可。</p><figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">pjax:</span> <span class="string">//cdn.jsdelivr.net/gh/theme-next/theme-next-pjax@0/pjax.min.js</span></span><br></pre></td></tr></table></figure><p>之后你博客的访问速度就能飞速提升啦～</p><h2 id="其余杂项优化"><a href="#其余杂项优化" class="headerlink" title="其余杂项优化"></a>其余杂项优化</h2><ul><li><code>minify</code>，意思是不<code>push</code>无关的东西，看着有用，改为<code>true</code>。</li><li><code>quciklink</code>，官方的解释是：<strong>Faster subsequent page-loads by prefetching in-viewport links during idle time</strong>。翻译过来的意思就是：在打开网页后，趁你不注意加载网页中的链接，以此加快后续网页的访问速度。看样子有用，先改为<code>true</code>，在将<code>vendors</code>选项里的<code>quicklink</code>注释给取消掉，方法同<code>pjax</code>。</li><li><code>lazyload</code>，同样先看官方解释：<strong>LazyLoad is a fast, lightweight and flexible script that speeds up your web application by loading your content images, videos and iframes only as they enter the viewport</strong>。翻译过来的意思就是：图片进入窗口我才加载，在网页最后面的图片、视频啥的文件我先不加载，看到那里在加载。因为用户可能看不到网页结尾，那么在打开网页的时候就加载网页结尾处的图片是没意义的，这不利于提高浏览速度。既然如此，增加这个配置，方法同<code>pjax</code>，先改为<code>true</code>，在去<code>vendors</code>里面加上链接。</li></ul><h1 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h1><p>以下情况仅供参考。关于<code>pjax</code>要补充一点，如果在页面里设置了不蒜子来统计人数和每篇文章的访问量，那么，最好不要开启<code>pjax</code>。因为设置<code>pjax</code>后，每个页面中的不蒜子统计可能不会加载，意思是，别人看了你的文章但是你没统计。就像这样：</p><p><img data-src="/2020/03/12/blog-optimize/3.png" alt></p><p>这只是我实践出来的结论，所以，我选择关闭<code>pjax</code>，开启<code>quicklink</code>和<code>lazyload</code>也同样能秒开博客～。</p><p>但是，在我更新<code>Next</code>后，就又没这个问题了，我现在是<code>pjax</code>和<code>lazyload</code>都在启用。</p><h1 id="代码参考"><a href="#代码参考" class="headerlink" title="代码参考"></a>代码参考</h1><p>我把上述全部的配置代码放到了网上，如果还是不懂如何设置可以对照参考。<a href="https://github.com/muyuuuu/My-Next-theme-blog-config-files/blob/master/_config.yml" target="_blank" rel="noopener">点我过去</a>。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;首先，如果你是想来搭建博客的小白，那么这篇文章不适合你，这篇文章写的优化博客是一些进阶操作。常见的：文章字体、打赏按钮、图片加载、数学公式、音乐链接、不蒜子统计等等等等，网上一搜一大把的我都认为是常规设置，本文在详细描述。而本文收录的进阶优化有：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;降低网页功耗&lt;/li&gt;
&lt;li&gt;快速加载博客&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;此外，本次优化仅针对&lt;code&gt;next&lt;/code&gt;主题，但其他主题也可参考借鉴优化思路，只是操作方法不敢保证完全一致。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="Blog-Optimize" scheme="https://muyuuuu.github.io/tags/Blog-Optimize/"/>
    
  </entry>
  
  <entry>
    <title>简易理解算法时间复杂度的重要性</title>
    <link href="https://muyuuuu.github.io/2020/03/02/algorithm-time-complexity/"/>
    <id>https://muyuuuu.github.io/2020/03/02/algorithm-time-complexity/</id>
    <published>2020-03-02T12:11:50.000Z</published>
    <updated>2020-04-13T06:35:41.489Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>本来是被老师安排了写毕业论文的，但是歪打正着发现了个有趣的东西，决定先更新一下再去写(毕竟谁闲的没事想写那两万字的论文)。</p><ul><li>通过两个新旧CPU的对比分析算法时间复杂度的重要性；</li><li>以顺序结构实现 插入排序 算法（非正解），时间复杂度$O(n^3)$；</li><li>以链式结构（双向链表）实现 插入排序 算法，时间复杂度$O(n^2)$；</li><li>对比两者，分析时间复杂度的影响。</li></ul><p>首先，默认本文的读者有数据结构的基础，或者从任何一本《数据结构》教材里寻找：时间复杂度、插入排序、双向链表的定义。对于外行人(数据结构基础四舍五入等于没有或者只想来看热闹)，可以选择只看本文你能看懂得部分，同样很有意思。</p><a id="more"></a><h1 id="时间复杂度"><a href="#时间复杂度" class="headerlink" title="时间复杂度"></a>时间复杂度</h1><p>参考任何一本《数据结构》的教材都能得到时间复杂度、空间复杂度的详细定义，这里不再解释。</p><p>那么，不如深刻来了解下算法复杂度对时间的影响。假设有两块<code>CPU</code>，第一块破旧的<code>CPU</code>为<code>A</code>，第二块<code>CPU</code>为<code>B</code>，<code>B</code>的性能比<code>A</code>好上10倍，这里假设为：<code>B</code>的计算速度是<code>A</code>的<code>10</code>倍。假设<code>A</code>计算规模为$N$的问题所需时间为$t$，理想情况下不难得到以下表格：</p><div class="table-container"><table><thead><tr><th>问题规模</th><th>CPU: A执行时间</th><th>CPU: B执行时间</th></tr></thead><tbody><tr><td>$N$</td><td>$t$</td><td>$t/10$</td></tr><tr><td>$N^2$</td><td>$t^2$</td><td>$t^2/10$</td></tr></tbody></table></div><p>那么对于同一个问题涉及了两种算法，第一种算法的时间复杂度为$O(n^2)$，而经过优化的算法的时间复杂度为$O(n)$，降低了一个数量级。那么，优化好的算法$O(n)$在<code>CPU A</code>上执行的时间为$t$，而另一个人感觉自己<code>CPU</code>很好没必要优化算法，那么没优化的算法$O(n^2)$在<code>CPU B</code>上执行的时间为$t^2/10$，设函数$g(t)$为$g(t)=t-t^2/10$，可以知道$g(t)$是开口向下的二次函数，也不难求出函数$g(t)$的两个根为$0, 10$。</p><p>当问题规模逐渐增加，问题逐步复杂，处理数据逐步增多时，即$n&gt;10$时，函数$g(t)&lt;0$，意味着优化过的算法在破旧的<code>CPU A</code>上的执行时间，要远小于性能好上十倍的<code>CPU B</code>。这里可以体现算法的时间复杂度的重要性。好的算法胜过好的处理器。所以，要想设计一款好软件，算法是必不可缺的功底，所以，只要还在计算机这个圈子里，数据结构就是逃不掉的。</p><p>况且在技术飞速发展的今天，硬盘空间显得不在像当初那么昂亏，可以花很便宜的价格买到容量几百个G的硬盘。所以，在不要求存储空间的问题中，可以放肆的使用硬盘空间来提升算法执行时间的效率，如：哈希算法、虚拟内存等技术，所以，一般情况下空间复杂度显得不是很重要。</p><h1 id="插入排序"><a href="#插入排序" class="headerlink" title="插入排序"></a>插入排序</h1><p>通过一个图片简单的理解插入排序：遍历数组，把当前元素移动到合适位置即可。</p><p><img data-src="/2020/03/02/algorithm-time-complexity/1.png" alt></p><h2 id="顺序结构实现"><a href="#顺序结构实现" class="headerlink" title="顺序结构实现"></a>顺序结构实现</h2><h3 id="正解"><a href="#正解" class="headerlink" title="正解"></a>正解</h3><p>以下是顺序结构实现插入排序算法正解的核心代码，时间复杂度不难看出为$O(n^2)$：</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; len; i++)&#123;</span><br><span class="line">    <span class="keyword">int</span> key = arr[i];</span><br><span class="line">    <span class="keyword">int</span> j = i - <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>((j &gt;= <span class="number">0</span>) &amp;&amp; (key &lt; arr[j]))&#123;</span><br><span class="line">        arr[j+<span class="number">1</span>] = arr[j];</span><br><span class="line">        j--;</span><br><span class="line">    &#125;</span><br><span class="line">    arr[j+<span class="number">1</span>] = key;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="错解"><a href="#错解" class="headerlink" title="错解"></a>错解</h3><p>错解的意思是：代码实现的逻辑不好，但是代码执行的结果是对的。</p><p>一个人编程水平不咋地，如我(我是真的写错了)，我用一种错误的逻辑写出了顺序结构的插入排序代码，时间复杂度为$O(n^3)$，对序列<code>3, 5, 4, 7, 1, 6, 8, 5, 9, 2</code>进行插入排序，好在得到了正确的结果。如本文开头所说，本文的内容纯属歪打正着，所以之后会使用如下的错解代码，得到另外的有趣的东西。</p><p><img data-src="/2020/03/02/algorithm-time-complexity/2.jpg" alt></p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;fstream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> <span class="keyword">const</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> len = <span class="number">10</span>;</span><br><span class="line">    <span class="keyword">int</span> arr[<span class="number">10</span>] = &#123;<span class="number">3</span>, <span class="number">5</span>, <span class="number">4</span>, <span class="number">7</span>, <span class="number">1</span>, <span class="number">6</span>, <span class="number">8</span>, <span class="number">5</span>, <span class="number">9</span>, <span class="number">2</span>&#125;;</span><br><span class="line">    <span class="keyword">int</span> temp;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; len; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; <span class="string">"Remove "</span> &lt;&lt; arr[i] &lt;&lt; <span class="string">" : "</span>;</span><br><span class="line">        <span class="keyword">if</span> (arr[i] &gt;= arr[i - <span class="number">1</span>])&#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; len; j++)</span><br><span class="line">                <span class="built_in">cout</span> &lt;&lt; arr[j] &lt;&lt; <span class="string">"  "</span>;</span><br><span class="line">            <span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span>&#123;</span><br><span class="line">            temp = arr[i];</span><br><span class="line">            <span class="keyword">int</span> k = i;</span><br><span class="line">            <span class="comment">// 不知道为啥写了这么个没用的循环</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = i - <span class="number">1</span>; j &gt;= <span class="number">0</span>; j--)&#123;</span><br><span class="line">                <span class="keyword">if</span> (arr[j] &lt;= arr[i] || (j == <span class="number">0</span> &amp;&amp; arr[j] &gt; arr[i]))&#123;   </span><br><span class="line">                    <span class="keyword">if</span> (j == <span class="number">0</span> &amp;&amp; arr[j] &gt; arr[i]) </span><br><span class="line">                        j = <span class="number">-1</span>;</span><br><span class="line">                    <span class="keyword">while</span> (k &gt; j + <span class="number">1</span>)&#123;</span><br><span class="line">                        arr[k] = arr[k - <span class="number">1</span>];</span><br><span class="line">                        k--;</span><br><span class="line">                    &#125;</span><br><span class="line">                    arr[j + <span class="number">1</span>] = temp;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; len; j++)</span><br><span class="line">            <span class="built_in">cout</span> &lt;&lt; arr[j] &lt;&lt; <span class="string">"  "</span>;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="链式结构实现"><a href="#链式结构实现" class="headerlink" title="链式结构实现"></a>链式结构实现</h2><p>因为在插入的过程中会访问当前元素之前的元素，所以有必要使用双向链表(任何一本《数据结构》的教材都能得到双向链表的定义，这里不在解释)。移动元素仔细分析指针该如何移动即可。对序列<code>3, 5, 4, 7, 1, 6, 8, 5, 9, 2</code>进行插入排序，不难看出我实现的代码的时间复杂度为$O(n)$。指针恐惧者慎用链式结构。</p><p><img data-src="/2020/03/02/algorithm-time-complexity/4.jpg" alt></p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 每个节点的类型 存储数据 指向前者 指向后者</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">node</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    node *pre;</span><br><span class="line">    <span class="keyword">int</span> data;</span><br><span class="line">    node *next;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 头节点 用于寻找链表在哪</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">head</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    node *first;</span><br><span class="line">    <span class="keyword">int</span> len = <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> <span class="keyword">const</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> arr[<span class="number">10</span>] = &#123;<span class="number">6</span>, <span class="number">5</span>, <span class="number">4</span>, <span class="number">7</span>, <span class="number">3</span>, <span class="number">1</span>, <span class="number">8</span>, <span class="number">5</span>, <span class="number">9</span>, <span class="number">2</span>&#125;;</span><br><span class="line">    <span class="keyword">int</span> len = <span class="number">10</span>;</span><br><span class="line">    node ans[len];</span><br><span class="line">    head L;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; len; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        ans[i].data = arr[i];</span><br><span class="line">        <span class="keyword">if</span> (i == <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">// 类型为指针 所以ans取地址</span></span><br><span class="line">            L.first = &amp;ans[i];</span><br><span class="line">            ans[i].next = &amp;ans[i + <span class="number">1</span>];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (i == len - <span class="number">1</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            ans[i].pre = &amp;ans[i - <span class="number">1</span>];</span><br><span class="line">            ans[i].next = <span class="literal">NULL</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            ans[i].pre = &amp;ans[i - <span class="number">1</span>];</span><br><span class="line">            ans[i].next = &amp;ans[i + <span class="number">1</span>];</span><br><span class="line">        &#125;</span><br><span class="line">        L.len++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="string">"The length of link is : "</span> &lt;&lt; L.len &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    node *p;</span><br><span class="line">    p = L.first;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span> &lt;&lt; <span class="string">"Raw Data : "</span>;</span><br><span class="line">    <span class="keyword">while</span> (p != <span class="literal">NULL</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; (*p).data &lt;&lt; <span class="string">" "</span>;</span><br><span class="line">        p = (*p).next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    <span class="comment">// 读取完毕 开始排序</span></span><br><span class="line">    p = L.first;</span><br><span class="line">    <span class="comment">// 从第二个元素开始插入</span></span><br><span class="line">    p = (*p).next;</span><br><span class="line">    node *s, *t;</span><br><span class="line">    <span class="keyword">while</span> (p != <span class="literal">NULL</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> ((*p).data &lt; (*(*p).pre).data)</span><br><span class="line">        &#123;</span><br><span class="line">            node *q;</span><br><span class="line">            q = L.first;</span><br><span class="line">            <span class="comment">// 用 q 表示合适的插入位置</span></span><br><span class="line">            <span class="comment">// q p 的位置不同时，操作不同，分四种情况</span></span><br><span class="line">            <span class="keyword">while</span> ((*q).data &lt;= (*p).data)</span><br><span class="line">                q = (*q).next;</span><br><span class="line">            <span class="comment">// case 1 pass</span></span><br><span class="line">            <span class="keyword">if</span> ((*p).next == <span class="literal">NULL</span> &amp;&amp; q == L.first)</span><br><span class="line">            &#123;</span><br><span class="line">                s = (*p).pre;</span><br><span class="line">                (*p).next = L.first;</span><br><span class="line">                (*q).pre = p;</span><br><span class="line">                (*(*p).pre).next = <span class="literal">NULL</span>;</span><br><span class="line">                (*p).pre = <span class="literal">NULL</span>;</span><br><span class="line">                L.first = p;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// case 2 pass</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> ((*p).next == <span class="literal">NULL</span> &amp;&amp; q != L.first)</span><br><span class="line">            &#123;</span><br><span class="line">                s = (*p).pre;</span><br><span class="line">                (*(*p).pre).next = <span class="literal">NULL</span>;</span><br><span class="line">                (*p).pre = (*q).pre;</span><br><span class="line">                (*(*q).pre).next = p;</span><br><span class="line">                (*p).next = q;</span><br><span class="line">                (*q).pre = p;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// case 3 pass</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (q == L.first &amp;&amp; (*p).next != <span class="literal">NULL</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                s = (*p).next;</span><br><span class="line">                (*(*p).pre).next = (*p).next;</span><br><span class="line">                (*(*p).next).pre = (*p).pre;</span><br><span class="line">                (*p).next = q;</span><br><span class="line">                (*q).pre = p;</span><br><span class="line">                (*p).pre = <span class="literal">NULL</span>;</span><br><span class="line">                L.first = p;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// case 4</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (q != L.first &amp;&amp; (*p).next != <span class="literal">NULL</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                s = (*p).next;</span><br><span class="line">                (*(*p).next).pre = (*p).pre;</span><br><span class="line">                (*(*p).pre).next = (*p).next;</span><br><span class="line">                (*p).next = q;</span><br><span class="line">                (*p).pre = (*q).pre;</span><br><span class="line">                (*(*q).pre).next = p;</span><br><span class="line">                (*q).pre = p;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="built_in">cout</span> &lt;&lt; <span class="string">"Remove "</span> &lt;&lt; (*p).data &lt;&lt; <span class="string">" :"</span>;</span><br><span class="line">            p = s;</span><br><span class="line">            t = L.first;</span><br><span class="line">            <span class="keyword">while</span> (t != <span class="literal">NULL</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="built_in">cout</span> &lt;&lt; <span class="string">" "</span> &lt;&lt; (*t).data;</span><br><span class="line">                t = (*t).next;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            p = (*p).next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="对比"><a href="#对比" class="headerlink" title="对比"></a>对比</h1><p>此时，我想起了上《算法设计》这门课老师很爱干的事情，实现一个xxx算法，然后分别读入：十万、百万的数据，记录算法执行时间，对比结果。那么，既然我都写错顺序结构实现插入排序的代码了，不如歪打正着，正好对比下$O(n^3)$的算法和$O(n^2)$的算法差异。</p><p>首先用<code>python</code>（别问，问就是不会C++）产生随机整数，规模分别为10万，11万，15万，20万，30万，将结果保存至<code>txt</code>文件中。<br><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="comment"># 第一个参数是左区间 a，第二个参数是右区间 b，第三个参数是个数 c</span></span><br><span class="line"><span class="comment"># 即会产生 c 个 [a, b) 区间内的整数</span></span><br><span class="line">a = np.random.randint(<span class="number">0</span>, <span class="number">500000</span>, <span class="number">500000</span>)</span><br><span class="line"><span class="comment"># fmt 表示取消科学计数法的保存形式</span></span><br><span class="line">np.savetxt(<span class="string">'Five_hundred_thousand.txt'</span>, a, fmt=<span class="string">'%d'</span>)</span><br></pre></td></tr></table></figure></p><p>之后，在<code>C++</code>内读取文件数据，并对排序部分进行计时，顺序结构代表了$O(n^3)$的算法，链式结构代表了$O(n^2)$的算法，修改后的代码如下：</p><h2 id="顺序结构计时"><a href="#顺序结构计时" class="headerlink" title="顺序结构计时"></a>顺序结构计时</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;fstream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;ctime&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> arr[<span class="number">1000002</span>] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> <span class="keyword">const</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> len = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// path 是读取的文件</span></span><br><span class="line">    <span class="built_in">string</span> path = <span class="string">"Five_hundred_thousand.txt"</span>;</span><br><span class="line">    <span class="comment">// out 是输出结构的文件</span></span><br><span class="line">    <span class="built_in">string</span> out = <span class="string">"Five_hundred_thousand_seq.txt"</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="function">ifstream <span class="title">in</span><span class="params">(path.c_str())</span></span>;</span><br><span class="line">    <span class="function">ofstream <span class="title">ou</span><span class="params">(out.c_str())</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (!in.is_open())</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cerr</span> &lt;&lt; <span class="string">"open file failed!"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!ou.is_open())</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cerr</span> &lt;&lt; <span class="string">"create file failed!"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 读取文件 存入数组</span></span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (in &gt;&gt; a)</span><br><span class="line">        arr[len++] = a;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">int</span> temp;</span><br><span class="line">    <span class="comment">// 开始计时</span></span><br><span class="line">    <span class="keyword">clock_t</span> start = clock();</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; len; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (arr[i] &gt;= arr[i - <span class="number">1</span>])&#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span>&#123;</span><br><span class="line">            temp = arr[i];</span><br><span class="line">            <span class="keyword">int</span> k = i;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = i - <span class="number">1</span>; j &gt;= <span class="number">0</span>; j--)&#123;</span><br><span class="line">                <span class="keyword">if</span> (arr[j] &lt;= arr[i] || (j == <span class="number">0</span> &amp;&amp; arr[j] &gt; arr[i]))&#123;   </span><br><span class="line">                    <span class="keyword">if</span> (j == <span class="number">0</span> &amp;&amp; arr[j] &gt; arr[i]) </span><br><span class="line">                        j = <span class="number">-1</span>;</span><br><span class="line">                    <span class="keyword">while</span> (k &gt; j + <span class="number">1</span>)&#123;</span><br><span class="line">                        arr[k] = arr[k - <span class="number">1</span>];</span><br><span class="line">                        k--;</span><br><span class="line">                    &#125;</span><br><span class="line">                    arr[j + <span class="number">1</span>] = temp;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">clock_t</span> end   = clock();</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt;  (<span class="keyword">double</span>)(end - start) / CLOCKS_PER_SEC &lt;&lt; <span class="string">" seconds"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    in.close();</span><br><span class="line">    <span class="comment">// 把结果记录到文件 检查算法是否正确</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; len; i++)</span><br><span class="line">        ou &lt;&lt; arr[i] &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    ou.close();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="链式结构计时"><a href="#链式结构计时" class="headerlink" title="链式结构计时"></a>链式结构计时</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;fstream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;ctime&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 同样 记录前驱、后继、和本身的数据</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">node</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    node *pre;</span><br><span class="line">    <span class="keyword">int</span> data;</span><br><span class="line">    node *next;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 头节点记录链表在哪</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">head</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    node *first;</span><br><span class="line">    <span class="keyword">int</span> len = <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 占据的空间大 所以声明在主函数外部</span></span><br><span class="line"><span class="keyword">int</span> arr[<span class="number">1000002</span>] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line">node ans[<span class="number">300002</span>];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> <span class="keyword">const</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> len = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">string</span> path = <span class="string">"one_hundred_thousand.txt"</span>;</span><br><span class="line">    <span class="built_in">string</span> out = <span class="string">"one_hundred_thousand_link.txt"</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="function">ifstream <span class="title">in</span><span class="params">(path.c_str())</span></span>;</span><br><span class="line">    <span class="function">ofstream <span class="title">ou</span><span class="params">(out.c_str())</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (!in.is_open())</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cerr</span> &lt;&lt; <span class="string">"open file failed!"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!ou.is_open())</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cerr</span> &lt;&lt; <span class="string">"create file failed!"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (in &gt;&gt; a)</span><br><span class="line">        arr[len++] = a;</span><br><span class="line">    </span><br><span class="line">    head L;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; len; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        ans[i].data = arr[i];</span><br><span class="line">        <span class="keyword">if</span> (i == <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            L.first = &amp;ans[i];</span><br><span class="line">            ans[i].next = &amp;ans[i + <span class="number">1</span>];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (i == len - <span class="number">1</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            ans[i].pre = &amp;ans[i - <span class="number">1</span>];</span><br><span class="line">            ans[i].next = <span class="literal">NULL</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            ans[i].pre = &amp;ans[i - <span class="number">1</span>];</span><br><span class="line">            ans[i].next = &amp;ans[i + <span class="number">1</span>];</span><br><span class="line">        &#125;</span><br><span class="line">        L.len++;</span><br><span class="line">    &#125;</span><br><span class="line">    node *p;</span><br><span class="line">    p = L.first;</span><br><span class="line">    p = (*p).next;</span><br><span class="line">    <span class="comment">// s 表示 p 下一步该指向哪里</span></span><br><span class="line">    node *s;</span><br><span class="line">    <span class="keyword">clock_t</span> start = clock();</span><br><span class="line">    <span class="keyword">while</span> (p != <span class="literal">NULL</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> ((*p).data &lt; (*(*p).pre).data)</span><br><span class="line">        &#123;</span><br><span class="line">            node *q;</span><br><span class="line">            q = L.first;</span><br><span class="line">            <span class="keyword">while</span> ((*q).data &lt;= (*p).data)</span><br><span class="line">                q = (*q).next;</span><br><span class="line">            <span class="comment">// q p 的位置不同时，操作不同，分四种情况</span></span><br><span class="line">            <span class="comment">// case 1 pass</span></span><br><span class="line">            <span class="keyword">if</span> ((*p).next == <span class="literal">NULL</span> &amp;&amp; q == L.first)</span><br><span class="line">            &#123;</span><br><span class="line">                s = (*p).pre;</span><br><span class="line">                (*p).next = L.first;</span><br><span class="line">                (*q).pre = p;</span><br><span class="line">                (*(*p).pre).next = <span class="literal">NULL</span>;</span><br><span class="line">                (*p).pre = <span class="literal">NULL</span>;</span><br><span class="line">                L.first = p;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// case 2 pass</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> ((*p).next == <span class="literal">NULL</span> &amp;&amp; q != L.first)</span><br><span class="line">            &#123;</span><br><span class="line">                s = (*p).pre;</span><br><span class="line">                (*(*p).pre).next = <span class="literal">NULL</span>;</span><br><span class="line">                (*p).pre = (*q).pre;</span><br><span class="line">                (*(*q).pre).next = p;</span><br><span class="line">                (*p).next = q;</span><br><span class="line">                (*q).pre = p;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// case 3 pass</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (q == L.first &amp;&amp; (*p).next != <span class="literal">NULL</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                s = (*p).next;</span><br><span class="line">                (*(*p).pre).next = (*p).next;</span><br><span class="line">                (*(*p).next).pre = (*p).pre;</span><br><span class="line">                (*p).next = q;</span><br><span class="line">                (*q).pre = p;</span><br><span class="line">                (*p).pre = <span class="literal">NULL</span>;</span><br><span class="line">                L.first = p;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// case 4</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (q != L.first &amp;&amp; (*p).next != <span class="literal">NULL</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                s = (*p).next;</span><br><span class="line">                (*(*p).next).pre = (*p).pre;</span><br><span class="line">                (*(*p).pre).next = (*p).next;</span><br><span class="line">                (*p).next = q;</span><br><span class="line">                (*p).pre = (*q).pre;</span><br><span class="line">                (*(*q).pre).next = p;</span><br><span class="line">                (*q).pre = p;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 每种情况对应的 s 都不一样 但最后都要指向 s</span></span><br><span class="line">            p = s;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            p = (*p).next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">clock_t</span> end = clock();</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt;  (<span class="keyword">double</span>)(end - start) / CLOCKS_PER_SEC &lt;&lt; <span class="string">" seconds"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    in.close();</span><br><span class="line">    p = L.first;</span><br><span class="line">    <span class="keyword">while</span> (p != <span class="literal">NULL</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        ou &lt;&lt; (*p).data &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">        p = (*p).next;</span><br><span class="line">    &#125;</span><br><span class="line">    ou.close();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="结果"><a href="#结果" class="headerlink" title="结果"></a>结果</h1><p>文档结构如下：(每个数据规模产生一个<code>txt</code>文件，用于读取，输出结果加<code>seq</code>或者<code>link</code>的后缀保存到<code>txt</code>文件中，用于检查排序是否正确，事实证明结果是正确的，尽管代码逻辑不好，但能保证执行结果对)</p><p><img data-src="/2020/03/02/algorithm-time-complexity/3.jpg" alt></p><p>在<code>i7-9750H</code>的CPU上执行结果如下：（执行三次，取平均）</p><div class="table-container"><table><thead><tr><th>数据规模</th><th>链式结构计时，单位：秒</th><th>顺序结构计时，单位：秒</th></tr></thead><tbody><tr><td>10万</td><td>24.7</td><td>8.477</td></tr><tr><td>11万</td><td>30.8</td><td>10.247</td></tr><tr><td>15万</td><td>62</td><td>19</td></tr><tr><td>20万</td><td>116.112</td><td>33.765</td></tr><tr><td>30万</td><td>272</td><td>75.5</td></tr></tbody></table></div><p>也许你会感到意外，为何$O(n^2)$的算法比$O(n^3)$的算法还要慢？不是不符合前文的规律？显然，这里$O(n^2)$的算法比$O(n^3)$的算法慢的原因是：链式结构要经常操作指针，来寻找下一个地址，时间耗费在地址空间的寻找上；而顺序结构中，逻辑相邻即物理相邻，不用浪费多余的时间去寻找下一个元素的地址，使用下标访问即可。</p><p>那么，如何证明前文的思想是对的？我本来是想用三次函数拟合顺序结构的数据，用二次函数拟合链式结构的数据，求两个函数的交点。结果发现数据量太少，居然只能拟合出开口向下的二次函数和单调递减的三次函数，这显然是不对的，因为这两个函数都在随着问题规模的增加，耗时逐步减少。</p><p>然而我还不想做更多的数据，拟合不对并不代表数据是错误的，所以，我决定用链式结构的时间减去顺序结构的时间，得到一个时间差。对时间差进行三次函数拟合（三次函数减二次函数仍然为三次函数），得到拟合结果：$g(t)=-0.0007601 x^3 + 0.2858 x^2 - 1.431  x + 2.719$，通过<code>sympy</code>求解，得到一个实数根为$370.95$，绘制$[10, 380]$区间内的函数图像：</p><p><img data-src="/2020/03/02/algorithm-time-complexity/4.png" alt></p><p>可以发现，在$n&gt;371$后，函数值小于0，说明在问题规模大于某个阈值后，$O(n^2)$的算法比$O(n^3)$的算法耗时要少，速度要快。那么这个时候，不管操作指针寻找地址多么的麻烦，时间开销总会比$O(n^3)$的算法小，只是前期可能会因为操作指针的关系影响执行的效率。</p><p>当然实际情况中，371并不代表三千多万的数据，毕竟实际情况会比单独的数据排序复杂得多。所以，也有必要根据实际问题的规模，合理的选择算法。</p><h1 id="程序下载"><a href="#程序下载" class="headerlink" title="程序下载"></a>程序下载</h1><p><a href="https://github.com/muyuuuu/Algorithm/tree/master/Insert_sort" target="_blank" rel="noopener">下载</a>。我把全部文件，包括原始的随机数据、排列好的数据都放上了，后缀为seq表示顺序结构排列的结果，后缀为link表示为链式结构排列的结果。第一是证明我写的代码是对的，第二是预留这些数据，供后来者玩耍。</p><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p><a href="https://www.geeksforgeeks.org/insertion-sort/" target="_blank" rel="noopener">https://www.geeksforgeeks.org/insertion-sort/</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本来是被老师安排了写毕业论文的，但是歪打正着发现了个有趣的东西，决定先更新一下再去写(毕竟谁闲的没事想写那两万字的论文)。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;通过两个新旧CPU的对比分析算法时间复杂度的重要性；&lt;/li&gt;
&lt;li&gt;以顺序结构实现 插入排序 算法（非正解），时间复杂度$O(n^3)$；&lt;/li&gt;
&lt;li&gt;以链式结构（双向链表）实现 插入排序 算法，时间复杂度$O(n^2)$；&lt;/li&gt;
&lt;li&gt;对比两者，分析时间复杂度的影响。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;首先，默认本文的读者有数据结构的基础，或者从任何一本《数据结构》教材里寻找：时间复杂度、插入排序、双向链表的定义。对于外行人(数据结构基础四舍五入等于没有或者只想来看热闹)，可以选择只看本文你能看懂得部分，同样很有意思。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="DataStructure" scheme="https://muyuuuu.github.io/tags/DataStructure/"/>
    
  </entry>
  
  <entry>
    <title>树的遍历算法及常见衍生问题的归纳整理</title>
    <link href="https://muyuuuu.github.io/2020/02/29/tree-algorithm/"/>
    <id>https://muyuuuu.github.io/2020/02/29/tree-algorithm/</id>
    <published>2020-02-28T20:56:41.000Z</published>
    <updated>2020-03-28T09:20:55.581Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>树是数据结构中很重要的一种结构，在现实生活中常用于表达层次关系和非单一的映射关系，如一个老板下对应有多个员工。通过一段时间的算法练习，本文将树常见的算法收录整理，包括：</p><ul><li>求二叉树叶子节点的数量</li><li>求节点权重和</li><li>求距离根节点最近、最远的叶节点</li><li>求每一层节点的数量</li><li>遍历二叉树，求给定权值的路径</li><li>二叉查找树</li></ul><p>而对于红黑树、2-3-4树等较为复杂的树结构，本文暂时不收录，仅收录较为简单容易理解的，本文适用于：编程不太好、但了解基本数据结构的人，你仅仅需要知道深度优先遍历、广度优先遍历、二叉树的根节点、普通节点、叶子节点的区别即可。</p><a id="more"></a><p>一类通常题目：求树的叶子数量，求每一层的节点数量，求树的最大高度，求叶子权重的和，都可以归类为对二叉树的遍历，所以本文更多的是树的遍历算法(深度优先或广度优先)的应用。其次，先序、中序、后序遍历树，对遍历顺序有严格的要求，一般不用此类方法遍历树去求解上述问题，一般都是解决先序中序转后序此类需求，在后续会写出此类问题的求解算法。</p><p>警告：尽管文本做了详细的代码注释和讲解，但如果你只是准备靠本文的讲解企图理解代码，恐怕不太行，还需要自己多加练习和多思考。</p><h1 id="求二叉树叶子节点数量"><a href="#求二叉树叶子节点数量" class="headerlink" title="求二叉树叶子节点数量"></a>求二叉树叶子节点数量</h1><p>PAT甲级 1004，<a href="https://pintia.cn/problem-sets/994805342720868352/problems/994805521431773184" target="_blank" rel="noopener">Counting Leaves</a> 。看样子是一道很经典的题目，输出每一层的叶子节点数。也就是，遍历二叉树，计算每层几个叶子节点，不一定要层次遍历，深度遍历也行。如下图所示：第一层的叶子节点数为0，第二层的叶子节点数为2，分别是2号节点和5号节点，第三层的叶子节点数是2，分别是6号节点和7号节点。</p><p><img data-src="/2020/02/29/tree-algorithm/1.png" alt></p><h2 id="思路"><a href="#思路" class="headerlink" title="思路"></a>思路</h2><p>深度遍历二叉树，如果遍历到的节点没有孩子，那么这一层的叶子节点数自增，所以我们知道了需要：输入节点时需要一个<code>vector</code>数组，将此节点的孩子给<code>push_back</code>进来，如果这个节点的<code>size</code>为0，那么表明这是一个叶子节点，此外还需要记录层数和标记这个节点属于哪一层。</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; v[<span class="number">100</span>];</span><br><span class="line"><span class="keyword">int</span> book[<span class="number">100</span>] = &#123;<span class="number">0</span>&#125;; </span><br><span class="line"><span class="keyword">int</span> maxdepth = <span class="number">-1</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dfs</span><span class="params">(<span class="keyword">int</span> index, <span class="keyword">int</span> depth)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 只有一个节点，那么他就是叶子节点</span></span><br><span class="line">    <span class="keyword">if</span> (v[index].<span class="built_in">size</span>() == <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 这一层的孩子数 ++ </span></span><br><span class="line">        book[depth]++;</span><br><span class="line">        <span class="comment">// 记录层数，最后遍历输出</span></span><br><span class="line">        maxdepth = <span class="built_in">max</span>(depth, maxdepth);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 如果有孩子，层次递归遍历</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; v[index].<span class="built_in">size</span>(); ++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// v[index][i] 是一个整数</span></span><br><span class="line">        <span class="comment">// 传进去就是第几号叶子节点</span></span><br><span class="line">        <span class="comment">// 如果有孩子，就继续这个循环</span></span><br><span class="line">        <span class="comment">// 向下递增，所以是深度优先</span></span><br><span class="line">        dfs(v[index][i], depth+<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> <span class="keyword">const</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> N, M;</span><br><span class="line">    <span class="keyword">int</span> node, num, node1;</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">"%d %d"</span>, &amp;N, &amp;M);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; M; ++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">scanf</span>(<span class="string">"%d %d"</span>, &amp;node, &amp;num);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; num; ++j)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">scanf</span>(<span class="string">"%d"</span>, &amp;node1);</span><br><span class="line">            <span class="comment">// 存入一个节点的孩子 </span></span><br><span class="line">            v[node].push_back(node1);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 根节点是1，深度是0，从根节点开始遍历</span></span><br><span class="line">    dfs(<span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%d"</span>, book[<span class="number">0</span>]);</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= maxdepth; i++)</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">" %d"</span>, book[i]);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="求节点的权重和"><a href="#求节点的权重和" class="headerlink" title="求节点的权重和"></a>求节点的权重和</h1><p>甲级 1079，<a href="https://pintia.cn/problem-sets/994805342720868352/problems/994805388447170560" target="_blank" rel="noopener">Total Sales of Supply Chain</a>。问题描述（原题描述的太绕了，一个单词没读到，结果理解不了题意，花了半小时读懂题）：每一个叶子节点有一个权重，此外还需要乘以一个系数，而这个系数和到根节点的距离成正比，最后计算叶子节点的权重和。</p><h2 id="思路-1"><a href="#思路-1" class="headerlink" title="思路"></a>思路</h2><p>和上文一样，仍然需要用一个<code>vector</code>数组来输入节点，将此节点的孩子给<code>push_back</code>进来，如果这个节点的<code>size</code>为0，那么表明这是一个叶子节点；遍历到叶子节点求权重即可。整体代码和上文差不多，如果这里看不懂请看上一节 <code>求二叉树叶子节点数量</code> 的代码注释即可。</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cmath&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">node</span> &#123;</span></span><br><span class="line">    <span class="keyword">double</span> data;</span><br><span class="line">    <span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; child;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">vector</span>&lt;node&gt; v;</span><br><span class="line"></span><br><span class="line"><span class="keyword">double</span> ans = <span class="number">0</span>, r, p;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dfs</span><span class="params">(<span class="keyword">int</span> index, <span class="keyword">int</span> depth)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (v[index].child.<span class="built_in">size</span>() == <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        ans += v[index].data * <span class="built_in">pow</span>((<span class="number">1</span> + r), depth);</span><br><span class="line">        <span class="comment">// 递归的出口</span></span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; v[index].child.<span class="built_in">size</span>(); i++)</span><br><span class="line">        &#123;</span><br><span class="line">            dfs(v[index].child[i], depth+<span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> num;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; num &gt;&gt; p &gt;&gt; r;</span><br><span class="line">    <span class="keyword">int</span> temp1; </span><br><span class="line">    <span class="keyword">int</span> temp;</span><br><span class="line">    <span class="keyword">double</span> data;</span><br><span class="line">    r = r / <span class="number">100</span>;</span><br><span class="line">    v.resize(num);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; num; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; temp;</span><br><span class="line">        <span class="keyword">if</span> (temp == <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">cin</span> &gt;&gt; data;</span><br><span class="line">            v[i].data = data;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; temp; j++)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="built_in">scanf</span>(<span class="string">"%d"</span>, &amp;temp1);</span><br><span class="line">                v[i].child.push_back(temp1);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> price;</span><br><span class="line">    dfs(<span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%.1f"</span>, ans * p);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="计算到根节点的距离"><a href="#计算到根节点的距离" class="headerlink" title="计算到根节点的距离"></a>计算到根节点的距离</h1><h2 id="求距离根节点最远的叶节点（DFS）"><a href="#求距离根节点最远的叶节点（DFS）" class="headerlink" title="求距离根节点最远的叶节点（DFS）"></a>求距离根节点最远的叶节点（DFS）</h2><p>甲级 1090 ，<a href="https://pintia.cn/problem-sets/994805342720868352/problems/994805376476626944" target="_blank" rel="noopener">Highest Price in Supply Chain</a>。除此之外还要求出有几个这样的节点存在。</p><h3 id="思路-2"><a href="#思路-2" class="headerlink" title="思路"></a>思路</h3><p>此时<code>for</code>循环遍历，判断是否为叶节点，如果是，那么判断深度，当前深度和最大深度相等，数量加一，以此来统计有几个这样的节点；如果当前深度大于最大深度，那么这样的节点又回归为一个。</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dfs</span><span class="params">(<span class="keyword">int</span> index, <span class="keyword">int</span> depth)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (v[index].<span class="built_in">size</span>() == <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (depth == maxdepth)</span><br><span class="line">        &#123;</span><br><span class="line">            maxnum++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (depth &gt; maxdepth)</span><br><span class="line">        &#123;</span><br><span class="line">            maxdepth = depth;</span><br><span class="line">            maxnum = <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; v[index].<span class="built_in">size</span>(); i++)</span><br><span class="line">    &#123;</span><br><span class="line">        dfs(v[index][i], depth + <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="求距离根节点最近的叶节点"><a href="#求距离根节点最近的叶节点" class="headerlink" title="求距离根节点最近的叶节点"></a>求距离根节点最近的叶节点</h2><p>甲级 1106，<a href="https://pintia.cn/problem-sets/994805342720868352/problems/994805362341822464" target="_blank" rel="noopener">Lowest Price in Supply Chain</a>。</p><p>和上面的思路一样。</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">double</span> level = <span class="number">100005</span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dfs</span><span class="params">(<span class="keyword">int</span> index, <span class="keyword">int</span> depth)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (v[index].<span class="built_in">size</span>() == <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (depth == level)</span><br><span class="line">        &#123;</span><br><span class="line">            num ++ ;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (depth &lt; level)</span><br><span class="line">        &#123;</span><br><span class="line">            level = depth;</span><br><span class="line">            num = <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; v[index].<span class="built_in">size</span>(); i++)</span><br><span class="line">       dfs(v[index][i], depth + <span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="求每一层的节点数量"><a href="#求每一层的节点数量" class="headerlink" title="求每一层的节点数量"></a>求每一层的节点数量</h1><p>甲级 1094，<a href="https://pintia.cn/problem-sets/994805342720868352/problems/994805372601090048" target="_blank" rel="noopener">The Largest Generation</a> 。给定一棵树，有一层的节点数量是最多的，求出这一层，和这一层的节点数。</p><h2 id="DFS"><a href="#DFS" class="headerlink" title="DFS"></a>DFS</h2><p>声明一个数组<code>gene</code>，当遍历到这一层时，<code>gene[depth]++</code>即可，因此遍历的时候传入当前深度<code>depth</code>参数即可。</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 初始化问题，所有的初始化为0</span></span><br><span class="line"><span class="comment">// 如果v.resize()，除非能确定大小</span></span><br><span class="line"><span class="comment">// 往里面压入具体的数，否则没压进去的位置size()是异常值。</span></span><br><span class="line"><span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; v[<span class="number">105</span>];</span><br><span class="line"><span class="keyword">int</span> gene[<span class="number">105</span>];</span><br><span class="line"><span class="keyword">int</span> n;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dfs</span><span class="params">(<span class="keyword">int</span> index, <span class="keyword">int</span> depth)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    gene[depth]++;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; v[index].<span class="built_in">size</span>(); i++)</span><br><span class="line">    &#123;</span><br><span class="line">        dfs(v[index][i], depth+<span class="number">1</span>); </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span>  m;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; n &gt;&gt; m;</span><br><span class="line">    <span class="keyword">int</span> temp;</span><br><span class="line">    <span class="keyword">int</span> cnt;</span><br><span class="line">    <span class="keyword">int</span> temp1;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; m; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; temp &gt;&gt; cnt;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; cnt; j++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">cin</span> &gt;&gt; temp1;</span><br><span class="line">            v[temp].push_back(temp1);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    dfs(<span class="number">1</span>, <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">int</span> <span class="built_in">max</span> = <span class="number">-1</span>, maxn = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">105</span>; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (gene[i] &gt; <span class="built_in">max</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">max</span> = gene[i];</span><br><span class="line">            maxn = i;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; <span class="built_in">max</span> &lt;&lt; <span class="string">" "</span> &lt;&lt; maxn ;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="BFS思路"><a href="#BFS思路" class="headerlink" title="BFS思路"></a>BFS思路</h2><p>对于广度优先遍历而言，一般会借助队列来实现。首先压入根节点，当队列非空时，弹出队列的第一个节点，而后压入节点的各个孩子，于此同时记录孩子数量和层数(即题目中的层数和每层几个孩子)。</p><ul><li><code>index</code>表示第几个节点；</li><li><code>level[index]</code>表示这个节点在第几层；</li><li><code>book[level[index]]</code>表示这层有几个节点；</li><li>更多解释参考程序注释。</li></ul><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cstdio&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;queue&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; v[<span class="number">100</span>];</span><br><span class="line"><span class="keyword">int</span> level[<span class="number">100</span>];</span><br><span class="line"><span class="keyword">int</span> book[<span class="number">100</span>];</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> n, m, a, k, c;</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">"%d %d"</span>, &amp;n, &amp;m);</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; m; i++) &#123;</span><br><span class="line">        <span class="built_in">scanf</span>(<span class="string">"%d %d"</span>,&amp;a, &amp;k);</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; k; j++) &#123;</span><br><span class="line">            <span class="built_in">scanf</span>(<span class="string">"%d"</span>, &amp;c);</span><br><span class="line">            v[a].push_back(c);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">queue</span>&lt;<span class="keyword">int</span>&gt; q;</span><br><span class="line">    <span class="comment">// 先压入根节点</span></span><br><span class="line">    q.push(<span class="number">1</span>);</span><br><span class="line">    <span class="comment">// 根节点是第一层</span></span><br><span class="line">    level[<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>(!q.empty()) &#123;</span><br><span class="line">        <span class="comment">// index 标记第几个孩子</span></span><br><span class="line">        <span class="keyword">int</span> index = q.front();</span><br><span class="line">        q.pop();</span><br><span class="line">        <span class="comment">// 记录这一层有几个孩子 level[index] 表示孩子处于第几层</span></span><br><span class="line">        book[level[index]]++;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; v[index].<span class="built_in">size</span>(); i++) &#123;</span><br><span class="line">            <span class="comment">// 当前孩子的层数 为 上一层的层数 + 1</span></span><br><span class="line">            level[v[index][i]] = level[index] + <span class="number">1</span>;</span><br><span class="line">            <span class="comment">// 压入当前孩子</span></span><br><span class="line">            q.push(v[index][i]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> maxnum = <span class="number">0</span>, maxlevel = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; <span class="number">100</span>; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span>(book[i] &gt; maxnum) &#123;</span><br><span class="line">            maxnum = book[i];</span><br><span class="line">            maxlevel = i;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%d %d"</span>, maxnum, maxlevel);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="求给定权值的路径"><a href="#求给定权值的路径" class="headerlink" title="求给定权值的路径"></a>求给定权值的路径</h1><p>甲级 1053，<a href="https://pintia.cn/problem-sets/994805342720868352/problems/994805424153280512" target="_blank" rel="noopener">Path of Equal Weight</a> 。 给出树的结构和权值，找从根结点到叶子结点的路径上的权值相加之和等于给定目标数的路径，如果路径不止一条，按节点标号从大到小输出路径。</p><h2 id="思路-3"><a href="#思路-3" class="headerlink" title="思路"></a>思路</h2><p>在读入节点后对节点排序，这样深度优先出的路径也会默认排好顺序。对于符合权重的路径，首先判断路径的重点是不是叶子节点，如果不是，那么这条路径不符合要求；如果是，将临时路径记录的节点依次添加即可。</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> weight_sum;</span><br><span class="line"><span class="keyword">int</span> weight[<span class="number">101</span>];</span><br><span class="line"><span class="keyword">int</span> temp_path[<span class="number">101</span>];</span><br><span class="line"><span class="keyword">int</span> path_num;</span><br><span class="line"></span><br><span class="line"><span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; v[<span class="number">101</span>];</span><br><span class="line"><span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; path[<span class="number">101</span>];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dfs</span><span class="params">(<span class="keyword">int</span> index, <span class="keyword">int</span> nodeNum, <span class="keyword">int</span> sum_temp)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 走过头了</span></span><br><span class="line">    <span class="keyword">if</span> (sum_temp &gt; weight_sum)</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    <span class="keyword">if</span>(sum_temp == weight_sum) </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (v[index].<span class="built_in">size</span>() != <span class="number">0</span>)  <span class="comment">// 不是叶子节点</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">return</span> ;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;   <span class="comment">// 0表示从根开始，记录经过的路径，nodeNum是最后一个</span></span><br><span class="line">            <span class="comment">// 深度优先，路径一定是竖着通到叶子节点的</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; nodeNum; i++)</span><br><span class="line">            &#123;</span><br><span class="line">                path[path_num].push_back(weight[temp_path[i]]);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 标记一共有几条路径</span></span><br><span class="line">            path_num++;</span><br><span class="line">            <span class="keyword">return</span> ;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; v[index].<span class="built_in">size</span>(); i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> node = v[index][i];</span><br><span class="line">        <span class="comment">// 走第二条路径的时候，里面的变量会被覆盖，因此没必要清空</span></span><br><span class="line">        temp_path[nodeNum] = v[index][i];</span><br><span class="line">        dfs(node, nodeNum + <span class="number">1</span>, sum_temp + weight[v[index][i]]);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">cmp</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> weight[a] &gt; weight[b];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> n, non;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; n &gt;&gt; non &gt;&gt; weight_sum;</span><br><span class="line">    <span class="keyword">int</span> temp;</span><br><span class="line">    <span class="keyword">int</span> id, num, a;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; n; j++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; weight[j];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; non; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">cin</span> &gt;&gt; id &gt;&gt; num;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; num; j++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">cin</span> &gt;&gt; a;</span><br><span class="line">            v[id].push_back(a);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 一个节点的孩子排序，之后深度优先会默认输出排序好的</span></span><br><span class="line">        sort(v[id].<span class="built_in">begin</span>(), v[id].<span class="built_in">end</span>(), cmp);</span><br><span class="line">    &#125;    </span><br><span class="line">    <span class="comment">// 1 是已经有一个节点，weight[0]是根节点的权重</span></span><br><span class="line">    dfs(<span class="number">0</span>, <span class="number">1</span>, weight[<span class="number">0</span>]);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">101</span>; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (path[i].<span class="built_in">size</span>() == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">auto</span> it = path[i].<span class="built_in">begin</span>(); it != path[i].<span class="built_in">end</span>(); it++)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="built_in">cout</span> &lt;&lt; *it ;</span><br><span class="line">                <span class="keyword">if</span> (it != path[i].<span class="built_in">end</span>() - <span class="number">1</span> ) <span class="comment">// 多输出了一个空格，全部错误</span></span><br><span class="line">                    <span class="built_in">cout</span> &lt;&lt; <span class="string">" "</span>;                </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (path[i+<span class="number">1</span>].<span class="built_in">size</span>() != <span class="number">0</span>)</span><br><span class="line">            <span class="built_in">cout</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="二叉查找树"><a href="#二叉查找树" class="headerlink" title="二叉查找树"></a>二叉查找树</h1><p>甲级 1115，<a href="https://pintia.cn/problem-sets/994805342720868352/problems/994805355987451904" target="_blank" rel="noopener">Counting Nodes in a BST</a>。大致要求是：给如数列，创建二叉查找树，求这个二叉查找树一共有几层，每层几个节点。 </p><p>递归创建二叉查找树的代码如下：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">node</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> v;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">node</span> *<span class="title">left</span>, *<span class="title">right</span>;</span> </span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function">node* <span class="title">build</span><span class="params">(node *root, <span class="keyword">int</span> v)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (root == <span class="literal">NULL</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        root = <span class="keyword">new</span> node();</span><br><span class="line">        root-&gt;v = v;</span><br><span class="line">        root-&gt;left = <span class="literal">NULL</span>;</span><br><span class="line">        root-&gt;right = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (v &lt;= root-&gt;v)</span><br><span class="line">    &#123;</span><br><span class="line">        root-&gt;left = build(root-&gt;left, v);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        root-&gt;right = build(root-&gt;right, v);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> root;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">node *root = <span class="literal">NULL</span>;</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; temp;</span><br><span class="line">    root = build(root, temp);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>此时计数每层几个叶子、这个树有几层的代码如下：</p><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> num[<span class="number">1000</span>] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dfs</span><span class="params">(node *root, <span class="keyword">int</span> depth)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (root == <span class="literal">NULL</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (depth &gt; maxdepth)</span><br><span class="line">            maxdepth = depth;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 记录这层几个叶子</span></span><br><span class="line">    num[depth]++;</span><br><span class="line">    dfs(root-&gt;left, depth+<span class="number">1</span>);</span><br><span class="line">    dfs(root-&gt;right, depth+<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p>本文全部参考了<a href="https://github.com/liuchuo/PAT/tree/master/AdvancedLevel_C%2B%2B" target="_blank" rel="noopener">柳神，甲级</a>的程序，而非我本人的原创。但是我都花精力研究了程序、重写程序、最后做了归纳整理。我也不知道这算不算抄袭，如果本文帮助了你，且你想打赏，我会把文本的收益全部转送给柳神的支付宝，为什么不把她支付宝放在这里呢，因为我懒。就像创作本文的目的一样，只是整理所学知识，而不是盈利。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;树是数据结构中很重要的一种结构，在现实生活中常用于表达层次关系和非单一的映射关系，如一个老板下对应有多个员工。通过一段时间的算法练习，本文将树常见的算法收录整理，包括：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;求二叉树叶子节点的数量&lt;/li&gt;
&lt;li&gt;求节点权重和&lt;/li&gt;
&lt;li&gt;求距离根节点最近、最远的叶节点&lt;/li&gt;
&lt;li&gt;求每一层节点的数量&lt;/li&gt;
&lt;li&gt;遍历二叉树，求给定权值的路径&lt;/li&gt;
&lt;li&gt;二叉查找树&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;而对于红黑树、2-3-4树等较为复杂的树结构，本文暂时不收录，仅收录较为简单容易理解的，本文适用于：编程不太好、但了解基本数据结构的人，你仅仅需要知道深度优先遍历、广度优先遍历、二叉树的根节点、普通节点、叶子节点的区别即可。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="DataStructure" scheme="https://muyuuuu.github.io/tags/DataStructure/"/>
    
  </entry>
  
  <entry>
    <title>图问题中的搜索算法</title>
    <link href="https://muyuuuu.github.io/2020/02/26/graph-algorithm-search/"/>
    <id>https://muyuuuu.github.io/2020/02/26/graph-algorithm-search/</id>
    <published>2020-02-26T05:55:53.000Z</published>
    <updated>2020-03-28T09:20:35.950Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>在面临一个搜索问题时，如找出一个矩阵中不一样的元素，可能大多人和我想的一样：几层for循环，变量自增继续访问下一个。看着简单，但是，当问题逐渐复杂，如搜索出矩阵中的元素，要求是这个元素和周围的元素不一样，要求相差超过某个阈值，此时在用for循环去写，遍历元素和元素周围的元素，便显得不够明智。</p><p>在搜索算法中，常用的为深度优先和广度优先。但是题目很少去单纯的考察搜索算法，搜索经常会和树、图的遍历一起使用，于是决定来总结一下图中的搜索算法，二叉树或多叉树中的搜索算法日后再说。</p><a id="more"></a><h1 id="深度优先与广度优先"><a href="#深度优先与广度优先" class="headerlink" title="深度优先与广度优先"></a>深度优先与广度优先</h1><p>深度优先和广度优先有共同点：图的遍历指每个节点仅仅被访问一次，都是一个递归的过程。</p><p>深度优先指：先遍历孩子结点，在考虑兄弟节点，即先往深了去搜；广度优先指：先遍历兄弟节点，在考虑孩子节点，即先往广了去搜。如下图所示，<code>1</code>为起始顶点，深度优先(DFS)的遍历顺序是：<code>1, 4, 3, 7, 5, 6, 2</code>：广度优先(BFS)的遍历顺序是：<code>1, 4, 2, 3, 5, 7, 6</code>。如果这是一张旅游景点的地图，你可以和你的小伙伴说，我们是深度优先玩呢，还是广度优先玩呢。</p><p><img data-src="/2020/02/26/graph-algorithm-search/1.png" alt></p><h1 id="二维搜索"><a href="#二维搜索" class="headerlink" title="二维搜索"></a>二维搜索</h1><p>来一个实际题目(PAT乙级1068)：要求你找出万绿丛中的一点红，即有独一无二颜色的那个像素点，并且该点的颜色与其周围 8 个相邻像素的颜色差充分大。输入第一行给出三个正整数，分别是 $M$ 和 $N(\leq 1000)$，即图像的分辨率；以及 TOL，是所求像素点与相邻点的颜色差阈值，色差超过 TOL 的点才被考虑。随后 $N$行，每行给出$M$个像素的颜色值，范围在$[0,2^{24}]$内。</p><p>在一行中按照 <code>(x, y): color</code> 的格式输出所求像素点的位置以及颜色值，其中位置 <code>x</code> 和 <code>y</code> 分别是该像素在图像矩阵中的列、行编号（从 1 开始编号）。如果这样的点不唯一，则输出 <code>Not Unique</code>；如果这样的点不存在，则输出 <code>Not Exist</code>。</p><h2 id="输入样例"><a href="#输入样例" class="headerlink" title="输入样例"></a>输入样例</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">8 6 200</span><br><span class="line">0  0   0    0    0      0       0        0</span><br><span class="line">65280  65280    65280    16711479 65280    65280    65280    65280</span><br><span class="line">16711479 65280    65280    65280    16711680 65280    65280    65280</span><br><span class="line">65280  65280    65280    65280    65280    65280    165280   165280</span><br><span class="line">65280  65280   16777015 65280    65280    165280   65480    165280</span><br><span class="line">16777215 16777215 16777215 16777215 16777215 16777215 16777215 16777215</span><br></pre></td></tr></table></figure><h2 id="输出样例"><a href="#输出样例" class="headerlink" title="输出样例"></a>输出样例</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(5, 3): 16711680</span><br></pre></td></tr></table></figure><h2 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h2><ul><li>我们可以知道，在二维搜索中，除边界元素外，一个元素有八个临界点，可以通过当前元素下标的$\pm 1, \pm 0$来访问；</li><li>此时定义周围8个元素的访问方式：</li></ul><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> dir[<span class="number">8</span>][<span class="number">2</span>] = &#123;&#123;<span class="number">-1</span>, <span class="number">-1</span>&#125;, &#123;<span class="number">-1</span>, <span class="number">0</span>&#125;, &#123;<span class="number">-1</span>, <span class="number">1</span>&#125;, &#123;<span class="number">0</span>, <span class="number">1</span>&#125;, &#123;<span class="number">1</span>, <span class="number">1</span>&#125;, &#123;<span class="number">1</span>, <span class="number">0</span>&#125;, &#123;<span class="number">1</span>, <span class="number">-1</span>&#125;, &#123;<span class="number">0</span>, <span class="number">-1</span>&#125;&#125;;</span><br></pre></td></tr></table></figure><ul><li>题目要求独一无二，所以设立<code>map</code>，记录每个元素出现的次数，超过1则不考虑；</li><li>因为此题并不是严格意义上的图，而是二维矩阵，且遍历的元素之间没有相关性，即不一定要根据当前元素才能找到下一个元素。(这里理解不了参考下文的三维搜索，下文的三维搜索元素间具有相关性，即搜索到一个元素，才能搜索下一个元素)，元素的搜索没有因果关系，所以遍历图可以考虑 for 循环，而没必要使用<code>bfs</code>或者<code>dfs</code>；</li><li>更多解释参考代码注释。</li></ul><h2 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;map&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">int</span> m, n, tol;</span><br><span class="line"><span class="built_in">vector</span>&lt;<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&gt; v;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义方向</span></span><br><span class="line"><span class="keyword">int</span> dir[<span class="number">8</span>][<span class="number">2</span>] = &#123;&#123;<span class="number">-1</span>, <span class="number">-1</span>&#125;, &#123;<span class="number">-1</span>, <span class="number">0</span>&#125;, &#123;<span class="number">-1</span>, <span class="number">1</span>&#125;, &#123;<span class="number">0</span>, <span class="number">1</span>&#125;, &#123;<span class="number">1</span>, <span class="number">1</span>&#125;, &#123;<span class="number">1</span>, <span class="number">0</span>&#125;, &#123;<span class="number">1</span>, <span class="number">-1</span>&#125;, &#123;<span class="number">0</span>, <span class="number">-1</span>&#125;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 判断</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">judge</span><span class="params">(<span class="keyword">int</span> i, <span class="keyword">int</span> j)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 8 个方向的遍历</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> k = <span class="number">0</span>; k &lt; <span class="number">8</span>; k++) &#123;</span><br><span class="line">        <span class="comment">// x 方向</span></span><br><span class="line">        <span class="keyword">int</span> tx = i + dir[k][<span class="number">0</span>];</span><br><span class="line">        <span class="comment">// y 方向</span></span><br><span class="line">        <span class="keyword">int</span> ty = j + dir[k][<span class="number">1</span>];</span><br><span class="line">        <span class="comment">// 不满足条件则直接退出</span></span><br><span class="line">        <span class="keyword">if</span> (tx &gt;= <span class="number">0</span> &amp;&amp; tx &lt; n &amp;&amp; ty &gt;= <span class="number">0</span> &amp;&amp; ty &lt; m &amp;&amp; v[i][j] - v[tx][ty] &gt;= <span class="number">0</span> - tol &amp;&amp; v[i][j] - v[tx][ty] &lt;= tol) </span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> cnt = <span class="number">0</span>, x = <span class="number">0</span>, y = <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">"%d%d%d"</span>, &amp;m, &amp;n, &amp;tol);</span><br><span class="line">    v.resize(n, <span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;(m));</span><br><span class="line">    <span class="built_in">map</span>&lt;<span class="keyword">int</span>, <span class="keyword">int</span>&gt; mapp;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; m; j++) &#123;</span><br><span class="line">            <span class="built_in">scanf</span>(<span class="string">"%d"</span>, &amp;v[i][j]);</span><br><span class="line">            <span class="comment">// 独一无二，出现一个元素标记加一，加到2则不考虑</span></span><br><span class="line">            mapp[v[i][j]]++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; m; j++) &#123;</span><br><span class="line">            <span class="comment">// 判读是否独一无二 且 满足要求</span></span><br><span class="line">            <span class="keyword">if</span> (mapp[v[i][j]] == <span class="number">1</span> &amp;&amp; judge(i, j) == <span class="literal">true</span>) &#123;</span><br><span class="line">                cnt++;</span><br><span class="line">                x = i + <span class="number">1</span>;</span><br><span class="line">                y = j + <span class="number">1</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (cnt == <span class="number">1</span>)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"(%d, %d): %d"</span>, y, x, v[x<span class="number">-1</span>][y<span class="number">-1</span>]);</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (cnt == <span class="number">0</span>)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"Not Exist"</span>);</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"Not Unique"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="https://github.com/liuchuo/PAT/tree/master/BasicLevel_C%2B%2B" target="_blank" rel="noopener">柳神的代码, 1068</a></p><h1 id="三维搜索（BFS）"><a href="#三维搜索（BFS）" class="headerlink" title="三维搜索（BFS）"></a>三维搜索（BFS）</h1><p>如果此时把搜索维度上升到三维空间，又该如何处理呢？先看题目(PAT 甲级 1091)：</p><h2 id="题目"><a href="#题目" class="headerlink" title="题目"></a>题目</h2><p>One important factor to identify acute stroke (急性脑卒中) is the volume of the stroke core. Given the results of image analysis in which the core regions are identified in each MRI slice, your job is to calculate the volume of the stroke core.</p><p>Each input file contains one test case. For each case, the first line contains 4 positive integers: $M, N, L$ and $T$, where $M$ and $N$ are the sizes of each slice (i.e. pixels of a slice are in an $M×N$ matrix, and the maximum resolution is 1286 by 128); $L (≤60)$ is the number of slices of a brain; and $T$ is the integer threshold (i.e. if the volume of a connected core is less than T, then that core must not be counted).</p><p>Then $L$ slices are given. Each slice is represented by an $M×N$ matrix of 0’s and 1’s, where 1 represents a pixel of stroke, and 0 means normal. Since the thickness of a slice is a constant, we only have to count the number of 1’s to obtain the volume. However, there might be several separated core regions in a brain, and only those with their volumes no less than $T$ are counted. Two pixels are <strong>connected</strong> and hence belong to the same region if they share a common side.</p><p>For each case, output in a line the total volume of the stroke core.</p><h2 id="样例输入"><a href="#样例输入" class="headerlink" title="样例输入"></a>样例输入</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">3 4 5 2</span><br><span class="line">1 1 1 1</span><br><span class="line">1 1 1 1</span><br><span class="line">1 1 1 1</span><br><span class="line">0 0 1 1</span><br><span class="line">0 0 1 1</span><br><span class="line">0 0 1 1</span><br><span class="line">1 0 1 1</span><br><span class="line">0 1 0 0</span><br><span class="line">0 0 0 0</span><br><span class="line">1 0 1 1</span><br><span class="line">0 0 0 0</span><br><span class="line">0 0 0 0</span><br><span class="line">0 0 0 1</span><br><span class="line">0 0 0 1</span><br><span class="line">1 0 0 0</span><br></pre></td></tr></table></figure><h2 id="样例输出"><a href="#样例输出" class="headerlink" title="样例输出"></a>样例输出</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">26</span><br></pre></td></tr></table></figure><h2 id="题解-1"><a href="#题解-1" class="headerlink" title="题解"></a>题解</h2><ul><li>题目的含义是：一共$L$个$M\times N$的切片，构成一个$M\times N \times L$的立方体，求这个立方体中数字为 1 连成的体积；</li><li>显然这是一个三维空间的搜索题目，需要在三维空间内寻找相连的 1 的数量，此时需要定义三个大方向：</li></ul><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> X[<span class="number">6</span>] = &#123;<span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">-1</span>, <span class="number">0</span>, <span class="number">0</span>&#125;;  <span class="comment">// M方向， -1表示回退方向</span></span><br><span class="line"><span class="keyword">int</span> Y[<span class="number">6</span>] = &#123;<span class="number">0</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">-1</span>, <span class="number">0</span>&#125;;  <span class="comment">// N方向， 回退时坐标减少，而不是增加</span></span><br><span class="line"><span class="keyword">int</span> Z[<span class="number">6</span>] = &#123;<span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">-1</span>&#125;;  <span class="comment">// L方向</span></span><br></pre></td></tr></table></figure><ul><li>如果一个 1 已经被访问，需要将它标记为<code>true</code>，防止多次访问；</li><li>三维图的 1 相连的方式多种多样，需要根据当前的 1 ，寻找下一个 1 ，所以 for 循环此时不够用，需要考虑广度优先遍历或深度优先遍历；</li><li>更多题解参考程序注释。</li></ul><h2 id="代码-1"><a href="#代码-1" class="headerlink" title="代码"></a>代码</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cstdio&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;queue&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">node</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> x, y, z;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> m, n, l, t;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 用于判断方向</span></span><br><span class="line"><span class="keyword">int</span> X[<span class="number">6</span>] = &#123;<span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">-1</span>, <span class="number">0</span>, <span class="number">0</span>&#125;;  <span class="comment">// M方向， -1表示回退方向</span></span><br><span class="line"><span class="keyword">int</span> Y[<span class="number">6</span>] = &#123;<span class="number">0</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">-1</span>, <span class="number">0</span>&#125;;  <span class="comment">// N方向， 回退时坐标减少，而不是增加</span></span><br><span class="line"><span class="keyword">int</span> Z[<span class="number">6</span>] = &#123;<span class="number">0</span>, <span class="number">0</span>, <span class="number">1</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">-1</span>&#125;;  <span class="comment">// L方向</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> arr[<span class="number">1300</span>][<span class="number">130</span>][<span class="number">80</span>];</span><br><span class="line"></span><br><span class="line"><span class="keyword">bool</span> visit[<span class="number">1300</span>][<span class="number">130</span>][<span class="number">80</span>];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">judge</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y, <span class="keyword">int</span> z)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 超出边界， 搜索停止</span></span><br><span class="line">    <span class="keyword">if</span>(x &lt; <span class="number">0</span> || x &gt;= m || y &lt; <span class="number">0</span> || y &gt;= n || z &lt; <span class="number">0</span> || z &gt;= l) </span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="comment">// 无风险不访问，已经访问过不访问</span></span><br><span class="line">    <span class="keyword">if</span>(arr[x][y][z] == <span class="number">0</span> || visit[x][y][z] == <span class="literal">true</span>) </span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="comment">// 其余情况均访问</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">bfs</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y, <span class="keyword">int</span> z)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> cnt = <span class="number">0</span>;</span><br><span class="line">    node temp;</span><br><span class="line">    <span class="comment">// 当前点的位置</span></span><br><span class="line">    temp.x = x, temp.y = y, temp.z = z;</span><br><span class="line">    <span class="comment">// 创建三维点的队列</span></span><br><span class="line">    <span class="built_in">queue</span>&lt;node&gt; q;</span><br><span class="line">    q.push(temp);</span><br><span class="line">    <span class="comment">// 当前点被访问</span></span><br><span class="line">    visit[x][y][z] = <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">while</span>(!q.empty()) &#123;</span><br><span class="line">        <span class="comment">// 取出队首元素</span></span><br><span class="line">        node top = q.front();</span><br><span class="line">        <span class="comment">// 弹出队首元素</span></span><br><span class="line">        q.pop();</span><br><span class="line">        <span class="comment">// 有一个1, 体积++</span></span><br><span class="line">        cnt++;</span><br><span class="line">        <span class="comment">// 六个方向开始寻找</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">6</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">int</span> tx = top.x + X[i];</span><br><span class="line">            <span class="keyword">int</span> ty = top.y + Y[i];</span><br><span class="line">            <span class="keyword">int</span> tz = top.z + Z[i];</span><br><span class="line">            <span class="keyword">if</span>(judge(tx, ty, tz)) &#123;</span><br><span class="line">                visit[tx][ty][tz] = <span class="literal">true</span>;</span><br><span class="line">                <span class="comment">// 当前节点没有被访问，就按着之前的三个方向探索下一个节点</span></span><br><span class="line">                temp.x = tx, temp.y = ty, temp.z = tz;</span><br><span class="line">                <span class="comment">// 压入队列，只要压入队列在下次循环时 cnt++ 用于计算体积</span></span><br><span class="line">                q.push(temp);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(cnt &gt;= t)</span><br><span class="line">        <span class="keyword">return</span> cnt;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">"%d %d %d %d"</span>, &amp;m, &amp;n, &amp;l, &amp;t);</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; l; i++)</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; m; j++)</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">int</span> k = <span class="number">0</span>; k &lt; n; k++)</span><br><span class="line">                <span class="comment">// M x N L个，这样的维度</span></span><br><span class="line">                <span class="built_in">scanf</span>(<span class="string">"%d"</span>, &amp;arr[j][k][i]);</span><br><span class="line">    <span class="keyword">int</span> ans = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; l; i++) &#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; m; j++) &#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">int</span> k = <span class="number">0</span>; k &lt; n; k++) &#123;</span><br><span class="line">                <span class="keyword">if</span>(arr[j][k][i] == <span class="number">1</span> &amp;&amp; visit[j][k][i] == <span class="literal">false</span>)</span><br><span class="line">                    ans += bfs(j, k, i);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%d"</span>, ans);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="参考-1"><a href="#参考-1" class="headerlink" title="参考"></a>参考</h2><p><a href="https://github.com/liuchuo/PAT/tree/master/AdvancedLevel_C%2B%2B" target="_blank" rel="noopener">柳神的代码, 1091</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在面临一个搜索问题时，如找出一个矩阵中不一样的元素，可能大多人和我想的一样：几层for循环，变量自增继续访问下一个。看着简单，但是，当问题逐渐复杂，如搜索出矩阵中的元素，要求是这个元素和周围的元素不一样，要求相差超过某个阈值，此时在用for循环去写，遍历元素和元素周围的元素，便显得不够明智。&lt;/p&gt;
&lt;p&gt;在搜索算法中，常用的为深度优先和广度优先。但是题目很少去单纯的考察搜索算法，搜索经常会和树、图的遍历一起使用，于是决定来总结一下图中的搜索算法，二叉树或多叉树中的搜索算法日后再说。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="DataStructure" scheme="https://muyuuuu.github.io/tags/DataStructure/"/>
    
  </entry>
  
  <entry>
    <title>通用的子串匹配问题求解算法</title>
    <link href="https://muyuuuu.github.io/2020/02/23/substring-match/"/>
    <id>https://muyuuuu.github.io/2020/02/23/substring-match/</id>
    <published>2020-02-23T12:30:13.000Z</published>
    <updated>2020-03-28T09:20:24.418Z</updated>
    
    <content type="html"><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="\assets\css\APlayer.min.css"><script src="\assets\js\APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>19年夏天出去面试的时候，被考了一道子串匹配的问题。当时也没回答上来，当时的学长说可以用类似二叉树的方式去查找，一个分支走不通的时候开始回溯到另一个分支，当时实在是太饿了，大概只听懂了这些吧。今天在刷题的时候又再次遇到了这个题目，也想到了更为简易(时间复杂度$O(n)$)和通用的算法，记录于此。</p><a id="more"></a><h1 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h1><p>字符串 <code>APPAPT</code> 中包含了两个单词 <code>PAT</code>，其中第一个 PAT 是第 2 位(P)，第 4 位(A)，第 6 位(T)；第二个 PAT 是第 3 位(P)，第 4 位(A)，第 6 位(T)。那么可以类比到：<code>PPPAAATTT</code>有27个<code>PAT</code>。现给定字符串，问一共可以形成多少个 <code>PAT</code>？由于结果可能比较大，只输出对 1000000007 取余数的结果。</p><h1 id="题目分析"><a href="#题目分析" class="headerlink" title="题目分析"></a>题目分析</h1><h2 id="一个常识"><a href="#一个常识" class="headerlink" title="一个常识"></a>一个常识</h2><script type="math/tex; mode=display">(a+b)\%c = a \% c + b \% c</script><p>如 $101 \% 100 + 50 \% 100 = (100 + 51) \% 100 = 51$</p><h2 id="错解思路"><a href="#错解思路" class="headerlink" title="错解思路"></a>错解思路</h2><p>当时的思路记不太清，我现在的想法是使用遍历的方法，先寻找<code>P</code>，在寻找<code>A</code>，在寻找<code>T</code>，当前字符不满足就跳到下一个字符，一条路走不通就走下一条路。以<code>APPAPT</code>第二位的<code>P</code>为例。从<code>P</code>开始，依次向后遍历字符，遇到错误的字符跳过，遇到正确的字符+1，如下图所示。</p><p><img data-src="/2020/02/23/substring-match/1.png" alt></p><p>第二位的<code>P</code>处理完毕后开始第三位<code>P</code>的处理，也很容易看出来，查找3个字符，复杂度为$O(n^3)$，查找4个字符，复杂度为$O(n^4)$，且代码维护不易容易超时。粗糙的代码如下：</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; s.length(); i++)&#123;</span><br><span class="line">    <span class="keyword">if</span> (s[i] != <span class="string">'P'</span>)</span><br><span class="line">        <span class="keyword">continue</span>;</span><br><span class="line">    <span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = i + <span class="number">1</span>; j &lt; s.length(); j++)&#123;</span><br><span class="line">            <span class="keyword">if</span> (s[j] != <span class="string">'A'</span>)</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            <span class="keyword">else</span>&#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="keyword">int</span> k = j + <span class="number">1</span>; j &lt; s.length(); k++)&#123;</span><br><span class="line">                    <span class="keyword">if</span> (s[k] == <span class="string">'T'</span>)&#123;</span><br><span class="line">                        count++;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="正解"><a href="#正解" class="headerlink" title="正解"></a>正解</h1><h2 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h2><p>既然容易超时，得想办法把时间复杂度给降下来，争取一次遍历在时间复杂度为$O(n)$内把问题解决掉。既然假设只能遍历一次，那么也可以理解为最终的个数是：有序字符<code>P,A,T</code>的排列组合数。</p><p>有序是指<code>P</code>要在<code>A</code>的前面，<code>A</code>要在<code>T</code>的前面，顺序错了的不能计算。最后，统计有几个有序的<code>P, A, T</code>，相乘一下即可解决。如<code>PPPAAATTT</code>中，有序的<code>P</code>是3个，<code>A</code>是3个，<code>T</code>是3个，那么最后的结果就是：$3\times3\times3=27$。</p><h2 id="思路"><a href="#思路" class="headerlink" title="思路"></a>思路</h2><p><code>T</code>的个数为<code>countt</code>，<code>P</code>的个数为<code>countp</code>，最终结果的个数为<code>result</code>。</p><ol><li>读取字符串，先统计<code>T</code>的个数。</li><li>开始遍历字符串：</li><li>遇到一个<code>P</code>，那么<code>P</code>的个数加一。</li><li>遇到一个<code>A</code>，开始计数，数量为<code>result += countt * countp</code>。</li><li>遇到一个<code>T</code>，<code>T</code>的个数减一。这样可以做到：<code>A</code>前面的<code>T</code>无效，遇到<code>A</code>后计算一次<code>P, T</code>的组合数，指针向后移动继续遍历，<code>T</code>的数量减少一个。如字符串<code>PPPAAAATTTTAT</code>，计算到倒数第二个<code>A</code>时，此时只有一个<code>T</code>。</li></ol><h2 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">string</span> s;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; s;</span><br><span class="line">    <span class="keyword">int</span> len = s.length(), result = <span class="number">0</span>, countp = <span class="number">0</span>, countt = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; len; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (s[i] == <span class="string">'T'</span>)</span><br><span class="line">            countt++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; len; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (s[i] == <span class="string">'P'</span>) countp++;</span><br><span class="line">        <span class="keyword">if</span> (s[i] == <span class="string">'T'</span>) countt--;</span><br><span class="line">        <span class="keyword">if</span> (s[i] == <span class="string">'A'</span>) result = (result + countp * countt) % <span class="number">1000000007</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; result;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="扩展"><a href="#扩展" class="headerlink" title="扩展"></a>扩展</h1><p>如果题目要求是寻找有几个<code>PSAT</code>呢，如果是寻找有几个<code>PNSAT</code>呢。错误的思路只会无限制的增加循环次数，而正解仍然能够在$O(n)$内解决问题。只需要把<code>PS</code>看作一个整体，在增加一个<code>S</code>的计数变量即可。如寻找<code>PSSSATTAAT</code>中<code>PSAT</code>的个数：</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">string</span> s;</span><br><span class="line">    <span class="built_in">cin</span> &gt;&gt; s;</span><br><span class="line">    <span class="keyword">int</span> len = s.length(), result = <span class="number">0</span>, countp = <span class="number">0</span>, countt = <span class="number">0</span>, counts = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; len; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (s[i] == <span class="string">'T'</span>)</span><br><span class="line">            countt++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; len; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (s[i] == <span class="string">'P'</span>) countp++;</span><br><span class="line">        <span class="keyword">if</span> (s[i] == <span class="string">'S'</span>) counts++;</span><br><span class="line">        <span class="keyword">if</span> (s[i] == <span class="string">'T'</span>) countt--;</span><br><span class="line">        <span class="keyword">if</span> (s[i] == <span class="string">'A'</span>) result = (result + countp * counts * countt) % <span class="number">1000000007</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">cout</span> &lt;&lt; result;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>输出为15。</p><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p><a href="https://github.com/liuchuo/PAT/tree/master/BasicLevel_C%2B%2B" target="_blank" rel="noopener">柳神的代码, 1040</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;19年夏天出去面试的时候，被考了一道子串匹配的问题。当时也没回答上来，当时的学长说可以用类似二叉树的方式去查找，一个分支走不通的时候开始回溯到另一个分支，当时实在是太饿了，大概只听懂了这些吧。今天在刷题的时候又再次遇到了这个题目，也想到了更为简易(时间复杂度$O(n)$)和通用的算法，记录于此。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="DataStructure" scheme="https://muyuuuu.github.io/tags/DataStructure/"/>
    
  </entry>
  
</feed>
